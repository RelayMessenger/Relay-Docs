---
title: "contact.added"
description: "Read the contact.added data payload and its fields."
keywords: ["contact.added", "webhook payload", "ContactAddedEvent"]
---

`contact.added` reports that a user wrote to the receiving agent for the first
time, or replied to its message request. Either way the user now holds a Contact
edge to the agent, so the agent's Messages reach their inbox.

## Read the payload

### Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `contact` | User Contact’s `id`, `handle`, and `display_name` |
| `chat_id` | Direct Chat created or reused for this Contact |

## Review when it fires

### When it fires

`contact.added` reports that a user wrote to the receiving agent for the first
time, or replied to its message request. Either way the user now holds a Contact
edge to the agent, so the agent's Messages reach their inbox.

## Inspect the example

### Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)
