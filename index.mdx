---
title: "Relay developer API"
description: "Connect an agent backend to the Relay v1 API with signed webhooks."
keywords: ["relay", "api", "agent", "webhooks"]
---

Connect an agent backend with Relay v1 HTTP routes and signed webhooks.

Relay owns messaging. Your backend owns the model, tools, memory, and agent behavior.

## Core flow

| Step | What happens |
| --- | --- |
| Receive | Relay sends `message.received` to the agent webhook |
| Accept | Your endpoint saves the event and returns `2xx` |
| Read | Your backend calls `POST /v1/chats/{chatId}/read` |
| Reply | Your backend calls `POST /v1/chats/{chatId}/messages` |

## Message state

| State | Meaning |
| --- | --- |
| Sent | Relay committed the Message |
| Delivered | Every snapshotted recipient acknowledged it |
| Read | Every snapshotted recipient marked it Read |

For an agent recipient, a successful `message.received` webhook is its Delivered acknowledgement. This is a Relay product decision.

<Columns cols={2}>
<Card title="Quickstart" icon="zap" href="/quickstart">Receive a Message and send a reply.</Card>
<Card title="Core concepts" icon="boxes" href="/concepts">Understand Contact, Handle, Message, and part.</Card>
<Card title="API reference" icon="square-terminal" href="/api-reference/overview">Read the current Relay v1 paths.</Card>
<Card title="Current evidence" icon="badge-check" href="/current-status">Separate API, build, and UI proof.</Card>
</Columns>

<Note>The current implementation is local. See [Current status](/current-status) for the exact boundary.</Note>

## Next steps

- [Complete the quickstart](/quickstart)
- [Understand Contact and Handle](/concepts#contact-and-handle)
- [Receive webhooks](/guides/webhooks)
