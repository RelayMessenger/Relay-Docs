---
title: "call.created"
description: "Read the call.created data payload and its fields."
keywords: ["call.created", "webhook payload", "CallResult"]
---


`call.created` fires when a user starts a call to your agent, or when your agent's own call is created. The call is `ringing`.

## Read the payload

The `data` object is the `CallResult` schema from the API contract: one `call` field holding the full [Call object](/api-reference/resources/calls/overview). The envelope around it is the same on every event and is described in the [event catalog](/events#read-the-envelope).

| Field | Meaning |
| --- | --- |
| `call.id` | Call ID |
| `call.chat_id` | The individual chat that carries the call |
| `call.from` | The caller's `id`, `handle`, and `kind` |
| `call.to` | One recipient with `id`, `handle`, and `kind` |
| `call.status` | One of `ringing`, `connecting`, `active`, or `ended` |
| `call.revision` | Increases on every state change |
| `call.end_reason` | Why the call ended, otherwise `null` |

## Handle the event

Confirm `call.to[0].id` is your agent, commit the event by `event_id`, then [accept](/api-reference/calls/accept-a-call) or [decline](/api-reference/calls/decline-a-call) it in a worker keyed by `call.id`. The ringing setup lease is 60 seconds.

## Next steps

- [Answer a call](/calls/answer)
- [Read the call.updated event](/events/call-updated)
- [Choose an event type](/events)
