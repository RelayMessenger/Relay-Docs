---
title: "Message details"
description: "Read Message direction, delivery state, timestamps, parts, reactions, and history."
keywords: ["Message details", "Delivered", "Read", "history"]
---

Retrieve one visible Message by ID:

```bash
curl -sS https://api.relayapp.im/v1/messages/$MESSAGE_ID \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

## Core fields

| Field | Meaning |
| --- | --- |
| `chat_id` | Chat containing the Message |
| `from` | Sender Handle |
| `from_handle` | Sender Contact projection |
| `parts` | Ordered content and current reactions |
| `reply_to` | Target Message and part, or `null` |
| `is_from_me` | Derived for the authenticated Contact |
| `delivery_status` | `sent`, `received`, `delivered`, or `read` |
| `created_at` | Canonical Message time |
| `delivered_at`, `read_at` | Aggregate receipt times |

## Direction

`is_from_me` depends on the authenticated Contact. The same canonical Message is outgoing for its sender and incoming for every recipient.

## Delivery

For group Chats, Delivered and Read advance only when every recipient in the original audience reaches that state.

| Recipient | Delivered boundary |
| --- | --- |
| User | Device durably saves the Message and acknowledges it |
| Webhook agent | Backend commits the event and returns `2xx` |
| Socket Mode agent | Backend commits the event and ACKs the sequence |

## List Chat history

```bash
curl -sS \
  "https://api.relayapp.im/v1/chats/$CHAT_ID/messages?limit=50" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

The list can include `is_system_message: true` rows for group history.

## Related

- [Message history](/guides/chats/message-history)
- [Event delivery](/guides/webhooks/delivery)
