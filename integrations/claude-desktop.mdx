---
title: "Claude Desktop"
description: "Connect Claude Desktop to Relay and check its messaging setup."
keywords: ["Relay", "Claude Desktop", "connect"]
---

## Before you start

- [Claude Desktop](https://claude.com/download), installed and signed in.
- Node.js 22.22.3 or newer.
- `RELAY_API_URL` set to `https://api.staging.relayapp.im`.

## Connect

Run this in a terminal to create an agent and add the Relay MCP server to Claude Desktop:

```bash
npx relaymessenger@staging connect claude-desktop
```

Approve the change, then message the new handle from your phone.

## Send it a message

Open Relay on your phone and message the handle. Claude Desktop does not wake up on its own, so tell it: `Read my Relay messages and answer them.` It reads the chat and sends its answer there.

## What it can do

These come from the connection code for `claude-desktop` and the Relay MCP tool list.

| Relay feature | Claude Desktop |
| --- | --- |
| Connect | Relay adds one `mcpServers.relay` entry to `claude_desktop_config.json` and keeps every other entry. |
| Start | restart Claude Desktop; connect tells you when. |
| Wakes when a message arrives | No. It reads the chat when you ask it to. |
| Reply in the chat | Yes, text up to 10,000 characters, with a stable idempotency key. |
| Send attachments | No. The MCP tools send text only. |
| Read chat history | Yes, by chat, oldest or newest first, one page at a time. |
| React, type, mark read | Yes, with reactions, typing indicators, and read marks. |
| Contact card | Yes, read and set the agent's name and picture. |

## When it fails

- `Relay cannot ask which agent to connect.` Run connect in a terminal, or pipe a token with `--with-token`.
- `Relay cannot ask you to confirm this plan.` Add `--yes`.
- `Relay would not accept that token, so nothing was changed.` The token and `RELAY_API_URL` come from different environments.
- `did not finish` Fix what the step names, then run the same command again; your agent and its token are saved.
- `No reply yet. Run: relay watch @<handle>` Claude Desktop has not read the chat yet; ask it again.

## Next steps

- [Send messages](/concepts/messages/send)
- [Read message history](/concepts/chats/history)
- [Handle retries](/live/retries)
