# Relay

# Introduction

> Build an agent and start talking to it in Relay.

Source: https://docs.staging.relayapp.im/index.md

Build an agent and start talking to it in Relay.

> Start with the [Quickstart](/getting-started/quickstart) to connect your first
> agent. Use the [API Reference](/api-reference/overview) for endpoint and
> schema details.

Use the official [TypeScript SDK](/getting-started/sdks) for a Node agent
backend, or call the same `/v1` operations over HTTPS.

## Prerequisites

To build an agent, you need:

- An agent and Agent Token from [Relay Console](https://console.relayapp.im)
- A durable database or queue for incoming events
- A public HTTPS endpoint for a saved webhook subscription, or an always-on
  process that connects when no subscription is saved

## What you can build

- **Personal agents** that live beside a user's other Chats
- **Support agents** that reply with text, media, audio, and files
- **Group agents** that respond when mentioned in a shared Chat
- **Webhook backends** that receive signed HTTPS requests
- **Always-on backends** that receive events over a WebSocket

## Key capabilities

| Capability | Description |
| --- | --- |
| Multipart Messages | Send ordered text, media, and link parts |
| Attachments | Upload images, video, audio, and files up to 100 MB |
| Group Chats | Add and remove Contacts, rename groups, and set group photos |
| Replies and reactions | Target an exact Message part |
| Contact Cards | Configure an agent's name and photo, then share it into an existing Chat |
| Delivery state | Track Sent, Delivered, and Read per recipient |
| Webhook events | Save a webhook subscription for signed HTTP delivery, or connect by WebSocket when none is saved |

## Authentication

Every agent request uses a bearer credential:

```http
Authorization: Bearer $RELAY_AGENT_TOKEN
```

Create and revoke Agent Tokens in Relay Console.

## Quick example

Send a Message to an existing Chat:

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{ type: "text", value: "I can help with that." }],
    idempotency_key: `reply-${eventId}`,
  },
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/chats/$CHAT_ID/messages \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: reply-$EVENT_ID" \
  -d '{
    "message": {
      "parts": [{"type":"text","value":"I can help with that."}]
    }
  }'
```
</CodeGroup>

## Next steps

<CardGroup cols={2}>
  <Card title="Quickstart" href="/getting-started/quickstart">
    Receive one Message and send one reply.
  </Card>
  <Card title="Client SDKs" href="/getting-started/sdks">
    Use the official TypeScript client.
  </Card>
  <Card title="Developer ecosystem" href="/ecosystem">
    Choose an adapter, channel, local tool, or coding-agent package.
  </Card>
  <Card title="Messaging" href="/guides/messaging">
    Send text, media, voice memos, replies, mentions, and reactions.
  </Card>
  <Card title="Webhooks" href="/guides/webhooks">
    Receive signed events at an HTTPS endpoint.
  </Card>
  <Card title="WebSocket" href="/guides/websocket">
    Receive events through an always-on connection.
  </Card>
</CardGroup>

# Quickstart

> Receive one user Message through a webhook and send one reply.

Source: https://docs.staging.relayapp.im/getting-started/quickstart.md

Connect an agent backend, receive one user Message, and send one reply.

## Prerequisites

- An agent created in [Relay Console](https://console.relayapp.im)
- The Agent Token shown when you create it
- A public HTTPS endpoint
- A durable database or queue for incoming events

## 1. Set your credentials

```bash
export RELAY_API_URL="https://api.relayapp.im"
export RELAY_AGENT_TOKEN="<agent-token>"
```

## 2. Choose the SDK or HTTPS

<Tabs>
  <Tab title="TypeScript SDK">
    ```bash
    npm install @relaymessenger/sdk@staging
    ```

    ```typescript
    import Relay from "@relaymessenger/sdk";

    const relay = new Relay({
      apiKey: process.env.RELAY_AGENT_TOKEN!,
    });
    ```
  </Tab>
  <Tab title="cURL">
    Use `$RELAY_API_URL` and `$RELAY_AGENT_TOKEN` from step 1.
  </Tab>
</Tabs>

## 3. Create a webhook subscription

<CodeGroup>
```typescript TypeScript SDK
const subscription = await relay.webhookSubscriptions.create({
  target_url: "https://agent.example/webhooks/relay",
  subscribed_events: ["message.received"],
});
```

```bash cURL
curl -sS -X POST "$RELAY_API_URL/v1/webhook-subscriptions" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "target_url":"https://agent.example/webhooks/relay",
    "subscribed_events":["message.received"]
  }'
```
</CodeGroup>

Save `signing_secret` from the response. Relay returns it once.

Creating the agent's first subscription selects Webhook delivery and closes
any connected agent sockets. Relay moves matching pending events to this
subscription with the same `event_id`.

```json
{
  "id":"01993d50-34ca-7613-8df2-d3f8cc975d04",
  "target_url":"https://agent.example/webhooks/relay",
  "subscribed_events":["message.received"],
  "is_active":true,
  "signing_secret":"whsec_<base64-key>",
  "created_at":"2026-08-29T06:20:00.000Z",
  "updated_at":"2026-08-29T06:20:00.000Z"
}
```

## 4. Accept the event durably

<Steps>
  <Step title="Verify">
    Verify the Standard Webhooks signature against the exact raw request body.
  </Step>
  <Step title="Deduplicate">
    Insert `event_id` under a unique constraint.
  </Step>
  <Step title="Commit">
    Save the event or a durable job before responding.
  </Step>
  <Step title="Respond">
    Return `200` or `204` within 10 seconds.
  </Step>
  <Step title="Process">
    Run the model and send the reply after the response.
  </Step>
</Steps>

**A webhook `2xx` is a transport acknowledgement only.** Return it after the
event commits durably. It does not advance Message Delivered or Read state.

Relay retries `429`, `5xx`, timeouts, and connection failures. Other `4xx`
responses end delivery, so reject only requests that must not be retried.

The SDK verifies and unwraps the unmodified body:

```typescript
const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  webhookSecret: process.env.RELAY_WEBHOOK_SECRET!,
});

const event = relay.webhooks.unwrap(rawBody, {
  headers: request.headers,
});

await inbox.insertOnce(event.event_id, event);
return new Response(null, { status: 204 });
```

## 5. Mark Read and reply

Read is optional. It advances only through
`POST /v1/chats/{chatId}/read`. Call that route when the agent reads the
Messages, then send the reply independently.

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.markAsRead(chatId);

await relay.chats.messages.send(chatId, {
  message: {
    parts: [{ type: "text", value: "Tomorrow at 2:00 PM works." }],
    reply_to: { message_id: messageId, part_index: 0 },
    idempotency_key: `reply-${eventId}`,
  },
});
```

```bash cURL
curl -sS -X POST "$RELAY_API_URL/v1/chats/$CHAT_ID/read" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"

curl -sS -X POST "$RELAY_API_URL/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: reply-$EVENT_ID" \
  --data-binary @- <<JSON
{
  "message": {
    "parts": [{"type":"text","value":"Tomorrow at 2:00 PM works."}],
    "reply_to": {"message_id":"$MESSAGE_ID","part_index":0}
  }
}
JSON
```
</CodeGroup>

## Review with an agent

**Audit this webhook receiver against the current contract.** Copy this prompt into your coding agent.

<CodeGroup>
```text Audit a Relay webhook receiver
Audit this Relay webhook receiver without changing code.

1. Read https://docs.relayapp.im/llms.txt and the current Relay OpenAPI first.
2. Locate Agent Token authentication and the webhook receiver.
3. Prove the signature is verified against the raw body.
4. Prove event_id is committed under a uniqueness rule before the 2xx response.
5. Prove model work happens after the response.
6. Prove replies use POST /v1/chats/{chatId}/messages with a stable idempotency key.
7. Report Check | Status | file:line evidence | Fix.
8. Mark anything you cannot prove as unknown.
```
</CodeGroup>

## Next steps

- [Authentication](/getting-started/authentication)
- [Client SDKs](/getting-started/sdks)
- [Sending Messages](/guides/messaging/sending-messages)
- [Webhooks](/guides/webhooks)
- [WebSocket](/guides/websocket)

# Authentication

> Authenticate Relay API requests with an Agent Token.

Source: https://docs.staging.relayapp.im/getting-started/authentication.md

Authenticate every Relay API request with a bearer credential.

```http
Authorization: Bearer $RELAY_AGENT_TOKEN
```

## Credentials

| Caller | Credential | Use |
| --- | --- | --- |
| Agent backend | Agent Token | Messages, Chats, Attachments, Contact Cards, webhooks, and WebSocket |

The Agent Token identifies the agent Contact and its allowed operations.

## Agent Tokens

Create and revoke Agent Tokens in Relay Console. The plaintext token appears
once. Relay stores its digest.

```bash
export RELAY_AGENT_TOKEN="<agent-token>"
```

**Keep Agent Tokens in server-side secret storage.** Keep them out of source,
logs, browser storage, and URLs.

## WebSocket authentication

Upgrade `wss://api.relayapp.im/v1/websocket` with the same bearer header:

```http
Authorization: Bearer $RELAY_AGENT_TOKEN
```

**Open the WebSocket from a trusted backend that can set upgrade headers.**

An agent with any saved webhook subscription receives HTTP `409` during the
WebSocket upgrade. Delete every subscription before connecting.

## Errors

| Status | Meaning |
| --- | --- |
| `400` | A WebSocket upgrade includes a query string |
| `401` | Credential is missing, invalid, expired, or revoked |
| `403` | Credential lacks permission for this operation |
| `404` | Resource is absent or outside this Contact's visibility |
| `409` | An agent WebSocket conflicts with a saved webhook subscription |

## Related

- [Key concepts](/getting-started/key-concepts)
- [WebSocket](/guides/websocket)
- [Error Codes](/error)

# Client SDKs

> Use the official TypeScript SDK for v1 commands and signed webhooks.

Source: https://docs.staging.relayapp.im/getting-started/sdks.md

`@relaymessenger/sdk` is the first-class TypeScript client for Relay v1.

## Install

```bash
npm install @relaymessenger/sdk@staging
```

The `latest` and `staging` npm tags both resolve to
`@relaymessenger/sdk@0.3.0-staging.4`.

The supported runtime is Node.js `22.22.3` or newer.

## Create a client

```typescript
import Relay from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  baseURL: process.env.RELAY_API_URL ?? "https://api.relayapp.im",
});
```

The default API origin is `https://api.relayapp.im`.
Set `RELAY_API_URL` to the matching staging origin during staging tests. Use a
token created in the same environment.

**Keep the Agent Token in trusted server infrastructure.** Do not include it
in browser JavaScript, source control, URLs, cookies, or logs.

## Send a Message

```typescript
const result = await relay.chats.messages.send(chatId, {
  message: {
    parts: [{ type: "text", value: "Hello from Relay." }],
    idempotency_key: crypto.randomUUID(),
  },
});
```

## Resources

| Resource | Current methods |
| --- | --- |
| Chats | `create`, `retrieve`, `update`, `listChats`, `leaveChat`, `markAsRead`, `shareContactCard`, `startTyping`, `stopTyping`, `sendVoicememo` |
| Chat Messages | `chats.messages.list`, `chats.messages.send` |
| Participants | `chats.participants.add`, `chats.participants.remove` |
| Messages | `create`, `retrieve`, `addReaction`, `listMessagesThread` |
| Attachments | `create`, `upload`, `retrieve`, `delete` |
| Contact Card | `create`, `retrieve`, `update` |
| Blocked Handles | `list`, `block`, `unblock` |
| Webhooks | `webhookEvents.list`, subscription CRUD, `webhooks.verify`, `webhooks.unwrap` |
| WebSocket | `websocket.run` |

## Pagination

Chat pages expose `.chats`; Message pages expose `.messages`. Both support
`.hasNextPage()`, `.getNextPage()`, and async iteration:

```typescript
const page = await relay.chats.listChats({ limit: 50 });

for await (const chat of page) {
  console.log(chat.id);
}
```

## Retries and idempotency

The client defaults to a 15-second request timeout and two retries.

It retries network failures, timeouts, HTTP `408`, `429`, and `5xx` only when
the operation is safe:

- reads and idempotent HTTP methods;
- commands explicitly marked retryable by the SDK;
- Message sends with an idempotency key.

**A Message POST without an idempotency key is not retried.**

```typescript
const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  timeout: 15_000,
  maxRetries: 2,
});
```

Per-request `timeout`, `maxRetries`, `signal`, and headers are also supported.

## Errors

```typescript
import Relay, { RelayAPIError } from "@relaymessenger/sdk";

try {
  await relay.chats.retrieve(chatId);
} catch (error) {
  if (error instanceof RelayAPIError) {
    console.error({
      status: error.status,
      code: error.code,
      traceId: error.traceId,
      docURL: error.docURL,
      retryAfter: error.retryAfter,
      retryable: error.retryable,
    });
  }
}
```

Use `code` for program logic and retain `traceId` for support and debugging.

## Webhook verification

Configure the signing secret and pass the unmodified request body:

```typescript
const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  webhookSecret: process.env.RELAY_WEBHOOK_SECRET!,
});

const event = relay.webhooks.unwrap(rawBody, {
  headers: request.headers,
});
```

`webhooks.unwrap` verifies Standard Webhooks headers before parsing JSON.
Commit the returned `event_id` durably before returning `2xx`.

## Browser limitation

The Agent Token client is a server-side Node.js package. Shipping an Agent
Token to a browser would expose it.

Use Relay's user-authenticated app surface for user clients. Do not proxy an
Agent Token into browser code.

## Runnable examples

- [Quickstart](/getting-started/quickstart)
- [Developer ecosystem](/ecosystem)
- [Chat SDK adapter](/ecosystem/chat-sdk)
- [Examples](/examples)
- [Attachments](/guides/messaging/attachments)
- [Webhooks](/guides/webhooks)
- [WebSocket](/guides/websocket)

## Related

- [Authentication](/getting-started/authentication)
- [Best Practices](/getting-started/best-practices)
- [API Reference](/api-reference/overview)

# Key concepts

> Understand Contacts, Handles, Chats, Messages, and Attachments.

Source: https://docs.staging.relayapp.im/getting-started/key-concepts.md

Relay uses these terms in every guide and endpoint.

## Contacts and Handles

A **Contact** is a user or agent profile. Every Contact owns one public **Handle**.

| Field | Meaning |
| --- | --- |
| `id` | Stable opaque UUIDv7 Contact ID |
| `handle` | Public messaging address, such as `weather.acme` |
| `kind` | `user` or `agent` |
| `display_name` | Current profile name |
| `image_url` | Current profile image |

Handles can change. Relationships such as membership, delivery, reactions,
and blocks use stable Contact IDs internally.

When a user adds an agent, Relay creates or reuses their direct Chat and sends
the agent backend `contact.added`. The agent can Message that user while the
user keeps it added.

## Chats

A **Chat** is a direct or group Message container.

- A direct Chat has two active Contacts.
- A group Chat has three or more active Contacts.
- Group membership records when a Contact joined, left, or was removed.
- A newly added Contact's history starts at `joined_at`.

## Messages and parts

A **Message** belongs to one Chat and contains 1 to 100 ordered `parts`.

| Part | Purpose |
| --- | --- |
| `text` | Plain text, optionally with one structured mention |
| `media` | An image, video, audio file, or document |
| `link` | One URL that renders as a rich preview |
| `system` | Visible Chat events such as group history or Contact Card sharing |

Parts are addressed by zero-based `part_index`. Replies and reactions can target an exact part.

## Attachments

An **Attachment** is an uploaded file up to 100 MB. Allocate it and upload the
declared bytes before using its `attachment_id` in a Message. The Contact that
allocated it remains its owner.

## Delivery

| State | Meaning |
| --- | --- |
| Sent | Relay committed the Message and delivery intent |
| Delivered | Every snapshotted recipient reached its recipient-specific Delivered boundary |
| Read | Every snapshotted recipient explicitly marked the Chat Read |

A user recipient reaches Delivered when the Relay client application applies
the Message on that recipient's device. An agent recipient reaches Delivered
when Relay commits the Message and it is readable through the Relay API.

Webhook `2xx` responses and WebSocket ACKs acknowledge event transport only.
They do not advance Delivered or Read. For an agent, Read is optional and
advances only through `POST /v1/chats/{chatId}/read`.

## Events

Saved webhook subscriptions determine event delivery:

| Configuration | Path |
| --- | --- |
| One or more webhook subscriptions | Matching active Webhooks |
| Zero webhook subscriptions | WebSocket |

Pending events retain the same `event_id` when the first subscription is
created or the last is deleted.

## Idempotency

Message sends accept an idempotency key up to 255 characters. The same key and
body return the original Message. The same key with different content returns
`409`.

## Related

- [Messaging overview](/guides/messaging)
- [Chats overview](/guides/chats)
- [Add requests](/guides/contacts/add-requests)
- [Webhook Event Types](/guides/webhooks/events)

# AI coding agents

> Give a coding agent the current docs and focused audit prompts.

Source: https://docs.staging.relayapp.im/getting-started/ai-agents.md

Ground coding agents in the current Relay docs before they inspect or change a
Relay agent backend.

## Documentation files

| File | Use |
| --- | --- |
| `https://docs.relayapp.im/llms.txt` | Concise page index |
| `https://docs.relayapp.im/llms-full.txt` | Complete documentation in one file |
| `https://docs.relayapp.im/mcp` | Hosted read-only docs search |
| [OpenAPI Reference](/api-reference/overview) | Exact paths, fields, limits, and errors |

Start with `llms.txt` and the pages needed for the task. Use `llms-full.txt`
when the agent needs the complete contract.

Use the [Relay Skill](/ecosystem/skills), [Relay for Codex](/ecosystem/codex), or
[Relay for Cursor](/ecosystem/cursor) for packaged guidance. Use
[Relay MCP](/ecosystem/mcp) when a trusted local MCP client should call Relay
API operations with an Agent Token.

## Relay agent prompt

Copy this prompt to give a coding agent Relay's current contract and operating
rules.

````text Relay agent prompt
---
name: relay
description: Build an agent and start talking to it in Relay.
---

# Relay developer guide

Build an agent and start talking to it in Relay.

Your backend owns the agent's model, tools, memory, and behavior. Relay carries
Messages between the agent and other users.

## Start

1. Read `https://docs.relayapp.im/llms.txt` and the current OpenAPI.
2. Read the Webhooks guide and choose Webhooks or WebSocket.
3. Set `RELAY_API_URL` for the target environment and use an Agent Token from
   that environment.
4. Store the Agent Token in server-side secret storage.
5. Configure the selected event path.
6. Commit each `event_id` once in durable storage before sending a Webhook
   `2xx` or WebSocket ACK.
7. Run model and tool work after acknowledgment.
8. Optionally mark the Chat Read only through
   `POST /v1/chats/{chatId}/read`.
9. Reply through `POST /v1/chats/{chatId}/messages` with a stable idempotency key.

## Vocabulary

- A Contact is a user or agent profile.
- Every Contact owns one public Handle.
- A user must add an agent before that agent can Message them.
- A username-scoped Handle can be added by users. A Premium Handle can also
  send an Add request through `POST /v1/contact_requests`.
- `contact.added` carries the user Contact and direct `chat_id` for the
  agent's next Message.
- `contact.removed` means the user removed or blocked the agent.
- A Chat is direct or group.
- A Message belongs to one Chat and contains ordered parts.
- Parts are `text`, `media`, or `link` on sends.
- Replies and reactions target zero-based `part_index`.
- Group membership controls which history a Contact can read.

## Webhook events

| Path | Configuration | Transport acknowledgement |
| --- | --- | --- |
| Webhooks | Save public HTTPS subscriptions for selected event types. | Verify the signature, deduplicate `event_id`, commit durably, then return `2xx`. |
| WebSocket | Connect to `/v1/websocket` with an empty subscription list. | Deduplicate `event_id`, commit durably, then send a cumulative ACK. |

Saving the first webhook subscription closes active agent sockets with code
`4410`. Matching pending events move to active Webhooks. Deleting the final
subscription moves pending events to WebSocket. Transferred events keep their
`event_id`.

Relay retains pending events for 30 days. A WebSocket upgrade returns HTTP
`409` while the agent has a saved webhook subscription.

## Accept events

For Webhooks:

```text
verify signature → deduplicate event_id → durable commit → 2xx → process
```

For WebSocket:

```text
deduplicate event_id → durable commit → cumulative ACK → process
```

Return `2xx` or send the ACK only after the durable commit. These are transport
acknowledgements only. They do not advance Delivered or Read.

For an agent recipient, Delivered means Relay committed the Message and it is
readable through the Relay API. Read is optional and advances only through
`POST /v1/chats/{chatId}/read`. Run model and tool work independently.

Webhook delivery is at least once. After the initial attempt, Relay retries
network errors, HTTP `429`, and HTTP `5xx` responses up to 10 times with delays
from 2 to 600 seconds. Each attempt has a 10-second response window.

Use a direct public HTTPS webhook destination. Relay validates DNS answers and
treats redirects as terminal delivery failures.

WebSocket ACKs are cumulative. Relay replays pending events after a reconnect.
Complete FULL sync when the checkpoint is older than retention. Relay sends a
ping every 30 seconds and requires a pong within 60 seconds.

Agent backends authenticate the `/v1/websocket` upgrade with
`Authorization: Bearer <Agent Token>`.

## Canonical contract

- Call the `/v1` paths defined by the current OpenAPI.
- Send Message commands through REST.
- Treat registered Handles as public messaging addresses.
- Treat every inbound `event_id` as at-least-once.
- Recover current state with ordinary REST reads or WebSocket FULL sync.
- Retain `trace_id` from API errors and webhook events for debugging.
- Use a staging API root and staging Agent Token together during staging tests.

Use the OpenAPI contract for exact fields, limits, and errors. Label unproved
behavior `unknown`.

## Developer tools

- Use `https://docs.relayapp.im/mcp` for read-only documentation search.
- Use the local `@relaymessenger/mcp` stdio server for Relay API tools with an
  Agent Token.
- Use Relay Skills, Relay for Codex, or Relay for Cursor for packaged coding
  guidance.
- Read the Developer ecosystem page before selecting Chat SDK, Cloudflare
  Think, OpenClaw, Claude Code, or Hermes.
````

## Build prompt

**Copy this prompt to add Relay to a codebase.**

<CodeGroup>
```text Build with Relay
Add Relay messaging to this codebase.

Before coding:
1. Read https://docs.relayapp.im/llms.txt.
2. Read the Quickstart, Authentication, Key concepts, Sending Messages, and the chosen event transport guide.
3. Read the current Relay OpenAPI for every endpoint you will call.

Requirements:
- Use https://api.relayapp.im/v1.
- Keep the Agent Token in server-side secret storage.
- Use active webhook subscriptions for each required event type, or connect by WebSocket with zero saved subscriptions.
- Commit each inbound event under a unique event_id before webhook 2xx or WebSocket ACK.
- Run model work after acknowledgement.
- Send replies through REST with a stable idempotency key.
- Use only paths, fields, event payloads, and WebSocket frames in the current contract.

Show the files changed, the tests added, and any requirement you could not prove.
```
</CodeGroup>

## Audit prompt

**Copy this prompt for a read-only review.**

<CodeGroup>
```text Audit a Relay agent backend
Audit this Relay agent backend without changing code.

Read the current Relay docs and OpenAPI first. Then report:
- Authentication
- Event verification
- Durable deduplication
- ACK timing
- Idempotent replies
- Attachment handling
- Error and retry behavior

For each item provide Status | file:line evidence | Risk | Fix.
Use unknown when the code does not prove a claim.
```
</CodeGroup>

## Related

- [Quickstart](/getting-started/quickstart)
- [Developer ecosystem](/ecosystem)
- [Relay Skill](/ecosystem/skills)
- [Best practices](/getting-started/best-practices)

# Best practices

> Build reliable agents with durable events and idempotent commands.

Source: https://docs.staging.relayapp.im/getting-started/best-practices.md

Use these rules for every Relay agent backend.

## Accept events before processing

```text
verify → insert event_id durably → commit → 2xx or ACK → process
```

Commit the event before acknowledging it. Run model inference after the
acknowledgement.

Webhook `2xx` responses and WebSocket ACKs acknowledge event transport only.
Neither transport acknowledgement advances Delivered or Read.

## Make commands idempotent

Use a stable key for every Message send. Derive reply keys from the triggering `event_id`.

```text
reply:<event_id>
```

Reuse the same key and body after timeouts or uncertain responses.

## Keep replies on REST

Relay sends each event to matching active webhook subscriptions when at least
one subscription is saved. With zero saved subscriptions, events queue for
WebSocket delivery. The `event_id` stays the same when a pending event moves.

Send replies through `POST /v1/chats/{chatId}/messages`.

## Treat IDs as opaque

Treat UUIDs as opaque values. Keep cursors unchanged and use each cursor with
the operation that returned it.

## Upload media before sending

Allocate the Attachment and upload the declared bytes. Confirm it is ready
before sending the Message. Keep transfer URLs out of logs.

## Respect membership visibility

A Contact can read the periods when it belonged to a Chat. Cache and display
only the history returned for that Contact.

## Handle duplicates

Webhook retries and WebSocket replay are at least once. A duplicate `event_id`
must return success without repeating tools, model work, or replies.

## Related

- [Idempotency](/guides/platform/idempotency)
- [Event delivery](/guides/webhooks/delivery)
- [Error Codes](/error)

# Messaging

> Send multipart Messages with text, media, links, replies, mentions, and reactions.

Source: https://docs.staging.relayapp.im/guides/messaging/index.md

A Message belongs to one Chat and contains an ordered `parts` array.

## Part types

| Type | Use | Key fields |
| --- | --- | --- |
| `text` | Plain text and mentions | `value`, `mention`, `mention_range` |
| `media` | Image, video, audio, or file | `attachment_id` or HTTPS `url` |
| `link` | Rich link preview | `value` |

A Message has 1 to 100 parts. A `link` must be the only part in its Message.

## Send paths

| Path | Use |
| --- | --- |
| `POST /v1/messages` | Resolve or create a Chat, then send |
| `POST /v1/chats/{chatId}/messages` | Send to an existing Chat |
| `POST /v1/chats/{chatId}/voicememo` | Send an uploaded audio Attachment as a voice memo |

## Message lifecycle

| State | Meaning |
| --- | --- |
| Sent | Relay committed the Message |
| Delivered | Every recipient reached its recipient-specific Delivered boundary |
| Read | Every recipient explicitly marked it Read |

Read [Delivery receipts](/guides/messaging/delivery-receipts) for the user and
agent Delivered boundaries and the optional Read operation.

## Next steps

<CardGroup cols={2}>
  <Card title="Sending Messages" href="/guides/messaging/sending-messages">Send text and multipart content.</Card>
  <Card title="Attachments" href="/guides/messaging/attachments">Upload and reuse files.</Card>
  <Card title="Voice memos" href="/guides/messaging/voice-memos">Send native audio messages.</Card>
  <Card title="Receiving Media" href="/guides/messaging/receiving-media">Download inbound files and pass them to a model.</Card>
  <Card title="Rich link previews" href="/guides/messaging/rich-link-previews">Send a URL as a link part.</Card>
  <Card title="Mentions" href="/guides/messaging/mentions">Address a Contact in a group.</Card>
  <Card title="Replies" href="/guides/messaging/replies">Reply to an exact part.</Card>
  <Card title="Reactions" href="/guides/messaging/reactions">Add standard or custom reactions.</Card>
  <Card title="Message details" href="/guides/messaging/message-details">Read status, timestamps, and history.</Card>
</CardGroup>

# Sending Messages

> Send a Message to a new or existing Chat.

Source: https://docs.staging.relayapp.im/guides/messaging/sending-messages.md

Send through an existing Chat when you already know its ID. A user must have
the agent added before the agent can Message them.

## Send to an existing Chat

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{ type: "text", value: "The report is ready." }],
    idempotency_key: "message-018f",
  },
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/chats/$CHAT_ID/messages \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: message-018f" \
  -d '{
    "message": {
      "parts": [{"type":"text","value":"The report is ready."}]
    }
  }'
```
</CodeGroup>

Relay returns `202` with the canonical Message projection.

## Resolve or create a Chat

Use `POST /v1/messages` when you know the recipient Handles but not the Chat ID:

<CodeGroup>
```typescript TypeScript SDK
const result = await relay.messages.create({
  to: ["alice"],
  message: {
    parts: [{ type: "text", value: "Welcome back." }],
    idempotency_key: "welcome-alice",
  },
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/messages \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: welcome-alice" \
  -d '{
    "to": ["alice"],
    "message": {
      "parts": [{"type":"text","value":"Welcome back."}]
    }
  }'
```
</CodeGroup>

The response includes `chat_id` and `created_new_chat`. Every user recipient
must have the agent added.

`POST /v1/messages` can use a link as the first Message. The explicit
`POST /v1/chats` route rejects a first Message that contains a link or an HTTP
or HTTPS URL in text.

## Send multiple parts

```json
{
  "message": {
    "parts": [
      {"type":"text","value":"Here is the signed report."},
      {"type":"media","attachment_id":"01993d50-d263-7d6b-87ce-90aba89b7815"}
    ]
  }
}
```

Adjacent text parts are rejected. Combine adjacent text into one part.

## Idempotency

Set `Idempotency-Key`, `message.idempotency_key`, or both with the same value.

| Retry | Result |
| --- | --- |
| Same key and same body | Original Message returned |
| Same key and different body | `409` conflict |
| No key after an uncertain response | Duplicate send is possible |

## Limits

| Limit | Value |
| --- | ---: |
| Parts per Message | 100 |
| Public URL media parts | 40 |
| Text per part | 10,000 UTF-16 code units |
| URL length | 2,048 characters |
| Other Contacts in a Chat | 31 |

## Next steps

- [Upload Attachments](/guides/messaging/attachments)
- [Reply to a Message](/guides/messaging/replies)
- [Add requests](/guides/contacts/add-requests)
- [Understand idempotency](/guides/platform/idempotency)

# Mentions

> Mention a current Contact in a group Chat using structured text metadata.

Source: https://docs.staging.relayapp.im/guides/messaging/mentions.md

Mentions are structured fields on a text part, not text-pattern matching.

## Mention a Contact

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{
      type: "text",
      value: "Echo, summarize this",
      mention: "echo.acme",
      mention_range: [0, 4],
    }],
  },
});
```

```bash cURL
curl -sS -X POST "https://api.relayapp.im/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message":{
      "parts":[{
        "type":"text",
        "value":"Echo, summarize this",
        "mention":"echo.acme",
        "mention_range":[0,4]
      }]
    }
  }'
```
</CodeGroup>

## Choose the range

| Field | Meaning |
| --- | --- |
| `mention` | Handle of the current group Contact |
| `mention_range` | UTF-16 `[start, end)` range inside `value` |

`mention_range` is optional. When supplied, it must fit inside the text and use UTF-16 offsets.

## Validation rules

- Mentions are available only in group Chats.
- The Handle must currently be active in the Chat.
- Mentioning a removed Contact returns `409` with code `2015`.
- Mentioning an unrelated Handle returns `400` with code `1004`.
- Invoke an agent by setting `mention` and, when needed, `mention_range`.

## Related

- [Group Chats](/guides/chats/group-chats)
- [Sending Messages](/guides/messaging/sending-messages)

# Message details

> Read Message direction, delivery state, timestamps, parts, reactions, and history.

Source: https://docs.staging.relayapp.im/guides/messaging/message-details.md

Retrieve one visible Message by ID.

## Retrieve a Message

<CodeGroup>
```typescript TypeScript SDK
const message = await relay.messages.retrieve(messageId);
```

```bash cURL
curl -sS https://api.relayapp.im/v1/messages/$MESSAGE_ID \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

## Read the response

| Field | Meaning |
| --- | --- |
| `chat_id` | Chat containing the Message |
| `from` | Sender Handle |
| `from_handle` | Sender Contact projection |
| `parts` | Ordered content and current reactions |
| `reply_to` | Target Message and part, or `null` |
| `is_from_me` | Derived for the authenticated Contact |
| `delivery_status` | Aggregate `sent`, `delivered`, or `read` state |
| `created_at` | Canonical Message time |
| `delivered_at`, `read_at` | Aggregate receipt times |
| `deliveries` | Per-recipient receipt state |

## Direction

`is_from_me` is relative to the authenticated Contact. It is `true` for the
sender and `false` for each recipient.

Read [Delivery receipts](/guides/messaging/delivery-receipts) for exact user
and agent Delivered boundaries. Webhook and WebSocket acknowledgements affect
event transport only.

## List Chat history

<CodeGroup>
```typescript TypeScript SDK
const page = await relay.chats.messages.list(chatId, { limit: 50 });
```

```bash cURL
curl -sS \
  "https://api.relayapp.im/v1/chats/$CHAT_ID/messages?limit=50" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

The list can include `is_system_message: true` rows for group history.

## Related

- [Message history](/guides/chats/message-history)
- [Delivery receipts](/guides/messaging/delivery-receipts)

# Message Parts

> Build ordered text, media, and link parts for a Message.

Source: https://docs.staging.relayapp.im/guides/messaging/message-parts.md

Every outbound Message contains 1 to 100 ordered `parts`.

## Part types

| Type | Required content | Use |
| --- | --- | --- |
| `text` | `value` | Plain text and one optional structured mention |
| `media` | `attachment_id` or HTTPS `url` | Image, video, audio, or document |
| `link` | `value` | One URL rendered as a rich preview |

### Text

```json
{
  "type": "text",
  "value": "Relay accepted the request."
}
```

Text must contain 1 to 10,000 UTF-16 code units. A mention adds `mention` and
`mention_range`; read [Mentions](/guides/messaging/mentions) before calculating
that range.

### Media

Reference one completed Relay Attachment:

```json
{
  "type": "media",
  "attachment_id": "01993d50-d263-7d6b-87ce-90aba89b7815"
}
```

For public files up to 10 MB, use one HTTPS URL instead:

```json
{
  "type": "media",
  "url": "https://cdn.example.com/photo.webp"
}
```

Use exactly one of `attachment_id` or `url`.

### Link

```json
{
  "type": "link",
  "value": "https://relayapp.im"
}
```

**A link must be the only part in its Message.** Put a URL inside a text part
when it should remain ordinary text.

## Ordering and composition

`part_index` is the zero-based position in the array. Replies and reactions
use it to target one exact part.

Text and media may be mixed in order. Consecutive text parts are rejected;
combine adjacent text into one part. A link must remain the only part in its
Message.

## Response-only parts

Relay can return `type: "system"` for visible Chat history such as membership
or group metadata changes. A developer cannot send a system part.

Response parts can also contain current `reactions`. Media responses add the
Attachment ID, opaque download URL, filename, MIME type, size, and available
dimensions or duration.

## Related

- [Sending Messages](/guides/messaging/sending-messages)
- [Message Details](/guides/messaging/message-details)
- [Attachments](/guides/messaging/attachments)
- [Rich Link Previews](/guides/messaging/rich-link-previews)

# Attachments

> Allocate, upload, send, download, and delete Attachments.

Source: https://docs.staging.relayapp.im/guides/messaging/attachments.md

Pre-upload files when you need trusted ownership, metadata, reuse, or files larger than 10 MB.

Staging separates API requests from byte transfer:

| Operation | Staging origin | Request | Access |
| --- | --- | --- | --- |
| Allocate or refresh | `https://api.staging.relayapp.im` | `POST /v1/attachments` or `GET /v1/attachments/{id}` | Agent Token |
| Upload | `https://uploads.staging.relayapp.im` | Raw `PUT` to the returned `upload_url` | Signed URL, valid for 15 minutes |
| Download | `https://cdn.staging.relayapp.im` | `GET` or `HEAD` on the returned `download_url` | Signed bearer URL, valid for 60 minutes |

**The `attachment_id` is permanent for the Attachment.** Store the ID and
treat upload and download URLs as short-lived capabilities.

## 1. Create an upload

<CodeGroup>
```typescript TypeScript SDK
const allocation = await relay.attachments.create({
  filename: "report.pdf",
  content_type: "application/pdf",
  size_bytes: bytes.byteLength,
});
```

```bash cURL
curl -sS -X POST https://api.staging.relayapp.im/v1/attachments \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "filename":"report.pdf",
    "content_type":"application/pdf",
    "size_bytes":48291
  }'
```
</CodeGroup>

The response contains an `attachment_id`, signed upload URL, HTTP method, exact required headers, and expiration time.

```json
{
  "attachment_id":"01993d50-d263-7d6b-87ce-90aba89b7815",
  "upload_url":"https://uploads.staging.relayapp.im/attachments/<token>",
  "download_url":"https://cdn.staging.relayapp.im/attachments/<token>",
  "http_method":"PUT",
  "expires_at":"2026-08-29T06:35:00.000Z",
  "required_headers":{
    "Content-Type":"application/pdf",
    "Content-Length":"48291"
  }
}
```

## 2. Upload the raw bytes

<CodeGroup>
```typescript TypeScript SDK
await relay.attachments.upload(allocation, bytes);
```

```bash cURL
curl -sS -X PUT "$UPLOAD_URL" \
  -H "Content-Type: application/pdf" \
  -H "Content-Length: 48291" \
  --data-binary @report.pdf
```
</CodeGroup>

Send the file bytes as the complete request body. Do not base64 encode them or
wrap them in multipart form data. The body length and `Content-Type` must match
the allocation.

The upload URL remains valid until `expires_at`, 15 minutes after allocation.
An interrupted or rejected upload remains `pending`, so retry the same URL
with the complete bytes while it is valid.

## 3. Confirm the upload

Retrieve the Attachment before sending it:

<CodeGroup>
```typescript TypeScript SDK
const attachment = await relay.attachments.retrieve(
  allocation.attachment_id,
);

if (attachment.status !== "complete") {
  throw new Error(`Attachment is ${attachment.status}.`);
}
```

```bash cURL
curl -sS \
  "https://api.staging.relayapp.im/v1/attachments/$ATTACHMENT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

Each successful retrieval returns the same permanent ID and a fresh
`download_url` that is valid for 60 minutes.

| Status | Meaning |
| --- | --- |
| `pending` | Relay is waiting for the complete upload |
| `complete` | The Attachment can be downloaded and sent |
| `failed` | The upload expired before completion |

Allocate a new Attachment after an upload expires.

## 4. Send the Attachment

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{
      type: "media",
      attachment_id: allocation.attachment_id,
    }],
  },
});
```

```bash cURL
curl -sS -X POST "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message":{
      "parts":[{
        "type":"media",
        "attachment_id":"01993d50-d263-7d6b-87ce-90aba89b7815"
      }]
    }
  }'
```
</CodeGroup>

## 5. Download or delete

`retrieve` returns a signed `download_url` on
`https://cdn.staging.relayapp.im`. The URL grants bearer access for 60
minutes, so do not store it as the Attachment identity. Call
`GET https://api.staging.relayapp.im/v1/attachments/{id}` with an Agent Token
for a fresh URL at any time.

<Info>
`GET /v1/attachments/{id}` answers the Contact that allocated the Attachment
and every Contact whose Chat membership covers a Message carrying it. An Agent
in the Chat can mint a fresh `download_url` for media a user sent, so you do
not have to race the link or keep your own copy. `DELETE` stays with the owner
and returns `404` to everyone else.
</Info>

Downloads support one byte range at a time for audio, video, and resumable
file reads:

```bash
curl -sS "$DOWNLOAD_URL" \
  -H "Range: bytes=0-1048575" \
  --output attachment.part
```

A valid range returns `206` with `Content-Range`. An invalid or multiple range
returns `416`.

Delete an unreferenced Attachment:

<CodeGroup>
```typescript TypeScript SDK
await relay.attachments.delete(attachmentId);
```

```bash cURL
curl -sS -X DELETE \
  "https://api.staging.relayapp.im/v1/attachments/$ATTACHMENT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

## Import a public media URL

For files up to 10 MB, a `media` part can use a public HTTPS URL instead of a
pre-uploaded Attachment:

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{
      type: "media",
      url: "https://cdn.yourdomain.com/report.pdf",
    }],
  },
});
```

```bash cURL
curl -sS -X POST "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message":{
      "parts":[{
        "type":"media",
        "url":"https://cdn.yourdomain.com/report.pdf"
      }]
    }
  }'
```
</CodeGroup>

Relay imports the bytes into an owned Attachment before committing the
Message.

| Check | Rule |
| --- | --- |
| Network | Every DNS answer must be on the public network |
| Redirects | Every hop is revalidated, with at most five redirects |
| Response | Final response must succeed |
| Size | Declared and actual body must each be 1 byte to 10 MB |
| Content type | Response Content-Type must be supported |

**Relay rejects credentials in the URL and blocks loopback, private,
link-local, reserved, and internal destinations.** A redirect cannot bypass
these checks.

## Media metadata

| Field | Applies to |
| --- | --- |
| `duration_ms` | Audio and video |
| `width`, `height` | Image and video |
| `filename` | Every Attachment |
| `content_type` | Every Attachment |
| `size_bytes` | Every Attachment |

Width and height must be supplied together.

## Image formats

Relay accepts JPEG, PNG, GIF, HEIC, TIFF, BMP, and WebP images. It rejects
SVG.

## Limits

| Boundary | Value |
| --- | ---: |
| Allocation size | 1 byte to 100 MB |
| Public HTTPS URL import | Up to 10 MB |
| Public URL media parts per Message | 40 |
| Total parts per Message | 100 |

## Ownership

| Action | Authorized Contact |
| --- | --- |
| Send or reuse | Contact that allocated the Attachment |
| Retrieve metadata and refresh the download URL | Contact that allocated the Attachment |
| Delete | Owner, while no Message or group photo references the Attachment |

Relay returns `404` when the caller does not own the Attachment. A signed
transfer URL grants bearer access until it expires, so keep it out of logs and
refresh it through the authenticated API rather than sharing a stored URL.

Media a user sends to you belongs to that user, so its URL cannot be refreshed.
Read [Receiving Media](/guides/messaging/receiving-media) for the inbound
path.

## Related

- [Receiving Media](/guides/messaging/receiving-media)
- [Voice memos](/guides/messaging/voice-memos)
- [Sending Messages](/guides/messaging/sending-messages)

# Voice memos

> Send an uploaded audio Attachment as a voice memo.

Source: https://docs.staging.relayapp.im/guides/messaging/voice-memos.md

Upload audio first, then send it through the dedicated voice memo route.

## Upload audio

<CodeGroup>
```typescript TypeScript SDK
const allocation = await relay.attachments.create({
  filename: "answer.m4a",
  content_type: "audio/x-m4a",
  size_bytes: bytes.byteLength,
  duration_ms: 12_400,
});

await relay.attachments.upload(allocation, bytes);
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/attachments \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "filename":"answer.m4a",
    "content_type":"audio/x-m4a",
    "size_bytes":183422,
    "duration_ms":12400
  }'
```
</CodeGroup>

Upload the raw bytes to the returned URL.

## Send the voice memo

<CodeGroup>
```typescript TypeScript SDK
const result = await relay.chats.sendVoicememo(chatId, {
  attachment_id: allocation.attachment_id,
});
```

```bash cURL
curl -sS -X POST \
  https://api.relayapp.im/v1/chats/$CHAT_ID/voicememo \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"attachment_id":"'$ATTACHMENT_ID'"}'
```
</CodeGroup>

Relay rejects non-audio Attachments on this route.

You can supply `voice_memo_url` instead of `attachment_id` for a public HTTPS
audio URL up to 10 MB. Relay applies the same public-network, redirect, size,
and Content-Type checks as other [URL media](/guides/messaging/attachments#import-a-public-media-url).

## Read the response

The response includes the Message ID, Chat, recipients, delivery status, and
audio metadata.

## Related

- [Attachments](/guides/messaging/attachments)
- [Message details](/guides/messaging/message-details)

# Receiving Media

> Read an inbound media part, download the bytes, and pass the file to a model.

Source: https://docs.staging.relayapp.im/guides/messaging/receiving-media.md

A user sends an image, voice memo, video, or document as a `media` part on a
`message.received` event. Download the bytes from the part's signed URL, then
pass them to your model.

## Inbound media parts

Every inbound `media` part carries the Attachment identity, one signed download
URL, and the file metadata:

```json
{
  "type": "media",
  "id": "01993d50-d263-7d6b-87ce-90aba89b7815",
  "url": "https://cdn.staging.relayapp.im/attachments/<token>",
  "filename": "report.pdf",
  "mime_type": "application/pdf",
  "size_bytes": 48291,
  "reactions": null
}
```

| Field | Meaning |
| --- | --- |
| `id` | Permanent Attachment ID |
| `url` | Signed link that returns the raw bytes |
| `filename` | File name the sender supplied |
| `mime_type` | IANA media type of the bytes |
| `size_bytes` | Exact byte length |

The full event envelope is on
[Webhook Event Types](/guides/webhooks/events#media-payload).

## Download the bytes

The signature inside `url` is the whole credential. Send the request with no
`Authorization` header.

<CodeGroup>
```typescript TypeScript
const part = event.data.parts.find((item) => item.type === "media");

const response = await fetch(part.url);
if (!response.ok) {
  throw new Error(`Attachment download returned ${response.status}.`);
}

const bytes = new Uint8Array(await response.arrayBuffer());
```

```bash cURL
curl -sS "$MEDIA_URL" --output inbound-media
```
</CodeGroup>

The response carries the Attachment `Content-Type` and `Content-Length`, and
returns the complete file in one body.

## URL lifetime

`url` is valid for 60 minutes from the moment Relay builds the event.

Relay stores the event body once. A retried webhook delivery, a WebSocket
replay, and a FULL sync all carry the same `url`, so the 60 minutes run from
the moment Relay built the event, not from the moment your receiver read it.

<Info>
`GET /v1/attachments/{id}` answers every Contact whose Chat membership covers a
Message carrying the Attachment. Call it with your Agent Token at any time for
a fresh `url`, so an expired link costs one request and never the file. Keep
your own copy of the bytes only when your product wants one.
</Info>

## Pass the file to a model

Relay carries the Message and never calls a model. Send the downloaded bytes to
your own provider as a file part. This example uses the Vercel AI SDK:

```typescript TypeScript
import { generateText } from "ai";

const result = await generateText({
  model,
  messages: [{
    role: "user",
    content: [
      { type: "text", text: "Describe what the user sent." },
      {
        type: "file",
        data: bytes,
        mediaType: part.mime_type,
        filename: part.filename,
      },
    ],
  }],
});
```

`mediaType` takes `mime_type` unchanged, and `data` accepts the `Uint8Array`
you downloaded.

A Relay Attachment runs to 100 MB, which can exceed the inline request limit of
the model you call. Read the limit your provider publishes and upload larger
files through that provider's own file API. Google's Gemini API accepts up to
100 MB of inline data per request, and 50 MB for a PDF.

## Related

- [Webhook Event Types](/guides/webhooks/events)
- [Attachments](/guides/messaging/attachments)
- [Message Parts](/guides/messaging/message-parts)
- [Voice memos](/guides/messaging/voice-memos)

# Rich link previews

> Send one URL as a link part.

Source: https://docs.staging.relayapp.im/guides/messaging/rich-link-previews.md

Send a `link` part when a URL should be the Message's primary content.

## Send a link part

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{
      type: "link",
      value: "https://relayapp.im/@weather.acme",
    }],
  },
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/chats/$CHAT_ID/messages \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": {
      "parts": [{
        "type":"link",
        "value":"https://relayapp.im/@weather.acme"
      }]
    }
  }'
```
</CodeGroup>

## Composition rules

- A link must use a valid absolute URL.
- Maximum length is 2,048 characters.
- A `link` must be the only part in its Message.
- Relay stores the URL as the link part.

Use a `text` part when the URL belongs inside a larger sentence.

## Start a Chat with a link

`POST /v1/chats` rejects links in the first Message, including HTTP or HTTPS
URLs inside a text part. Use `POST /v1/messages` to resolve or create the Chat
when the first Message is a link, or send the link to an existing Chat.

## Related

- [Sending Messages](/guides/messaging/sending-messages)
- [Message details](/guides/messaging/message-details)

# Replies

> Reply to an exact Message part in a Chat.

Source: https://docs.staging.relayapp.im/guides/messaging/replies.md

Set `reply_to` on a new Message to quote an earlier part.

## Reply to a Message

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{ type: "text", value: "That image looks correct." }],
    reply_to: {
      message_id: messageId,
      part_index: 1,
    },
  },
});
```

```bash cURL
curl -sS -X POST "https://api.relayapp.im/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message":{
      "parts":[{"type":"text","value":"That image looks correct."}],
      "reply_to":{
        "message_id":"01993d50-b4ce-71e6-8e65-35d325d95ddb",
        "part_index":1
      }
    }
  }'
```
</CodeGroup>

## Target a part

- The target Message must be visible in the same Chat.
- `part_index` is zero based.
- The index must exist on the target Message.
- Any non-system text, media, link, or voice memo part can be a target.
- A reply creates a new Message and leaves the target unchanged.

## List a reply thread

<CodeGroup>
```typescript TypeScript SDK
const thread = await relay.messages.listMessagesThread(messageId, {
  order: "asc",
  limit: 50,
});
```

```bash cURL
curl -sS \
  "https://api.relayapp.im/v1/messages/$MESSAGE_ID/thread?order=asc&limit=50" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

Pass `next_cursor` unchanged to read another page.

## Related

- [Message details](/guides/messaging/message-details)
- [Reactions](/guides/messaging/reactions)

# Reactions

> Add or remove a standard or custom reaction on one Message part.

Source: https://docs.staging.relayapp.im/guides/messaging/reactions.md

React to an exact Message part.

## Add a reaction

<CodeGroup>
```typescript TypeScript SDK
await relay.messages.addReaction(messageId, {
  operation: "add",
  type: "love",
  part_index: 0,
});
```

```bash cURL
curl -sS -X POST \
  https://api.relayapp.im/v1/messages/$MESSAGE_ID/reactions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "operation":"add",
    "type":"love",
    "part_index":0
  }'
```
</CodeGroup>

## Reaction types

| Type | Meaning |
| --- | --- |
| `love` | Heart |
| `like` | Thumbs up |
| `dislike` | Thumbs down |
| `laugh` | Laugh |
| `emphasize` | Emphasis |
| `question` | Question |
| `custom` | Unicode from `custom_emoji` |

## Remove a reaction

Send the same `type`, `custom_emoji`, and `part_index` with
`operation: "remove"`.

<CodeGroup>
```typescript TypeScript SDK
await relay.messages.addReaction(messageId, {
  operation: "remove",
  type: "love",
  part_index: 0,
});
```

```bash cURL
curl -sS -X POST \
  https://api.relayapp.im/v1/messages/$MESSAGE_ID/reactions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"operation":"remove","type":"love","part_index":0}'
```
</CodeGroup>

Repeated add or remove operations are idempotent. Each Contact has one
reaction slot on each Message part.

## Events

Agents can subscribe to:

- `reaction.added`
- `reaction.removed`

## Related

- [Webhook Event Types](/guides/webhooks/events)
- [Replies](/guides/messaging/replies)

# Delivery receipts

> Read aggregate and per-recipient Delivered and Read state for direct and group Chats.

Source: https://docs.staging.relayapp.im/guides/messaging/delivery-receipts.md

Every Message response includes `delivery_status`. Receipt timestamps and the
per-recipient `deliveries` array are optional response fields.

## Response fields

| Field | Contract | Meaning |
| --- | --- | --- |
| `delivery_status` | Required | Aggregate `sent`, `delivered`, or `read` state |
| `delivered_at` | Optional, nullable | Time every recipient reached Delivered |
| `read_at` | Optional, nullable | Time every recipient reached Read |
| `deliveries` | Optional | Independent Delivered and Read times by recipient |

```json
{
  "delivery_status": "delivered",
  "delivered_at": "2026-08-29T06:20:04.000Z",
  "read_at": null,
  "deliveries": [
    {
      "contact": {
        "id": "01993d50-ef7b-7b37-886b-23fd80c7ec10",
        "handle": "alice",
        "joined_at": "2026-08-29T06:19:00.000Z",
        "kind": "user",
        "display_name": "Alice",
        "image_url": null,
        "tagline": null,
        "verified": false
      },
      "delivered_at": "2026-08-29T06:20:04.000Z",
      "read_at": null
    }
  ]
}
```

**The aggregate state advances when every recipient reaches that state.** Read
implies Delivered.

## Delivered boundaries

| Recipient | Delivered means |
| --- | --- |
| User | The Relay client application applied the Message on that recipient's device |
| Agent | Relay committed the Message and it is readable through the Relay API |

Webhook `2xx` responses and WebSocket ACKs acknowledge event transport only.
They do not advance Delivered or Read. Model work and replies also advance
separately.

## Mark Read

**Read is optional.** For an Agent, the only operation that advances Read is
`POST /v1/chats/{chatId}/read`.

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.markAsRead(chatId);
```

```bash cURL
curl -sS -X POST \
  "https://api.relayapp.im/v1/chats/$CHAT_ID/read" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

Call the route when the Agent actually reads the Chat's Messages. A transport
acknowledgement or reply does not mark them Read.

## Direct and group presentation

The Relay iOS app shows Delivered and Read labels only in direct Chats. It does
not show those labels in group Chats. The Message API remains the source of
truth for per-recipient state in direct and group Chats.

## Related

- [Message details](/guides/messaging/message-details)
- [Webhook delivery](/guides/webhooks/delivery)
- [WebSocket protocol](/guides/websocket/protocol)

# Chats

> Create and manage direct and group Chats.

Source: https://docs.staging.relayapp.im/guides/chats/index.md

A Chat contains Contacts, Messages, current group metadata, and membership history.

## Chat types

| Type | Active Contacts | Metadata |
| --- | ---: | --- |
| Direct | 2 | Contact names and avatars |
| Group | 3 to 32 | Optional display name and photo |

## Create a Chat

<CodeGroup>
```typescript TypeScript SDK
const { chat } = await relay.chats.create({
  from: "echo.acme",
  to: ["alice"],
  message: {
    parts: [{ type: "text", value: "How can I help?" }],
  },
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/chats \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "from":"echo.acme",
    "to":["alice"],
    "message": {
      "parts":[{"type":"text","value":"How can I help?"}]
    }
  }'
```
</CodeGroup>

The authenticated Handle must match `from`. Every Handle in `to` must belong
to a registered Contact. Each user recipient must have the agent added.

## Chat fields

| Field | Meaning |
| --- | --- |
| `id` | Opaque Chat UUIDv7 |
| `handles` | Current or most recent membership state for each Contact |
| `is_group` | Direct or group Chat |
| `display_name` | Current group name |
| `group_chat_icon` | Signed group-photo download URL |
| `created_at`, `updated_at` | Visible activity bounds for the caller |

## Next steps

<CardGroup cols={2}>
  <Card title="Group Chats" href="/guides/chats/group-chats">Manage membership, names, and photos.</Card>
  <Card title="Message history" href="/guides/chats/message-history">Page through visible Messages and group events.</Card>
  <Card title="Blocked Handles" href="/guides/chats/blocked-handles">Stop direct traffic with a Contact.</Card>
  <Card title="Add requests" href="/guides/contacts/add-requests">Learn when an agent can ask a user to add it.</Card>
  <Card title="Sharing Contact Card" href="/guides/chats/share-contact-card">Share an agent's active card into an existing Chat.</Card>
</CardGroup>

# Group Chats

> Create a group Chat and update its name or photo.

Source: https://docs.staging.relayapp.im/guides/chats/group-chats.md

A group Chat has at least three and at most 32 active Contacts.

## Create a group

`POST /v1/chats` accepts 2 to 31 recipient Handles plus the sender.
Each user recipient must have the agent added.

<CodeGroup>
```typescript TypeScript SDK
const group = await relay.chats.create({
  from: "echo.acme",
  to: ["alice", "bob"],
  message: {
    parts: [{ type: "text", value: "Group created." }],
    idempotency_key: crypto.randomUUID(),
  },
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/chats \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: $IDEMPOTENCY_KEY" \
  -d '{
    "from":"echo.acme",
    "to":["alice","bob"],
    "message":{
      "parts":[{"type":"text","value":"Group created."}]
    }
  }'
```
</CodeGroup>

## Limits

- A group starts with the sender plus 2 to 31 recipient Contacts.
- A group has at most 32 active Contacts.
- Removing or leaving must keep at least three active Contacts.

## Rename the group

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.update(chatId, {
  display_name: "Weekend plans",
});
```

```bash cURL
curl -sS -X PUT "https://api.relayapp.im/v1/chats/$CHAT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"display_name":"Weekend plans"}'
```
</CodeGroup>

## Set a group photo

Upload an image Attachment, then pass its ID:

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.update(chatId, {
  group_chat_icon: attachmentId,
});
```

```bash cURL
curl -sS -X PUT "https://api.relayapp.im/v1/chats/$CHAT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"group_chat_icon":"01993d50-d263-7d6b-87ce-90aba89b7815"}'
```
</CodeGroup>

Use a completed image Attachment owned by the acting Contact. The
`group_chat_icon` response field contains its signed download URL.

## Group metadata events

| Event | Meaning |
| --- | --- |
| `chat.group_name_updated` | Group name changed |
| `chat.group_icon_updated` | Group photo changed |

Membership changes have their own routes, visibility rules, and events.

## Related

- [Participants and Membership](/guides/chats/participants)
- [Typing Indicators](/guides/chats/typing-indicators)
- [Mentions](/guides/messaging/mentions)
- [Delivery Receipts](/guides/messaging/delivery-receipts)

# Participants and Membership

> Manage Contacts and membership periods in a group Chat.

Source: https://docs.staging.relayapp.im/guides/chats/participants.md

Participant routes change a Contact's membership in an existing group Chat.

A participant is a Contact joined to a Chat through its Handle.

## Add a Contact

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.participants.add(chatId, {
  handle: "charlie",
});
```

```bash cURL
curl -sS -X POST \
  https://api.relayapp.im/v1/chats/$CHAT_ID/participants \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"charlie"}'
```
</CodeGroup>

The Contact sees its add event and later activity. Its history starts at
`joined_at`.

## Remove a Contact

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.participants.remove(chatId, {
  handle: "charlie",
});
```

```bash cURL
curl -sS -X DELETE \
  https://api.relayapp.im/v1/chats/$CHAT_ID/participants \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"charlie"}'
```
</CodeGroup>

The removed Contact's visible history ends with its removal event.

## Leave

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.leaveChat(chatId);
```

```bash cURL
curl -sS -X POST \
  https://api.relayapp.im/v1/chats/$CHAT_ID/leave \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

Adding, removing, or leaving must keep at least three active Contacts.

## Membership periods

| Field | Meaning |
| --- | --- |
| `status` | `active`, `left`, or `removed` |
| `joined_at` | Start of the current visible period |
| `left_at` | End of that period, or `null` |

A rejoin starts a new visible period. The Contact sees Messages whose dates
fall inside its membership periods.

## Events

| Event | Meaning |
| --- | --- |
| `participant.added` | A Contact joined or rejoined |
| `participant.removed` | A Contact left or was removed |

The same changes appear as ordered system Messages in visible Chat history.

## Related

- [Group Chats](/guides/chats/group-chats)
- [Message History](/guides/chats/message-history)
- [Webhook Event Types](/guides/webhooks/events)

# Typing indicators

> Publish and receive typing events for a Chat.

Source: https://docs.staging.relayapp.im/guides/chats/typing-indicators.md

An active Contact can publish typing events for a Chat.

## Start

Call the route when typing begins:

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.startTyping(chatId);
```

```bash cURL
curl -sS -X POST \
  "https://api.relayapp.im/v1/chats/$CHAT_ID/typing" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

Relay returns `204`. Each call emits
`chat.typing_indicator.started` to receiving agents.

## Keep typing active

Send Start every 60 seconds while typing continues. The indicator expires
automatically 85 to 90 seconds after the latest Start call, and Relay emits
`chat.typing_indicator.stopped`.

## Stop

Stop when the composer clears, the Contact cancels, or the app leaves the
Chat:

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.stopTyping(chatId);
```

```bash cURL
curl -sS -X DELETE \
  "https://api.relayapp.im/v1/chats/$CHAT_ID/typing" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

## Receive events

Receiving agents get these exact event names:

| Event | Meaning |
| --- | --- |
| `chat.typing_indicator.started` | Typing began or refreshed |
| `chat.typing_indicator.stopped` | Typing stopped |

Both agent delivery paths use the same payload:

```json
{
  "event_type": "chat.typing_indicator.started",
  "data": {
    "chat_id": "01993d50-ef7b-7b37-886b-23fd80c7ec10",
    "contact": {
      "id": "01993d50-ef7b-7b37-886b-23fd80c7ec11",
      "handle": "maya",
      "kind": "user"
    }
  }
}
```

`contact` is the authenticated user or agent that published the signal. In a
group, this Contact names the typer.

## API reference

- [Start typing indicator](/api-reference/chats/start-typing-indicator)
- [Stop typing indicator](/api-reference/chats/stop-typing-indicator)

## Related

- [Chats](/guides/chats)
- [Webhook Event Types](/guides/webhooks/events)
- [WebSocket protocol](/guides/websocket/protocol)

# Sharing Contact Card

> Share the authenticated agent's active Contact Card into an existing Chat.

Source: https://docs.staging.relayapp.im/guides/chats/share-contact-card.md

Share an agent's configured name and photo into a specific Chat.

## Before sharing

**The authenticated agent needs an active Contact Card and access to the target Chat.**

1. [Configure the Contact Card](/guides/contact-cards).
2. Retrieve it and confirm `is_active` is `true`.
3. Create or reuse the Chat where the card should appear.

## Share the card

Call the endpoint on the existing Chat:

<CodeGroup>
```typescript TypeScript SDK
await relay.chats.shareContactCard(chatId);
```

```bash cURL
curl -sS -X POST \
  "https://api.relayapp.im/v1/chats/$CHAT_ID/share_contact_card" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

**Send an empty request.** Relay selects the authenticated agent's active
Contact Card.

A `204 No Content` response confirms that Relay accepted the share action.
Each explicit call performs one share action.

## Keep configuration separate

| Action | Endpoint | Result |
| --- | --- | --- |
| Upsert | `POST /v1/contact_card` | Sets or replaces the authenticated active agent's Contact Card |
| Update | `PATCH /v1/contact_card` | Changes the configured name or photo |
| Share | `POST /v1/chats/{chatId}/share_contact_card` | Shares the active card into one existing Chat |

Changing the card updates future shares. Call the Chat endpoint each time the
agent shares its card.

## Related

- [Contact Cards](/guides/contact-cards)
- [Chats](/guides/chats)
- [Sending Messages](/guides/messaging/sending-messages)

# Message history

> Page through visible Messages and ordered group-history events.

Source: https://docs.staging.relayapp.im/guides/chats/message-history.md

List the portion of a Chat visible to the authenticated Contact.

<CodeGroup>
```typescript TypeScript SDK
const page = await relay.chats.messages.list(chatId, { limit: 50 });
```

```bash cURL
curl -sS \
  "https://api.relayapp.im/v1/chats/$CHAT_ID/messages?limit=50" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

## Pagination

The response contains `messages` and `next_cursor`. Pass the cursor unchanged:

<CodeGroup>
```typescript TypeScript SDK
const nextPage = await page.getNextPage();
```

```bash cURL
curl -sS \
  "https://api.relayapp.im/v1/chats/$CHAT_ID/messages?limit=50&cursor=$NEXT_CURSOR" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

Use a cursor with the list operation that returned it.

## Group-history rows

Relay returns group activity in the same ordered timeline:

```json
{
  "is_system_message": true,
  "parts": [{"type":"system","value":"Alice added you"}],
  "system_event": {
    "type":"participant_added",
    "actor":{"handle":"alice","kind":"user"},
    "subject":{"handle":"bob","kind":"user"}
  }
}
```

Replies and reactions target regular Message rows.

## Membership visibility

| Time | Visible |
| --- | --- |
| Before `joined_at` | Hidden |
| At and after `joined_at` | Visible |
| Removal event at `left_at` | Visible |
| After `left_at` | Hidden |
| New rejoin period | Visible from the new `joined_at` |

## Agent recovery

Read Chats and Messages after a restart or an uncertain REST response. Receive
new webhook events through webhooks or WebSocket.

## Related

- [Message details](/guides/messaging/message-details)
- [Group Chats](/guides/chats/group-chats)

# Add requests

> Let users add your agent, or ask a user to add a Premium Handle.

Source: https://docs.staging.relayapp.im/guides/contacts/add-requests.md

Users can add any agent, while an agent with a Premium Handle can ask a user
to add it.

## How Add works

| Handle | Users can add the agent | Agent can Message users who added it | Agent can send an Add request |
| --- | --- | --- | --- |
| Username-scoped Handle, `@name.anything`, free | Yes | Yes | No |
| Premium Handle, `@name` | Yes | Yes | Yes |

When a user adds your agent, Relay creates or reuses their direct Chat and
sends your backend `contact.added`. Use the event's `chat_id` to send the
agent's first Message through the normal Message endpoint.

## Send an Add request

An Add request shows the agent's name and Handle. Send the target user's
Handle without the leading `@`:

<CodeGroup>
```typescript TypeScript SDK
const request = await relay.contactRequests.create({
  handle: "advait",
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/contact_requests \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"advait"}'
```
</CodeGroup>

## Read the response

Relay returns `201` when it accepts the request:

```json
{
  "state": "pending"
}
```

For the user's privacy, `pending` reveals only that Relay accepted the
request. `contact.added` is the signal that the user added the agent.

| Status | Meaning |
| --- | --- |
| `201` | Relay accepted the Add request |
| `402` | The authenticated agent does not have a Premium Handle |
| `403` | The request is not allowed |
| `404` | The target Handle does not resolve to an active user |

## Receive Contact events

`contact.added` includes the user Contact and the direct Chat to use:

```json
{
  "api_version": "v1",
  "webhook_version": "2026-08-30",
  "event_type": "contact.added",
  "event_id": "01993d50-4133-7178-8e16-7c1455c91d43",
  "created_at": "2026-08-31T16:00:00.000Z",
  "trace_id": "b923d71e50be43ba9e0fe1e34a7676c2",
  "agent_id": "01993d50-d2a8-7fe2-8b76-9eaf04816377",
  "data": {
    "contact": {
      "id": "01993d50-ef7b-7b37-886b-23fd80c7ec10",
      "handle": "advait",
      "display_name": "Advait"
    },
    "chat_id": "01993d50-754d-7f51-a51b-5da552024fd1"
  }
}
```

| Event | Meaning | Data disclosed |
| --- | --- | --- |
| `contact.added` | A user added the receiving agent | User Contact `id`, `handle`, and `display_name`, plus `chat_id` |
| `contact.removed` | A user removed or blocked the receiving agent | User Contact `id`, `handle`, and `display_name` |

These events use the agent's configured [event path](/guides/webhooks).

## Related

- [Sending Messages](/guides/messaging/sending-messages)
- [Webhook Event Types](/guides/webhooks/events)
- [Webhooks](/guides/webhooks)
- [Authentication](/getting-started/authentication)

# Contact Cards

> Configure the name and photo an agent can share into a Chat.

Source: https://docs.staging.relayapp.im/guides/contact-cards.md

A Contact Card stores the name and photo an agent can share with Contacts in a Chat.

## How Contact Cards work

**Configuration and sharing are separate actions.**

<Steps>
  <Step title="Configure the card">
    Upsert the authenticated active agent's Contact Card.
  </Step>
  <Step title="Share it into a Chat">
    Call the share endpoint for an existing Chat. Relay uses the active card automatically.
  </Step>
</Steps>

Configuring a Contact Card changes the agent's card. Sharing makes that card available in one Chat.

## Retrieve the card

<CodeGroup>
```typescript TypeScript SDK
const cards = await relay.contactCard.retrieve({
  handle: "weather.acme",
});
```

```bash cURL
curl -sS https://api.relayapp.im/v1/contact_card \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

## Upsert the card

<CodeGroup>
```typescript TypeScript SDK
const card = await relay.contactCard.create({
  handle: "weather.acme",
  first_name: "Weather",
  last_name: "Agent",
  image_url: "https://agent.example/avatar.png",
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/contact_card \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "handle":"weather.acme",
    "first_name":"Weather",
    "last_name":"Agent",
    "image_url":"https://agent.example/avatar.png"
  }'
```
</CodeGroup>

**`POST` is an upsert for the authenticated active agent.** The `handle` must
match the Agent Token. Repeating the request replaces the card fields and
returns the active card instead of failing because a card already exists.

Omitting `last_name` or `image_url` from `POST` clears that field. Use `PATCH`
when omitted fields should keep their current values.

## Update the card

<CodeGroup>
```typescript TypeScript SDK
const card = await relay.contactCard.update({
  handle: "weather.acme",
  image_url: "https://agent.example/new-avatar.png",
});
```

```bash cURL
curl -sS -X PATCH \
  "https://api.relayapp.im/v1/contact_card?handle=weather.acme" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"image_url":"https://agent.example/new-avatar.png"}'
```
</CodeGroup>

Omitted fields keep their current values.

## Fields

| Field | Meaning |
| --- | --- |
| `handle` | Handle owned by the authenticated agent |
| `first_name` | First part of the shared name |
| `last_name` | Optional last part of the shared name |
| `image_url` | URL for the shared photo |
| `is_active` | Whether the card is ready to share |
| `kind` | Always `agent` for this Agent Token |

## Sharing is separate

Share the configured card through the target Chat. Follow
[Sharing Contact Card](/guides/chats/share-contact-card) for the complete
request.

## Related

- [Sharing Contact Card](/guides/chats/share-contact-card)
- [Chats](/guides/chats)
- [API Reference](/api-reference/overview)

# Blocked Handles

> Block direct messages while preserving existing history.

Source: https://docs.staging.relayapp.im/guides/chats/blocked-handles.md

Block a registered Handle for the authenticated Contact.

## Block

<CodeGroup>
```typescript TypeScript SDK
await relay.blockedHandles.block({
  handle: "echo.acme",
  reason: "No direct messages",
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/blocked_handles \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"echo.acme","reason":"No direct messages"}'
```
</CodeGroup>

Blocking is idempotent. The block follows the stable Contact when its Handle
changes.

## Behavior

| Traffic | Result |
| --- | --- |
| Direct Message in either direction | Rejected with `403`, code `2026` |
| Group Message containing both Contacts | Allowed |
| Existing history | Preserved |
| Notification to blocked Contact | None |

The block does not remove an existing Chat. It stops direct Message delivery
while active.

## List

<CodeGroup>
```typescript TypeScript SDK
const result = await relay.blockedHandles.list();
```

```bash cURL
curl -sS https://api.relayapp.im/v1/blocked_handles \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

## Unblock

<CodeGroup>
```typescript TypeScript SDK
await relay.blockedHandles.unblock({ handle: "echo.acme" });
```

```bash cURL
curl -sS -X DELETE https://api.relayapp.im/v1/blocked_handles \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"echo.acme"}'
```
</CodeGroup>

Unblocking a Handle that is not blocked returns `404`, code `2025`.
Unblocking restores direct messaging in an existing Chat.

## Related

- [Chats](/guides/chats)
- [Add requests](/guides/contacts/add-requests)
- [Error Codes](/error)

# Webhooks

> Receive signed webhook events at an HTTPS endpoint.

Source: https://docs.staging.relayapp.im/guides/webhooks/index.md

Save a webhook subscription to deliver webhook events to an HTTPS endpoint you
control.

**See all 15 names and payloads in [Webhook Event Types](/guides/webhooks/events).**

<Note>
  With one or more saved subscriptions, Relay uses active Webhooks that include
  each event's type. It closes connected agent sockets when the first
  subscription is created, then moves matching pending events to those
  Webhooks.
</Note>

## Flow

```text
Relay event → signed HTTP POST → verify → durable insert → 2xx → process
```

## Create a subscription

<CodeGroup>
```typescript TypeScript SDK
const subscription = await relay.webhookSubscriptions.create({
  target_url: "https://agent.example/webhooks/relay",
  subscribed_events: [
    "message.received",
    "message.delivered",
    "message.read",
    "reaction.added",
    "reaction.removed",
  ],
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/webhook-subscriptions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "target_url":"https://agent.example/webhooks/relay",
    "subscribed_events":[
      "message.received",
      "message.delivered",
      "message.read",
      "reaction.added",
      "reaction.removed"
    ]
  }'
```
</CodeGroup>

Save `signing_secret` from the `201` response. Relay returns it once. Delete
the last subscription to move pending events to
[WebSocket](/guides/websocket).

## Verify the signature

Relay uses Standard Webhooks headers:

| Header | Use |
| --- | --- |
| `webhook-id` | Stable event ID |
| `webhook-timestamp` | Unix timestamp in seconds |
| `webhook-signature` | `v1,<base64-signature>` |

Verify the exact raw body before parsing JSON.

<Tabs>
  <Tab title="TypeScript SDK">
    ```typescript
    const event = relay.webhooks.unwrap(rawBody, {
      headers: request.headers,
    });
    ```
  </Tab>
  <Tab title="Signing input">
    ```text
    webhook-id.webhook-timestamp.raw-body
    ```
  </Tab>
</Tabs>

## Acknowledge safely

<Steps>
  <Step title="Verify">Reject invalid signatures before parsing.</Step>
  <Step title="Deduplicate">Insert `event_id` under a unique constraint.</Step>
  <Step title="Commit">Save the exact event or a durable job.</Step>
  <Step title="Respond">Return `200` or `204` within 10 seconds.</Step>
  <Step title="Process">Run tools, model work, and replies after acknowledgement.</Step>
</Steps>

A successful response acknowledges webhook transport only. It does not advance
Delivered or Read. An agent recipient reaches Delivered when Relay commits the
Message and it is readable through the Relay API.

`message.received` names the incoming event. Message state uses `sent`,
`delivered`, or `read`.

## Review with an agent

**Audit a webhook receiver against Relay's durable acceptance boundary.** Copy
this prompt into your coding agent.

```text
You are auditing a codebase that receives Relay webhooks.

This is read-only. Do not change code unless I ask.

1. Read https://docs.relayapp.im/llms.txt, the Webhooks guides, and the current Relay OpenAPI.
2. Locate the webhook route, raw-body handling, signature verification, durable inbox, deduplication, response, and reply code.
3. Prove the signature is checked before JSON parsing.
4. Prove event_id is committed under a uniqueness rule before the 2xx response.
5. Prove duplicates return success without repeating model work, tools, or replies.
6. Prove replies use POST /v1/chats/{chatId}/messages with a stable idempotency key.
7. Prove redirects are not followed and unsafe destination addresses are rejected.
8. Report Check | Status | file:line evidence | Fix.
9. Mark anything you cannot prove as unknown.
```

## Related

- [Subscriptions](/guides/webhooks/subscriptions)
- [Webhook Event Types](/guides/webhooks/events)
- [Delivery and retries](/guides/webhooks/delivery)

# Webhook Event Types

> See every event type available to Relay webhook subscriptions, with a payload example for each.

Source: https://docs.staging.relayapp.im/guides/webhooks/events.md

Use this page to choose the event names for a Webhook subscription. Relay uses the same event envelope inside an acknowledged WebSocket `event` frame, while WebSocket connection and replay behavior lives in the [WebSocket guide](/guides/websocket).

## All event types

| Event | Fires when | Payload in `data` |
| --- | --- | --- |
| `message.sent` | An Agent Message is committed | `MessageEvent` |
| `message.received` | A User Message is committed for the Agent | `MessageEvent` |
| `message.delivered` | a Message reaches the recipient-specific Delivered boundary | `MessageEvent` |
| `message.read` | a recipient marks a Message Read | `MessageEvent` |
| `reaction.added` | a Contact adds a Reaction | `ReactionEventBase` |
| `reaction.removed` | a Contact removes a Reaction | `ReactionEventBase` |
| `participant.added` | a Contact joins a group Chat | `ParticipantAddedEvent` |
| `participant.removed` | a Contact leaves or is removed from a group Chat | `ParticipantRemovedEvent` |
| `chat.created` | Relay creates a Chat for the Agent | `ChatCreatedEvent` |
| `chat.group_name_updated` | a group Chat name changes | `ChatGroupNameUpdatedEvent` |
| `chat.group_icon_updated` | a group Chat icon changes | `ChatGroupIconUpdatedEvent` |
| `chat.typing_indicator.started` | a Contact starts or refreshes typing | `ChatTypingIndicatorStartedEvent` |
| `chat.typing_indicator.stopped` | a Contact stops typing | `ChatTypingIndicatorStoppedEvent` |
| `contact.added` | a User adds the Agent | `ContactAddedEvent` |
| `contact.removed` | a User removes or blocks the Agent | `ContactRemovedEvent` |

## List event types

The API returns the authoritative list of names accepted by `subscribed_events`.

<CodeGroup>
```typescript TypeScript SDK
const { events } = await relay.webhookEvents.list();
```

```bash cURL
curl -sS https://api.relayapp.im/v1/webhook-events \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

## Envelope

Every Webhook request has the same top-level shape. The `event_type` selects the shape of `data`.

Use this fixed payload version: `2026-08-30`.

```json
{
  "api_version": "v1",
  "webhook_version": "2026-08-30",
  "event_type": "contact.added",
  "event_id": "00000000-0000-7000-8000-000000000010",
  "created_at": "2026-08-31T06:00:00.000Z",
  "trace_id": "0123456789abcdef0123456789abcdef",
  "agent_id": "00000000-0000-7000-8000-000000000011",
  "data": {
    "contact": {
      "id": "00000000-0000-7000-8000-000000000012",
      "handle": "advait",
      "display_name": "Advait"
    },
    "chat_id": "00000000-0000-7000-8000-000000000013"
  }
}
```

Commit `event_id` under a uniqueness rule before returning the Webhook `2xx`. Relay may retry the same event with the same ID and body.

## Event payload examples

The following blocks show the event-specific `data` object. The top-level envelope stays the same for every event.

### `message.sent`

```json
{
  "chat": { "id": "00000000-0000-7000-8000-000000000101", "is_group": false },
  "id": "00000000-0000-7000-8000-000000000102",
  "idempotency_key": "reply-000000000102",
  "direction": "outbound",
  "sender_handle": {
    "id": "00000000-0000-7000-8000-000000000103",
    "handle": "relay",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "agent",
    "display_name": "Relay",
    "image_url": null,
    "tagline": "A Relay agent",
    "verified": true
  },
  "parts": [{ "type": "text", "value": "Hello from Relay" }],
  "sent_at": "2026-08-31T06:01:00.000Z",
  "delivered_at": null,
  "read_at": null,
  "reply_to": null
}
```

### `message.received`

```json
{
  "chat": { "id": "00000000-0000-7000-8000-000000000111", "is_group": false },
  "id": "00000000-0000-7000-8000-000000000112",
  "idempotency_key": null,
  "direction": "inbound",
  "sender_handle": {
    "id": "00000000-0000-7000-8000-000000000113",
    "handle": "advait",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "user",
    "display_name": "Advait",
    "image_url": null,
    "tagline": null,
    "verified": false
  },
  "parts": [{ "type": "text", "value": "Can you check this?" }],
  "sent_at": "2026-08-31T06:02:00.000Z",
  "delivered_at": null,
  "read_at": null,
  "reply_to": null
}
```

### Media payload

Use the `media` part fields when a Message contains an Attachment. The `url` is a signed Relay download URL.

```json
{
  "chat": { "id": "00000000-0000-7000-8000-000000000115", "is_group": false },
  "id": "00000000-0000-7000-8000-000000000116",
  "idempotency_key": null,
  "direction": "inbound",
  "sender_handle": {
    "id": "00000000-0000-7000-8000-000000000117",
    "handle": "advait",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "user",
    "display_name": "Advait",
    "image_url": null,
    "tagline": null,
    "verified": false
  },
  "parts": [{
    "type": "media",
    "id": "00000000-0000-7000-8000-000000000118",
    "url": "https://cdn.relayapp.im/attachments/opaque-download-token",
    "filename": "report.pdf",
    "mime_type": "application/pdf",
    "size_bytes": 48291
  }],
  "sent_at": "2026-08-31T06:02:00.000Z",
  "delivered_at": null,
  "read_at": null,
  "reply_to": null
}
```

### `message.delivered`

```json
{
  "chat": { "id": "00000000-0000-7000-8000-000000000121", "is_group": false },
  "id": "00000000-0000-7000-8000-000000000122",
  "idempotency_key": "reply-000000000122",
  "direction": "outbound",
  "sender_handle": {
    "id": "00000000-0000-7000-8000-000000000123",
    "handle": "relay",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "agent",
    "display_name": "Relay",
    "image_url": null,
    "tagline": "A Relay agent",
    "verified": true
  },
  "parts": [{ "type": "text", "value": "The result is ready." }],
  "sent_at": "2026-08-31T06:03:00.000Z",
  "delivered_at": "2026-08-31T06:03:01.000Z",
  "read_at": null,
  "reply_to": null
}
```

### `message.read`

```json
{
  "chat": { "id": "00000000-0000-7000-8000-000000000131", "is_group": false },
  "id": "00000000-0000-7000-8000-000000000132",
  "idempotency_key": "reply-000000000132",
  "direction": "outbound",
  "sender_handle": {
    "id": "00000000-0000-7000-8000-000000000133",
    "handle": "relay",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "agent",
    "display_name": "Relay",
    "image_url": null,
    "tagline": "A Relay agent",
    "verified": true
  },
  "parts": [{ "type": "text", "value": "Thanks." }],
  "sent_at": "2026-08-31T06:04:00.000Z",
  "delivered_at": "2026-08-31T06:04:01.000Z",
  "read_at": "2026-08-31T06:04:02.000Z",
  "reply_to": null
}
```

### `reaction.added`

```json
{
  "chat_id": "00000000-0000-7000-8000-000000000141",
  "from_handle": {
    "id": "00000000-0000-7000-8000-000000000142",
    "handle": "advait",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "user",
    "display_name": "Advait",
    "image_url": null,
    "tagline": null,
    "verified": false
  },
  "message_id": "00000000-0000-7000-8000-000000000143",
  "part_index": 0,
  "reaction_type": "like",
  "custom_emoji": null,
  "is_from_me": false,
  "reacted_at": "2026-08-31T06:05:00.000Z"
}
```

### `reaction.removed`

```json
{
  "chat_id": "00000000-0000-7000-8000-000000000151",
  "from_handle": {
    "id": "00000000-0000-7000-8000-000000000152",
    "handle": "advait",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "user",
    "display_name": "Advait",
    "image_url": null,
    "tagline": null,
    "verified": false
  },
  "message_id": "00000000-0000-7000-8000-000000000153",
  "part_index": 0,
  "reaction_type": "like",
  "custom_emoji": null,
  "is_from_me": false,
  "reacted_at": "2026-08-31T06:06:00.000Z"
}
```

### `participant.added`

```json
{
  "chat_id": "00000000-0000-7000-8000-000000000161",
  "participant": {
    "id": "00000000-0000-7000-8000-000000000162",
    "handle": "morgan",
    "status": "active",
    "joined_at": "2026-08-31T06:07:00.000Z",
    "left_at": null,
    "is_me": false,
    "kind": "user",
    "display_name": "Morgan",
    "image_url": null,
    "tagline": null,
    "verified": false
  },
  "added_at": "2026-08-31T06:07:00.000Z"
}
```

### `participant.removed`

```json
{
  "chat_id": "00000000-0000-7000-8000-000000000171",
  "participant": {
    "id": "00000000-0000-7000-8000-000000000172",
    "handle": "morgan",
    "status": "removed",
    "joined_at": "2026-08-31T06:07:00.000Z",
    "left_at": "2026-08-31T06:08:00.000Z",
    "is_me": false,
    "kind": "user",
    "display_name": "Morgan",
    "image_url": null,
    "tagline": null,
    "verified": false
  },
  "removed_at": "2026-08-31T06:08:00.000Z"
}
```

### `chat.created`

```json
{
  "id": "00000000-0000-7000-8000-000000000181",
  "display_name": "Relay, Advait",
  "group_chat_icon": null,
  "handles": [
    {
      "id": "00000000-0000-7000-8000-000000000182",
      "handle": "relay",
      "status": "active",
      "joined_at": "2026-08-31T06:00:00.000Z",
      "left_at": null,
      "is_me": true,
      "kind": "agent",
      "display_name": "Relay",
      "image_url": null,
      "tagline": "A Relay agent",
      "verified": true
    },
    {
      "id": "00000000-0000-7000-8000-000000000183",
      "handle": "advait",
      "status": "active",
      "joined_at": "2026-08-31T06:00:00.000Z",
      "left_at": null,
      "is_me": false,
      "kind": "user",
      "display_name": "Advait",
      "image_url": null,
      "tagline": null,
      "verified": false
    }
  ],
  "is_group": false,
  "created_at": "2026-08-31T06:00:00.000Z",
  "updated_at": "2026-08-31T06:00:00.000Z"
}
```

### `chat.group_name_updated`

```json
{
  "chat_id": "00000000-0000-7000-8000-000000000191",
  "old_value": "Relay, Advait",
  "new_value": "Launch team",
  "changed_by_handle": {
    "id": "00000000-0000-7000-8000-000000000192",
    "handle": "advait",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "user",
    "display_name": "Advait",
    "image_url": null,
    "tagline": null,
    "verified": false
  },
  "updated_at": "2026-08-31T06:09:00.000Z"
}
```

### `chat.group_icon_updated`

```json
{
  "chat_id": "00000000-0000-7000-8000-000000000201",
  "old_value": null,
  "new_value": "https://cdn.relayapp.im/chats/00000000-0000-7000-8000-000000000202.png",
  "changed_by_handle": {
    "id": "00000000-0000-7000-8000-000000000203",
    "handle": "advait",
    "joined_at": "2026-08-31T06:00:00.000Z",
    "kind": "user",
    "display_name": "Advait",
    "image_url": null,
    "tagline": null,
    "verified": false
  },
  "updated_at": "2026-08-31T06:10:00.000Z"
}
```

### `chat.typing_indicator.started`

```json
{
  "chat_id": "00000000-0000-7000-8000-000000000211",
  "contact": {
    "id": "00000000-0000-7000-8000-000000000212",
    "handle": "advait",
    "kind": "user"
  }
}
```

### `chat.typing_indicator.stopped`

```json
{
  "chat_id": "00000000-0000-7000-8000-000000000221",
  "contact": {
    "id": "00000000-0000-7000-8000-000000000222",
    "handle": "advait",
    "kind": "user"
  }
}
```

### `contact.added`

```json
{
  "contact": {
    "id": "00000000-0000-7000-8000-000000000231",
    "handle": "advait",
    "display_name": "Advait"
  },
  "chat_id": "00000000-0000-7000-8000-000000000232"
}
```

### `contact.removed`

```json
{
  "contact": {
    "id": "00000000-0000-7000-8000-000000000241",
    "handle": "advait",
    "display_name": "Advait"
  }
}
```

## Payload fields

| Field | Meaning |
| --- | --- |
| `api_version` | Relay API version, currently `v1` |
| `webhook_version` | Payload version, currently `2026-08-30` |
| `event_type` | Event name |
| `event_id` | Stable deduplication key |
| `created_at` | Event creation time |
| `trace_id` | Event correlation ID |
| `agent_id` | Receiving Agent Contact ID |
| `data` | Event-specific payload |

Message media parts include an Attachment ID and a signed download URL. If the URL expires, refresh it with `GET /v1/attachments/{attachmentId}`.

## Delivery paths

| Path | Event location | Accept after |
| --- | --- | --- |
| Webhook | The envelope is the HTTPS request body | Persisting `event_id`, then return `2xx` |
| WebSocket | The envelope is inside an `event` frame | Persisting `event_id`, then send a cumulative ACK |

Relay retains pending events for 30 days. Pending events keep the same `event_id` when delivery moves between Webhooks and WebSocket.

## Related

- [Webhooks](/guides/webhooks)
- [Webhook subscriptions](/guides/webhooks/subscriptions)
- [Webhook delivery](/guides/webhooks/delivery)
- [WebSocket frames](/guides/websocket/protocol)
- [Delivery receipts](/guides/messaging/delivery-receipts)

# Webhook Subscriptions

> Create, list, update, and delete webhook subscriptions.

Source: https://docs.staging.relayapp.im/guides/webhooks/subscriptions.md

A subscription connects one agent to one target URL and event set.

**With one or more saved subscriptions, Relay delivers each event to every
active subscription that includes its type.** Keep every required event type
covered by an active subscription.

## Create

<CodeGroup>
```typescript TypeScript SDK
const subscription = await relay.webhookSubscriptions.create({
  target_url: "https://agent.example/webhooks/relay",
  subscribed_events: ["message.received"],
});
```

```bash cURL
curl -sS -X POST https://api.relayapp.im/v1/webhook-subscriptions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "target_url":"https://agent.example/webhooks/relay",
    "subscribed_events":["message.received"]
  }'
```
</CodeGroup>

The target must use HTTPS. At delivery time, Relay rejects any URL that
resolves to localhost, a private network, a link-local network, or a cloud
metadata address. A redirect ends that delivery attempt.

Creating the first subscription closes every connected agent socket. Matching
pending events then move to active Webhooks with their original `event_id`.

## Store the signing secret

```json
{
  "id":"01993d50-34ca-7613-8df2-d3f8cc975d04",
  "target_url":"https://agent.example/webhooks/relay",
  "subscribed_events":["message.received"],
  "is_active":true,
  "signing_secret":"whsec_<base64-key>",
  "created_at":"2026-08-29T06:20:00.000Z",
  "updated_at":"2026-08-29T06:20:00.000Z"
}
```

`signing_secret` appears only in the create response.

Every event uses the fixed `2026-08-30` webhook payload version.

## List, retrieve, update, or delete

| Operation | Path |
| --- | --- |
| List | `relay.webhookSubscriptions.list()` |
| Retrieve | `relay.webhookSubscriptions.retrieve(subscriptionId)` |
| Update | `relay.webhookSubscriptions.update(subscriptionId, body)` |
| Delete | `relay.webhookSubscriptions.delete(subscriptionId)` |
| Event catalog | `relay.webhookEvents.list()` |

Target URLs must be unique per agent.

Delete a subscription by ID:

<CodeGroup>
```typescript TypeScript SDK
await relay.webhookSubscriptions.delete(subscriptionId);
```

```bash cURL
curl -sS -X DELETE \
  "https://api.relayapp.im/v1/webhook-subscriptions/$SUBSCRIPTION_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

Deleting the last subscription moves pending events to WebSocket. Events wait
durably while the agent backend is disconnected. Exact WebSocket replay
remains available for 30 days.

## Related

- [Webhooks](/guides/webhooks)
- [Webhook Event Types](/guides/webhooks/events)

# Webhook delivery

> Handle webhook timeouts, retries, duplicates, and terminal delivery.

Source: https://docs.staging.relayapp.im/guides/webhooks/delivery.md

Relay delivers each webhook at least once and keeps its request body and
`event_id` unchanged across retries.

## Delivery policy

| Setting | Value |
| --- | --- |
| Initial delivery | 1 immediate attempt |
| Retries | Up to 10 |
| Response window | 10 seconds per attempt |
| Backoff | Exponential from 2 seconds to a 10-minute cap |
| Total failed attempts | 11 |
| Duplicate delivery | Possible |

The nominal retry delays are `2s`, `4s`, `8s`, `16s`, `32s`, `64s`, `128s`,
`256s`, `512s`, and `600s`.

## Retry classes

| Result | Relay action |
| --- | --- |
| HTTP `2xx` | Succeed |
| HTTP `429` | Retry |
| HTTP `5xx` | Retry |
| Response timeout | Retry |
| Temporary connection or network failure | Retry |
| Invalid URL or unsafe destination | Terminal |
| HTTP `3xx` | Terminal, redirect is not followed |
| Any other HTTP response | Terminal |

Relay marks a retryable delivery dead only after the initial attempt and all
ten retries fail. A terminal response stops earlier.

Relay resolves the destination for each attempt. It rejects localhost,
private, link-local, and cloud metadata addresses before sending.

## Receiver pattern

```text
verify signature
  → insert event_id if absent
  → commit durable inbox
  → return 2xx
  → process asynchronously
```

If the backend commits but the response is lost, Relay retries with the same
`event_id`. Store that ID under a uniqueness rule and return success for a
duplicate without repeating model work, tools, or replies.

## Delivered meaning

An agent recipient reaches Delivered when Relay commits the Message and it is
readable through the Relay API. A webhook `2xx` acknowledges event transport
only. It does not advance Delivered or Read.

Commit the event durably before returning `2xx` so retries remain safe.

## Terminal delivery

Relay stops after a terminal response or after every retryable attempt fails.
Recover current Chat and Message state through ordinary REST reads.

## Review with an agent

**Audit a webhook receiver for retries and durable deduplication.** Copy this
prompt into your coding agent.

```text
You are auditing a codebase that receives Relay webhooks.

This is read-only. Do not change code unless I ask.

1. Read https://docs.relayapp.im/llms.txt, the Webhook guides, and the current Relay OpenAPI.
2. Locate raw-body signature verification, the event_id uniqueness rule, durable inbox commit, 2xx response, retry handling, and reply code.
3. Prove the receiver returns within 10 seconds and responds only after its durable commit.
4. Prove duplicate event_id values return success without repeating model work, tools, or replies.
5. Prove redirects are not followed and SSRF checks reject non-public destinations.
6. Prove ordinary 4xx responses are used only for terminal requests.
7. Prove recovery uses ordinary REST reads after terminal delivery.
8. Report Check | Status | file:line evidence | Fix.
9. Mark anything you cannot prove as unknown.
```

## Related

- [Webhooks](/guides/webhooks)
- [Webhook Event Types](/guides/webhooks/events)
- [Idempotency](/guides/platform/idempotency)

# WebSocket

> Receive webhook events over a durable WebSocket connection.

Source: https://docs.staging.relayapp.im/guides/websocket/index.md

Use WebSocket for an always-on agent backend with zero saved webhook
subscriptions.

**WebSocket carries all 15 [webhook event types](/guides/webhooks/events).**

**A WebSocket connection requires zero saved webhook subscriptions.**

## Select WebSocket delivery

<CodeGroup>
```typescript TypeScript SDK
const { subscriptions } = await relay.webhookSubscriptions.list();

if (subscriptions.length > 0) {
  throw new Error("Delete webhook subscriptions before connecting.");
}
```

```bash cURL
curl -sS https://api.relayapp.im/v1/webhook-subscriptions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```
</CodeGroup>

The subscription list must be empty. A WebSocket upgrade with any saved
subscription returns HTTP `409` before the upgrade completes.

Deleting the last subscription drains pending events to WebSocket with the
same `event_id`. Pending events wait durably while the backend is disconnected,
with a 30-day exact replay window.

## Connect

The TypeScript SDK keeps the connection alive, answers heartbeats, reconnects
with backoff, and sends a cumulative ACK after `onEvent` returns:

```typescript
await relay.websocket.run({
  async onEvent(event) {
    await durableInbox.insertOnce(event.event_id, event);
  },
  async onFullSync({ throughSequence }) {
    const snapshot = await readRelaySnapshot(relay);
    await durableInbox.replaceSnapshot(throughSequence, snapshot);
  },
  onError(error) {
    console.error(error);
  },
});
```

`durableInbox` and `readRelaySnapshot` represent your database and complete
REST recovery. Both callbacks must return only after their writes commit.

For a raw client, upgrade `wss://api.relayapp.im/v1/websocket` with:

```http
Authorization: Bearer $RELAY_AGENT_TOKEN
```

An upgrade URL with a query string returns HTTP `400`.

Creating the first webhook subscription closes every connected agent socket
and moves matching pending events to active Webhooks.

- Use `wss://` and connect only from trusted server infrastructure.
- Remove `Authorization` headers from proxy, access, and error logs.
- Never expose the token to browser JavaScript.
- Revoke and replace the Agent Token if any upgrade log leaks it.

An agent may have multiple connected sockets. They receive the same sequenced
events and share one cumulative checkpoint.

## Review with an agent

**Audit a WebSocket consumer for secure authentication and replay-safe
acknowledgements.** Copy this prompt into your coding agent.

```text
You are auditing a codebase that consumes Relay WebSocket events.

This is read-only. Do not change code unless I ask.

1. Read https://docs.relayapp.im/llms.txt, the WebSocket guides, and the current Relay OpenAPI.
2. Locate the direct wss://api.relayapp.im/v1/websocket upgrade, Authorization header, event persistence, deduplication, cumulative ACK, heartbeat, reconnect, FULL sync, and reply code.
3. Prove the Agent Token never enters a URL, cookie, browser bundle, or log.
4. Prove each ACK is sent only after the event is durably committed.
5. Prove FULL sync rebuilds and commits complete REST state before full_sync_complete.
6. Prove replayed event_id values do not repeat model work, tools, or replies.
7. Prove HTTP 409 stops connection attempts while a webhook subscription exists.
8. Prove the webhook-configured close, revoked, heartbeat_timeout, restart, and server close codes each have an explicit stop or reconnect path.
9. Prove replies use POST /v1/chats/{chatId}/messages with a stable idempotency key.
10. Report Check | Status | file:line evidence | Fix.
11. Mark anything you cannot prove as unknown.
```

## Related

- [WebSocket protocol](/guides/websocket/protocol)
- [WebSocket acknowledgements](/guides/websocket/acknowledgements)
- [WebSocket FULL sync](/guides/websocket/full-sync)
- [Idempotency](/guides/platform/idempotency)

# WebSocket frames

> See every WebSocket frame and the complete Webhook Event Types catalog.

Source: https://docs.staging.relayapp.im/guides/websocket/protocol.md

Relay exchanges JSON text frames after the authenticated WebSocket upgrade.

| Frame | Direction | Purpose |
| --- | --- | --- |
| `ready` | Relay to backend | Confirm the connection and current checkpoint |
| `event` | Relay to backend | Deliver one sequenced [Webhook event](/guides/webhooks/events) |
| `full_sync` | Relay to backend | Pause delivery and set the recovery boundary |
| `error` | Relay to backend | Report a protocol or delivery failure |
| `disconnect` | Relay to backend | State why Relay is closing the connection |
| `ping` | Relay to backend | Check connection liveness |
| `ack` | Backend to Relay | Accept all events through one sequence |
| `full_sync_complete` | Backend to Relay | Commit recovery through the required boundary |
| `pong` | Backend to Relay | Answer a heartbeat |

## Ready frame

Relay sends `ready` after the connection opens:

```json
{
  "type":"ready",
  "connection_id":"01993d50-ef7b-7b37-886b-23fd80c7ec10",
  "acked_through":"41",
  "full_sync_required":false,
  "full_sync_through":null,
  "heartbeat_interval_ms":30000,
  "max_in_flight":64
}
```

| Field | Meaning |
| --- | --- |
| `connection_id` | ID for this connection |
| `acked_through` | Highest sequence Relay has durably accepted |
| `full_sync_required` | Whether normal event delivery is paused for recovery |
| `full_sync_through` | Exact recovery boundary, or `null` |
| `heartbeat_interval_ms` | Server ping interval |
| `max_in_flight` | Maximum unacknowledged events on this connection |

## Event frame

```json
{
  "type":"event",
  "sequence":"42",
  "event": {
    "api_version":"v1",
    "webhook_version":"2026-08-30",
    "event_id":"01993d50-ef7b-7b37-886b-23fd80c7ec11",
    "event_type":"contact.removed",
    "created_at":"2026-08-29T06:20:00.000Z",
    "trace_id":"b923d71e50be43ba9e0fe1e34a7676c2",
    "agent_id":"01993d50-d2a8-7fe2-8b76-9eaf04816377",
    "data": {
      "contact": {
        "id":"01993d50-ef7b-7b37-886b-23fd80c7ec12",
        "handle":"advait",
        "display_name":"Advait"
      }
    }
  }
}
```

Sequences are decimal strings scoped to one receiving agent. Relay sends
events oldest first. A replay can repeat an `event_id`. Read
[WebSocket Acknowledgements](/guides/websocket/acknowledgements) before
accepting an event.

**An `event` frame can carry any of Relay's 15 webhook event types.** See the
[complete event table](/guides/webhooks/events#all-event-types).

## Error frame

```json
{
  "type":"error",
  "code":"stale_connection",
  "message":"This Agent Token is revoked.",
  "fatal":true,
  "retryable":false
}
```

| Field | Action |
| --- | --- |
| `code` | Stable machine-readable error name |
| `message` | Diagnostic explanation |
| `fatal` | Stop using the current connection when `true` |
| `retryable` | Whether a fresh connection can retry the condition |

**A fatal error ends consumption on that connection.** Correct the named
authentication or configuration problem before reconnecting. Use backoff
after a Relay server failure.

| Code | Action |
| --- | --- |
| `invalid_frame` | Correct the client frame |
| `ack_out_of_range` | Correct the cumulative ACK |
| `stale_connection` | Stop the connection and restore valid authentication |
| `ack_failed` | Reconnect with backoff. Relay did not save the ACK |
| `delivery_failed` | Reconnect with backoff. Relay could not load the next event |
| `full_sync_required` | Complete recovery before sending an ACK |
| `full_sync_mismatch` | Send the exact required recovery boundary |

Protocol mistakes require a corrected frame or state before retrying.

## Backpressure

Relay pauses delivery when the connection reaches `ready.max_in_flight`
unacknowledged events. A cumulative ACK opens the next window.

## Heartbeats

Relay sends a JSON ping frame every 30 seconds:

```json
{"type":"ping","sent_at":"2026-08-29T06:20:30.000Z"}
```

Reply with a JSON pong frame:

```json
{"type":"pong"}
```

Relay requires a pong within 60 seconds and closes the connection after that
timeout. Heartbeats prove connection liveness. Event ACKs advance delivery.

## Disconnects

| Reason | Action |
| --- | --- |
| `webhook_configured` | Delete every webhook subscription, then reconnect |
| `revoked` | Stop, restore a valid Agent Token, then reconnect |
| `heartbeat_timeout` | Reconnect with backoff |
| `restart` | Reconnect with backoff |

Close code `1011` is a transient delivery failure. Code `1012` is a Relay
restart. Reconnect to either with exponential backoff and jitter. Code `4401`
means the credential is invalid or revoked. Code `4408` means the heartbeat
timed out. Code `4410` means the first webhook subscription closed every agent
socket and moved matching pending events to active Webhooks.

## Related

- [WebSocket](/guides/websocket)
- [Webhook Event Types](/guides/webhooks/events)
- [WebSocket Acknowledgements](/guides/websocket/acknowledgements)
- [WebSocket FULL Sync](/guides/websocket/full-sync)
- [Best practices](/getting-started/best-practices)

# WebSocket Acknowledgements

> Commit WebSocket events durably before sending a cumulative ACK.

Source: https://docs.staging.relayapp.im/guides/websocket/acknowledgements.md

A WebSocket ACK confirms that an agent backend durably accepted every event
through one sequence.

## Frame

```json
{"type":"ack","through_sequence":"42"}
```

`through_sequence` is a decimal string. It is scoped to one receiving agent.

**ACK is cumulative and monotonic.** Before acknowledging `42`, commit every
event at or below `42` that Relay sent on this connection.

## Safe order

```text
read event
  → insert event_id under a unique constraint
  → commit the event or durable job
  → ACK through_sequence
  → run tools and model work
```

Send the ACK after the durable commit shown above.

## Delivery meaning

An agent recipient reaches Delivered when Relay commits the Message and it is
readable through the Relay API. A WebSocket ACK acknowledges event transport
only. It does not advance Delivered or Read.

Model work and replies advance separately. Read is optional and advances only
through `POST /v1/chats/{chatId}/read`.

## Replay

If a connection closes before the ACK commits, the event can appear again.
Return the existing durable inbox row for a repeated `event_id`. Then advance
the cumulative ACK while running each side effect once.

## Errors

| Code | Meaning | Action |
| --- | --- | --- |
| `ack_out_of_range` | ACK is above the highest sequence Relay sent | Correct the sequence |
| `stale_connection` | The connection can no longer advance the checkpoint | Stop this consumer |
| `full_sync_required` | Normal ACKs are paused | Complete FULL sync first |

## Review with an agent

**Audit cumulative ACK safety before running the consumer.** Copy this prompt
into your coding agent.

```text
You are auditing a Relay WebSocket consumer.

This is read-only. Do not change code unless I ask.

1. Read the current Relay WebSocket, Acknowledgements, FULL Sync, and OpenAPI pages.
2. Locate the event_id uniqueness rule, durable transaction, through_sequence calculation, ACK send, and post-ACK processing.
3. Prove every sequence at or below an ACK is committed before the frame is sent.
4. Prove replayed event_id values cannot repeat tools, model work, or replies.
5. Prove a message.received ACK advances only the transport checkpoint.
6. Report Check | Status | file:line evidence | Fix.
7. Mark anything you cannot prove as unknown.
```

## Related

- [WebSocket](/guides/websocket)
- [WebSocket Protocol](/guides/websocket/protocol)
- [FULL Sync](/guides/websocket/full-sync)
- [Idempotency](/guides/platform/idempotency)

# WebSocket FULL Sync

> Reconnect, replay current events, and rebuild durable state when a checkpoint is outside retention.

Source: https://docs.staging.relayapp.im/guides/websocket/full-sync.md

Reconnect with the same Agent Token and resume from Relay's durable
checkpoint.

## Normal reconnect

1. Upgrade `wss://api.relayapp.im/v1/websocket` with the bearer header.
2. Read `acked_through` from `ready`.
3. Deduplicate replayed events by `event_id`.
4. Commit events in sequence order.
5. Cumulatively ACK the highest committed sequence.

Relay can resend an event after an uncertain connection close. The stable
`event_id` makes that replay safe: every replay retains the same `event_id`.

## When Relay requires FULL sync

If the saved checkpoint falls outside the 30-day delivery retention window,
`ready.full_sync_required` is `true`. Relay then sends:

```json
{
  "type":"full_sync",
  "through_sequence":"4182",
  "reason":"checkpoint_outside_retention"
}
```

**A FULL sync rebuilds durable agent state from REST.** The frame marks the
recovery boundary.

<Steps>
  <Step title="Pause normal ACKs">
    Resume ACKs after `full_sync_required` becomes false.
  </Step>
  <Step title="Read authoritative state">
    Page through `GET /v1/chats`, then read visible history with
    `GET /v1/chats/{chatId}/messages`.
  </Step>
  <Step title="Rebuild durably">
    Commit the recovered Chat, Message, membership, reaction, and receipt
    state before continuing.
  </Step>
  <Step title="Complete the boundary">
    Send the exact `through_sequence` from the `full_sync` frame.
  </Step>
</Steps>

## Commit the snapshot

Treat the REST reads as one recovery unit. Replace the durable inbox after
every Chat page and Message page has been read successfully.

```json
{"type":"full_sync_complete","through_sequence":"4182"}
```

Relay rejects a different sequence with `full_sync_mismatch`. On success, it
marks pending events through that boundary as superseded, advances the
checkpoint, and resumes with events after the boundary.

Completing recovery acknowledges the recovery transport boundary only. It does
not advance Message Delivered or Read state.

## Events during sync

Relay continues assigning sequences while recovery runs.
`through_sequence` sets the boundary covered by the snapshot. After completion,
newer pending events resume. Deduplicate state that appears in both the
snapshot and a later event.

## Retention

Relay retains pending webhook events for 30 days.

## Failure handling

Send `full_sync_complete` after every page is present, every cursor advances,
and the durable snapshot commits. Fix read or storage failures and reconnect
before completing the sync.

## Related

- [WebSocket](/guides/websocket)
- [WebSocket Protocol](/guides/websocket/protocol)
- [WebSocket Acknowledgements](/guides/websocket/acknowledgements)
- [Message History](/guides/chats/message-history)

# Idempotency

> Retry Message sends without creating duplicates.

Source: https://docs.staging.relayapp.im/guides/platform/idempotency.md

Use an idempotency key on every Message send that may be retried.

## Supply a key

Use the header:

```http
Idempotency-Key: reply-01993d50
```

Or set it in the Message:

```json
{
  "message": {
    "idempotency_key":"reply-01993d50",
    "parts":[{"type":"text","value":"Done."}]
  }
}
```

When both are present, they must match.

## Retry behavior

| Request | Result |
| --- | --- |
| Same sender, key, and body | Original Message returned |
| Same sender and key, different body | `409` conflict |
| Different sender, same key | Separate idempotency scope |

Keys can contain 1 to 255 characters.

## Derive reply keys from events

```text
reply:<event_id>
```

This makes webhook and WebSocket redelivery converge on the same reply.

## Event idempotency

Event delivery is at least once. Store `event_id` under a unique constraint
before acknowledgement. Use Message idempotency for sends and event
deduplication for incoming events.

## Related

- [Best practices](/getting-started/best-practices)
- [Webhook delivery](/guides/webhooks/delivery)

# Limits

> Review Message, Attachment, Chat, pagination, and delivery limits.

Source: https://docs.staging.relayapp.im/guides/platform/rate-limits.md

Validate these limits before sending a request.

## Messages

| Limit | Value |
| --- | ---: |
| Parts per Message | 100 |
| Public URL media parts per Message | 40 |
| Text part | 10,000 UTF-16 code units |
| Link URL | 2,048 characters |
| Idempotency key | 255 characters |

## Chats

| Limit | Value |
| --- | ---: |
| Other Contacts in `to` | 31 |
| Total active Contacts | 32 |
| Page size | 1 to 100 |

## Attachments

| Limit | Value |
| --- | ---: |
| Pre-uploaded Attachment | 1 byte to 100 MB |
| HTTPS URL import | Up to 10 MB |
| Upload ticket lifetime | Returned as `expires_at` |

## Webhook events

| Limit | Value |
| --- | ---: |
| Webhook response timeout | 10 seconds |
| Webhook retries after the first attempt | Up to 10 |
| WebSocket active connections | Multiple per agent, sharing one checkpoint |
| WebSocket in-flight window | Returned in `ready.max_in_flight` |

Relay returns `400`, `413`, `415`, or `422` depending on the rejected
boundary. Use the numeric error code for program logic.

## Related

- [Attachments](/guides/messaging/attachments)
- [Error Codes](/error)

# Debugging

> Use trace IDs, event IDs, and resource IDs to debug a Relay agent backend.

Source: https://docs.staging.relayapp.im/guides/platform/debugging.md

Capture Relay identifiers without logging credentials or signed URLs.

## IDs to record

| ID | Use |
| --- | --- |
| `trace_id` | Correlate one failed API request or Webhook event |
| `event_id` | Deduplicate and trace one Webhook event |
| Chat ID | Identify the Chat |
| Message ID | Identify one canonical send |
| Attachment ID | Identify uploaded metadata |
| WebSocket sequence | Diagnose one agent checkpoint |

## Error response

```json
{
  "error": {
    "status":409,
    "code":1005,
    "message":"Idempotency key was already used for different message content.",
    "doc_url":"https://docs.relayapp.im/error/codes/1xxx/1005"
  },
  "success":false,
  "trace_id":"b923d71e50be43ba9e0fe1e34a7676c2"
}
```

Record `trace_id`, numeric `code`, HTTP status, route, and the time of the request.

## Safe logs

Do not log:

- Agent Tokens
- Webhook signing secrets
- WebSocket upgrade `Authorization` headers
- Attachment upload or download URLs
- Raw user Message content unless your privacy policy explicitly permits it

## Event debugging

For webhooks, compare `webhook-id` with the envelope `event_id`. For WebSocket,
record `event_id`, `sequence`, and the last committed `acked_through`.

## Related

- [Error Codes](/error)
- [Webhook delivery](/guides/webhooks/delivery)

# Developer ecosystem

> Choose a Relay package, agent channel, coding-agent package, or runnable example.

Source: https://docs.staging.relayapp.im/ecosystem/index.md

Choose the Relay project that matches where your agent or developer tool runs.

**Webhooks fit serverless-friendly request handlers. Acknowledged WebSocket
delivery fits an always-on process.**

## Choose by runtime

| Project | Current source | Runs as | Delivery path |
| --- | --- | --- | --- |
| [Chat SDK adapter](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/chat-sdk-adapter) | `@relaymessenger/chat-sdk-adapter@0.3.0-staging.0` | Vercel Chat SDK adapter | Signed Webhooks |
| [Cloudflare Think starter](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/cloudflare-think-agent) | Cookbook recipe `0.1.0` | Cloudflare Think messenger | Signed Webhooks |
| [OpenClaw channel](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/openclaw) | `@relaymessenger/openclaw-plugin@0.4.0-staging.0` | OpenClaw gateway channel | Acknowledged WebSocket |
| [Claude Code channel](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/claude-code) | `relay-claude-channel@0.3.0-staging.0` | Claude Code channel process | Acknowledged WebSocket |
| [Relay Hermes](https://github.com/RelayMessenger/Relay-Hermes) | `relay-hermes@1.0.0rc1` | Hermes gateway plugin | Acknowledged WebSocket |

The Webhook projects receive signed HTTP requests and send Messages through
REST. The WebSocket projects commit each event durably before the SDK or plugin
sends a cumulative ACK, then send Messages through REST.

## Choose local tools

| Project | Current source | Use |
| --- | --- | --- |
| [Relay CLI](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/cli) | `@relaymessenger/cli@0.5.0-staging.0` | Terminal commands, profiles, diagnostics, and local event forwarding |
| [Relay MCP](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/mcp) | `@relaymessenger/mcp@0.1.0-staging.1` | Local stdio tools for Relay Chats, Messages, reactions, Read state, Contact Cards, and Add requests |
| [Relay Skill](https://github.com/RelayMessenger/Relay-SDK/tree/staging/skills/relay) | `1.0.0` | Portable Relay guidance and hosted docs MCP configuration |
| [Relay Codex](https://github.com/RelayMessenger/Relay-Codex) | Generated mirror `1.0.0` | Codex plugin generated from the Relay Skill |
| [Relay Cursor](https://github.com/RelayMessenger/Relay-Cursor) | Generated mirror `1.0.0` | Cursor plugin generated from the Relay Skill |
| [Relay Cookbook](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook) | Six canonical recipes | Webhooks, WebSocket, Think, text, image, and voice memo examples |

The local Relay MCP uses an Agent Token to call Relay API operations. The
hosted docs MCP searches documentation and requires no Agent Token.

Relay-SDK is the canonical source for the TypeScript packages, channels, Skill,
generated-distribution tooling, and Cookbook. Relay-Claude-Code is the Claude
plugin install mirror. Relay-Codex and Relay-Cursor are generated install
mirrors. Relay-Hermes remains a separate Python plugin repository.

## Staging package status

| Package | Current npm tags |
| --- | --- |
| `@relaymessenger/sdk@0.3.0-staging.4` | `latest` and `staging` |
| `@relaymessenger/chat-sdk-adapter@0.3.0-staging.0` | `latest` and `staging`, with provenance |
| `@relaymessenger/cli@0.5.0-staging.0` | `latest` and `staging`, with provenance |
| `@relaymessenger/mcp@0.1.0-staging.1` | `latest` and `staging`, with provenance |
| `@relaymessenger/openclaw-plugin@0.4.0-staging.0` | `latest` and `staging`, with provenance |
| `relay-claude-channel@0.3.0-staging.0` | `latest` and `staging`, with provenance |

**All six live `latest` tags select the versions shown above.** Use each
project page's staging command for this docs candidate. Relay-Hermes remains
the Git-installed `relay-hermes@1.0.0rc1` release candidate.

## Related

- [Client SDKs](/getting-started/sdks)
- [Webhook events](/guides/webhooks)
- [Examples](/examples)

# Chat SDK adapter

> Receive Relay Webhooks through Vercel Chat SDK and send retry-safe Relay Messages.

Source: https://docs.staging.relayapp.im/ecosystem/chat-sdk.md

Add Relay to Vercel Chat SDK with the official Relay adapter.

**The adapter uses signed Webhooks, so it fits a serverless-friendly HTTPS
handler.**

Source:
[Relay-SDK `packages/chat-sdk-adapter`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/chat-sdk-adapter)

## Staging package

The current published staging package is
`@relaymessenger/chat-sdk-adapter@0.3.0-staging.0` and targets
`chat@4.39.0`. Both the `latest` and `staging` npm tags select this version.

```bash
npm install chat@4.39.0 @chat-adapter/state-memory@4.39.0 \
  @relaymessenger/chat-sdk-adapter@staging
```

| Registry field | Current value |
| --- | --- |
| npm tags | `latest`, `staging` |
| Published artifact source commit | `469a9c1aafed7e31cdc4e8581df4dd6a34c94e17` |
| Integrity | `sha512-IuWa2VVv3hKArnQPO6SV4Ntq+/9pp7eEIzWgVSBgg6E5pWpVV+hxTFCwfwwBJvmhYjzVgOFxrrk6haL05ANquw==` |
| Provenance | Published by the staging workflow with npm provenance |

## Use the adapter

Relay Chats map to stable Chat SDK thread IDs in the form
`relay:<Chat UUID>`. Mount `chat.webhooks.relay()` at the public HTTPS
destination saved in the Relay Webhook subscription.

```typescript
const relay = createRelayAdapter({
  token: process.env.RELAY_AGENT_TOKEN,
  webhookSecret: process.env.RELAY_WEBHOOK_SECRET,
});
```

The adapter verifies the exact raw Webhook body. A
`message.received` event enters the Chat SDK handler, and each non-empty
response is buffered into one Relay Message.

## Media boundary

**Post a stable public HTTPS media URL through the adapter.**

For local bytes or files:

1. Allocate and upload the bytes with
   `@relaymessenger/sdk@0.3.0-staging.4`.
2. Retain the prepared Attachment identity for recovery.
3. Pass its stable public HTTPS URL to the adapter.

The adapter rejects an automatic local byte or file upload before sending. A
fresh Attachment allocation after an ambiguous send would change the Message
body, so unknown-outcome retries must reuse the prepared identity and URL.

## Delivery behavior

| Send source | Idempotency behavior |
| --- | --- |
| Inbound Webhook turn | `relay-chat-sdk:<event_id>:<send ordinal>` |
| Think Action or another recovered task | Supply `idempotencyKeyResolver` with the stable Action or delivery identity |
| Empty response | Local no-op, with no Relay Message |

Disable adapter typing with `typing: false` when Think owns typing timing.

## Related

- [Cloudflare Think starter](/ecosystem/agent-starter)
- [Attachments](/guides/messaging/attachments)
- [Webhooks](/guides/webhooks)

# Relay CLI

> Use Relay v1 resources, staging profiles, diagnostics, and local event forwarding from a terminal.

Source: https://docs.staging.relayapp.im/ecosystem/cli.md

Use Relay API resources and inspect local configuration from the terminal.

Source:
[Relay-SDK `packages/cli`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/cli)

## Staging package

The current package is `@relaymessenger/cli@0.5.0-staging.0` and requires
Node.js `22.22.3` or newer. Both the `latest` and `staging` npm tags select
this version. Install the published staging tag:

```bash
npm install --global @relaymessenger/cli@staging
relay --version
```

The `relaymessenger` executable remains an alias for `relay`.

## Configure staging

Create an isolated profile and provide the staging Agent Token through stdin:

```bash
relay profiles add staging --api-url https://api.staging.relayapp.im
relay profiles use staging
printf '%s' "$STAGING_RELAY_AGENT_TOKEN" |
  relay auth login --profile staging --token-stdin
relay doctor
```

Every resource command prints JSON. Use `relay --help` for Chats, Messages,
Attachments, blocked Handles, Webhook subscriptions, Contact Cards, and Add
requests.

## Use local event forwarding

`relay events listen` is a local development bridge backed by Relay's
acknowledged WebSocket. It requires an explicit non-production profile and
confirmation that this dedicated Agent may advance its durable checkpoint:

```bash
relay --profile staging events listen --acknowledge-events
relay --profile staging events listen --acknowledge-events \
  --forward-to http://127.0.0.1:3000/relay-events
```

The loopback body carries the original Relay event envelope and
`x-relay-dev-forwarded: 1`, without a Standard Webhooks signature. A non-`2xx`
local response remains unacknowledged, so the local receiver must deduplicate
by `event_id`.

The listener refuses the production API and FULL sync. Use a public signed
Webhook receiver for serverless testing, and keep this listener on an Agent
whose checkpoint has no other owner.

## Related

- [Relay MCP](/ecosystem/mcp)
- [Webhooks](/guides/webhooks)
- [WebSocket](/guides/websocket)

# Relay MCP

> Run local Relay messaging tools over MCP v2 stdio with an Agent Token resolved by the host process.

Source: https://docs.staging.relayapp.im/ecosystem/mcp.md

Give a trusted local MCP client explicit Relay messaging tools.

Source:
[Relay-SDK `packages/mcp`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/mcp)

## Staging package

The current package is `@relaymessenger/mcp@0.1.0-staging.1` and requires
Node.js `22.22.3` or newer. Both the `latest` and `staging` npm tags select
this version. Install the published staging tag:

```bash
npm install --global @relaymessenger/mcp@staging
relay-mcp --version
```

## Configure a local client

The server uses local process-spawned stdio. Resolve the Agent Token from the
environment or the profile shared with Relay CLI:

```json
{
  "mcpServers": {
    "relay": {
      "command": "relay-mcp",
      "env": {
        "RELAY_PROFILE": "staging"
      }
    }
  }
}
```

The server exposes named tools for reading and sending Messages, reactions,
typing and Read state, Contact Cards, and Add requests. Message-send tools
require a caller-supplied idempotency key.

## Choose the MCP surface

| MCP surface | Transport | Credential | Purpose |
| --- | --- | --- | --- |
| Local Relay MCP | Process-spawned stdio | Relay Agent Token | Call Relay API operations |
| Relay docs MCP | Hosted HTTP | None | Search current Relay documentation |

The local server opens no listening port. The MCP host is its security
boundary, so configure it only in a trusted local client.

## Related

- [Relay CLI](/ecosystem/cli)
- [Relay Skill](/ecosystem/skills)
- [Authentication](/getting-started/authentication)

# Relay for OpenClaw

> Connect an OpenClaw gateway to Relay through durable acknowledged WebSocket delivery.

Source: https://docs.staging.relayapp.im/ecosystem/openclaw.md

Run Relay as a native channel inside an always-on OpenClaw gateway.

Source:
[Relay-SDK `packages/openclaw`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/openclaw)

## Staging package

The current package is
`@relaymessenger/openclaw-plugin@0.4.0-staging.0`. Its OpenClaw peer range is
`>=2026.8.1 <2026.9.0`, and it uses
`@relaymessenger/sdk@0.3.0-staging.4`.

Both the `latest` and `staging` npm tags select this version. Install the
published staging tag:

```bash
openclaw plugins install @relaymessenger/openclaw-plugin@staging
```

## Configure the channel

Keep the Agent Token in an owner-only file:

```json
{
  "channels": {
    "relay": {
      "enabled": true,
      "tokenFile": "/run/secrets/relay-agent-token"
    }
  }
}
```

Use `RELAY_BASE_URL` only when the Agent Token belongs to the same
non-production Relay environment.

One gateway can back multiple Relay Contacts. Set `token` or `tokenFile` on
every named account; channel-level credentials belong to the default account.

## Delivery behavior

The channel connects to `GET /v1/websocket` with zero saved Webhook
subscriptions. For every event, it:

1. commits `event_id` in OpenClaw's ingress queue or its local SQLite queue;
2. returns to the Relay SDK after the durable transaction;
3. lets the SDK send the cumulative ACK;
4. starts an OpenClaw turn for every inbound user-authored direct
   `message.received` event.

In a group Chat, a turn starts when a text part's canonical `mention` matches
the Chat's `owner_handle`, or when the Message replies to this agent's Message
in the same Chat.

The SDK owns sequence checks, heartbeat responses, reconnect replay, and FULL
sync. Outbound text uses stable idempotency keys derived from OpenClaw's
delivery queue identity.

## Related

- [WebSocket acknowledgements](/guides/websocket/acknowledgements)
- [WebSocket FULL sync](/guides/websocket/full-sync)
- [Examples](/examples)

# Relay channel for Claude Code

> Connect a running Claude Code session to Relay through durable acknowledged WebSocket delivery.

Source: https://docs.staging.relayapp.im/ecosystem/claude-code.md

Use Relay as a two-way channel for a running Claude Code session.

Canonical source:
[Relay-SDK `packages/claude-code`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/claude-code)

Install mirror:
[RelayMessenger/Relay-Claude-Code](https://github.com/RelayMessenger/Relay-Claude-Code)

## Install the plugin

The current channel package is
`relay-claude-channel@0.3.0-staging.0`, and the bundled Claude plugin uses the
same version. It uses `@relaymessenger/sdk@0.3.0-staging.4` and requires
Claude Code Channels research-preview support.

**Both the `latest` and `staging` npm tags select this published channel
version.** Install the Claude plugin from the exact public staging mirror
through Claude Code's local marketplace.

Clone the staging branch:

```bash
git clone --branch staging \
  https://github.com/RelayMessenger/Relay-Claude-Code.git
```

Then install its local marketplace inside Claude Code:

```text
/plugin marketplace add /absolute/path/to/Relay-Claude-Code
/plugin install relay@relay-messenger
```

The plugin starts disabled. Enable it only for the Claude Code session that
should receive Relay Messages.

## Configure the channel

Claude Code requests:

| Setting | Value |
| --- | --- |
| Relay Agent Token | Agent Token stored in Claude Code secure credential storage |
| Allowed Relay senders | Comma-separated exact user UUIDs or Handles |
| Relay API origin | Origin that issued the Agent Token |

The Agent needs zero saved Webhook subscriptions before the channel connects.

## Delivery behavior

The process connects to `/v1/websocket`, commits the full event and local cursor
in SQLite, then allows the SDK to send its cumulative ACK.

**Only addressed group Messages start Claude turns.** Direct Messages are
eligible. A group Message is eligible when its structured `parts[].mention`
matches this Agent's canonical Handle or when it replies to a Message
authenticated as sent by this Agent in the same Chat.

During FULL sync, the channel reconciles only unread, addressed, allowlisted
inbound Messages. It determines this Agent's Read state from the
`deliveries[]` row whose `contact.is_me` is `true`, never from aggregate
top-level `read_at`.

Claude calls `begin_processing` before handling content. That tool marks the
Chat Read through Relay REST. The separate `reply` tool requires a stable
caller-selected `send_id`; its `chat_id` must be the authenticated origin of
the active turn, and `reply_to_message_id` must be that turn's Message.

`reply` closes the active turn automatically. When Claude finishes without a
reply, it must call `complete_processing`; never leave a Relay turn open.
Failure, supersession, expiry, shutdown, and restart also clear the active
origin.

Claude permission prompts remain local to Claude Code. The channel does not
forward tool inputs or accept permission verdicts through Relay because Claude
Code exposes no reliable turn-complete/cancel signal for that authority.

Keep the process running while Relay should reach the Claude Code session.

## Related

- [WebSocket](/guides/websocket)
- [Delivery receipts](/guides/messaging/delivery-receipts)
- [Idempotency](/guides/platform/idempotency)

# Relay for Hermes

> Connect Hermes Agent to Relay as an always-on acknowledged WebSocket process.

Source: https://docs.staging.relayapp.im/ecosystem/hermes.md

Run Relay as an always-on messaging platform inside Hermes Agent.

Source: [RelayMessenger/Relay-Hermes](https://github.com/RelayMessenger/Relay-Hermes)

## Install the plugin

The current source package is the `relay-hermes@1.0.0rc1` release candidate
for Python `3.11` through `3.13`. Install the Git-hosted directory plugin:

```bash
hermes plugins install RelayMessenger/Relay-Hermes --enable
```

The plugin registers the Hermes platform ID `relayapp`.

## Configure staging

Use a staging Agent Token, staging API origin, and separate SQLite directory:

```bash
export RELAY_AGENT_TOKEN='<staging-agent-token>'
export RELAY_BASE_URL='https://api.staging.relayapp.im'
export RELAY_STATE_DIR="$HOME/.hermes/relay-staging"
hermes gateway run
```

For an always-on host, use `hermes gateway install` and
`hermes gateway start`. The Agent needs zero saved Webhook subscriptions.

## Delivery behavior

For each Relay event, the plugin commits the complete envelope and `event_id`
to SQLite before sending a cumulative WebSocket ACK. It starts a Hermes turn
for an inbound `message.received` event, marks the Chat Read when processing
starts, and sends through Relay REST with an idempotency key.

During FULL sync, historical Messages rebuild the local snapshot. Normal
Hermes turns begin from newly delivered events.

Relay slash-command Messages are ignored before Hermes dispatch. The pinned
Hermes core does not resolve slash authority per profile, so Relay fails
closed for `/update` and every other slash command while ordinary Contact chat
continues. Use a trusted local CLI or authenticated dashboard for operator
commands.

Token, API origin, Contact allowlist, state directory, and delivery settings
resolve inside the active Hermes profile. Multiplexed profiles do not inherit
another profile's Relay credentials or SQLite state.

## Related

- [WebSocket acknowledgements](/guides/websocket/acknowledgements)
- [WebSocket FULL sync](/guides/websocket/full-sync)
- [Delivery receipts](/guides/messaging/delivery-receipts)

# Cloudflare Think starter

> Run a Relay agent as a serverless-friendly Cloudflare Think messenger with signed Webhook ingress.

Source: https://docs.staging.relayapp.im/ecosystem/agent-starter.md

Fork a minimal Cloudflare Think messenger for a Relay agent.

**The starter receives signed Webhooks in a Worker. Choose an always-on
channel page when the runtime should keep an acknowledged WebSocket open.**

Source:
[Relay-SDK `cookbook/cloudflare-think-agent`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/cloudflare-think-agent)

## Current source

The public cookbook recipe is versioned as
`@relaymessenger/cookbook-cloudflare-think-agent@0.1.0` and pins:

| Dependency | Version |
| --- | --- |
| `@cloudflare/think` | `0.17.0` |
| `@relaymessenger/chat-sdk-adapter` | `0.3.0-staging.0` |
| `@relaymessenger/sdk` | `0.3.0-staging.4` |
| `chat` | `4.39.0` |
| `agents` | `0.22.0` |

The lockfile pins the published staging Chat SDK adapter and its registry
integrity. Clone the staging branch and install its exact dependencies:

```bash
git clone --branch staging \
  https://github.com/RelayMessenger/Relay-SDK.git
cd Relay-SDK/cookbook/cloudflare-think-agent
npm ci
```

## Configure staging

Set the staging Agent Token and one-time Webhook signing secret in
`.dev.vars`. Keep these non-secret values in `wrangler.jsonc`:

```text
RELAY_API_ORIGIN=https://api.staging.relayapp.im
RELAY_AGENT_HANDLE=your_agent_handle
MODEL_ID=@cf/openai/gpt-oss-120b
```

Run `npm run dev`, expose the Worker's exact `/webhooks/relay` path through
HTTPS, and save that URL through
`POST /v1/webhook-subscriptions`.

## Delivery behavior

Relay's Chat SDK adapter verifies the raw signed body. Think maps
`relay:<Chat UUID>` to one durable thread, runs the model in a recoverable
fiber, and requires the model to call the native `reply` Action once.

The Action commits one complete Relay Message with a stable idempotency key
derived from the inbound Relay Message ID. Think owns durable turn recovery
and its Action ledger.

## Related

- [Chat SDK adapter](/ecosystem/chat-sdk)
- [Webhooks](/guides/webhooks)
- [Relay Cookbook](/examples)

# Relay Skills

> Install the Relay Skill and prepare hosted search for /v1/websocket, /v1/webhook-subscriptions, and webhook version 2026-08-30.

Source: https://docs.staging.relayapp.im/ecosystem/skills.md

Install portable Relay guidance for coding agents and connect them to current
Relay docs search.

Source:
[Relay-SDK `skills/relay`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/skills/relay)

## Install

The current portable Agent Plugin and Claude plugin manifest version is
`1.0.0`:

```bash
git clone --branch staging \
  https://github.com/RelayMessenger/Relay-SDK.git
npx skills add ./Relay-SDK/skills/relay
```

The Relay Skill is the editable source for the generated Codex and Cursor
packages. Its checked-in references lock the Relay v1 OpenAPI and maintained
TypeScript SDK.

## Choose the docs MCP

The skill configures a hosted, read-only docs MCP. It searches documentation
and requires no Relay Agent Token.

| Docs source | MCP URL |
| --- | --- |
| Published docs | `https://docs.relayapp.im/mcp` |
| Staging verification | `https://docs.staging.relayapp.im/mcp` |

Use [Relay MCP](/ecosystem/mcp) instead when the local coding agent should call
Relay messaging tools with an Agent Token.

## Prepare hosted proof

The local docs candidate keeps the current event paths and payload version in
`llms.txt` for these later hosted queries:

```text
Relay v1 /v1/websocket /v1/webhook-subscriptions full_sync
message.received webhook_version 2026-08-30 api_version v1
```

The expected markers are `/v1/websocket`, `/v1/webhook-subscriptions`,
`webhook_version`, `2026-08-30`, and `api_version`.

Run the strict hosted-search test only after this docs candidate is pushed and
the hosted staging index refreshes:

```bash
RELAY_DOCS_MCP_URL=https://docs.staging.relayapp.im/mcp npm run test:live
```

Local Docs validation prepares these source markers. It does not prove the
hosted preview or MCP index.

## Related

- [Relay for Codex](/ecosystem/codex)
- [Relay for Cursor](/ecosystem/cursor)
- [AI coding agents](/getting-started/ai-agents)

# Relay for Codex

> Install the generated Relay Codex plugin with the Relay Skill, locked references, and docs MCP search.

Source: https://docs.staging.relayapp.im/ecosystem/codex.md

Add Relay's generated skill, references, example, and docs search to Codex.

Generated install mirror:
[RelayMessenger/Relay-Codex](https://github.com/RelayMessenger/Relay-Codex)

Canonical source:
[Relay-SDK `skills/relay`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/skills/relay)

## Install locally

The current generated distribution version is `1.0.0`. Use Codex CLI
`0.152.0` for this staging candidate, then install the staging source through
its local marketplace:

```bash
npm install --global @openai/codex@0.152.0
git clone --branch staging \
  https://github.com/RelayMessenger/Relay-Codex.git
codex plugin marketplace add /absolute/path/to/Relay-Codex
codex plugin add relay@relay-plugin-marketplace
```

Start a new Codex session after installation.

## Use the package

The package contains:

- the Relay Skill and self-contained locked references;
- hosted Relay docs MCP configuration;
- a tested `@relaymessenger/sdk@0.3.0-staging.4` Message-send example;
- source and generated-file provenance from the Relay Skill.

The generated files come from the canonical Relay Skill and
[distribution tooling](https://github.com/RelayMessenger/Relay-SDK/tree/staging/tooling/skills-distributions).
Update the source Skill, then regenerate this mirror.

## Prepare hosted proof

Run the package's strict hosted-search test only after this docs candidate is
pushed and the hosted staging index refreshes:

```bash
RELAY_DOCS_MCP_URL=https://docs.staging.relayapp.im/mcp npm run test:live
```

The test requires search results for `/v1/websocket`,
`/v1/webhook-subscriptions`, and `webhook_version: 2026-08-30`.
Local Docs validation does not claim that hosted result.

## Related

- [Relay Skill](/ecosystem/skills)
- [Relay MCP](/ecosystem/mcp)
- [AI coding agents](/getting-started/ai-agents)

# Relay for Cursor

> Install the generated Relay Cursor plugin with the Relay Skill, locked references, and docs MCP search.

Source: https://docs.staging.relayapp.im/ecosystem/cursor.md

Add Relay's generated skill, references, example, and docs search to Cursor.

Generated install mirror:
[RelayMessenger/Relay-Cursor](https://github.com/RelayMessenger/Relay-Cursor)

Canonical source:
[Relay-SDK `skills/relay`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/skills/relay)

## Install locally

The current generated distribution version is `1.0.0`. Copy or link the
staging source checkout to:

```bash
git clone --branch staging \
  https://github.com/RelayMessenger/Relay-Cursor.git

ln -s /absolute/path/to/Relay-Cursor ~/.cursor/plugins/local/relay
```

The installed path is:

```text
~/.cursor/plugins/local/relay
```

Reload Cursor, open **Customize**, and confirm that the Relay skill and docs
MCP server are present.

## Use the package

The package contains:

- the Relay Skill and self-contained locked references;
- hosted Relay docs MCP configuration;
- a tested `@relaymessenger/sdk@0.3.0-staging.4` Message-send example;
- source and generated-file provenance from the Relay Skill.

The generated files come from the canonical Relay Skill and
[distribution tooling](https://github.com/RelayMessenger/Relay-SDK/tree/staging/tooling/skills-distributions).
Update the source Skill, then regenerate this mirror.

## Prepare hosted proof

Run the package's strict hosted-search test only after this docs candidate is
pushed and the hosted staging index refreshes:

```bash
RELAY_DOCS_MCP_URL=https://docs.staging.relayapp.im/mcp npm run test:live
```

The test requires search results for `/v1/websocket`,
`/v1/webhook-subscriptions`, and `webhook_version: 2026-08-30`.
Local Docs validation does not claim that hosted result.

## Related

- [Relay Skill](/ecosystem/skills)
- [Relay MCP](/ecosystem/mcp)
- [AI coding agents](/getting-started/ai-agents)

# Examples

> Run Relay Cookbook recipes for signed Webhooks, acknowledged WebSocket delivery, Cloudflare Think, text, images, and voice memos.

Source: https://docs.staging.relayapp.im/examples/index.md

Start with the smallest source example that matches how the Relay agent runs.

Source:
[Relay-SDK `cookbook`](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook)

## Choose an example

| Example | Runtime | What it proves |
| --- | --- | --- |
| [Webhook receiver](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/webhook-receiver) | Server or serverless-friendly HTTPS handler | Exact-body verification, SQLite acceptance before `2xx`, replay deduplication, and idempotent replies |
| [WebSocket agent](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/websocket-agent) | Always-on Node.js process | Durable acceptance before SDK-managed ACK, replay deduplication, and complete REST-backed FULL sync |
| [Cloudflare Think agent](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/cloudflare-think-agent) | Cloudflare Worker | `@cloudflare/think@0.17.0`, Relay Chat SDK Webhook ingress, and Action-owned idempotent sends |
| [Send a Message](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/send-a-message) | Local Node.js command | Send one idempotent text Message to a Chat |
| [Send an image](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/send-an-image) | Local Node.js command | Allocate and upload one image, then send its stable Attachment identity |
| [Send a voice memo](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/send-a-voice-memo) | Local Node.js command | Allocate and upload audio, then send it through the voice memo operation |

Webhooks and WebSocket carry the same event envelope. A saved Webhook
subscription selects signed HTTP delivery. With zero subscriptions, the
always-on process connects to `/v1/websocket`.

## Package versions

The current examples pin:

| Package | Version |
| --- | --- |
| `@relaymessenger/sdk` | `0.3.0-staging.4` |
| `@relaymessenger/chat-sdk-adapter` | `0.3.0-staging.0` |
| `@cloudflare/think` | `0.17.0` |
| `chat` | `4.39.0` |

The lockfile resolves the Chat SDK adapter to the published staging registry
artifact and verifies its integrity during installation.

## Run locally

Use Node.js `22.22.3` or newer:

```bash
git clone --branch staging https://github.com/RelayMessenger/Relay-SDK.git
cd Relay-SDK
npm ci
npm run validate:cookbook
```

The root validation command uses local mocks and dry builds. It contacts
neither Relay nor Cloudflare.

## Related

- [Developer ecosystem](/ecosystem)
- [Webhooks](/guides/webhooks)
- [WebSocket acknowledgements](/guides/websocket/acknowledgements)
- [Cloudflare Think starter](/ecosystem/agent-starter)
- [Attachments](/guides/messaging/attachments)

# Error Codes

> Find every Relay API error code and its retry rule.

Source: https://docs.staging.relayapp.im/error/index.md

Every Relay API error uses one JSON shape.

## Error response

```json
{
  "error": {
    "status":404,
    "code":2001,
    "message":"Resource not found.",
    "doc_url":"https://docs.relayapp.im/error/codes/2xxx/2001"
  },
  "success":false,
  "trace_id":"b923d71e50be43ba9e0fe1e34a7676c2"
}
```

**Branch on `error.code`, not `error.message`.** Save `trace_id` for
debugging and support.

| Field | Use |
| --- | --- |
| `error.status` | HTTP status |
| `error.code` | Stable numeric program key |
| `error.message` | Readable diagnostic explanation |
| `error.doc_url` | Direct link to the code page |
| `error.retry_after` | Seconds before retry, when present |
| `trace_id` | Request correlation ID |

`error.retry_after` appears on rate-limit responses.

## 1xxx request errors

| Code | HTTP | Meaning | Recovery |
| --- | --- | --- | --- |
| [1004](/error/codes/1xxx/1004) | `400` | Mentioned Handle is not in the Chat | Fix the mention |
| [1005](/error/codes/1xxx/1005) | `400` | Request or current state is invalid | Fix the request |

## 2xxx errors

| Code | HTTP | Meaning | Recovery |
| --- | --- | --- | --- |
| [2001](/error/codes/2xxx/2001) | `404` | Resource not found | Refresh the resource ID |
| [2003](/error/codes/2xxx/2003) | `403` | Permission denied | Wait for access to change |
| [2004](/error/codes/2xxx/2004) | `401` | Authentication required | Restore a valid Agent Token |
| [2005](/error/codes/2xxx/2005) | `500` | Attachment owner unavailable | Contact support with `trace_id` |
| [2006](/error/codes/2xxx/2006) | `413`, `415`, or `422` | Invalid or unsupported content | Fix the content |
| [2007](/error/codes/2xxx/2007) | `404` | Attachment is not ready | Complete the upload |
| [2008](/error/codes/2xxx/2008) | `429` | Rate limited | Wait for `retry_after` |
| [2015](/error/codes/2xxx/2015) | `409` | Mentioned Handle left the Chat | Refresh membership |
| [2023](/error/codes/2xxx/2023) | `409` | Mentions require a group Chat | Remove the mention |
| [2025](/error/codes/2xxx/2025) | `404` | Blocked Handle not found | Refresh the block list |
| [2026](/error/codes/2xxx/2026) | `403` | Handle is blocked | Unblock before sending |

## 3xxx server errors

| Code | HTTP | Meaning | Recovery |
| --- | --- | --- | --- |
| [3006](/error/codes/3xxx/3006) | `500` | Unexpected server error | Retry only if idempotent |

## Related

- [Debugging](/guides/platform/debugging)
- [Idempotency](/guides/platform/idempotency)
- [API Reference](/api-reference/overview)

# Error 1004: Handle is not in the Chat

> The mentioned Handle is not active in this Chat.

Source: https://docs.staging.relayapp.im/error/codes/1xxx/1004.md

The mentioned Handle is not an active Contact in this Chat.

| HTTP status | Relay code |
| --- | --- |
| `400` | `1004` |

## Troubleshooting

1. List the Chat and confirm the Handle is active.
2. Use structured mention fields only for a current group Contact.

**Retry:** Do not retry the same request unchanged.

## Related

- [Mentions](/guides/messaging/mentions)
- [Group Chats](/guides/chats/group-chats)

# Error 1005: Invalid request

> The request or current Relay state is invalid.

Source: https://docs.staging.relayapp.im/error/codes/1xxx/1005.md

The request body, query, cursor, idempotency key, or current state is invalid.

| HTTP status | Relay code |
| --- | --- |
| `400` | `1005` |

## Troubleshooting

1. Compare the request with the generated API Reference.
2. Correct the field or state named in `error.message` before retrying.

**Retry:** Do not retry the same request unchanged.

## Related

- [API Reference](/api-reference/overview)
- [Debugging](/guides/platform/debugging)
- [Idempotency](/guides/platform/idempotency)

# Error 2001: Resource not found

> The resource does not exist or is not visible to the authenticated Contact.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2001.md

The resource does not exist or is not visible to the authenticated Contact.

| HTTP status | Relay code |
| --- | --- |
| `404` | `2001` |

## Troubleshooting

1. Confirm the resource ID and credential.
2. Reconcile the current Chat or Message list before retrying.

**Retry:** Do not retry the same request unchanged.

## Related

- [API Reference](/api-reference/overview)
- [Debugging](/guides/platform/debugging)

# Error 2003: Permission denied

> The credential is valid but cannot perform this operation.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2003.md

The credential is valid but cannot perform this operation.

| HTTP status | Relay code |
| --- | --- |
| `403` | `2003` |

## Troubleshooting

1. Use the Contact that belongs to the resource.
2. Do not retry until authorization or membership changes.

**Retry:** Do not retry the same request unchanged.

## Related

- [Authentication](/getting-started/authentication)
- [Debugging](/guides/platform/debugging)

# Error 2004: Authentication required

> The bearer credential is missing, invalid, expired, or revoked.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2004.md

The bearer credential is missing, invalid, expired, or revoked.

| HTTP status | Relay code |
| --- | --- |
| `401` | `2004` |

## Troubleshooting

1. Restore the correct Agent Token.
2. Keep credentials in server-side secret storage.

**Retry:** Do not retry the same request unchanged.

## Related

- [Authentication](/getting-started/authentication)
- [Debugging](/guides/platform/debugging)

# Error 2005: Attachment owner unavailable

> Relay could not resolve the trusted owner of an Attachment operation.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2005.md

Relay could not resolve the trusted owner of an Attachment operation.

| HTTP status | Relay code |
| --- | --- |
| `500` | `2005` |

## Troubleshooting

1. Stop retrying the same malformed state.
2. Record `trace_id` and contact Relay support.

**Retry:** Do not retry the same request unchanged.

## Related

- [Attachments](/guides/messaging/attachments)
- [Debugging](/guides/platform/debugging)

# Error 2006: Invalid content

> A Message part, Attachment, media URL, reaction, or part index is invalid.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2006.md

A Message part, Attachment, media URL, reaction, or part index is unsupported or invalid.

| HTTP status | Relay code |
| --- | --- |
| `413`, `415`, or `422` | `2006` |

## Troubleshooting

1. Read `error.message` for the rejected content boundary.
2. Correct the content type, size, URL, or part index before retrying.

**Retry:** Do not retry the same request unchanged.

## Related

- [Message Parts](/guides/messaging/message-parts)
- [Attachments](/guides/messaging/attachments)

# Error 2007: Attachment not ready

> The Attachment exists, but its upload is incomplete.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2007.md

The Attachment exists but its upload is not complete.

| HTTP status | Relay code |
| --- | --- |
| `404` | `2007` |

## Troubleshooting

1. Finish the raw upload with the returned headers.
2. Retrieve the Attachment and retry only after status is complete.

**Retry:** Do not retry the same request unchanged.

## Related

- [Attachments](/guides/messaging/attachments)
- [Sending Messages](/guides/messaging/sending-messages)

# Error 2008: Rate limited

> Relay rejected the request because a rate limit was reached.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2008.md

Relay temporarily rejected the request because a limit was reached.

| HTTP status | Relay code |
| --- | --- |
| `429` | `2008` |

## Troubleshooting

1. Wait for `error.retry_after` or `Retry-After`.
2. Reuse the original idempotency key when retrying a Message command.

**Retry:** Use an idempotent retry after the stated delay.

## Related

- [Rate Limits](/guides/platform/rate-limits)
- [Idempotency](/guides/platform/idempotency)

# Error 2015: Mentioned Handle left the Chat

> The structured mention targets a Contact that left or was removed.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2015.md

The structured mention targets a Contact that left or was removed.

| HTTP status | Relay code |
| --- | --- |
| `409` | `2015` |

## Troubleshooting

1. Refresh the Chat membership.
2. Remove or replace the mention before sending again.

**Retry:** Do not retry the same request unchanged.

## Related

- [Mentions](/guides/messaging/mentions)
- [Group Chats](/guides/chats/group-chats)

# Error 2023: Mentions require a group Chat

> Structured mentions require a group Chat.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2023.md

Structured mentions are not supported in direct Chats.

| HTTP status | Relay code |
| --- | --- |
| `409` | `2023` |

## Troubleshooting

1. Remove mention and mention_range for a direct Message.
2. Use mentions only with a current group Contact.

**Retry:** Do not retry the same request unchanged.

## Related

- [Mentions](/guides/messaging/mentions)
- [Group Chats](/guides/chats/group-chats)

# Error 2025: Blocked Handle not found

> The Handle is not on the authenticated Contact’s block list.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2025.md

The Handle is not currently on the authenticated Contact's block list.

| HTTP status | Relay code |
| --- | --- |
| `404` | `2025` |

## Troubleshooting

1. List `GET /v1/blocked_handles`.
2. Unblock only a Handle returned by that list.

**Retry:** Do not retry the same request unchanged.

## Related

- [Blocked Handles](/guides/chats/blocked-handles)

# Error 2026: Handle is blocked

> Direct traffic is blocked between the two Contacts.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2026.md

Direct traffic is blocked between the two Contacts.

| HTTP status | Relay code |
| --- | --- |
| `403` | `2026` |

## Troubleshooting

1. Stop the direct send.
2. Unblock the Handle before intentionally resuming direct traffic.

**Retry:** Do not retry the same request unchanged.

## Related

- [Blocked Handles](/guides/chats/blocked-handles)
- [Sending Messages](/guides/messaging/sending-messages)

# Error 3006: Unexpected server error

> Relay could not complete the request because of an unexpected server failure.

Source: https://docs.staging.relayapp.im/error/codes/3xxx/3006.md

Relay could not complete the request because of an unexpected server failure.

| HTTP status | Relay code |
| --- | --- |
| `500` | `3006` |

## Troubleshooting

1. Record `trace_id` and the request time.
2. Retry only when the command is idempotent.

**Retry:** Retry only with an idempotency key.

## Related

- [Idempotency](/guides/platform/idempotency)
- [Debugging](/guides/platform/debugging)

# API Reference

> Use the generated Relay v1 endpoint and schema reference.

Source: https://docs.staging.relayapp.im/api-reference/overview.md

Relay API uses the `/v1` namespace at:

```text
https://api.relayapp.im/v1
```

## Endpoints

| Resource | Method | Endpoint |
| --- | --- | --- |
| Chats | `POST` | `/v1/chats` |
| Chats | `GET` | `/v1/chats` |
| Chats | `GET` | `/v1/chats/{chatId}` |
| Chats | `PUT` | `/v1/chats/{chatId}` |
| Chats | `POST` | `/v1/chats/{chatId}/participants` |
| Chats | `DELETE` | `/v1/chats/{chatId}/participants` |
| Chats | `POST` | `/v1/chats/{chatId}/leave` |
| Chats | `POST` | `/v1/chats/{chatId}/typing` |
| Chats | `DELETE` | `/v1/chats/{chatId}/typing` |
| Chats | `POST` | `/v1/chats/{chatId}/read` |
| Chats | `POST` | `/v1/chats/{chatId}/share_contact_card` |
| Messages | `POST` | `/v1/messages` |
| Messages | `POST` | `/v1/chats/{chatId}/messages` |
| Messages | `GET` | `/v1/chats/{chatId}/messages` |
| Messages | `GET` | `/v1/messages/{messageId}/thread` |
| Messages | `POST` | `/v1/chats/{chatId}/voicememo` |
| Messages | `GET` | `/v1/messages/{messageId}` |
| Messages | `POST` | `/v1/messages/{messageId}/reactions` |
| Attachments | `POST` | `/v1/attachments` |
| Attachments | `GET` | `/v1/attachments/{attachmentId}` |
| Attachments | `DELETE` | `/v1/attachments/{attachmentId}` |
| Blocked Handles | `GET` | `/v1/blocked_handles` |
| Blocked Handles | `POST` | `/v1/blocked_handles` |
| Blocked Handles | `DELETE` | `/v1/blocked_handles` |
| Webhooks | `GET` | `/v1/webhook-events` |
| Webhooks | `POST` | `/v1/webhook-subscriptions` |
| Webhooks | `GET` | `/v1/webhook-subscriptions` |
| Webhooks | `GET` | `/v1/webhook-subscriptions/{subscriptionId}` |
| Webhooks | `PUT` | `/v1/webhook-subscriptions/{subscriptionId}` |
| Webhooks | `DELETE` | `/v1/webhook-subscriptions/{subscriptionId}` |
| Contact Card | `GET` | `/v1/contact_card` |
| Contact Card | `POST` | `/v1/contact_card` |
| Contact Card | `PATCH` | `/v1/contact_card` |
| WebSocket | `GET` | `/v1/websocket` |
| Contacts | `POST` | `/v1/contact_requests` |

## Conventions

| Property | Value |
| --- | --- |
| Authentication | Bearer credential |
| REST request and response bodies | `application/json` |
| Resource IDs | Opaque UUIDv7 strings |
| Pagination | Opaque `cursor` and `next_cursor` |
| Send idempotency | `Idempotency-Key` or `message.idempotency_key` |
| Event payload version | `2026-08-30` |

Endpoint pages are generated from Relay's OpenAPI 3.1 contract. Their request
and response fields are authoritative.

## Errors

Use `error.code` for program logic and retain `trace_id` for debugging.

```json
{
  "error": {
    "status": 404,
    "code": 2001,
    "message": "Resource not found.",
    "doc_url": "https://docs.relayapp.im/error/codes/2xxx/2001"
  },
  "success": false,
  "trace_id": "b923d71e50be43ba9e0fe1e34a7676c2"
}
```

## Related

- [Authentication](/getting-started/authentication)
- [Error Codes](/error)
- [Limits](/guides/platform/rate-limits)

# Relay API OpenAPI

> Exact Relay API v1 paths, fields, limits, and errors.

Source: https://docs.staging.relayapp.im/api-reference/openapi.yaml

````yaml api-reference/openapi.yaml
openapi: 3.1.0
info:
  title: Relay API
  version: 1.0.0
  description: |-
    Create conversations between users and agents.

    Send multipart Messages, manage Chats, upload Attachments, and receive agent events.
  license:
    name: Proprietary
    identifier: LicenseRef-Proprietary
servers:
  - url: https://api.relayapp.im
    description: Relay API
security:
  - BearerAuth: []
tags:
  - name: Chats
    x-page-title: Chats
    description: Create, retrieve, and update direct or group chats.
  - name: Messages
    x-page-title: Messages
    description: Send and retrieve messages, replies, reactions, and receipts.
  - name: Attachments
    x-page-title: Attachments
    description: Allocate, upload, retrieve, and delete attachment bytes.
  - name: Blocked Handles
    x-page-title: Blocked Handles
    description: Block or unblock registered Relay Handles.
  - name: Contact Card
    x-page-title: Contact Card
    description: Manage the authenticated agent's Relay contact card.
  - name: Webhooks
    x-page-title: Webhooks
    description: Register signed webhook destinations.
  - name: WebSocket
    description: Receive agent events over a durable acknowledged WebSocket.
  - name: Contacts
    description: Request a user Contact.
paths:
  /v1/chats:
    post:
      operationId: createChat
      summary: Create a new chat
      tags:
        - Chats
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChatRequest"
      responses:
        "201":
          description: Chat created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateChatResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: Mentions require a group chat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: Optional 1–255 character key for safe retries. If message.idempotency_key is also present, the two values
            must match.
          schema:
            type: string
            minLength: 1
            maxLength: 255
      description: Create a direct or group Chat with its first Message.
    get:
      operationId: listChats
      summary: List all chats
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          description: Maximum number of chats to return per page
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: cursor
          in: query
          required: false
          description: |
            Pagination cursor from the previous response's `next_cursor` field.
            Omit this parameter for the first page of results.
          schema:
            type: string
      responses:
        "200":
          description: List of chats retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListChatsResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: List Chats visible to the authenticated Contact.
  /v1/chats/{chatId}:
    get:
      operationId: getChat
      summary: Get a chat by ID
      description: Retrieve one visible Chat.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Chat found and returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Chat"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    put:
      operationId: updateChat
      summary: Update a chat
      description: Update a group Chat name or photo.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChatRequest"
      responses:
        "202":
          description: Update request accepted and queued for processing
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  chat_id:
                    type: string
                    format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/participants:
    post:
      operationId: addParticipant
      summary: Add a participant to a chat
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddParticipantRequest"
      responses:
        "202":
          description: Participant addition queued successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  trace_id:
                    type: string
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Add a registered Contact to a group Chat.
    delete:
      operationId: removeParticipant
      summary: Remove a participant from a chat
      description: Remove a Contact from a group Chat.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RemoveParticipantRequest"
      responses:
        "202":
          description: Participant removal queued successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  trace_id:
                    type: string
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/leave:
    post:
      operationId: leaveChat
      summary: Leave a group chat
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat to leave
          schema:
            type: string
            format: uuid
      responses:
        "202":
          description: Leave group chat request queued successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  trace_id:
                    type: string
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Leave a group Chat.
  /v1/chats/{chatId}/typing:
    post:
      operationId: startTyping
      summary: Start typing indicator
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: |
            Request accepted. Does not guarantee the typing indicator is visible
            to the recipient (see behavior notes above).
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Start or refresh the authenticated Contact's typing indicator.
    delete:
      operationId: stopTyping
      summary: Stop typing indicator
      description: Stop the authenticated Contact's typing indicator.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Request accepted.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/read:
    post:
      operationId: markChatAsRead
      summary: Mark chat as read
      description: Explicitly mark visible Messages in a Chat as Read. Webhook responses and WebSocket acknowledgements do
        not mark Messages Read.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Chat marked as read successfully
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/share_contact_card:
    post:
      operationId: shareContactWithChat
      summary: Share your contact card with a chat
      description: Share the authenticated agent's configured Contact Card into an existing Chat. Each successful call shares
        the card once.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Contact shared successfully
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/messages:
    post:
      operationId: sendMessage
      summary: Resolve a Chat and send a Message
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: |
            Optional idempotency key for the send. Reuse the same key to safely
            retry without sending twice. May also be supplied as
            `message.idempotency_key`.
          schema:
            type: string
            maxLength: 255
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageRequest"
      responses:
        "202":
          description: Message accepted for delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendMessageResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          description: The request conflicts with current Chat, membership, or idempotency state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
        "503":
          description: |
            The send could not be completed. The condition is temporary — retry
            the request shortly.

            Retrying is safe, but send an `Idempotency-Key` header (or
            `message.idempotency_key`) so a retry cannot produce a duplicate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      description: Resolve or create a Chat, then send a Message.
  /v1/chats/{chatId}/messages:
    post:
      operationId: sendMessageToChat
      summary: Send a message to an existing chat
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: false
          description: Optional 1–255 character key for safe retries. If message.idempotency_key is also present, the two values
            must match.
          schema:
            type: string
            minLength: 1
            maxLength: 255
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageToChatRequest"
      responses:
        "202":
          description: Message accepted for delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendMessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Chat is unavailable, a mention was used in a non-group chat, or a mention target is no longer in the chat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Send a Message to an existing Chat.
    get:
      operationId: getMessages
      summary: Get messages from a chat
      description: List visible Messages and group-history events in a Chat.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          description: Pagination cursor from previous next_cursor response
          schema:
            type: string
        - name: limit
          in: query
          description: Maximum number of messages to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Messages retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetMessagesResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/messages/{messageId}/thread:
    get:
      operationId: getMessageThread
      summary: Get all messages in a thread
      description: List a Message and its replies.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          description: ID of any message in the thread (can be originator or any reply)
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          description: Pagination cursor from previous next_cursor response
          schema:
            type: string
        - name: limit
          in: query
          description: Maximum number of messages to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: order
          in: query
          description: Sort order for messages (asc = oldest first, desc = newest first)
          schema:
            type: string
            enum:
              - asc
              - desc
            default: asc
      responses:
        "200":
          description: Thread retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetThreadResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/voicememo:
    post:
      operationId: sendVoiceMemoToChat
      summary: Send a voice memo to a chat
      description: Send an uploaded audio Attachment as a voice memo.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendVoiceMemoToChatRequest"
      responses:
        "202":
          description: Voice memo accepted for delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendVoiceMemoToChatResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: Voice memo file too large
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/messages/{messageId}:
    get:
      operationId: getMessage
      summary: Get a message by ID
      description: Retrieve one visible Message.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          description: Unique identifier of the message to retrieve
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Message found and returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/messages/{messageId}/reactions:
    post:
      operationId: sendReaction
      summary: Add or remove a reaction to a message
      description: Add or remove a reaction on one Message part.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          description: Unique identifier of the message to react to
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendReactionRequest"
      responses:
        "202":
          description: Reaction queued successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  trace_id:
                    type: string
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/attachments:
    post:
      operationId: requestUpload
      summary: Pre-upload a file
      description: Create an Attachment upload allocation.
      tags:
        - Attachments
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestUploadRequest"
      responses:
        "200":
          description: Upload URL generated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestUploadResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/attachments/{attachmentId}:
    get:
      operationId: getAttachment
      summary: Get attachment metadata
      description: Retrieve Attachment metadata and a download URL.
      tags:
        - Attachments
      security:
        - BearerAuth: []
      parameters:
        - name: attachmentId
          in: path
          required: true
          description: Unique identifier of the attachment
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Attachment found and returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Attachment"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      operationId: deleteAttachment
      summary: Delete an attachment
      description: Delete an unreferenced Attachment owned by the caller.
      tags:
        - Attachments
      security:
        - BearerAuth: []
      parameters:
        - name: attachmentId
          in: path
          required: true
          description: Unique identifier of the attachment to delete
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Attachment deleted (no body).
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/blocked_handles:
    get:
      operationId: listBlockedHandles
      summary: List blocked handles
      description: List Handles blocked by the authenticated Contact.
      tags:
        - Blocked Handles
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Blocked handles retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBlockedHandlesResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
    post:
      operationId: blockHandle
      summary: Block a handle
      tags:
        - Blocked Handles
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlockHandleRequest"
      responses:
        "201":
          description: Handle blocked successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlockedHandleResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Block direct traffic with a registered Handle.
    delete:
      operationId: unblockHandle
      summary: Unblock a handle
      description: Remove a Handle from the authenticated Contact's block list.
      tags:
        - Blocked Handles
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnblockHandleRequest"
      responses:
        "204":
          description: Handle unblocked successfully
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/webhook-events:
    get:
      operationId: listWebhookEvents
      summary: List available webhook event types
      description: List event names available to agent backends.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      responses:
        "200":
          description: List of available webhook event types
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEventsResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/webhook-subscriptions:
    post:
      operationId: createWebhookSubscription
      summary: Create a new webhook subscription
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWebhookSubscriptionRequest"
      responses:
        "201":
          description: Webhook subscription created successfully. The signing_secret is only returned in this response - store it
            securely.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookSubscriptionCreatedResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Create a signed webhook subscription.
    get:
      operationId: listWebhookSubscriptions
      summary: List all webhook subscriptions
      description: List webhook subscriptions for the authenticated agent.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      responses:
        "200":
          description: List of webhook subscriptions retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListWebhookSubscriptionsResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/webhook-subscriptions/{subscriptionId}:
    get:
      operationId: getWebhookSubscription
      summary: Get a webhook subscription by ID
      description: Retrieve one webhook subscription.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      parameters:
        - name: subscriptionId
          in: path
          required: true
          description: Unique identifier of the webhook subscription
          schema:
            type: string
      responses:
        "200":
          description: Webhook subscription found and returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookSubscriptionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    put:
      operationId: updateWebhookSubscription
      summary: Update a webhook subscription
      description: Replace a webhook subscription's settings.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      parameters:
        - name: subscriptionId
          in: path
          required: true
          description: Unique identifier of the webhook subscription
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWebhookSubscriptionRequest"
      responses:
        "200":
          description: Webhook subscription updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookSubscriptionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      operationId: deleteWebhookSubscription
      summary: Delete a webhook subscription
      description: Delete a webhook subscription.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      parameters:
        - name: subscriptionId
          in: path
          required: true
          description: Unique identifier of the webhook subscription
          schema:
            type: string
      responses:
        "204":
          description: Webhook subscription deleted successfully
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/contact_card:
    get:
      operationId: getContactCard
      summary: Get contact cards
      tags:
        - Contact Card
      security:
        - BearerAuth: []
      parameters:
        - name: handle
          in: query
          required: false
          description: Relay Handle. Agents can access only their own card.
          schema:
            type: string
      responses:
        "200":
          description: Contact card(s) returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetContactCardResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Retrieve the authenticated agent's Contact Card.
    post:
      operationId: setupContactCard
      summary: Setup contact card
      tags:
        - Contact Card
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetContactCardRequest"
      responses:
        "200":
          description: Contact card created/updated and sync attempted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SetContactCardResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Activate the authenticated agent's Contact Card.
    patch:
      operationId: updateContactCard
      summary: Update contact card
      tags:
        - Contact Card
      security:
        - BearerAuth: []
      parameters:
        - name: handle
          in: query
          required: true
          description: Relay Handle. Agents can access only their own card.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateContactCardRequest"
      responses:
        "200":
          description: Contact card updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SetContactCardResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Update the authenticated agent's Contact Card.
  /v1/websocket:
    get:
      operationId: connectAgentWebSocket
      summary: Connect an Agent WebSocket
      tags:
        - WebSocket
      security:
        - BearerAuth: []
      responses:
        "101":
          description: WebSocket connection accepted
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
      description: "Upgrade wss://api.relayapp.im/v1/websocket with Authorization: Bearer <Agent Token>. If the Agent has any
        saved webhook subscription, Relay returns 409; delete the subscriptions to connect by WebSocket."
  /v1/contact_requests:
    post:
      operationId: createContactRequest
      summary: Request a Contact
      description: Send an Add request to one user as the authenticated agent. The request contains the agent identity and no
        Message content. This operation is available to Agents with Premium Handles.
      tags:
        - Contacts
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateContactRequest"
      responses:
        "201":
          description: Add request accepted.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateContactRequestResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "402":
          $ref: "#/components/responses/PaymentRequired"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
components:
  schemas:
    AddParticipantRequest:
      type: object
      required:
        - handle
      properties:
        handle:
          type: string
          description: Relay Handle.
    Attachment:
      type: object
      required:
        - id
        - filename
        - content_type
        - size_bytes
        - status
        - created_at
      properties:
        id:
          type: string
          description: Unique identifier for the attachment (UUID)
        filename:
          type: string
          description: Original filename of the attachment
        content_type:
          $ref: "#/components/schemas/SupportedContentType"
        size_bytes:
          type: integer
          format: int64
          description: Size of the attachment in bytes
        status:
          type: string
          description: Current upload state.
          enum:
            - pending
            - complete
            - failed
        download_url:
          type: string
          format: uri
          description: URL to download the attachment
        created_at:
          type: string
          format: date-time
          description: When the attachment was created
        duration_ms:
          type:
            - integer
            - "null"
          minimum: 0
          description: Audio or video duration in milliseconds when known.
        width:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel width when known. Width and height are supplied together.
        height:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel height when known. Width and height are supplied together.
    BlockHandleRequest:
      type: object
      required:
        - handle
      properties:
        handle:
          type: string
          description: Registered Relay Handle.
        reason:
          type: string
          description: Optional free-text note on why the handle was blocked
    BlockedHandleEntry:
      type: object
      required:
        - handle
        - blocked_at
      properties:
        handle:
          type: string
          description: Registered Relay Handle blocked by the authenticated Contact.
        reason:
          type: string
          description: Optional note recorded when the handle was blocked
        blocked_at:
          type: string
          format: date-time
          description: When the handle was blocked
    BlockedHandleResult:
      type: object
      required:
        - blocked_handle
      properties:
        blocked_handle:
          $ref: "#/components/schemas/BlockedHandleEntry"
    Chat:
      type: object
      required:
        - id
        - display_name
        - handles
        - is_group
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Unique identifier for the chat
          format: uuid
        display_name:
          type:
            - string
            - "null"
          description: Display name for the chat. Defaults to a comma-separated list of recipient handles. Can be updated for
            group chats.
        group_chat_icon:
          type:
            - string
            - "null"
          format: uri
          description: URL of the group chat icon. Only set for group chats that have an icon; `null` otherwise.
        handles:
          type: array
          items:
            $ref: "#/components/schemas/ChatHandle"
        is_group:
          type: boolean
          description: Whether this is a group chat
          default: false
        created_at:
          type: string
          format: date-time
          description: When the chat was created
        updated_at:
          type: string
          format: date-time
          description: When the chat was last updated
    ChatCreatedEvent:
      type: object
      required:
        - id
        - display_name
        - handles
        - is_group
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Unique identifier for the chat
          format: uuid
        display_name:
          type:
            - string
            - "null"
          description: Display name for the chat. Defaults to a comma-separated list of recipient handles. Can be updated for
            group chats.
        group_chat_icon:
          type:
            - string
            - "null"
          format: uri
          description: URL of the group chat icon. Only set for group chats that have an icon; `null` otherwise.
        handles:
          type: array
          items:
            $ref: "#/components/schemas/ChatHandle"
        is_group:
          type: boolean
          description: Whether this is a group chat
          default: false
        created_at:
          type: string
          format: date-time
          description: When the chat was created
        updated_at:
          type: string
          format: date-time
          description: When the chat was last updated
      description: Chat resource created for the receiving agent.
    ChatCreatedWebhook:
      description: Complete webhook payload for chat.created events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatCreatedEvent"
    ChatGroupIconUpdatedEvent:
      type: object
      description: Payload for chat.group_icon_updated webhook events
      required:
        - chat_id
        - updated_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID) of the group chat
        old_value:
          type:
            - string
            - "null"
          description: Previous icon URL (null if no previous icon)
        new_value:
          type:
            - string
            - "null"
          description: New icon URL (null if the icon was removed)
        changed_by_handle:
          description: The handle who made the change.
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        updated_at:
          type: string
          format: date-time
          description: When the update occurred
    ChatGroupIconUpdatedWebhook:
      description: Complete webhook payload for chat.group_icon_updated events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatGroupIconUpdatedEvent"
    ChatGroupNameUpdatedEvent:
      type: object
      description: Payload for chat.group_name_updated webhook events
      required:
        - chat_id
        - updated_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID) of the group chat
        old_value:
          type:
            - string
            - "null"
          description: Previous group name (null if no previous name)
        new_value:
          type:
            - string
            - "null"
          description: New group name (null if the name was removed)
        changed_by_handle:
          description: The handle who made the change.
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        updated_at:
          type: string
          format: date-time
          description: When the update occurred
    ChatGroupNameUpdatedWebhook:
      description: Complete webhook payload for chat.group_name_updated events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatGroupNameUpdatedEvent"
    ChatHandle:
      type: object
      required:
        - id
        - handle
        - joined_at
        - kind
        - display_name
        - image_url
        - tagline
        - verified
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for this handle
        handle:
          type: string
          description: Relay Handle.
        status:
          type:
            - string
            - "null"
          description: Participant status
          enum:
            - active
            - left
            - removed
          default: active
        joined_at:
          type: string
          format: date-time
          description: When this participant joined the chat
        left_at:
          type:
            - string
            - "null"
          format: date-time
          description: When they left (if applicable)
        is_me:
          type:
            - boolean
            - "null"
          description: Whether this handle is the authenticated caller.
        kind:
          type: string
          enum:
            - user
            - agent
          description: Whether this Contact is a user or an agent.
        display_name:
          type:
            - string
            - "null"
          description: Current Contact display name.
        image_url:
          type:
            - string
            - "null"
          format: uri
          description: >-
            Current Contact picture, as a permanent address served by Relay.
            It does not expire and may be cached indefinitely.
        tagline:
          type:
            - string
            - "null"
          maxLength: 60
          description: Short description shown for an agent. User Contacts return null.
        verified:
          type: boolean
          description: Whether Relay has verified this agent.
    ChatInfo:
      type: object
      required:
        - id
        - handles
        - is_group
      properties:
        id:
          type: string
          format: uuid
          description: Chat identifier
        handles:
          type: array
          description: Chat participants
          items:
            $ref: "#/components/schemas/ChatHandle"
        is_group:
          type: boolean
          description: Whether this is a group chat
    ChatTypingIndicatorStartedEvent:
      type: object
      additionalProperties: false
      description: Payload for chat.typing_indicator.started events.
      required:
        - chat_id
        - contact
      properties:
        chat_id:
          type: string
          format: uuid
          description: Chat identifier.
        contact:
          $ref: "#/components/schemas/TypingContact"
    ChatTypingIndicatorStartedWebhook:
      description: Complete webhook payload for chat.typing_indicator.started events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatTypingIndicatorStartedEvent"
    ChatTypingIndicatorStoppedEvent:
      type: object
      additionalProperties: false
      description: Payload for chat.typing_indicator.stopped events.
      required:
        - chat_id
        - contact
      properties:
        chat_id:
          type: string
          format: uuid
          description: Chat identifier.
        contact:
          $ref: "#/components/schemas/TypingContact"
    ChatTypingIndicatorStoppedWebhook:
      description: Complete webhook payload for chat.typing_indicator.stopped events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatTypingIndicatorStoppedEvent"
    ContactAddedEvent:
      type: object
      additionalProperties: false
      required:
        - contact
        - chat_id
      properties:
        contact:
          $ref: "#/components/schemas/ContactEventContact"
        chat_id:
          type: string
          format: uuid
          description: Direct Chat created or reused for this Contact.
    ContactAddedWebhook:
      description: Complete webhook payload for contact.added events.
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ContactAddedEvent"
    ContactCardItem:
      type: object
      required:
        - handle
        - first_name
        - is_active
        - kind
        - last_name
        - image_url
      properties:
        first_name:
          type: string
        last_name:
          type:
            - string
            - "null"
        image_url:
          type:
            - string
            - "null"
          format: uri
        is_active:
          type: boolean
        handle:
          type: string
          description: Relay Handle associated with this contact card.
        kind:
          type: string
          enum:
            - user
            - agent
          description: Whether the Contact is a user or an agent.
    ContactEventContact:
      type: object
      additionalProperties: false
      required:
        - id
        - handle
        - display_name
      properties:
        id:
          type: string
          format: uuid
          description: User Contact identifier.
        handle:
          type: string
          description: Current user Handle.
        display_name:
          type: string
          description: Current user display name.
    ContactRemovedEvent:
      type: object
      additionalProperties: false
      required:
        - contact
      properties:
        contact:
          $ref: "#/components/schemas/ContactEventContact"
    ContactRemovedWebhook:
      description: Complete webhook payload for contact.removed events.
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ContactRemovedEvent"
    CreateChatRequest:
      type: object
      required:
        - from
        - to
        - message
      properties:
        from:
          type: string
          description: Authenticated sender's Relay Handle.
        to:
          type: array
          minItems: 1
          maxItems: 31
          items:
            type: string
        message:
          description: Initial message for the new Chat.
          $ref: "#/components/schemas/MessageContent"
    CreateChatResult:
      type: object
      description: Response for creating a new chat with an initial message
      required:
        - chat
      properties:
        chat:
          type: object
          required:
            - id
            - display_name
            - is_group
            - handles
            - message
          properties:
            id:
              type: string
              format: uuid
              description: Unique identifier for the created chat (UUID)
            display_name:
              type:
                - string
                - "null"
              description: Display name for the chat. Defaults to a comma-separated list of recipient handles. Can be updated for
                group chats.
            is_group:
              type: boolean
              description: Whether this is a group chat
            handles:
              type: array
              items:
                $ref: "#/components/schemas/ChatHandle"
            message:
              $ref: "#/components/schemas/SentMessage"
    CreateContactRequest:
      type: object
      additionalProperties: false
      required:
        - handle
      properties:
        handle:
          type: string
          minLength: 1
          maxLength: 255
          description: User Handle to request.
    CreateContactRequestResult:
      type: object
      additionalProperties: false
      required:
        - state
      properties:
        state:
          type: string
          enum:
            - pending
          description: The Add request was accepted.
    CreateWebhookSubscriptionRequest:
      type: object
      required:
        - target_url
        - subscribed_events
      properties:
        target_url:
          type: string
          format: uri
          description: URL where webhook events will be sent. Must be HTTPS.
        subscribed_events:
          type: array
          description: List of event types to subscribe to
          minItems: 1
          items:
            $ref: "#/components/schemas/WebhookEventType"
    DeliveryStatus:
      type: string
      description: Current aggregate receipt state. Sent means at least one recipient has not reached its delivery boundary;
        Delivered means every recipient has; Read means every recipient explicitly marked the Message Read.
      enum:
        - sent
        - delivered
        - read
    ErrorCode:
      type: integer
      description: Relay API error code.
    ErrorDetail:
      type: object
      required:
        - status
        - code
        - message
        - doc_url
      properties:
        status:
          type: integer
          description: HTTP status code (e.g., 400, 404, 500)
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
          description: Human-readable error message
        doc_url:
          type: string
          description: Link to documentation for this error code
        retry_after:
          type: integer
          description: Number of seconds to wait before retrying. Only present on 429 rate limit errors.
    ErrorResponse:
      type: object
      required:
        - error
        - success
      properties:
        error:
          $ref: "#/components/schemas/ErrorDetail"
        success:
          type: boolean
          description: Always false for error responses
        trace_id:
          type: string
          description: Unique trace ID for request tracing and debugging
    GetContactCardResponse:
      type: object
      required:
        - contact_cards
      properties:
        contact_cards:
          type: array
          items:
            $ref: "#/components/schemas/ContactCardItem"
    GetMessagesResult:
      type: object
      required:
        - messages
      properties:
        messages:
          type: array
          description: List of messages
          items:
            $ref: "#/components/schemas/Message"
        next_cursor:
          type:
            - string
            - "null"
          description: |
            Cursor for fetching the next page of results.
            Null if there are no more results to fetch.
            Pass this value as the `cursor` parameter in the next request.
    GetThreadResponse:
      type: object
      description: Response containing messages in a thread with pagination
      required:
        - messages
      properties:
        messages:
          type: array
          description: Messages in the thread, ordered by the specified order parameter
          items:
            $ref: "#/components/schemas/Message"
        next_cursor:
          type:
            - string
            - "null"
          description: Cursor for fetching the next page of results (null if no more results)
    LinkPart:
      type: object
      required:
        - type
        - value
      properties:
        type:
          type: string
          enum:
            - link
          description: Indicates this is a rich link preview part
        value:
          type: string
          format: uri
          description: |
            URL to send with a rich link preview. The recipient will see an inline card
            with the page's title, description, and preview image (when available).

            A `link` part must be the **only** part in the message. To send a URL as plain
            text (no preview card), use a `text` part instead.
          minLength: 1
          maxLength: 2048
    LinkPartResponse:
      type: object
      description: A rich link preview part
      required:
        - type
        - value
        - reactions
      properties:
        type:
          type: string
          enum:
            - link
          description: Indicates this is a rich link preview part
        value:
          type: string
          description: The URL
        reactions:
          type:
            - array
            - "null"
          description: Reactions on this message part
          items:
            $ref: "#/components/schemas/Reaction"
    ListBlockedHandlesResult:
      type: object
      required:
        - blocked_handles
      properties:
        blocked_handles:
          type: array
          description: Contacts blocked by the authenticated Contact, newest first.
          items:
            $ref: "#/components/schemas/BlockedHandleEntry"
    ListChatsResult:
      type: object
      required:
        - chats
      properties:
        chats:
          type: array
          description: List of chats
          items:
            $ref: "#/components/schemas/Chat"
        next_cursor:
          type:
            - string
            - "null"
          description: |
            Cursor for fetching the next page of results.
            Null if there are no more results to fetch.
            Pass this value as the `cursor` parameter in the next request.
    ListWebhookSubscriptionsResult:
      type: object
      required:
        - subscriptions
      properties:
        subscriptions:
          type: array
          description: List of webhook subscriptions
          items:
            $ref: "#/components/schemas/WebhookSubscriptionResponse"
    MediaPart:
      type: object
      required:
        - type
      properties:
        type:
          type: string
          enum:
            - media
          description: Indicates this is a media attachment part
        url:
          type: string
          format: uri
          description: Public HTTPS media URL. Relay blocks non-public networks and validates every redirect, response status,
            declared and actual size, and supported Content-Type.
          pattern: ^https://
        attachment_id:
          type: string
          format: uuid
          description: Reference an Attachment allocated through POST /v1/attachments. Use either url or attachment_id, not both.
    MediaPartResponse:
      type: object
      description: A media attachment part
      required:
        - type
        - id
        - url
        - filename
        - mime_type
        - size_bytes
        - reactions
      properties:
        type:
          type: string
          enum:
            - media
          description: Indicates this is a media attachment part
        id:
          type: string
          format: uuid
          description: Unique attachment identifier
        url:
          type: string
          format: uri
          description: Opaque Relay download URL for the Attachment.
        filename:
          type: string
          description: Original filename
        mime_type:
          type: string
          description: MIME type of the file
        size_bytes:
          type: integer
          description: File size in bytes
        reactions:
          type:
            - array
            - "null"
          description: Reactions on this message part
          items:
            $ref: "#/components/schemas/Reaction"
        duration_ms:
          type:
            - integer
            - "null"
          minimum: 0
          description: Audio or video duration in milliseconds when known.
        width:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel width when known. Width and height are supplied together.
        height:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel height when known. Width and height are supplied together.
    Message:
      type: object
      required:
        - id
        - chat_id
        - is_from_me
        - delivery_status
        - created_at
        - updated_at
        - is_system_message
      properties:
        id:
          type: string
          description: Unique identifier for the message
          format: uuid
        chat_id:
          type: string
          description: ID of the chat this message belongs to
          format: uuid
        from:
          type:
            - string
            - "null"
          description: Sender Relay Handle.
        from_handle:
          description: The sender of this message as a full handle object
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        parts:
          type:
            - array
            - "null"
          description: Message parts in order (text, media, and link)
          items:
            oneOf:
              - $ref: "#/components/schemas/TextPartResponse"
              - $ref: "#/components/schemas/MediaPartResponse"
              - $ref: "#/components/schemas/LinkPartResponse"
              - $ref: "#/components/schemas/SystemPartResponse"
        reply_to:
          oneOf:
            - $ref: "#/components/schemas/ReplyTo"
            - type: "null"
        is_from_me:
          type: boolean
          description: Whether this message was sent by the authenticated user
        delivery_status:
          $ref: "#/components/schemas/DeliveryStatus"
        created_at:
          type: string
          format: date-time
          description: When the message was created
        updated_at:
          type: string
          format: date-time
          description: When the message was last updated
        sent_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was sent
        delivered_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was delivered
        read_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was read
        deliveries:
          type: array
          items:
            $ref: "#/components/schemas/MessageDelivery"
          description: Per-recipient Delivered and Read truth for direct and group Chats. Relay derives this because it controls
            every recipient endpoint.
        is_system_message:
          type: boolean
          description: Whether this timeline item is a visible Chat event.
        system_event:
          oneOf:
            - $ref: "#/components/schemas/SystemEvent"
            - type: "null"
    MessageContent:
      type: object
      description: One message containing 1–100 ordered text, media, or link parts. A link must be the only part.
      properties:
        parts:
          x-go-type-skip-optional-pointer: true
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/MessagePart"
        reply_to:
          $ref: "#/components/schemas/ReplyTo"
          description: Reply to another message to create a threaded conversation
        idempotency_key:
          type: string
          description: Optional key for a safe retry. The same authenticated sender, key, and Message body return the original
            Message. Reusing the key with a different body returns a conflict.
          maxLength: 255
      required:
        - parts
    MessageDeliveredWebhook:
      description: Complete webhook payload for message.delivered events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEvent"
    MessageDelivery:
      type: object
      additionalProperties: false
      required:
        - contact
        - delivered_at
        - read_at
      properties:
        contact:
          $ref: "#/components/schemas/ChatHandle"
          description: Message recipient whose independent receipt state this row records.
        delivered_at:
          type:
            - string
            - "null"
          format: date-time
          description: When this recipient reached Delivered. A user reaches Delivered when a Relay device durably applies
            the Message. An agent reaches Delivered when Relay commits the Message and makes it available through the Agent
            API.
        read_at:
          type:
            - string
            - "null"
          format: date-time
          description: When this recipient marked the Chat Read. Read implies Delivered.
    MessageEvent:
      type: object
      description: Unified payload for the current Relay message.sent, message.received, message.delivered, and message.read
        webhooks. direction is relative to the receiving agent.
      required:
        - chat
        - id
        - direction
        - sender_handle
        - parts
      properties:
        chat:
          type: object
          description: Chat information
          required:
            - id
          properties:
            id:
              type: string
              format: uuid
              description: Chat identifier
            is_group:
              type:
                - boolean
                - "null"
              description: Whether this is a group chat
            owner_handle:
              oneOf:
                - $ref: "#/components/schemas/ChatHandle"
                - type: "null"
        id:
          type: string
          format: uuid
          description: Message identifier
        idempotency_key:
          type:
            - string
            - "null"
          description: Idempotency key for deduplication of outbound messages.
        direction:
          type: string
          description: Message direction - "outbound" if sent by you, "inbound" if received
          enum:
            - inbound
            - outbound
        sender_handle:
          description: The handle that sent this message
          $ref: "#/components/schemas/ChatHandle"
        parts:
          type: array
          description: Message parts (text and/or media)
          items:
            oneOf:
              - $ref: "#/components/schemas/schemas-TextPartResponse"
              - $ref: "#/components/schemas/schemas-MediaPartResponse"
              - $ref: "#/components/schemas/schemas-LinkPartResponse"
            discriminator:
              propertyName: type
              mapping:
                text: "#/components/schemas/schemas-TextPartResponse"
                media: "#/components/schemas/schemas-MediaPartResponse"
                link: "#/components/schemas/schemas-LinkPartResponse"
        sent_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was sent. Null if not yet sent.
        delivered_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was delivered. Null if not yet delivered.
        read_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was read. Null if not yet read.
        reply_to:
          type:
            - object
            - "null"
          description: Reference to the message this is replying to (for threaded replies)
          properties:
            message_id:
              type: string
              format: uuid
              description: ID of the message being replied to
            part_index:
              type: integer
              format: int32
              minimum: 0
              description: Index of the part being replied to
    MessagePart:
      type: object
      required:
        - type
      discriminator:
        propertyName: type
        mapping:
          text: "#/components/schemas/TextPart"
          media: "#/components/schemas/MediaPart"
          link: "#/components/schemas/LinkPart"
      oneOf:
        - $ref: "#/components/schemas/TextPart"
        - $ref: "#/components/schemas/MediaPart"
        - $ref: "#/components/schemas/LinkPart"
    MessageReadWebhook:
      description: Complete webhook payload for message.read events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEvent"
    MessageReceivedWebhook:
      description: Complete webhook payload for message.received events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEvent"
    MessageSentWebhook:
      description: Complete webhook payload for message.sent events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEvent"
    ParticipantAddedEvent:
      type: object
      description: Payload for participant.added webhook events
      required:
        - chat_id
        - participant
        - added_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID) of the group chat
        participant:
          description: The added participant as a full handle object
          $ref: "#/components/schemas/ChatHandle"
        added_at:
          type: string
          format: date-time
          description: When the participant was added
    ParticipantAddedWebhook:
      description: Complete webhook payload for participant.added events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ParticipantAddedEvent"
    ParticipantRemovedEvent:
      type: object
      description: Payload for participant.removed webhook events
      required:
        - chat_id
        - participant
        - removed_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID) of the group chat
        participant:
          description: The removed participant as a full handle object
          $ref: "#/components/schemas/ChatHandle"
        removed_at:
          type: string
          format: date-time
          description: When the participant was removed
    ParticipantRemovedWebhook:
      description: Complete webhook payload for participant.removed events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ParticipantRemovedEvent"
    Reaction:
      type: object
      required:
        - is_me
        - handle
        - type
      properties:
        is_me:
          type: boolean
          description: Whether this reaction is from the current user
        handle:
          $ref: "#/components/schemas/ChatHandle"
        type:
          $ref: "#/components/schemas/ReactionType"
        custom_emoji:
          type:
            - string
            - "null"
          description: Custom emoji if type is "custom", null otherwise
    ReactionAddedEvent:
      description: Payload for reaction.added webhook events
      $ref: "#/components/schemas/ReactionEventBase"
    ReactionAddedWebhook:
      description: Complete webhook payload for reaction.added events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ReactionAddedEvent"
    ReactionEventBase:
      type: object
      required:
        - chat_id
        - from_handle
        - message_id
        - part_index
        - reaction_type
        - is_from_me
        - reacted_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID)
        from_handle:
          description: The person who added/removed the reaction as a full handle object
          $ref: "#/components/schemas/ChatHandle"
        message_id:
          type: string
          description: Message identifier (UUID) that the reaction was added to or removed from
        part_index:
          type: integer
          format: int32
          minimum: 0
          description: Index of the message part that was reacted to (0-based)
        reaction_type:
          $ref: "#/components/schemas/ReactionType"
        custom_emoji:
          type:
            - string
            - "null"
          description: The actual emoji when reaction_type is "custom". Null for standard tapbacks.
        is_from_me:
          type: boolean
        reacted_at:
          type: string
          format: date-time
          description: When the reaction was added or removed
    ReactionRemovedEvent:
      description: Payload for reaction.removed webhook events
      $ref: "#/components/schemas/ReactionEventBase"
    ReactionRemovedWebhook:
      description: Complete webhook payload for reaction.removed events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ReactionRemovedEvent"
    ReactionType:
      type: string
      description: Six standard tapbacks or a custom Unicode emoji.
      enum:
        - love
        - like
        - dislike
        - laugh
        - emphasize
        - question
        - custom
    RemoveParticipantRequest:
      type: object
      required:
        - handle
      properties:
        handle:
          type: string
          description: Relay Handle.
    ReplyTo:
      type: object
      description: Indicates this message is a threaded reply to another message
      required:
        - message_id
      properties:
        message_id:
          type: string
          format: uuid
          description: The ID of the message to reply to
        part_index:
          type: integer
          format: int32
          description: |
            The specific message part to reply to (0-based index).
            Defaults to 0 (first part) if not provided.
            Use this when replying to a specific part of a multipart message.
          minimum: 0
    RequestUploadRequest:
      type: object
      required:
        - filename
        - content_type
        - size_bytes
      properties:
        filename:
          type: string
          description: Name of the file to upload
          minLength: 1
          maxLength: 255
        content_type:
          $ref: "#/components/schemas/SupportedContentType"
        size_bytes:
          type: integer
          format: int64
          description: Size of the file in bytes (max 100MB)
          minimum: 1
          maximum: 104857600
        duration_ms:
          type:
            - integer
            - "null"
          minimum: 0
          description: Audio or video duration in milliseconds when known.
        width:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel width when known. Width and height are supplied together.
        height:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel height when known. Width and height are supplied together.
    RequestUploadResult:
      type: object
      required:
        - attachment_id
        - upload_url
        - download_url
        - http_method
        - expires_at
        - required_headers
      properties:
        attachment_id:
          type: string
          description: Unique identifier for the attachment
          format: uuid
        upload_url:
          type: string
          format: uri
          description: Opaque Relay upload URL. PUT the raw bytes with the exact required_headers before expires_at.
        download_url:
          type: string
          format: uri
          description: Opaque Relay download URL for the Attachment.
        http_method:
          type: string
          description: HTTP method to use for upload (always PUT)
          enum:
            - PUT
        expires_at:
          type: string
          format: date-time
          description: When the upload URL expires (15 minutes from now)
        required_headers:
          type: object
          description: Exact HTTP headers required by the upload URL.
          additionalProperties:
            type: string
    SendMessageRequest:
      type: object
      description: Resolve or reuse the Chat for the recipient Handles, then send a Message as the authenticated Contact.
      required:
        - to
        - message
      properties:
        to:
          type: array
          minItems: 1
          maxItems: 31
          items:
            type: string
        message:
          description: The Message to send. Text, link, and media parts are supported. Voice memos use POST
            /v1/chats/{chatId}/voicememo.
          $ref: "#/components/schemas/MessageContent"
    SendMessageResponse:
      type: object
      description: Response for sending a message to a chat
      required:
        - chat_id
        - message
      properties:
        chat_id:
          type: string
          format: uuid
          description: Unique identifier of the chat this message was sent to
        message:
          $ref: "#/components/schemas/SentMessage"
    SendMessageResult:
      type: object
      description: The resolved Chat and resulting Message.
      required:
        - chat_id
        - created_new_chat
        - from
        - is_group
        - handles
        - message
      properties:
        chat_id:
          type: string
          format: uuid
          description: The reused or newly created Chat.
        created_new_chat:
          type: boolean
        from:
          type: string
          description: Handle of the authenticated sending Contact.
        is_group:
          type: boolean
          description: Whether the resolved chat is a group chat.
        handles:
          type: array
          description: Contacts in the resolved Chat.
          items:
            $ref: "#/components/schemas/ChatHandle"
        message:
          description: The primary message that was sent.
          $ref: "#/components/schemas/SentMessage"
    SendMessageToChatRequest:
      type: object
      required:
        - message
      properties:
        message:
          $ref: "#/components/schemas/MessageContent"
    SendReactionRequest:
      type: object
      required:
        - operation
        - type
      properties:
        operation:
          type: string
          description: Whether to add or remove the reaction
          enum:
            - add
            - remove
        type:
          $ref: "#/components/schemas/ReactionType"
        custom_emoji:
          type: string
          description: |
            Custom emoji string. Required when type is "custom".
        part_index:
          type: integer
          description: |
            Optional index of the message part to react to.
            If not provided, reacts to the entire message (part 0).
      if:
        properties:
          type:
            const: custom
      then:
        required:
          - custom_emoji
        properties:
          custom_emoji: {}
    SendVoiceMemoToChatRequest:
      type: object
      description: |
        Request to send a voice memo to a chat (chat_id provided in path).
        Exactly one of `voice_memo_url` or `attachment_id` must be provided.
      properties:
        voice_memo_url:
          type: string
          format: uri
          description: |
            URL of the voice memo audio file. Must be a publicly accessible HTTPS URL.

            Either `voice_memo_url` or `attachment_id` must be provided, but not both.
        attachment_id:
          type: string
          format: uuid
          description: |
            Reference to a voice memo file pre-uploaded via `POST /v1/attachments`.
            The file is already stored, so sends using this ID skip the download step.

            Either `voice_memo_url` or `attachment_id` must be provided, but not both.
      oneOf:
        - required:
            - voice_memo_url
          properties:
            voice_memo_url: {}
            attachment_id: false
        - required:
            - attachment_id
          properties:
            voice_memo_url: false
            attachment_id: {}
    SendVoiceMemoToChatResult:
      type: object
      description: Response for sending a voice memo to a chat
      required:
        - voice_memo
      properties:
        voice_memo:
          type: object
          required:
            - id
            - from
            - to
            - status
            - voice_memo
            - created_at
            - chat
          properties:
            id:
              type: string
              format: uuid
              description: Message identifier
            from:
              type: string
            to:
              type: array
              items:
                type: string
            status:
              type: string
              description: Current delivery status
            voice_memo:
              $ref: "#/components/schemas/VoiceMemoAttachment"
            created_at:
              type: string
              format: date-time
              description: When the voice memo was created
            chat:
              $ref: "#/components/schemas/ChatInfo"
    SentMessage:
      type: object
      description: A message that was sent (used in CreateChat and SendMessage responses)
      required:
        - id
        - parts
        - created_at
        - sent_at
        - delivery_status
      properties:
        id:
          type: string
          format: uuid
          description: Message identifier (UUID)
        parts:
          type: array
          description: Message parts in order (text, media, and link)
          items:
            oneOf:
              - $ref: "#/components/schemas/TextPartResponse"
              - $ref: "#/components/schemas/MediaPartResponse"
              - $ref: "#/components/schemas/LinkPartResponse"
        created_at:
          type: string
          format: date-time
          description: When the message was created
        sent_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was actually sent (null if still queued)
        delivered_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was delivered
        delivery_status:
          $ref: "#/components/schemas/DeliveryStatus"
        from_handle:
          description: The sender of this message as a full handle object
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        reply_to:
          oneOf:
            - $ref: "#/components/schemas/ReplyTo"
            - type: "null"
    SetContactCardRequest:
      type: object
      required:
        - handle
        - first_name
      properties:
        first_name:
          type: string
          description: First name for the contact card. Required.
        last_name:
          type: string
          description: Last name for the contact card. Optional.
        image_url:
          type: string
          description: >-
            Profile image for the contact card, as a permanent address served
            by Relay. Addresses on other hosts are rejected.
        handle:
          type: string
          description: Relay Handle associated with this contact card.
    SetContactCardResponse:
      type: object
      required:
        - handle
        - first_name
        - is_active
        - kind
        - last_name
        - image_url
      properties:
        first_name:
          type: string
          description: First name on the contact card
        last_name:
          type:
            - string
            - "null"
        image_url:
          type:
            - string
            - "null"
          format: uri
        is_active:
          type: boolean
          description: Whether the contact card was successfully applied to the device
        handle:
          type: string
          description: Relay Handle associated with this contact card.
        kind:
          type: string
          enum:
            - user
            - agent
          description: Whether the Contact is a user or an agent.
    SupportedContentType:
      type: string
      description: Supported declared MIME types for uploaded Attachments and media URLs. Relay stores and returns the
        original bytes unchanged. Relay accepts WebP and rejects SVG as an explicit content-safety decision.
      enum:
        - image/jpeg
        - image/png
        - image/gif
        - image/heic
        - image/heif
        - image/tiff
        - image/bmp
        - image/webp
        - image/x-icon
        - video/mp4
        - video/quicktime
        - video/mpeg
        - video/mpeg2
        - video/x-m4v
        - video/x-msvideo
        - video/3gpp
        - audio/mpeg
        - audio/mp3
        - audio/x-m4a
        - audio/mp4
        - audio/x-caf
        - audio/x-wav
        - audio/x-aiff
        - audio/aiff
        - audio/aac
        - audio/midi
        - audio/amr
        - application/pdf
        - application/vnd.apple.pkpass
        - text/plain
        - text/markdown
        - text/vcard
        - text/rtf
        - text/csv
        - text/html
        - text/calendar
        - application/msword
        - application/vnd.openxmlformats-officedocument.wordprocessingml.document
        - application/vnd.ms-excel
        - application/vnd.openxmlformats-officedocument.spreadsheetml.sheet
        - application/vnd.ms-powerpoint
        - application/vnd.openxmlformats-officedocument.presentationml.presentation
        - application/x-iwork-pages-sffpages
        - application/x-iwork-numbers-sffnumbers
        - application/x-iwork-keynote-sffkey
        - application/epub+zip
        - text/xml
        - application/json
        - application/zip
        - application/x-gzip
    SystemEvent:
      type: object
      additionalProperties: false
      required:
        - type
        - actor
        - subject
        - value
        - icon_attachment_id
        - contact_card
      properties:
        type:
          type: string
          enum:
            - chat_created
            - participant_added
            - participant_removed
            - group_name_updated
            - group_icon_updated
            - contact_card_shared
        actor:
          $ref: "#/components/schemas/SystemEventParty"
        subject:
          oneOf:
            - $ref: "#/components/schemas/SystemEventParty"
            - type: "null"
        value:
          type:
            - string
            - "null"
        icon_attachment_id:
          type:
            - string
            - "null"
          format: uuid
        contact_card:
          oneOf:
            - $ref: "#/components/schemas/ContactCardItem"
            - type: "null"
    SystemEventParty:
      type: object
      additionalProperties: false
      required:
        - id
        - handle
        - kind
      properties:
        id:
          type: string
          format: uuid
        handle:
          type: string
        kind:
          type: string
          enum:
            - user
            - agent
    SystemPartResponse:
      type: object
      additionalProperties: false
      required:
        - type
        - value
        - reactions
      properties:
        type:
          type: string
          enum:
            - system
        value:
          type: string
        reactions:
          type: "null"
    TextPart:
      type: object
      required:
        - type
        - value
      properties:
        type:
          type: string
          enum:
            - text
          description: Indicates this is a text message part
        value:
          type: string
          description: Plain text content, measured in UTF-16 code units for mention ranges.
          minLength: 1
          maxLength: 10000
        mention:
          type:
            - string
            - "null"
          description: Relay Handle mentioned by this text part.
        mention_range:
          type:
            - array
            - "null"
          prefixItems:
            - type: integer
              minimum: 0
            - type: integer
              minimum: 1
          minItems: 2
          maxItems: 2
          description: UTF-16 [start,end) range of the mention inside value.
    TextPartResponse:
      type: object
      description: A text message part
      required:
        - type
        - value
        - reactions
      properties:
        type:
          type: string
          enum:
            - text
          description: Indicates this is a text message part
        value:
          type: string
          description: The text content
        mention:
          type:
            - string
            - "null"
          description: Relay Handle mentioned by this text part.
        mention_range:
          type:
            - array
            - "null"
          prefixItems:
            - type: integer
              minimum: 0
            - type: integer
              minimum: 1
          minItems: 2
          maxItems: 2
          description: UTF-16 [start,end) range of the mention inside value.
        reactions:
          type:
            - array
            - "null"
          description: Reactions on this message part
          items:
            $ref: "#/components/schemas/Reaction"
    TypingContact:
      type: object
      additionalProperties: false
      required:
        - id
        - handle
        - kind
      description: Contact currently publishing typing state.
      properties:
        id:
          type: string
          format: uuid
          description: Contact identifier.
        handle:
          type: string
          description: Current Relay Handle.
        kind:
          type: string
          enum:
            - user
            - agent
          description: Whether the Contact is a user or an agent.
    UnblockHandleRequest:
      type: object
      required:
        - handle
      properties:
        handle:
          type: string
          description: Registered Relay Handle.
    UpdateChatRequest:
      type: object
      properties:
        display_name:
          type: string
          description: New display name for the chat (group chats only)
        group_chat_icon:
          type:
            - string
            - "null"
          format: uuid
          description: Completed image Attachment ID to use as the group photo, or null to clear it.
    UpdateContactCardRequest:
      type: object
      properties:
        first_name:
          type: string
          description: Updated first name. If omitted, the existing value is kept.
        last_name:
          type:
            - string
            - "null"
          description: Updated last name. If omitted, the existing value is kept.
        image_url:
          type:
            - string
            - "null"
          description: >-
            Updated profile image, as a permanent address served by Relay.
            Addresses on other hosts are rejected. If omitted, the existing
            image is kept.
          format: uri
    UpdateWebhookSubscriptionRequest:
      type: object
      properties:
        target_url:
          type: string
          format: uri
          description: New target URL for webhook events
        subscribed_events:
          type: array
          description: Updated list of event types to subscribe to
          minItems: 1
          items:
            $ref: "#/components/schemas/WebhookEventType"
        is_active:
          type: boolean
          description: Activate or deactivate the subscription
    VoiceMemoAttachment:
      type: object
      required:
        - id
        - url
        - filename
        - mime_type
        - size_bytes
      properties:
        id:
          type: string
          format: uuid
          description: Attachment identifier
        url:
          type: string
          format: uri
          description: Opaque Relay download URL for the voice memo Attachment.
        filename:
          type: string
          description: Original filename
        mime_type:
          type: string
          description: Audio MIME type
        size_bytes:
          type: integer
          description: File size in bytes
        duration_ms:
          type:
            - integer
            - "null"
          minimum: 0
          description: Audio or video duration in milliseconds when known.
        width:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel width when known. Width and height are supplied together.
        height:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel height when known. Width and height are supplied together.
    WebSocketAckFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - through_sequence
      properties:
        type:
          type: string
          enum:
            - ack
        through_sequence:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
    WebSocketDisconnectFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - reason
      properties:
        type:
          type: string
          enum:
            - disconnect
        reason:
          type: string
          enum:
            - revoked
            - heartbeat_timeout
            - restart
            - webhook_configured
    WebSocketErrorFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - code
        - message
        - fatal
        - retryable
      properties:
        type:
          type: string
          enum:
            - error
        code:
          type: string
          enum:
            - invalid_frame
            - ack_out_of_range
            - stale_connection
            - ack_failed
            - delivery_failed
            - full_sync_required
            - full_sync_mismatch
        message:
          type: string
        fatal:
          type: boolean
        retryable:
          type: boolean
    WebSocketEventFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - sequence
        - event
      properties:
        type:
          type: string
          enum:
            - event
        sequence:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
        event:
          $ref: "#/components/schemas/WebhookEnvelopeBase"
    WebSocketFullSyncCompleteFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - through_sequence
      properties:
        type:
          type: string
          enum:
            - full_sync_complete
        through_sequence:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
    WebSocketFullSyncFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - through_sequence
        - reason
      properties:
        type:
          type: string
          enum:
            - full_sync
        through_sequence:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
        reason:
          type: string
          enum:
            - checkpoint_outside_retention
    WebSocketPingFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - sent_at
      properties:
        type:
          type: string
          enum:
            - ping
        sent_at:
          type: string
          format: date-time
    WebSocketPongFrame:
      type: object
      additionalProperties: false
      required:
        - type
      properties:
        type:
          type: string
          enum:
            - pong
    WebSocketReadyFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - connection_id
        - acked_through
        - full_sync_required
        - full_sync_through
        - heartbeat_interval_ms
        - max_in_flight
      properties:
        type:
          type: string
          enum:
            - ready
        connection_id:
          type: string
          format: uuid
        acked_through:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
        full_sync_required:
          type: boolean
        full_sync_through:
          type:
            - string
            - "null"
          pattern: ^(0|[1-9][0-9]*)$
        heartbeat_interval_ms:
          type: integer
          minimum: 1
        max_in_flight:
          type: integer
          minimum: 1
    WebhookEnvelopeBase:
      type: object
      required:
        - api_version
        - webhook_version
        - event_type
        - event_id
        - created_at
        - data
        - trace_id
        - agent_id
      properties:
        api_version:
          type: string
          enum:
            - v1
          description: Relay API version for this webhook envelope.
        webhook_version:
          type: string
          description: Fixed Relay webhook payload version.
          enum:
            - 2026-08-30
        event_type:
          $ref: "#/components/schemas/WebhookEventType"
        event_id:
          type: string
          format: uuid
          description: Unique identifier for this event (for deduplication)
        created_at:
          type: string
          format: date-time
          description: When the event was created
        trace_id:
          type: string
          description: Trace ID for debugging and correlation across systems.
        agent_id:
          type: string
          format: uuid
          description: Contact identifier of the receiving agent.
        data:
          type: object
          description: Event payload. Its shape is selected by event_type.
          additionalProperties: true
    WebhookEventType:
      type: string
      enum:
        - message.sent
        - message.received
        - message.read
        - message.delivered
        - reaction.added
        - reaction.removed
        - participant.added
        - participant.removed
        - chat.created
        - chat.group_name_updated
        - chat.group_icon_updated
        - chat.typing_indicator.started
        - chat.typing_indicator.stopped
        - contact.added
        - contact.removed
    WebhookEventsResult:
      type: object
      required:
        - events
        - doc_url
      properties:
        events:
          type: array
          description: List of all available webhook event types
          items:
            $ref: "#/components/schemas/WebhookEventType"
        doc_url:
          type: string
          format: uri
          description: URL to the webhook events documentation
          const: https://docs.relayapp.im/guides/webhooks/events
    WebhookSubscriptionCreatedResponse:
      type: object
      description: Response returned when creating a webhook subscription. Includes the signing secret which is only shown once.
      required:
        - id
        - target_url
        - signing_secret
        - subscribed_events
        - is_active
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Unique identifier for the webhook subscription
        target_url:
          type: string
          format: uri
          description: URL where webhook events will be sent
        signing_secret:
          type: string
          description: Secret for verifying webhook signatures. Store this securely - it cannot be retrieved again.
        subscribed_events:
          type: array
          description: List of event types this subscription receives
          items:
            $ref: "#/components/schemas/WebhookEventType"
        is_active:
          type: boolean
          description: Whether this subscription is currently active
        created_at:
          type: string
          format: date-time
          description: When the subscription was created
        updated_at:
          type: string
          format: date-time
          description: When the subscription was last updated
    WebhookSubscriptionResponse:
      type: object
      required:
        - id
        - target_url
        - subscribed_events
        - is_active
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Unique identifier for the webhook subscription
        target_url:
          type: string
          format: uri
          description: URL where webhook events will be sent
        subscribed_events:
          type: array
          description: List of event types this subscription receives
          items:
            $ref: "#/components/schemas/WebhookEventType"
        is_active:
          type: boolean
          description: Whether this subscription is currently active
        created_at:
          type: string
          format: date-time
          description: When the subscription was created
        updated_at:
          type: string
          format: date-time
          description: When the subscription was last updated
    schemas-LinkPartResponse:
      type: object
      description: A rich link preview part
      required:
        - type
        - value
      properties:
        type:
          type: string
          enum:
            - link
          description: Indicates this is a rich link preview part
        value:
          type: string
          description: The URL
    schemas-MediaPartResponse:
      type: object
      description: A media attachment part
      required:
        - type
        - id
        - url
        - filename
        - mime_type
        - size_bytes
      properties:
        type:
          type: string
          enum:
            - media
          description: Indicates this is a media attachment part
        id:
          type: string
          format: uuid
          description: Unique attachment identifier
        url:
          type: string
          format: uri
          description: Opaque Relay download URL for the Attachment.
        filename:
          type: string
          description: Original filename
        mime_type:
          type: string
          description: MIME type of the file
        size_bytes:
          type: integer
          description: File size in bytes
    schemas-TextPartResponse:
      type: object
      description: A text message part
      required:
        - type
        - value
      properties:
        type:
          type: string
          enum:
            - text
          description: Indicates this is a text message part
        value:
          type: string
          description: The text content
        mention:
          type:
            - string
            - "null"
        mention_range:
          type:
            - array
            - "null"
          items:
            type: integer
          minItems: 2
          maxItems: 2
          description: |
            Character range `[start, end)` in `value` highlighted as the mention, as sent.
            `null` when the send omitted it (the whole `value` is highlighted) or the part
            carries no mention.
            *Characters are measured as UTF-16 code units. Most characters count as 1; some emoji count as 2.*
  responses:
    BadRequest:
      description: Invalid request - validation error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: The request conflicts with the current state of the resource
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Forbidden - no access to this resource
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    PaymentRequired:
      description: The authenticated Agent cannot send Add requests.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Unauthorized - missing or invalid authentication
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    UnprocessableEntity:
      description: Unprocessable Entity - request is valid but cannot be processed
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      description: |
        Bearer token authentication. Include your API token in the Authorization header.

        Format: `Authorization: Bearer <your-token>`
x-tagGroups:
  - name: Messaging
    tags:
      - Chats
      - Messages
      - Attachments
  - name: Contacts
    tags:
      - Contacts
      - Contact Card
      - Blocked Handles
  - name: Agent events
    tags:
      - Webhooks
      - WebSocket
x-relay-websocket-frames:
  ready:
    $ref: "#/components/schemas/WebSocketReadyFrame"
  event:
    $ref: "#/components/schemas/WebSocketEventFrame"
  ack:
    $ref: "#/components/schemas/WebSocketAckFrame"
  full_sync:
    $ref: "#/components/schemas/WebSocketFullSyncFrame"
  full_sync_complete:
    $ref: "#/components/schemas/WebSocketFullSyncCompleteFrame"
  ping:
    $ref: "#/components/schemas/WebSocketPingFrame"
  pong:
    $ref: "#/components/schemas/WebSocketPongFrame"
  error:
    $ref: "#/components/schemas/WebSocketErrorFrame"
  disconnect:
    $ref: "#/components/schemas/WebSocketDisconnectFrame"
x-relay-websocket-close-codes:
  "1011": Relay could not load or commit delivery state; reconnect and resume.
  "1012": Relay is restarting; reconnect and resume.
  "4401": The Agent Token is invalid or revoked.
  "4408": The heartbeat timed out.
  "4410": Webhook delivery is now configured for this Agent.
x-relay-webhooks:
  message.sent.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageSentV20260830
      summary: Message sent
      description: An agent-authenticated message was committed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageSentWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.received.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageReceivedV20260830
      summary: Message received
      description: A user message was committed for the receiving agent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageReceivedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.read.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageReadV20260830
      summary: Message read
      description: Every message recipient explicitly marked the message Read.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageReadWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.delivered.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageDeliveredV20260830
      summary: Message delivered
      description: Every message recipient reached its Delivered boundary.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageDeliveredWebhook"
      responses:
        "200":
          description: Webhook received successfully
  reaction.added.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookReactionAddedV20260830
      summary: Reaction added
      description: A chat member added or replaced a reaction.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReactionAddedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  reaction.removed.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookReactionRemovedV20260830
      summary: Reaction removed
      description: A chat member removed its matching reaction.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReactionRemovedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  participant.added.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookParticipantAddedV20260830
      summary: Participant added
      description: A handle was added to a group chat.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ParticipantAddedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  participant.removed.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookParticipantRemovedV20260830
      summary: Participant removed
      description: A handle was removed from a group chat.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ParticipantRemovedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.group_name_updated.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatGroupNameUpdatedV20260830
      summary: Group name updated
      description: A group chat name changed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatGroupNameUpdatedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.group_icon_updated.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatGroupIconUpdatedV20260830
      summary: Group icon updated
      description: A group chat icon URL changed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatGroupIconUpdatedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.created.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatCreatedV20260830
      summary: Chat created
      description: A chat containing the receiving agent was created.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatCreatedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.typing_indicator.started.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatTypingIndicatorStartedV20260830
      summary: Typing indicator started
      description: The identified Contact started or refreshed typing state in the Chat.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatTypingIndicatorStartedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.typing_indicator.stopped.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatTypingIndicatorStoppedV20260830
      summary: Typing indicator stopped
      description: The identified Contact stopped typing or its typing state expired.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatTypingIndicatorStoppedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  contact.added.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookContactAddedV20260830
      summary: Contact added
      description: A user added the receiving agent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactAddedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  contact.removed.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookContactRemovedV20260830
      summary: Contact removed
      description: A user removed or blocked the receiving agent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactRemovedWebhook"
      responses:
        "200":
          description: Webhook received successfully
````
