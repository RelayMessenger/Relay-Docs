# Relay

# Introduction

> Connect your agent to Relay or build messaging into your backend.

Source: https://docs.staging.relayapp.im/index.md

Relay is a messenger where people talk to agents.
Your agent runs in a coding tool or a backend that you control.
Relay gives it a handle that people can open on their phone.
Your code receives messages and sends replies through the Relay API.
Chats support text, files, voice memos, reactions, and groups.

<Columns cols={3}>
  <Card title="Connect a coding agent" href="/connect/index">Use Relay from a coding tool on your computer.</Card>
  <Card title="Build on the API" href="/start/authentication">Connect your own backend with an Agent Token.</Card>
  <Card title="Read the CLI" href="/cli/index">Install, connect, and inspect your agent from a terminal.</Card>
</Columns>

## Next steps

- [Quickstart](/start/quickstart)
- [Key concepts](/start/key-concepts)
- [Authentication](/start/authentication)

# Quickstart

> Install the CLI, connect a coding agent, and open its live message view.

Source: https://docs.staging.relayapp.im/start/quickstart.md

Connect a coding agent on your computer, then open its chat in Relay.

## Before you start

- Node.js 22.22.3 or newer.
- A supported coding agent on this computer, such as Claude Code, Codex, or Cursor.
- Relay on your phone.
- For the existing-agent path below, an Agent Token in `RELAY_AGENT_TOKEN` from the same API environment.

## Install the CLI

Install the command with npm. The Connect step also runs it directly with `npx`, without a separate installation.

```bash npm
npm install relaymessenger@0.1.6-staging.5 --no-audit --no-fund
```


The npm installation returned this output. npm's update notice depends on your installed npm version.

```text Output
added 44 packages in 2s
npm notice
npm notice New major version of npm available! 10.9.8 -> 12.0.2
npm notice Changelog: https://github.com/npm/cli/releases/tag/v12.0.2
npm notice To update run: npm install -g npm@12.0.2
npm notice
```

## Connect your agent

Choose your runtime in [Connect](/connect/index). This captured run uses Cursor and an existing staging token. It writes the MCP entry and saves the token; open Cursor afterward so the runtime can answer.

```bash Connect Cursor
export RELAY_API_URL="https://api.staging.relayapp.im"
printf '%s\n' "$RELAY_AGENT_TOKEN" | npx relaymessenger@0.1.6-staging.5 --agent no connect cursor \
  --with-token --yes --no-start --no-skill --api-url "$RELAY_API_URL"
```

The captured run used an isolated home directory. Your configuration path will be under your own home directory.

```text Output
Cursor was not found on this computer; you named it, so Relay will try anyway
That token is @relay_staging_fullsync  token saved privately on this computer
Relay will do 1 thing. Continue?
  1  add  mcpServers.relay  to  /Users/advaitpaliwal/Code/Relay/_artifacts/docs-guides-20260910/docs-nav-start-20260910-captures/home/.cursor/mcp.json  (every other entry kept)
Relay MCP server added to Cursor  /Users/advaitpaliwal/Code/Relay/_artifacts/docs-guides-20260910/docs-nav-start-20260910-captures/home/.cursor/mcp.json
Relay is ready for Cursor.
You might have to restart 'Cursor'.
Later:  relay watch relay_staging_fullsync  ·  relay doctor
```

## Watch messages arrive

Use the handle printed by connect. This is the command for the captured staging agent.

```bash Watch
npx relaymessenger@0.1.6-staging.5 --agent no watch relay_staging_fullsync
```

The terminal includes a QR code, a share link, and these status lines, copied from the live view.

```text Output
Agent: connection not checked
Live view: watching only; your agent still receives every message
This view does not start or stop your agent.
```

Open the share link on your phone and send `hello`. Keep the runtime running and look for its reply in the chat.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## Stop viewing

Press `q` or `Ctrl-C` in the live view.

```text Output
Stopped viewing. Agent and runtime were not changed.
```

## Next steps

- [Connect Claude Code](/connect/claude-code)
- [Authenticate API requests](/start/authentication)
- [Read the agent setup instructions](/connect/agent-prompt#relay-agent-prompt)

# Key concepts

> Understand agents, handles, chats, messages, requests, and webhooks.

Source: https://docs.staging.relayapp.im/start/key-concepts.md

Relay carries messages between people and the agents they choose.

## Agent

An agent is the identity that your code uses in Relay. Its name and photo appear beside its messages. Your coding tool or backend supplies its behavior; Relay supplies messaging.

## Handle

A handle is a public messaging address, such as `weather.acme`. People use it to find an agent or open its share link. Store the Contact ID for lasting relationships because a handle can change.

## Chat

A chat holds messages and the list of participants. A user-facing chat contains one person and one or more agents. The API also supports chats between agents.

## Message

A message is an ordered list of parts sent to a chat. Parts can contain text, media, or links. Keep the message ID to reply, react, edit, or unsend later.

## Message request

The first message to a new contact is the request. A reply accepts it, so there is no separate request body to send. Blocking a handle prevents further contact where the block applies.

## Webhook

A webhook sends an HTTP request to your backend when something changes in Relay. You register a URL and select events, then verify each delivery's signature. Your backend receives the event and decides what the agent does next.

## Next steps

- [Send a message](/build/messages/send)
- [Receive webhooks](/build/events/webhooks)
- [Set your agent's name and photo](/build/identity/contact-card)

# Authentication

> Use an Agent Token to authenticate requests to the Relay API.

Source: https://docs.staging.relayapp.im/start/authentication.md

Authenticate as your agent by sending its token in the bearer header.

## Before you start

- [Create an agent](/build/agents/create-agent) and keep the token returned at creation.
- Store that token in `RELAY_AGENT_TOKEN` in your server environment.
- Set `RELAY_API_URL` to the API environment that issued the token.
- For TypeScript, [install the SDK](/start/sdks).

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Verify your token

Read one chat to confirm that Relay accepts your credentials. This request does not send a message or start your runtime.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" "$RELAY_API_URL/v1/chats?limit=1" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript
import Relay from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN,
  baseURL: process.env.RELAY_API_URL,
});
const result = await relay.chats.listChats({ limit: 1 });
console.log(JSON.stringify({
  chats: result.chats,
  next_cursor: result.nextCursor,
}, null, 2));
```
</CodeGroup>

## What you get back

HTTP `200` confirms access. This is the complete HTTP response captured from the staging agent; your chat list will differ. The SDK also exposes pagination helpers, so the TypeScript example prints the equivalent response fields.

```json Response
{
  "chats": [
    {
      "id": "01a05224-50ba-743c-b078-6458f4186e07",
      "display_name": "relay_staging_probe",
      "group_chat_icon": null,
      "handles": [
        {
          "id": "01a05100-bfcc-740a-ab5a-ddc249cec43a",
          "handle": "relay_staging_probe",
          "status": "active",
          "joined_at": "2026-08-30T10:08:26.863Z",
          "left_at": null,
          "is_me": false,
          "kind": "agent",
          "display_name": "Staging Probe",
          "image_url": null,
          "about": "A Relay agent, created with relay agents create.",
          "verified": false,
          "is_removable": true
        },
        {
          "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
          "handle": "relay_staging_fullsync",
          "status": "active",
          "joined_at": "2026-08-30T10:08:26.863Z",
          "left_at": null,
          "is_me": true,
          "kind": "agent",
          "display_name": "Staging Full Sync",
          "image_url": null,
          "about": "A Relay agent, created with relay agents create.",
          "verified": false,
          "is_removable": true
        }
      ],
      "is_group": false,
      "created_at": "2026-08-30T10:08:27.179Z",
      "updated_at": "2026-08-30T10:08:27.179Z"
    }
  ],
  "next_cursor": null
}
```

## When it fails

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2004](/error/codes/2xxx/2004) | 401 | The token is missing, invalid, or revoked | Check the token and its API environment. |
| [2003](/error/codes/2xxx/2003) | 403 | The credential cannot perform the operation | Use the credential for the agent that has access. |

## Next steps

- [Connect your own backend](/connect/your-own-backend)
- [Send a message](/build/messages/send)
- [Receive events](/build/events/webhooks)

# TypeScript SDK

> Install the Relay TypeScript SDK and create a server-side client.

Source: https://docs.staging.relayapp.im/start/sdks.md

Use `@relaymessenger/sdk` to call the Relay API from your backend.

## Install

Use Node.js 22.22.3 or newer:

```bash
npm install @relaymessenger/sdk@staging
```

## Create a client

```typescript
import Relay from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  baseURL: "https://api.staging.relayapp.im",
});
```

**Keep the Agent Token on the server.** Pair it with the API origin for the
environment that issued it, and always set `baseURL` explicitly.

## Make a request

```typescript
const page = await relay.chats.listChats({ limit: 1 });
```

An empty `page.chats` is a successful read. Task guides show SDK calls beside
their equivalent HTTPS requests.

<span id="browser-limitation" />
<span id="errors" />
<span id="pagination" />
<span id="related" />
<span id="resources" />
<span id="retries-and-idempotency" />
<span id="runnable-examples" />
<span id="send-a-message" />
<span id="webhook-verification" />

## Next steps

- [Send a Message](/build/messages/send)
- [Page through history](/build/chats/history)
- [Configure SDK retries](/build/live/retries)
- [Handle SDK errors](/build/live/errors)
- [Verify Webhooks](/build/events/verify-signatures)
- [Run an example](/examples)

# Pick your agent

> Choose the runtime that will read and answer your Relay messages.

Source: https://docs.staging.relayapp.im/connect/index.md

Choose the product you already use to connect it to Relay.

| Agent | What the connection does |
| --- | --- |
| [Claude Code](/connect/claude-code) | Receive messages in a running session through the Relay channel. |
| [Codex](/connect/codex) | Give the runtime authenticated Relay messaging tools over MCP. |
| [Cursor](/connect/cursor) | Give the runtime authenticated Relay messaging tools over MCP. |
| [OpenCode](/connect/opencode) | Give the runtime authenticated Relay messaging tools over MCP. |
| [Cline](/connect/cline) | Give the runtime authenticated Relay messaging tools over MCP. |
| [VS Code](/connect/vs-code) | Give the runtime authenticated Relay messaging tools over MCP. |
| [Gemini CLI](/connect/gemini-cli) | Give the runtime authenticated Relay messaging tools over MCP. |
| [Claude Desktop](/connect/claude-desktop) | Give the runtime authenticated Relay messaging tools over MCP. |
| [Hermes](/connect/hermes) | Receive messages and send replies through the gateway. |
| [OpenClaw](/connect/openclaw) | Receive messages and send replies through the gateway. |

## Next steps

- [Configure another MCP client](/connect/mcp)
- [Connect your own backend](/connect/your-own-backend)
- [Add Relay guidance](/connect/skills)

# Claude Code

> Connect Claude Code to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/claude-code.md

Receives Relay messages through the Claude Code channel.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to Claude Code with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://github.com/anthropics/claude-code#installation */}

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect claude-code --dry-run --no-start --no-skill
```

```text captured-output
Claude Code found  /Users/advaitpaliwal/.local/bin/claude
Relay will do 4 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  run  claude plugin marketplace add RelayMessenger/Relay-SDK@staging
  3  run  claude plugin install relay@relay-messenger --yes, then  claude plugin enable relay@relay-messenger
  4  write  /tmp/docs-connect-20260910/home/.claude/channels/relay/.env  (token, API address, allowed senders)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

The channel file sets `RELAY_AGENT_TOKEN`, `RELAY_BASE_URL`, and `RELAY_ALLOWED_SENDERS`. The plugin commands also update Claude Code’s plugin registry.

### Install the plugin

| Requirement | Value |
| --- | --- |
| Node.js | `22.22.3` or newer. |
| Claude Code | Channels research-preview support. |
| Relay | A staging Agent Token with zero saved Webhook subscriptions. |
| Senders | At least one exact Contact UUID or Handle to allowlist. |

Inside Claude Code, add the marketplace and install the plugin:

```text
/plugin marketplace add RelayMessenger/Relay-SDK@staging
/plugin install relay@relay-messenger
```

If prompted after installation, run `/reload-plugins`.

Source: [Relay Claude Code channel](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/claude-code)
and [marketplace](https://github.com/RelayMessenger/Relay-SDK/blob/staging/.claude-plugin/marketplace.json).


### Configure the channel

The plugin starts disabled. Enable it in `/plugin` only for the session that
should receive Relay Messages, then supply:

| Setting | Value |
| --- | --- |
| Relay Agent Token | Your staging token, saved by Connect in the channel’s owner-only `.env` file. |
| Allowed Relay senders | Comma-separated exact Contact UUIDs or Handles for the intended users and agents. |
| Relay API origin | `https://api.staging.relayapp.im`. |

**Explicitly allow each intended sender.** Agent Contacts use the same
allowlist as user Contacts. Sender permissions and reply-origin checks do not
create separate Claude model contexts for each Chat.

For optional CLI configuration, follow [native setup](/connect/native-setup).


### Start the selected session

For a custom channel in the Channels research preview, launch Claude Code with:

```bash
claude --dangerously-load-development-channels plugin:relay@relay-messenger
```

Keep the session running and follow the
[native messaging success checks](/connect/native-setup#verify-messaging).
Claude Code permission prompts remain local to Claude Code.


### Handle a Relay turn

| Step | Channel behavior |
| --- | --- |
| Accept an event | Save the complete event and local cursor in SQLite before the SDK sends its ACK. |
| Start processing | Claude calls `begin_processing`, which marks the Chat Read. |
| Reply | Claude calls `reply` with a stable `send_id` and the active turn's authenticated Chat. A reply closes the turn. |
| Finish without a reply | Claude calls `complete_processing` to close the turn. |

Direct Messages are eligible after sender checks. Group Messages must
canonically mention this agent or reply to its Message in the same Chat.
FULL sync reconciles unread, addressed, allowlisted inbound Messages using this
agent's own delivery row for Read state.

Delivery into Claude is at least once. Before repeating a non-idempotent tool
operation after a restart, check whether it already succeeded. For an uncertain
reply, reuse the same `send_id` and arguments.


### Answer a second Message during a reply

The channel notifies every delivery into the session at once, and one turn
runs at a time. What happens to the second Message depends on when Claude
calls `begin_processing` for it.

| Second Message arrives | Outcome |
| --- | --- |
| Claude replies to the first, then calls `begin_processing` | Each Message gets its own reply. |
| Claude calls `begin_processing` before replying to the first | The first turn closes as superseded. Its Message goes back to pending and is notified again on the first flush after the new turn closes, so Claude opens a fresh turn for it. |
| The first turn passes its ten-minute lease | The same as superseded: the Message returns to pending and is notified again once no turn is running. |

A Message answered by `reply` or closed by `complete_processing` stays closed
and is never answered twice. A Message an older channel version left stuck is
put back to pending the next time the channel starts.






## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Handle retries](/build/live/retries)

# Codex

> Connect Codex to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/codex.md

Adds Relay messaging tools to Codex.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to Codex with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

Pipe the token from your secret store into the Connect command below. `--with-token` reads it from stdin.

## Install

{/* Runtime installation source: https://github.com/openai/codex#quickstart */}

```bash
npm install --global @openai/codex
```

You can also run the runtime without a global install.

```bash
npx @openai/codex
```

## Connect

```bash
npx relaymessenger@staging connect codex --with-token --yes --no-start --no-skill --api-url "$RELAY_API_URL"
```

```text captured-output
Codex found  /Users/advaitpaliwal/.local/bin/codex
That token is @relay_staging_fullsync  token saved privately on this computer
Relay will do 1 thing. Continue?
  1  run  codex mcp add relay --env RELAY_CONFIG_PATH=/tmp/docs-connect-20260910/home/relay.json -- npx -y @relaymessenger/mcp@staging --profile relay_staging_fullsync  (adds the Relay MCP server to /tmp/docs-connect-20260910/home/.codex/config.toml)
Relay MCP server added to Codex  /tmp/docs-connect-20260910/home/.codex/config.toml
Relay is ready for Codex.
Later:  relay watch relay_staging_fullsync  ·  relay doctor
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `relaymessenger@0.1.6-staging.5` and Codex 0.154.0. Connect saved the existing agent token in `/tmp/docs-connect-20260910/home/relay.json` and registered `mcp_servers.relay` in `/tmp/docs-connect-20260910/home/.codex/config.toml`. The MCP process receives `RELAY_CONFIG_PATH` and the saved profile name. Your normal paths are `~/.config/relay/config.json` and `~/.codex/config.toml`.

The `relay` MCP entry launches `npx -y @relaymessenger/mcp@staging --profile <profile>`. The runtime calls these tools during a session. Keep the runtime running and ask it to read and answer the Relay chat.

The message-reading probe used `codex -c features.mcp_2026_07_28=true` to expose the MCP tools in Codex 0.154.0. This is an under-development Codex feature. Review tool permissions in the local session.

### Install locally

Use Codex CLI `0.152.0` with the Relay plugin:

```bash
npm install --global @openai/codex@0.152.0
git clone --branch staging https://github.com/RelayMessenger/Relay-SDK.git
cd Relay-SDK
codex plugin marketplace add "$PWD"
codex plugin add relay@relay-plugin-marketplace
```

Start a new Codex session after installation. The marketplace name is
`relay-plugin-marketplace`; its Relay entry points to `plugins/relay`.

Source: [Relay plugin](https://github.com/RelayMessenger/Relay-SDK/tree/staging/plugins/relay)
and [Codex marketplace](https://github.com/RelayMessenger/Relay-SDK/blob/staging/.agents/plugins/marketplace.json).


### Confirm the installation

Confirm that the Relay skill is available in the new session and that the
`relayDocs` MCP server is configured. Ask Codex to use the Relay skill for your
next Relay integration task.

For the distinction between documentation search and authenticated messaging
tools, see [documentation search](/connect/skills#connect-documentation-search).






## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Add Relay guidance](/connect/skills)

# Cursor

> Connect Cursor to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/cursor.md

Adds Relay messaging tools to Cursor.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to Cursor with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://cursor.com/docs/cli/installation */}

```bash
curl https://cursor.com/install -fsS | bash
```

The installer above installs Cursor CLI. For the editor, [download Cursor](https://cursor.com/downloads).

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect cursor --dry-run --no-start --no-skill
```

```text captured-output
Cursor was not found on this computer; you named it, so Relay will try anyway
Relay will do 2 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  add  mcpServers.relay  to  /tmp/docs-connect-20260910/home/.cursor/mcp.json  (every other entry kept)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

The `relay` MCP entry launches `npx -y @relaymessenger/mcp@staging --profile <profile>`. The runtime calls these tools during a session. Keep the runtime running and ask it to read and answer the Relay chat.

### Install locally

Clone the staging source, then link the plugin directory into Cursor's local
plugins directory:

```bash
git clone --branch staging https://github.com/RelayMessenger/Relay-SDK.git
cd Relay-SDK
mkdir -p "$HOME/.cursor/plugins/local"
ln -s "$PWD/plugins/relay" "$HOME/.cursor/plugins/local/relay"
```

If `relay` already exists at that path, inspect the existing installation before
changing it. Keep the source checkout while using the linked plugin.

Source: [Relay plugin](https://github.com/RelayMessenger/Relay-SDK/tree/staging/plugins/relay).


### Confirm the installation

Reload Cursor, open **Customize**, and confirm that the Relay skill and
`relayDocs` MCP server are present. Ask Cursor to use the Relay skill for your
next Relay integration task.

For the distinction between documentation search and authenticated messaging
tools, see [documentation search](/connect/skills#connect-documentation-search).






## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Add Relay guidance](/connect/skills)

# OpenCode

> Connect OpenCode to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/opencode.md

Adds Relay messaging tools to OpenCode.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to OpenCode with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://opencode.ai/docs/ */}

```bash
curl -fsSL https://opencode.ai/install | bash
```

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect opencode --dry-run --no-start --no-skill
```

```text captured-output
OpenCode was not found on this computer; you named it, so Relay will try anyway
Relay will do 2 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  add  mcp.relay  to  /tmp/docs-connect-20260910/home/.config/opencode/opencode.json  (every other entry kept)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

The `relay` MCP entry launches `npx -y @relaymessenger/mcp@staging --profile <profile>`. The runtime calls these tools during a session. Keep the runtime running and ask it to read and answer the Relay chat.

## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Handle retries](/build/live/retries)

# Cline

> Connect Cline to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/cline.md

Adds Relay messaging tools to Cline.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to Cline with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://docs.cline.bot/cline-cli/overview */}

```bash
npm install --global cline
```

You can also run the runtime without a global install.

```bash
npx cline
```

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect cline --dry-run --no-start --no-skill
```

```text captured-output
Cline was not found on this computer; you named it, so Relay will try anyway
Relay will do 2 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  run  cline mcp add --yes relay -- npx -y @relaymessenger/mcp@staging --profile <profile>  (adds the Relay MCP server to /tmp/docs-connect-20260910/home/.cline/data/settings/cline_mcp_settings.json)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

The `relay` MCP entry launches `npx -y @relaymessenger/mcp@staging --profile <profile>`. The runtime calls these tools during a session. Keep the runtime running and ask it to read and answer the Relay chat.

## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Handle retries](/build/live/retries)

# VS Code

> Connect VS Code to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/vs-code.md

Adds Relay messaging tools to VS Code.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to VS Code with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://code.visualstudio.com/docs/setup/mac */}

[Download VS Code](https://code.visualstudio.com/docs/setup/mac), install the application, and sign in. Enable Copilot and its agent mode.

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect vscode --dry-run --no-start --no-skill
```

```text captured-output
VS Code was not found on this computer; you named it, so Relay will try anyway
Relay will do 2 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  add  servers.relay  to  /tmp/docs-connect-20260910/home/Library/Application Support/Code/User/mcp.json  (every other entry kept)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

The `relay` MCP entry launches `npx -y @relaymessenger/mcp@staging --profile <profile>`. The runtime calls these tools during a session. Keep the runtime running and ask it to read and answer the Relay chat.

## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Handle retries](/build/live/retries)

# Gemini CLI

> Connect Gemini CLI to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/gemini-cli.md

Adds Relay messaging tools to Gemini CLI.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to Gemini CLI with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://geminicli.com/docs/get-started/installation/ */}

```bash
npm install --global @google/gemini-cli
```

You can also run the runtime without a global install.

```bash
npx @google/gemini-cli
```

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect gemini-cli --dry-run --no-start --no-skill
```

```text captured-output
Gemini CLI was not found on this computer; you named it, so Relay will try anyway
Relay will do 2 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  run  gemini mcp add -s user relay npx -- -y @relaymessenger/mcp@staging --profile <profile>  (adds the Relay MCP server to /tmp/docs-connect-20260910/home/.gemini/settings.json)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

The `relay` MCP entry launches `npx -y @relaymessenger/mcp@staging --profile <profile>`. The runtime calls these tools during a session. Keep the runtime running and ask it to read and answer the Relay chat.

## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Handle retries](/build/live/retries)

# Claude Desktop

> Connect Claude Desktop to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/claude-desktop.md

Adds Relay messaging tools to Claude Desktop.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to Claude Desktop with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://claude.com/download */}

[Download Claude Desktop](https://claude.com/download), install the application, and sign in. Enable the local MCP server in the desktop application.

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect claude-desktop --dry-run --no-start --no-skill
```

```text captured-output
Claude Desktop was not found on this computer; you named it, so Relay will try anyway
Relay will do 2 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  add  mcpServers.relay  to  /tmp/docs-connect-20260910/home/Library/Application Support/Claude/claude_desktop_config.json  (every other entry kept)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

The `relay` MCP entry launches `npx -y @relaymessenger/mcp@staging --profile <profile>`. The runtime calls these tools during a session. Keep the runtime running and ask it to read and answer the Relay chat.

## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Handle retries](/build/live/retries)

# Hermes

> Connect Hermes to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/hermes.md

Receives Relay messages through the Hermes channel.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to Hermes with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://github.com/NousResearch/hermes-agent#installation */}

```bash
curl -fsSL https://hermes-agent.nousresearch.com/install.sh | bash
```

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect hermes --dry-run --no-start --no-skill
```

```text captured-output
Hermes was not found on this computer; you named it, so Relay will try anyway
Relay will do 3 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  run  hermes plugins install RelayMessenger/Relay-Hermes --enable
  3  write  /tmp/docs-connect-20260910/home/.hermes/.env  (token, API address, state folder)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

### Install the plugin

Use Python `3.11` through `3.13` with the `relay-hermes@1.0.0rc2` source
package. Install the Git-hosted plugin:

```bash
hermes plugins install RelayMessenger/Relay-Hermes --enable
```

The plugin registers the Hermes platform ID `relayapp`.

Source: [Relay-Hermes](https://github.com/RelayMessenger/Relay-Hermes).


### Configure staging

Use a staging Agent Token with zero saved Webhook subscriptions. Save the
following settings through the selected Hermes profile's secret/configuration
workflow:

| Setting | Value |
| --- | --- |
| `RELAY_AGENT_TOKEN` | The staging Agent Token from your secret store. |
| `RELAY_BASE_URL` | `https://api.staging.relayapp.im`. |
| `RELAY_STATE_DIR` | An absolute, separate state directory for this agent and API origin. |
| `RELAY_ALLOWED_CONTACTS` | Optional comma-separated Contact UUIDs allowed to start turns. |

**Keep each agent's state directory paired with its token and API origin.**
Changing the identity while reusing its SQLite state fails before startup.
Hermes resolves these settings within the active profile.

For optional CLI configuration, follow [native setup](/connect/native-setup).


### Start the gateway

With the selected profile's environment active, run:

```bash
hermes gateway run
```

For an always-on host, use Hermes's service commands:

```bash
hermes gateway install
hermes gateway start
```

Follow the [native messaging success checks](/connect/native-setup#verify-messaging).

| Condition | Behavior or action |
| --- | --- |
| Saved Webhook subscription | The WebSocket upgrade is refused. Choose the intended [delivery path](/build/events/websocket). |
| Restart or replay | The SQLite inbox retains accepted events and deduplicates their `event_id` values. |
| FULL sync | Historical Messages rebuild the snapshot; they do not start new Hermes turns. |
| Message begins with `/` | Relay slash commands are withheld from Hermes. Run operator commands through a trusted local CLI or authenticated dashboard. |

The plugin saves each event before sending its WebSocket ACK, marks the Chat
Read when processing starts, and sends replies through Relay REST with an
idempotency key.


### Answer a second Message during a reply

Hermes decides this with its own `display.busy_input_mode` setting in
`~/.hermes/config.yaml`. The plugin adds no setting of its own. It marks the
Chat Read the moment a Message is accepted, before Hermes chooses, so every
Message shows Read.

| `display.busy_input_mode` | Outcome on Relay |
| --- | --- |
| `interrupt` (default) | Hermes folds the Message into the running reply. It shows Read, and one reply answers both, without a quote. |
| `queue` | The Message waits and gets its own reply after the running one ends. |
| `steer` | Hermes hands the Message to the running turn after its next tool call, as Hermes documents. It shows Read, and one reply answers both. |

A folded Message is settled in the plugin's inbox, so a gateway restart does
not answer it again. A reply quotes a Message only when it answers one that
did not start the latest turn.






## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Handle retries](/build/live/retries)

# OpenClaw

> Connect OpenClaw to Relay and check its messaging setup.

Source: https://docs.staging.relayapp.im/connect/openclaw.md

Receives Relay messages through the OpenClaw channel.

## Before you start

Use Node.js 22.22.3 or newer and an Agent Token from [Relay authentication](/start/authentication). Sign in to OpenClaw with its own account or model provider before connecting.

```bash Environment
export RELAY_API_URL="https://api.staging.relayapp.im"
```

## Install

{/* Runtime installation source: https://docs.openclaw.ai/start/getting-started */}

```bash
npm install --global openclaw@2026.8.1
```

## Connect

This captured command prints the plan only. Remove `--dry-run` when ready to connect.

```bash
npx relaymessenger@staging connect openclaw --dry-run --no-start --no-skill
```

```text captured-output
OpenClaw was not found on this computer; you named it, so Relay will try anyway
Relay will do 4 things. Continue?
  1  create a new agent and save its token privately on this computer
  2  run  openclaw plugins install @relaymessenger/openclaw-plugin@staging --force --accept-capabilities
  3  write  /tmp/docs-connect-20260910/home/.openclaw/secrets/relay-<handle>.token  (the token alone, owner-only)
  4  add  channels.relay  to  /tmp/docs-connect-20260910/home/.openclaw/openclaw.json  (every other setting kept)
Dry run: nothing was changed.
```

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

The output above was captured with `--dry-run` using `relaymessenger@0.1.6-staging.5`. No runtime settings or tokens were changed. The printed paths use an isolated home directory; your paths use your own home. Remove `--dry-run` to apply the plan.

On a real run, Connect saves the selected agent token and API address in the shared CLI profile at `~/.config/relay/config.json`, then applies the files and commands listed in the plan. Existing unrelated settings are kept.

Connect sets `channels.relay.enabled`, `channels.relay.baseUrl`, `channels.relay.tokenFile`, and the allowed senders in `channels.relay.allowFrom`. Plugin installation also updates OpenClaw’s managed plugin state.

### Install the plugin

| Requirement | Value |
| --- | --- |
| Node.js | `22.22.3` or newer. |
| OpenClaw | `>=2026.8.1 <2026.9.0`. |
| Relay | A staging Agent Token with zero saved Webhook subscriptions. |

```bash
openclaw plugins install @relaymessenger/openclaw-plugin@staging
```

Source: [Relay OpenClaw plugin](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/openclaw).


### Configure the channel

Keep the token in an owner-only file and configure the selected gateway:

```json
{
  "channels": {
    "relay": {
      "enabled": true,
      "baseUrl": "https://api.staging.relayapp.im",
      "tokenFile": "/run/secrets/relay-agent-token",
      "allowFrom": ["00000000-0000-7000-8000-000000000901"]
    }
  }
}
```

Replace the token-file path and illustrative UUID with your actual values.
**Use Contact UUIDs in `allowFrom`; a Handle alone does not pass the stable-ID
authorization gate.** User and agent Contacts pass the same sender checks.

For multiple Relay accounts, give every named account its own `token` or
`tokenFile`. Channel-level credentials belong to the default account. Keep
each token paired with its API origin, and do not reuse a token across accounts.

For optional CLI configuration, follow [native setup](/connect/native-setup).


### Start the gateway

Start the selected gateway through OpenClaw's native launcher and follow the
[native messaging success checks](/connect/native-setup#verify-messaging).
Keep it running while the agent should receive Messages.

| Condition | Behavior or action |
| --- | --- |
| Saved Webhook subscription | The WebSocket connection is refused. Choose the intended [delivery path](/build/events/websocket). |
| Direct Message | The sender must pass the account's configured admission policy. |
| Group Message | The sender must pass admission, and the Message must canonically mention this agent or reply to its Message in the same Chat. |
| Restart or replay | Events are durably accepted before ACK; outbound replies reuse stable delivery keys. |

Keep the gateway's native DM session scope under review separately from sender
admission. An allowed sender is not evidence of isolated model memory.








## When it fails

| CLI output | Next step |
| --- | --- |
| `Relay cannot ask which agent to connect.` | Pipe your existing token with `--with-token`, or run Connect in a terminal to choose an agent. |
| `Profile default has no saved token. Run npx relaymessenger auth login --with-token to save one.` | [Save a token](/cli/auth) and select that profile. |
| `Relay would not accept that token, so nothing was changed.` | Check the token and its API environment, then retry Connect. |
| `<Runtime> was not found on this computer; you named it, so Relay will try anyway` | Install the runtime above before retrying. This line is a notice, not a successful installation. |
| `<command> did not finish. It said: <runtime output>` | Follow the runtime's error, then repeat Connect with `--yes`. The CLI retains the agent token. |

## Next steps

- [Send messages](/build/messages/send)
- [Read message history](/build/chats/history)
- [Reconcile full sync](/build/events/websocket/full-sync)

# MCP server

> Give a trusted local MCP client authenticated Relay messaging tools.

Source: https://docs.staging.relayapp.im/connect/mcp.md

Give a trusted local MCP client authenticated Relay messaging tools.

## Before you start

Use Node.js 22.22.3 or newer. [Save an Agent Token](/cli/auth) in a CLI profile and set `RELAY_PROFILE` to its name. Match `RELAY_API_URL` to the token environment, `https://api.staging.relayapp.im` for these examples.

## Install

{/* Installation source: https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/mcp */}

```bash npm
npm install --global @relaymessenger/mcp@staging
```

The client can instead launch `npx -y @relaymessenger/mcp@staging` without a global installation.

## Connect

For a supported runtime, use its [Connect guide](/connect/index). For another trusted local MCP host, configure this stdio process and replace `staging` with your saved profile name.

```json
{
  "mcpServers": {
    "relay": {
      "command": "npx",
      "args": ["-y", "@relaymessenger/mcp@staging", "--profile", "staging"]
    }
  }
}
```

Start the client and call `relay_list_chats`.

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

This manual setup adds one server entry to your MCP client's configuration. It uses the existing CLI token and profile rather than creating an agent. If the profile file is at a custom path, also pass `RELAY_CONFIG_PATH` in the server environment.

The server provides local stdio tools. Remote HTTP and remote OAuth are not implemented. The trusted local MCP host controls tool approval. `RELAY_AGENT_TOKEN` and `RELAY_API_URL` override saved credentials when supplied to the process; keep the token and API address paired.

[Maintained MCP source](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/mcp).

## When it fails

| Result | Next step |
| --- | --- |
| No saved token | Import a token with [CLI authentication](/cli/auth). |
| Server cannot be started | Make Node.js and `npx` available to the MCP host. |
| Tool permission refused | Review the tool in the local client's permission interface. |

## Next steps

- [Send messages](/build/messages/send)
- [Use idempotency keys](/build/live/idempotency)
- [Add documentation search](/connect/skills)

# Your own backend

> Run the code that reads a Relay message and chooses its reply.

Source: https://docs.staging.relayapp.im/connect/your-own-backend.md

Run the code that reads a Relay message and chooses its reply.

## Before you start

Use an Agent Token from the same environment as your API address. Your backend owns model access and its execution environment.

## Install

Choose [Chat SDK](/connect/chat-sdk) for an HTTP handler or [Cloudflare Think](/connect/cloudflare-think) for a Worker with durable model turns. Follow the chosen guide's package installation steps.

## Connect

Register a [webhook subscription](/build/events/subscriptions) for your public HTTPS route and retain its signing secret. Pass the unchanged signed body to your handler.

If your host needs a persistent connection instead, use [WebSocket delivery](/build/events/websocket) with zero saved webhook subscriptions. These are backend delivery paths, not targets for `connect <agent>`.

## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

Your chosen adapter receives Relay events and sends replies through the Relay API. Your application owns its model, prompts, hosting, and local configuration. The CLI does not write application code or deploy the backend.

## When it fails

| Result | Next step |
| --- | --- |
| No incoming message | Check the selected delivery path and agent identity. |
| Webhook rejected | Verify the signing secret and original request bytes. |
| Message arrives without a reply | Check your handler and model logs. |

## Next steps

- [Verify signatures](/build/events/verify-signatures)
- [Send a reply](/build/messages/replies)
- [Debug requests](/build/live/debugging)

# Chat SDK

> Connect Chat SDK to receive Relay messages and send replies.

Source: https://docs.staging.relayapp.im/connect/chat-sdk.md

Connect Chat SDK to receive Relay messages and send replies.

## Before you start

Use Node.js 22.22.3 or newer, an Agent Token, and a public HTTPS webhook route. Set `RELAY_API_URL` to `https://api.staging.relayapp.im`.

## Install

{/* Installation source: https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/chat-sdk-adapter */}

Use Node.js `22.22.3` or newer for this setup:

```bash
npm install chat@4.39.0 @chat-adapter/state-memory@4.39.0 \
  @relaymessenger/chat-sdk-adapter@staging
```

Source: [Relay Chat SDK adapter](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/chat-sdk-adapter).



## Connect


| Input | Value |
| --- | --- |
| `RELAY_AGENT_TOKEN` | Agent Token issued by the staging API, loaded through your secret store. |
| `RELAY_WEBHOOK_SECRET` | The one-time signing secret from the [Webhook subscription](/build/events/subscriptions). |
| Public endpoint | An HTTPS route that passes the original request body and headers to the adapter. |

Create a Chat SDK instance and register a direct-message handler:

```typescript
import { Chat } from "chat";
import { createMemoryState } from "@chat-adapter/state-memory";
import { createRelayAdapter } from "@relaymessenger/chat-sdk-adapter";

const chat = new Chat({
  userName: "Relay example",
  concurrency: { strategy: "burst", debounceMs: 2000 },
  adapters: {
    relay: createRelayAdapter({
      baseUrl: "process.env.RELAY_API_URL!,
      token: process.env.RELAY_AGENT_TOKEN,
      webhookSecret: process.env.RELAY_WEBHOOK_SECRET,
      markReadOnReceipt: true,
      abortActiveTurnOnReceipt: true,
    }),
  },
  state: createMemoryState(),
});

chat.onDirectMessage(async (thread, message) => {
  await thread.post(`Echo: ${message.text}`);
});

export async function POST(request: Request): Promise<Response> {
  return chat.webhooks.relay(request);
}
```

Mount `POST` at your public HTTPS Webhook route and register that exact URL in
the subscription. This example uses in-memory Chat SDK state for local setup.
For a standalone Node HTTP handler, use the
[source example](https://github.com/RelayMessenger/Relay-SDK/blob/staging/packages/chat-sdk-adapter/examples/server.mjs)
and set its adapter's `baseUrl` to the staging origin.

**Pass the raw body unchanged so the adapter can verify the signature.**
Send a direct Message to the agent and confirm that the echo returns in the
same Chat.



## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

Your backend receives signed Relay webhooks. The CLI does not install or deploy this backend. These setup instructions were checked against the maintained source; the reply remains unverified below.

### Handle retries and media

| Situation | Behavior or action |
| --- | --- |
| Reply during an inbound Webhook turn | The adapter derives the send key from `event_id` and the send ordinal. |
| Send outside a Webhook turn | Supply an `idempotencyKeyResolver` based on a stable task identity. |
| Empty response | The adapter sends no Relay Message. |
| Invalid signature | Check the signing secret and exact request bytes before retrying delivery. |
| Uncertain send | Reuse the same idempotency key and request content. |

The adapter accepts public HTTPS media URLs and file bytes. For an uncertain
media send, preserve the prepared Attachment identity rather than allocating a
new one on retry; see [Attachments](/build/messages/attachments).


### Answer a second Message during a reply

Chat SDK `4.39.0` defaults `concurrency` to `drop`. Under `drop`, a Message
that arrives while the handler is still running throws a `LockError` out of
`chat.webhooks.relay`, the Webhook fails, and Relay
[retries the delivery](/build/events/delivery). The three settings in the
example above replace that.

| Setting | Effect |
| --- | --- |
| `concurrency: { strategy: "burst", debounceMs: 2000 }` | Waits 2 seconds after the first Message, then runs the handler once with the latest Message; earlier ones arrive in `context.skipped`. A Message that arrives mid-run is queued and handled when the run ends. |
| `markReadOnReceipt: true` | Marks the Chat read as soon as the Webhook is verified, before the wait. |
| `abortActiveTurnOnReceipt: true` | A newer Message aborts the running turn through `thread.signal`. Pass that signal to your model call, or the old turn finishes and posts. |

Join the skipped Messages so one reply covers the burst:

```typescript
chat.onDirectMessage(async (thread, message, channel, context) => {
  const text = [...(context?.skipped ?? []), message]
    .map((m) => m.text)
    .join("\n\n");
  await thread.post(`Echo: ${text}`);
});
```









## When it fails

| Result | Next step |
| --- | --- |
| Signature verification fails | Use the subscription signing secret and the unchanged request body. |
| No reply | Check backend logs and the handler that starts a turn. |
| Send result is uncertain | Reuse the same idempotency key and request. |

## Next steps

- [Verify webhook signatures](/build/events/verify-signatures)
- [Handle delivery retries](/build/events/delivery)
- [Send attachments](/build/messages/attachments)

# Cloudflare Think

> Connect Cloudflare Think to receive Relay messages and send replies.

Source: https://docs.staging.relayapp.im/connect/cloudflare-think.md

Connect Cloudflare Think to receive Relay messages and send replies.

## Before you start

Use Node.js 22.22.3 or newer, an Agent Token, and a public HTTPS webhook route. Set `RELAY_API_URL` to `https://api.staging.relayapp.im`. You also need a Cloudflare account with Workers AI access.

## Install

{/* Installation source: https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/cloudflare-think-agent */}

| Requirement | Value |
| --- | --- |
| Node.js | `22.22.3` or newer. |
| Cloudflare | An account with Workers AI access. |
| Relay | A staging Agent Token and a public HTTPS Webhook destination. |

Clone the source and install the starter:

```bash
git clone --branch staging https://github.com/RelayMessenger/Relay-SDK.git
cd Relay-SDK/cookbook/cloudflare-think-agent
npm ci
cp .dev.vars.example .dev.vars
```

Source: [Cloudflare Think starter](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/cloudflare-think-agent).



## Connect


[Register a Webhook subscription](/build/events/subscriptions) for your
public HTTPS `/webhooks/relay` destination and retain its one-time signing
secret. Set the secrets in `.dev.vars`, keeping that file out of source control:

Load `RELAY_AGENT_TOKEN` and `RELAY_WEBHOOK_SECRET` from your secret store into the local Worker environment. Use the one-time signing secret returned for this subscription.

In `wrangler.jsonc`, set these non-secret values under `env.staging.vars`:

| Variable | Value |
| --- | --- |
| `RELAY_API_ORIGIN` | `https://api.staging.relayapp.im` |
| `RELAY_AGENT_HANDLE` | The Handle returned for your agent. |
| `MODEL_ID` | `@cf/openai/gpt-oss-120b` |

Start the Worker and route the registered HTTPS destination to it:

```bash
npm run dev
```



## Send it a message

Start your runtime and keep it open. Set `RELAY_SENDER_PROFILE` to a **different agent's** saved CLI profile and `RELAY_CHAT_ID` to a chat shared with the connected agent. Set `RELAY_PROFILE` to the connected agent's profile. The sender must be allowed by the runtime's admission rules.

In one terminal, watch the connected agent. In another, send the message.

```bash Watch
npx relaymessenger@staging --profile "$RELAY_PROFILE" watch
```

```bash Send from another agent
npx relaymessenger@staging --profile "$RELAY_SENDER_PROFILE" chats messages send "$RELAY_CHAT_ID" \
  --text "Hello, can you read this Relay message?" --idempotency-key "$RELAY_MESSAGE_KEY"
```

Set `RELAY_MESSAGE_KEY` to a fresh key for this message, and reuse it only when retrying the same send. Look for the agent's reply in the chat and the live view.

```text Reply
(unverified: reply not captured 2026-09-10)
```

## What this changed

Your backend receives signed Relay webhooks. The CLI does not install or deploy this backend. These setup instructions were checked against the maintained source; the reply remains unverified below.

### Answer a second Message during a reply

Think builds the Chat SDK instance itself with
`concurrency: { strategy: "burst", debounceMs: 600 }` and runs each reply with
`waitForCompletion: true`, so the Chat stays locked for the whole turn. The
starter cannot change that window.

| Second Message arrives | Outcome |
| --- | --- |
| Within 600 ms of the first | One turn runs for the latest Message; Think's handlers never read the earlier ones. |
| While the turn is running | It waits in the queue and gets its own reply when the turn ends. |
| Any time | Marked read on receipt, because the starter sets `markReadOnReceipt: true`. |

A newer Message never cancels a running turn. Think's handlers take only
`(thread, message)` and never read `thread.signal`, so
`abortActiveTurnOnReceipt` has no effect under Think and the starter leaves it
off. The
[starter README](https://github.com/RelayMessenger/Relay-SDK/blob/staging/cookbook/cloudflare-think-agent/README.md)
records why.







## When it fails

| Result | Next step |
| --- | --- |
| Signature verification fails | Use the subscription signing secret and the unchanged request body. |
| No reply | Check backend logs and the handler that starts a turn. |
| Send result is uncertain | Reuse the same idempotency key and request. |

## Next steps

- [Verify webhook signatures](/build/events/verify-signatures)
- [Handle delivery retries](/build/events/delivery)
- [Send attachments](/build/messages/attachments)

# Agent instructions

> Copy the complete instructions for a coding agent that connects a Relay backend.

Source: https://docs.staging.relayapp.im/connect/agent-prompt.md

Copy these instructions into your coding agent, or let it read `/skill.md` directly.
For a short task prompt, use [AI coding agents](/connect/coding-agents).

## Relay agent prompt

````text Relay agent prompt
---
name: relay
description: Build an agent and start talking to it in Relay.
---

# Relay developer guide

Build an agent and start talking to it in Relay.

Your backend owns the agent's model, tools, memory, and behavior. Relay carries
Messages in user-facing Chats between one user and one or more agents. The
developer API also supports agent-to-agent Chats with zero users.

## Start

1. Read the target environment's current OpenAPI and matching local docs first.
   In a Relay workspace, use `Relay-Server/contracts/developer/openapi.yaml`.
   Use `https://docs.staging.relayapp.im/llms.txt` for setup instructions and the
   page index, not as authority over a
   newer local contract. If the contract cannot be read, stop and report unknown.
2. Choose the identity path: use anonymous `POST /v1/agents` when the user asks
   for a new identity, or reuse their existing ordinary Agent Token. Neither
   path requires a Console account. Pair `RELAY_API_URL` with the token's issuing
   environment. Save a newly returned token privately before connecting code;
   keep every token out of source, logs, command output, and client-side code.
3. Verify access with `GET /v1/chats?limit=1`. HTTP `200`, including an empty
   `chats` array, verifies this read. Do not require `/v1/agents/me` or invent
   an identity endpoint.
4. Read the matching WebSocket or Webhooks guide. For the current
   always-on backend, use `/v1/websocket`, not legacy `/events` guidance.
   WebSocket requires zero saved webhook subscriptions; do not create a
   subscription as a WebSocket setup step or delete existing ones silently.
5. Commit each `event_id` once in durable storage before sending a Webhook
   `2xx` or WebSocket ACK. Run model and tool work after acknowledgment.
6. Send the first Message, or wait for the user to write first. The first
   Message is the request: a user who never wrote to the agent holds it as a
   silent message request until they reply, delete, or block it. A reply moves
   the request to `accepted`, and `chat.request.updated` reports the answer.
   `contact.added` carries the user Contact and the direct `chat_id` once the
   user writes first or replies.
7. Optionally mark the Chat Read only through `POST /v1/chats/{chatId}/read`.
   Reply through `POST /v1/chats/{chatId}/messages` with a stable idempotency key.

## Identity and runtime setup

Use `npx relaymessenger@staging` with the matching environment. Read the task
before running it:

- [Create an agent](https://docs.staging.relayapp.im/build/agents/create-agent.md): anonymous creation and private token storage. Create only when explicitly asked; never retry an uncertain creation blindly.
- [Use an existing token](https://docs.staging.relayapp.im/cli/auth.md): invalid credentials never trigger fallback creation. Keep supplied credentials on the existing-identity path.
- [Configure a runtime](https://docs.staging.relayapp.im/connect/native-setup.md): select the actual native account/session, obtain explicit configuration consent, and stop the selected runtime before writing. Preserve its permissions, model configuration, and state.
- [Profile photos](https://docs.staging.relayapp.im/build/identity/profile-photos.md): use the existing image and recipe contract. After partial upload failure, repair the saved identity instead of creating another.
- [Delete an agent](https://docs.staging.relayapp.im/build/agents/delete-agent.md): authenticate as that removable identity. Keep credentials on uncertain results; never fabricate acknowledgements to clear pending events.
- [Install Skills](https://docs.staging.relayapp.im/connect/skills.md): separate, consented use of the standard installer. Runtime credential consent does not authorize installing instructions.

A saved token or `connected: false` configuration result is not connection proof.
Start the selected runtime and verify real processing and a reply. Diagnostic
observation never acknowledges events or substitutes for the real consumer.
Use returned `share_url` and `image_url` values, preserving a custom image.

## Connect an existing agent

When asked to connect an agent, use the supplied Agent Token for that existing
agent. Do not create another agent. The supplied agent Handle identifies the
agent to connect, not a user.

Read the setup instructions here, then the matching runtime and
transport guides before acting. If the docs or required runtime capabilities
are unavailable, report the blocker instead of inventing setup commands.

| Setup input | Rule |
| --- | --- |
| Agent Token | Load through trusted server-side secret storage. Never echo it, embed it in source, or print credential-bearing requests or responses. |
| Environment | These docs default to `https://api.staging.relayapp.im/v1`. Use a token from staging. A supplied API base overrides the default only with matching environment docs and credentials. |
| API URL format | `RELAY_API_URL` and the TypeScript SDK `baseURL` use the origin `https://api.staging.relayapp.im`; documented HTTP paths include `/v1`. Never append `/v1` twice. |
| Connection method | Honor the supplied Webhook or WebSocket choice. A Webhook URL selects Webhook onboarding when no method is stated. Otherwise inspect the runtime and saved subscriptions before choosing a supported path. |
| Webhook URL | Use the supplied HTTPS receiver. If absent or set to `Find the webhook URL for me.`, find a receiver in the user's backend or ask for deployment access. Do not invent a URL. |
| Existing subscription | List saved subscriptions first, even when registration is not mentioned. Reuse the matching target URL rather than creating a duplicate. |

### Webhook onboarding

1. Follow the [Webhook subscriptions guide](https://docs.staging.relayapp.im/build/events/subscriptions.md)
   and [receiver guide](https://docs.staging.relayapp.im/build/events/webhooks.md).
   With the supplied Agent Token, call `GET /v1/webhook-subscriptions` and
   `GET /v1/webhook-events`. Onboarding subscribes to all event names returned
   by the current catalog unless the user explicitly requests a narrower set.
   Populate `subscribed_events` with the response's `events` array, not a `*`
   wildcard.
2. For a new target, call `POST /v1/webhook-subscriptions` with `target_url`
   and `subscribed_events`. Capture the one-time `signing_secret` directly
   into trusted secret storage without printing the response. Keep the
   secret out of source control, logs, and client-side code.
3. For a matching target, inspect its event coverage and active state. Use
   `PUT /v1/webhook-subscriptions/{subscriptionId}` when the requested setup
   requires updating its settings. Preserve unrelated subscriptions. Reuse
   the stored signing secret; if unavailable, ask the user to provide it
   through secure storage. Do not delete and recreate a subscription merely
   to obtain a new secret.
4. Configure Standard Webhooks signature verification over the exact raw
   request bytes using the saved secret, including timestamp validation.
   Prove invalid signatures are rejected and valid events are committed
   durably with `event_id` deduplication before a `2xx` response. Run model
   and tool work after acknowledgment; make outgoing replies idempotent.
5. Verify that the supplied endpoint is a receiver you can configure, not
   merely a request capture URL. If receiver code, secret storage, or runtime
   access is missing, report what remains pending. Subscription creation or
   an HTTP `2xx` alone does not prove signature verification or a working agent.

### Runtime and completion

Use the [Integrations overview](https://docs.staging.relayapp.im/connect/index.md)
to find the documented package for the actual runtime. A coding-agent
skill or docs connection alone is not a running Relay event consumer.
For WebSocket, follow the
[WebSocket guide](https://docs.staging.relayapp.im/build/events/websocket.md)
and preserve existing subscriptions unless the user authorizes changing the
event path. Do not silently switch a requested Webhook setup to WebSocket.

Start the configured backend or runtime connection and verify its event path.
Report the connection result without secrets. If only registration, code
generation, or tests completed, say so and leave the connection pending.

## Vocabulary

- A Contact is a user or agent profile.
- Every Contact owns one public Handle.
- Any Contact can Message any Handle. Nobody adds anyone: the first Message is
  the request. There is no request endpoint.
- An agent receives every Message from any user or agent, with no request and
  no approval. It blocks a Handle to refuse one.
- A user who never wrote to the agent and never replied to it holds the agent's
  first Message as a silent message request. The user's `message_requests_from`
  setting is `everyone` or `verified_agents`; a first Message it screens out
  returns `403`, code `2030`, and a blocked pair returns `403`, code `2026`.
- A Chat carries `request_state` only for a caller who was asked; an agent
  never is. `chat.request.updated` reports `accepted`, the user replied, or
  `deleted`.
- `contact.added` carries the user Contact and direct `chat_id` when the user
  writes to the agent first or replies to its request.
- `contact.removed` means the user removed or blocked the agent.
- A user-facing Chat contains one user and one or more agents: direct with one
  agent, group with multiple agents. The developer API also supports
  agent-to-agent Chats with zero users.
- New or reused Chats include at least one agent and at most one user, counting
  the authenticated sender. Every Chat has at most 7 active Contacts total,
  the sender plus at most 6 others.
- Both the user and an authorized agent can create and manage Chats. Managing
  an existing Chat requires active membership.
- Creating a Chat applies the message request rule to every recipient. Adding
  an agent to a Chat containing a user is refused only when that agent and the
  user block each other.
- Self-leave follows the existing membership rules. Contact edges, Chat
  membership, and history have separate lifecycles. Do not claim that removing
  a Contact removes the agent from all Chats or erases history.
- Only agents can be introduced. Admission is not conversational approval or
  company policy. Do not invent approval prompts or company-policy UI. Existing
  agent-only communication remains supported.
- The user stays a Contact, member, and sender. Only agent Contacts can be added
  to an existing Chat, leave, or be removed. Participant routes and events keep
  their generic names.
- A Message belongs to one Chat and contains ordered parts.
- Parts are `text`, `media`, or `link` on sends.
- Replies and reactions target zero-based `part_index`.
- Group membership controls which history a Contact can read.

## Webhook events

| Path | Configuration | Transport acknowledgement |
| --- | --- | --- |
| Webhooks | Save public HTTPS subscriptions for selected event types. | Verify the signature, deduplicate `event_id`, commit durably, then return `2xx`. |
| WebSocket | Connect to `/v1/websocket` with an empty subscription list. | Deduplicate `event_id`, commit durably, then send a cumulative ACK. |

Saving the first webhook subscription closes active agent sockets with code
`4410`. Matching pending events move to active Webhooks. Deleting the final
subscription moves pending events to WebSocket. Transferred events keep their
`event_id`.

Relay retains pending events for 30 days. A WebSocket upgrade returns HTTP
`409` while the agent has a saved webhook subscription.

## Accept events

For Webhooks:

```text
verify signature → deduplicate event_id → durable commit → 2xx → process
```

For WebSocket:

```text
deduplicate event_id → durable commit → cumulative ACK → process
```

Return `2xx` or send the ACK only after the durable commit. These are transport
acknowledgements only. They do not advance Delivered or Read.

Delivered means Relay accepted and stored the Message. Read is optional and advances only through
`POST /v1/chats/{chatId}/read`. Run model and tool work independently.

Webhook delivery is at least once. After the initial attempt, Relay retries
network errors, HTTP `429`, and HTTP `5xx` responses up to 10 times with delays
from 2 to 600 seconds. Each attempt has a 10-second response window.

Use a direct public HTTPS webhook destination. Relay validates DNS answers and
treats redirects as terminal delivery failures.

WebSocket ACKs are cumulative. Relay replays pending events after a reconnect.
Complete FULL sync when the checkpoint is older than retention. Relay sends a
ping every 30 seconds and requires a pong within 60 seconds.

Agent backends authenticate the `/v1/websocket` upgrade with
`Authorization: Bearer <Agent Token>`.

## Canonical contract

- Call the `/v1` paths defined by the current OpenAPI.
- Send Message commands through REST.
- Treat registered Handles as public messaging addresses.
- Treat every inbound `event_id` as at-least-once.
- Recover current state with ordinary REST reads or WebSocket FULL sync.
- Retain `trace_id` from API errors and webhook events for debugging.
- Use an API root and Agent Token from the same environment for every request.

Use the OpenAPI contract for exact fields, limits, and errors. Label unproved
behavior `unknown`.

## Developer tools

- Use `https://docs.staging.relayapp.im/mcp` for read-only documentation search.
- Use the local `@relaymessenger/mcp` stdio server for Relay API tools with an
  Agent Token.
- Use the Skills, Codex, or Cursor integrations for packaged coding guidance.
- Read the Integrations overview before selecting Vercel Chat SDK, Cloudflare
  Think, OpenClaw, Claude Code, or Hermes.
````

## See also

- [AI coding agents](/connect/coding-agents)
- [Quickstart](/start/quickstart)

# Install the Relay skill

> Install Relay guidance in your coding agent with the Skills installer.

Source: https://docs.staging.relayapp.im/connect/skills.md

Install the Relay skill to give your coding agent Relay setup and implementation guidance.

## Install

Clone the staging source and run the Skills installer:

```bash
git clone --branch staging https://github.com/RelayMessenger/Relay-SDK.git
npx skills@1.5.24 add ./Relay-SDK/skills/relay --skill relay
```

Choose the coding agent and project or global scope in the installer. Confirm
that `relay` appears in that agent's installed skills.

Source: [Relay skill](https://github.com/RelayMessenger/Relay-SDK/tree/staging/skills/relay).

The [interactive CLI](/cli/index#open-the-interactive-menu) also offers
optional skill installation before setup. You can decline that offer and
continue creating an agent or importing a token.

## Connect documentation search

The skill declares the staging docs MCP dependency. **Installing a skill alone
does not confirm that its host has connected the MCP server.** If your host
requires separate MCP setup, add:

```text
https://docs.staging.relayapp.im/mcp
```

| Surface | Credential | Job |
| --- | --- | --- |
| Docs MCP | None | Read-only documentation search. |
| [Relay API MCP](/connect/mcp) | Relay Agent Token | Call messaging operations from a trusted local client. |

For a plugin installation that includes docs MCP configuration, use the
[Codex](/connect/codex) or [Cursor](/connect/cursor) setup.

<span id="choose-the-docs-mcp" />
<span id="offer-skills-before-setup" />
<span id="prepare-hosted-proof" />
<span id="related" />

## Next steps

- [Create an agent](/build/agents/create-agent)
- [AI coding agents](/connect/coding-agents)
- [Relay API MCP](/connect/mcp)

# Connect a runtime

> Connect a coding agent on this computer to a new or existing Relay agent with one CLI command, then verify a Message and reply.

Source: https://docs.staging.relayapp.im/connect/native-setup.md

Connect the runtime on this computer to a Relay agent with one command, then verify a Message and a reply.

Source: [CLI connect](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/cli/src/runtime-connect).

## Prepare

| Input | Requirement |
| --- | --- |
| CLI | [Install the staging CLI](/cli/index#install), using Node.js `22.22.3` or newer. |
| Runtime | Install [Claude Code](/connect/claude-code) first. Relay looks for it on this computer. |
| Agent | None needed. To reuse an agent, keep its Agent Token in your secret store; to save a token without connecting, follow [CLI authentication](/cli/auth). |
| Environment | A staging token pairs with `https://api.staging.relayapp.im`. |
| Delivery | The agent must have zero saved Webhook subscriptions. |
| Phone | Relay on your phone, to add the agent and send it the first message. |

## Connect

`connect`: connect a coding agent to Relay, new or by token, and prove it answers.

Supported agents are Claude Code (`claude-code`), Codex (`codex`), Cursor
(`cursor`), OpenCode (`opencode`), Cline (`cline`), VS Code (`vscode`), Gemini CLI
(`gemini-cli`), Claude Desktop (`claude-desktop`), Hermes (`hermes`), and
OpenClaw (`openclaw`). The example below connects Claude Code.

Run the command in a terminal:

```bash
npx relaymessenger@staging connect claude-code
```

Relay says what it found, prints its plan, and asks `Continue?` before it
writes anything. Leave `claude-code` out and Relay asks which installed runtime will
answer as this agent. Then it:

1. Creates a new agent, or uses the one you name with `--token`, and saves the
   token privately on this computer.
2. Adds the Relay marketplace to Claude Code and installs and enables the
   `relay@relay-messenger` plugin.
3. Writes the channel file, owner-only, with `RELAY_AGENT_TOKEN`,
   `RELAY_BASE_URL`, and `RELAY_ALLOWED_SENDERS`. The path is
   `channels/relay/.env` under the Claude configuration directory, or
   `RELAY_CHANNEL_DIR` when set.
4. Shows the agent's QR and waits up to three minutes for the first message,
   then asks whether to allow that sender.
5. Offers to start Claude Code with the Relay channel loaded.

To reuse an agent you already have, pass its token from your secret store:

```bash
npx relaymessenger@staging connect claude-code --token "$RELAY_AGENT_TOKEN"
```

| Flag | Effect |
| --- | --- |
| `--new` | create a new agent instead of using one you already have |
| `--handle <handle>` | the .dev handle you want for a new agent; leave it out and Relay picks one |
| `--name <name>` | the name people see next to a new agent |
| `--about <text>` | the one line people see above your agent's first message |
| `--image <path-or-url>` | a picture for a new agent: a file on this computer, or an https:// address |
| `--token <token>` | use an agent you already have, by its token; visible in ps and shell history |
| `--with-token` | use an agent you already have; its token is read from a pipe |
| `--all` | connect every detected coding agent |

## Run it from a script

`--no-input`, `--non-interactive`: never ask a question; fail with exit 2 where one is required.
No environment variables are needed. RELAY_AGENT_TOKEN is honored in scripts only.
This example reads an existing token from a script variable:

```bash
npx relaymessenger@staging connect claude-code \
  --token "$RELAY_AGENT_TOKEN" \
  --allow alice,ops_bot.dev \
  --yes --no-start --json
```

| Flag | Effect |
| --- | --- |
| `--allow <handles>` | the handles allowed to message this agent, separated by commas; skips waiting for a first message |
| `--yes` | take the plan as it is |
| `--no-start` | do not start the agent at the end; print its command instead |
| `--no-skill` | do not offer the Relay skill at the end |
| `--dry-run` | print the plan and change nothing |
| `--api-url <url>` | the Relay API address to use |
| `--json` | print the result as JSON |

Inspect the plan without changing anything:

```bash
npx relaymessenger@staging connect claude-code --dry-run --json
```

The dry-run result has this shape. The home path depends on your computer:

```json
{
  "ok": true,
  "dry_run": true,
  "agents": [
    {
      "agent": "claude-code",
      "files": [
        "/Users/you/.claude/channels/relay/.env"
      ],
      "commands": [
        "claude plugin marketplace add RelayMessenger/Relay-SDK@staging",
        "claude plugin install relay@relay-messenger --yes",
        "claude plugin enable relay@relay-messenger"
      ]
    }
  ],
  "steps": [
    "1  create a new agent and save its token privately on this computer",
    "2  run  claude plugin marketplace add RelayMessenger/Relay-SDK@staging",
    "3  run  claude plugin install relay@relay-messenger --yes, then  claude plugin enable relay@relay-messenger",
    "4  write  /Users/you/.claude/channels/relay/.env  (token, API address, allowed senders)",
    "5  start Claude Code with Relay when you are ready"
  ]
}
```

Use the identity and start command returned when you connect your agent.
Run `npx relaymessenger@staging connect --help` to read the command's help.

## Verify messaging

1. Start Claude Code with the Relay channel loaded, from the start command
   Relay printed, and keep the session running.
2. Send a direct Message from a Contact named in `RELAY_ALLOWED_SENDERS`.
3. Confirm that the session receives the Message and a reply arrives in the
   same Chat.

**A saved token, a written channel file, or an open connection alone does not
prove the path.** If no reply arrives, run `npx relaymessenger@staging doctor`,
then check the sender permissions in the channel file, Webhook subscriptions,
and the session's own output. The [live view](/cli/watch)
shows whether a runtime is connected and what arrives, without taking any event.

<span id="create-and-configure-together" />
<span id="inspect-the-result" />
<span id="prepare-the-selected-context" />
<span id="reuse-an-existing-agent-token" />
<span id="review-with-an-agent" />
<span id="select-the-runtime" />

## Next steps

- [Claude Code](/connect/claude-code)
- [OpenClaw](/connect/openclaw)
- [Hermes](/connect/hermes)
- [Create an agent](/build/agents/create-agent)
- [CLI authentication](/cli/auth)

# AI coding agents

> Give a coding agent the current Relay docs and one specific task.

Source: https://docs.staging.relayapp.im/connect/coding-agents.md

Give your coding agent the docs index and the task you want it to complete.

## Connect an existing agent

Keep the existing Agent Token in your secret store, then use:

```text Connect with Webhooks
Connect my existing Relay agent @<agent-handle> following
https://docs.staging.relayapp.im/llms.txt.
Use its Agent Token from RELAY_AGENT_TOKEN, without printing it.
Webhook URL: <https-receiver-url>
```

For an always-on connection, replace the Webhook URL line with
`Connection method: WebSocket`.

<span id="relay-agent-prompt" />

The [complete agent instructions](/connect/agent-prompt#relay-agent-prompt)
cover credential safety, runtime setup, and event acceptance. The navbar's
**Copy agent prompt** copies those instructions.

## Build an integration

```text Build with Relay
Add Relay messaging to this codebase. Read
https://docs.staging.relayapp.im/llms.txt and the relevant task guides first.
Use the current OpenAPI for every request. Keep credentials private, commit
events before acknowledgment, and make Message retries idempotent.
Show the changed files, tests, and anything you could not verify.
```

## Audit an integration

```text Audit an integration
Audit this Relay integration without changing code.
Read https://docs.staging.relayapp.im/llms.txt, the relevant transport guide,
and the current OpenAPI. Check authentication, signature verification,
durable deduplication, acknowledgment timing, idempotent replies, and recovery.
For each finding, give file:line evidence and the owning documentation page.
Mark anything you cannot prove as unknown.
```

<span id="audit-prompt" />
<span id="build-prompt" />
<span id="documentation-files" />
<span id="minimal-connection-prompt" />
<span id="related" />

## See also

- [Install the Relay Skill](/connect/skills)
- [Choose an integration](/integrations)
- [Reliability checklist](/build/live/best-practices)

# Manage agents

> Create a messaging identity, inspect saved agents, or delete a developer-managed agent.

Source: https://docs.staging.relayapp.im/build/agents/lifecycle.md

Create and manage the Relay messaging identity that your backend uses to send and receive Messages.

**Relay supplies the identity and messaging. Your runtime supplies the agent's behavior.**

## Choose the identity

| Your task | Guide |
| --- | --- |
| Create a developer-managed `.dev` agent and save its token | [Create an agent with the CLI](/build/agents/create-agent) |
| Create an agent from your own code | [Create an agent through the API](/build/agents/create-agent-api) |
| Create an organization-managed agent | [Create an agent in Relay Console](/build/agents/console-agent) |
| Inspect the agents saved on this machine | [List agents](/build/agents/list-agents) |
| Remove a developer-managed agent | [Delete an agent](/build/agents/delete-agent) |
| Connect an identity whose Agent Token you already have | [Native setup](/connect/native-setup) |
| Set a profile photo or supply a rendered avatar recipe | [Profile photos](/build/identity/profile-photos) |

<span id="choose-a-profile-and-avatar" />
<span id="connect-and-share" />
<span id="create-through-the-api" />
<span id="create-with-the-cli" />
<span id="creation-failures" />
<span id="delete-a-developer-managed-agent" />
<span id="list-locally-configured-agents" />
<span id="prerequisites" />
<span id="supply-a-rendered-image-with-a-recipe" />
<span id="use-a-local-image" />

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Create an agent

> Create a Relay agent with the CLI and save its Agent Token in a local profile.

Source: https://docs.staging.relayapp.im/build/agents/create-agent.md

`agents create`: create an agent and save its token privately on this computer, with no account and no sign-in.

**Creating an agent requires no login, Console account, or existing Agent Token.**

## Before you start

| Requirement | Detail |
| --- | --- |
| Node.js | Version 22.22.3 or later, with npm. |
| Private storage | A writable, private location for the CLI's saved credentials. |

## Create with the CLI

```bash
npx relaymessenger@staging agents create
```

| Option | Effect |
| --- | --- |
| `--api-url <url>` | the Relay API address to use |
| `--json` | print the result as JSON |

No environment variables are needed. RELAY_AGENT_TOKEN is honored in scripts only.
Keep the saved token paired with its API origin when connecting your runtime.

## Use the returned identity

A representative JSON result is:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "profile": "brave_golden_cangoo.dev",
  "handle": "brave_golden_cangoo.dev",
  "display_name": "Brave Golden Cangoo",
  "image_url": "https://api.staging.relayapp.im/assets/relay-agent-default-ddda5a75f2a8850b.png",
  "share_url": "https://staging.relayapp.im/@brave_golden_cangoo.dev",
  "api_url": "https://api.staging.relayapp.im",
  "token": "stored"
}
```

| Field | Use |
| --- | --- |
| `profile` | `--profile <name>`: which saved profile on this computer to use. The default name is the returned Handle, with a suffix if that local profile name is occupied. |
| `handle` | Address the created agent using this exact Handle. |
| `display_name` | Display the agent's name. |
| `image_url` | Display the returned avatar. Creation supplies a default image when you omit one. |
| `share_url` | Share the returned HTTPS link or its QR. |
| `api_url` | Connect to the API origin where the identity was created. |
| `token` | `"stored"` confirms local storage; the credential stays in the saved profile. |

Use your returned values in subsequent commands. The example identity is
illustrative. You can inspect saved identities with [List agents](/build/agents/list-agents).

### Choose a profile and avatar

| Option | Effect |
| --- | --- |
| `--handle <handle>` | the .dev handle you want; leave it out and Relay picks one |
| `--name <name>` | the name people see next to this agent |
| `--about <text>` | the one line people see above your agent's first message |
| `--image <path-or-url>` | a picture: a file on this computer, or an https:// address |
| `--image-url <url>` | a picture at an https:// address (same as --image with a URL) |
| `--image-recipe <json-file>` | a Relay picture recipe file; needs --image or --image-url as well |
| `--token-name <name>` | a label for the new token, so you can tell it apart later |

 For optional profile fields and their validation rules, use the
[Create an agent API reference](/api-reference/agents/create-an-agent).

Follow [Profile photos](/build/identity/profile-photos) for local image uploads,
public image URLs, and rendered avatar recipes.

## Connect and share

Connect the saved identity through [native setup](/connect/native-setup),
[WebSocket](/build/events/websocket), or [Webhooks](/build/events/webhooks). Share the returned
link, then verify that an incoming Message reaches your runtime and receives a reply.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Creation failures

<Warning>
**Investigate an uncertain creation result before creating again.**
The CLI makes no automatic retry. A timeout can follow a committed creation,
and a local storage failure can follow a successful response.
</Warning>

| Outcome | Next action |
| --- | --- |
| Private-storage preflight fails | Fix the storage permissions before trying again. The CLI has sent no creation request. |
| HTTP `409` | Choose another Handle or explicitly omit the Handle option for generation. A requested Handle is never silently replaced. |
| HTTP `429` | Wait for `Retry-After` before another attempt. |
| Timeout, HTTP `500` or `503`, or unconfirmed response | Preserve any saved credential and inspect local profiles before deciding whether another creation is needed. |
| Agent created, local storage check failed | The token is returned only once. Follow the CLI's diagnostic for the assigned Handle and check private configuration and permissions before continuing. |
| Agent created, image update failed | Retry the [profile photo update](/build/identity/profile-photos) on that saved identity. |

See [API creation failures](/build/agents/create-agent-api#creation-failures)
for request validation and rate limits.


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Create an agent through the API

> Create a developer-managed agent from code and save its one-time Agent Token privately.

Source: https://docs.staging.relayapp.im/build/agents/create-agent-api.md

Create a developer-managed `.dev` identity with `POST /v1/agents` and save its one-time Agent Token before connecting your backend.

For automatic local credential storage, use [Create an agent with the CLI](/build/agents/create-agent).

## Before you start

Choose private storage for the complete response before sending the request.
The bootstrap request is unauthenticated. The examples below use
`https://api.staging.relayapp.im`; use the same origin with the returned token.

For the TypeScript example, install the SDK:

```bash
npm install @relaymessenger/sdk@staging
```

## Create and save the response

**Keep the creation response out of terminal output, logs, and caches.**
These POSIX examples reserve a new private directory before requesting an identity.
On Windows, use owner-private credential storage with verified access controls,
or use the CLI's private-storage checks.

<CodeGroup>
```bash HTTPS
umask 077
BOOTSTRAP_DIR="$(mktemp -d)" &&
curl -sS -A "relay-docs/1.0" --fail-with-body \
  --request POST https://api.staging.relayapp.im/v1/agents \
  --header 'Content-Type: application/json' \
  --data '{"token_name":"Relay CLI"}' \
  --output "$BOOTSTRAP_DIR/agent-bootstrap.json" \
  --write-out '%{http_code}\n'
```

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";
import { mkdtemp, writeFile } from "node:fs/promises";

const directory = await mkdtemp("./relay-bootstrap-");
const created = await Relay.createAgent(
  { token_name: "Relay CLI" },
  { baseURL: "https://api.staging.relayapp.im", maxRetries: 0 },
);
await writeFile(`${directory}/agent-bootstrap.json`, JSON.stringify(created), {
  mode: 0o600,
  flag: "wx",
});
console.log({ handle: created.agent.handle, share_url: created.share_url });
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The response is HTTP `201` with `Cache-Control: no-store`. Its shape is shown
below with the secret redacted:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "agent": {
    "handle": "brave_golden_cangoo.dev",
    "first_name": "Brave Golden Goose",
    "last_name": null,
    "image_url": "https://api.staging.relayapp.im/assets/relay-agent-default-ddda5a75f2a8850b.png",
    "is_active": true,
    "kind": "agent"
  },
  "secret": "<one-time-agent-token>",
  "share_url": "https://staging.relayapp.im/@brave_golden_cangoo.dev"
}
```

| Field | Handling |
| --- | --- |
| `agent` | The created Contact Card. Use its returned Handle and avatar. |
| `secret` | The one-time Agent Token. Save it privately for authenticated API requests. |
| `share_url` | The public HTTPS link to share with users. |

For optional profile fields and token labels, use the
[Create an agent API reference](/api-reference/agents/create-an-agent).
For image uploads and rendered recipes, follow [Profile photos](/build/identity/profile-photos).


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Creation failures

<Warning>
**Each successful create makes a new identity.**
The SDK never automatically retries this request, including after a timeout.
If a response or local save is uncertain, preserve any received response and
inspect private storage before deciding whether to create again.
</Warning>

| Outcome | Handling |
| --- | --- |
| HTTP `400` | Correct the JSON body, content type, or field values using the API reference. |
| HTTP `409` | The requested Handle is occupied. Choose another or explicitly omit it for generation. |
| HTTP `413` | Keep the JSON body within 8192 bytes. |
| HTTP `429` | Respect `Retry-After`. Creation is limited to 10 creations per caller address per day. |
| HTTP `500` or `503`, timeout, or invalid response | Investigate the outcome before another create. A failed response does not establish that creation failed. |
| Response received, saving failed | Preserve the one-time secret in private storage before continuing. |


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Create an agent in Relay Console

> Create an organization-managed agent, save its one-time token, and choose how to connect your backend.

Source: https://docs.staging.relayapp.im/build/agents/console-agent.md

Create an organization-managed agent in Relay Console and save its Agent Token for your backend.

For a developer-managed `.dev` identity, use [Create an agent with the CLI](/build/agents/create-agent).

## Before you start

- A [Relay Console](https://console.staging.relayapp.im) account with access to your organization and permission to create agents.
- Private server-side storage for the returned Agent Token.

## Create the agent

Open **Create agent** in Relay Console and fill in the identity fields:

| Field | What to enter |
| --- | --- |
| Name | The display name users see in Relay. |
| Handle | For the default organization Handle, enter the local part: 3 to 32 lowercase letters, numbers, or underscores, starting with a letter. Console appends your organization's namespace, so `forecast` becomes `forecast.yourorg`. |
| About | An optional description. |

Press **Create agent**. For avatar setup, follow [Profile photos](/build/identity/profile-photos).

## Choose a connection and save the token

<Steps>
  <Step title="Choose your connection">
    Select **WebSocket** or **Webhook** in the creation flow. Follow the
    [WebSocket](/build/events/websocket) or [Webhooks](/build/events/webhooks) guide for the
    connection you choose.
  </Step>
  <Step title="Copy the Agent Token">
    In **Connect your backend**, choose **Manually**, then **Copy token**.
    Store the token in private server-side storage before closing the dialog.
  </Step>
  <Step title="Connect your backend">
    Use the saved token with the matching API origin,
    `https://api.staging.relayapp.im` for the Console linked above.
    Continue with your selected connection guide and verify an incoming Message
    reaches your backend and receives a reply.
  </Step>
</Steps>

**The creation flow shows the full Agent Token once.** Keep it private when
configuring your backend.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Recover from setup failures

| Outcome | Next action |
| --- | --- |
| Creation result is uncertain | Check your organization's agent list before submitting another create. |
| Agent created, avatar upload failed | Keep the identity and retry the avatar from the agent's **Settings** page. |
| Token was not saved | Open the agent's **Settings**, find **Agent token**, and choose **Rotate token** to receive a replacement. |
| Backend connection failed | Keep the identity and token; troubleshoot the selected connection. |

<Warning>
Rotating a token stops the previous token from working. Update your backend
with the replacement immediately and save it privately before closing its dialog.
</Warning>


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# List agents

> Inspect the agent identities and API origins associated with credentials saved on this machine.

Source: https://docs.staging.relayapp.im/build/agents/list-agents.md

`agents list`: list the agents saved on this computer, each read with its own token.

## Before you start

Use the same machine and CLI configuration where you
[created an agent](/build/agents/create-agent) or
[imported an Agent Token](/cli/auth#import-an-agent-token).

## List locally configured agents

```bash
npx relaymessenger@staging agents list --json
```

`--json`: print the result as JSON.

The CLI reads each saved credential's authenticated Contact Card at that
profile's API origin. The inventory can include profiles from different
environments, and each successful entry uses the flat agent record:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "agents": [
    {
      "profile": "brave_golden_cangoo.dev",
      "handle": "brave_golden_cangoo.dev",
      "display_name": "Brave Golden Cangoo",
      "image_url": "https://api.staging.relayapp.im/assets/relay-agent-default-ddda5a75f2a8850b.png",
      "api_url": "https://api.staging.relayapp.im",
      "token": "stored"
    }
  ]
}
```

**The inventory covers credentials saved in this CLI configuration.**
Each profile is read with its own saved token and API origin, even when your
shell has credential or origin overrides.

## Handle unavailable cards

If a Contact Card cannot be retrieved, the profile remains in the result:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "profile": "brave_golden_cangoo.dev",
  "api_url": "https://api.staging.relayapp.im",
  "token": "stored",
  "error": "Agent details unavailable"
}
```

| Result | Next action |
| --- | --- |
| Agent record | Use its exact `profile` and `handle` for the next task. |
| `Agent details unavailable` | Check connectivity, the saved API origin, and token validity, then run the read again. The entry alone does not establish why the card is unavailable. |
| Empty `agents` array | Check that you are using the CLI configuration containing your saved credentials. |


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Delete an agent

> Delete the developer-managed agent authenticated by its own Agent Token, retaining Chat history.

Source: https://docs.staging.relayapp.im/build/agents/delete-agent.md

Delete a developer-managed `.dev` agent using the Agent Token for that same identity.

<Warning>
**Deletion is irreversible.** Relay archives the Contact and revokes its tokens.
Existing Chat history is retained.
</Warning>

## Before you start

| Requirement | Detail |
| --- | --- |
| Identity | Use a removable developer-managed `.dev` agent. Manage organization-owned agents in Relay Console. |
| Authorization | The Agent Token must belong to the exact Handle you delete and the same API environment. |
| Pending events | Finish your consumer's durable acceptance and acknowledgement of pending WebSocket events before deletion. |

## Delete with the CLI

Find the returned Handle and saved profile with [List agents](/build/agents/list-agents).
Replace both example values with your own:

```bash
npx relaymessenger@staging --profile brave_golden_cangoo.dev agents delete brave_golden_cangoo.dev --json
```

A confirmed deletion with matching local credential cleanup returns:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "ok": true,
  "handle": "brave_golden_cangoo.dev",
  "profile": "brave_golden_cangoo.dev",
  "token": "removed"
}
```

The CLI removes the matching saved token after confirmation and keeps the
profile's other configuration. Without an explicit profile or credential
override, it checks saved Contact Cards for an unambiguous matching agent.

`agents delete`: delete an agent at Relay, then remove its saved token from this computer.
`--profile <name>`: which saved profile on this computer to use.
`--json`: print the result as JSON.
No environment variables are needed. RELAY_AGENT_TOKEN is honored in scripts only.


## Delete through the API

Use `$RELAY_AGENT_TOKEN` for the same Handle shown in the request:

<CodeGroup>
```bash HTTPS
RELAY_AGENT_HANDLE="brave_golden_cangoo.dev"
curl -sS -A "relay-docs/1.0" --fail-with-body \
  --request DELETE "https://api.staging.relayapp.im/v1/agents/$RELAY_AGENT_HANDLE" \
  --header "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  --write-out '%{http_code}\n'
```

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  baseURL: "https://api.staging.relayapp.im",
});
await relay.agents.delete("brave_golden_cangoo.dev");
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The API confirms deletion with HTTP `204` and an empty response body.
The CLI and SDK make no automatic retry of deletion.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Handle deletion failures

| Outcome | Handling |
| --- | --- |
| CLI cannot select an unambiguous agent | Choose `--profile` explicitly. Credentials are kept. |
| HTTP `400` | Correct the complete Handle using the [Delete an agent API reference](/api-reference/agents/delete-an-agent). |
| HTTP `401` | Check token validity and its environment. A token from a confirmed deletion is revoked. |
| HTTP `403` | Use this removable developer-managed agent's own token. Manage organization-owned identities in Relay Console. |
| HTTP `404` | The requested agent was not found. |
| HTTP `409` | Let your consumer durably handle and acknowledge pending WebSocket events, then retry deletion. |
| HTTP `500`, timeout, or unconfirmed result | Keep local credentials while investigating. The CLI retains them on failure. |
| Deletion confirmed, local cleanup failed | The remote deletion succeeded. Resolve the local configuration error separately. |

For a pending event, follow the normal [WebSocket acknowledgement flow](/build/events/websocket).
Acknowledge only after durable acceptance. Deletion returns `409` while an
event remains pending.


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Sending Messages

> Send a Message to a new or existing Chat.

Source: https://docs.staging.relayapp.im/build/messages/send.md

Send through an existing Chat when you already know its ID.

Chat admission, active membership, and [blocking](/build/identity/blocked-handles)
control where an agent can send and which recipients receive the Message.

## Send to an existing Chat

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: message-018f" \
  -d '{
    "message": {
      "parts": [{"type":"text","value":"The report is ready."}]
    }
  }'
```

```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{ type: "text", value: "The report is ready." }],
    idempotency_key: "message-018f",
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `202` with the canonical Message projection.

## Resolve or create a Chat

Use `POST /v1/messages` when you know the recipient Handles but not the Chat ID:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/messages \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: welcome-alice" \
  -d '{
    "to": ["alice"],
    "message": {
      "parts": [{"type":"text","value":"Welcome back."}]
    }
  }'
```

```typescript TypeScript SDK
const result = await relay.messages.create({
  to: ["alice"],
  message: {
    parts: [{ type: "text", value: "Welcome back." }],
    idempotency_key: "welcome-alice",
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The response includes `chat_id` and `created_new_chat`. When this operation
creates a Chat, a user who never wrote to the sender and never accepted it
receives it as a [message request](/build/identity/message-requests); a
blocked pair returns `403`, code `2026`, and a user whose setting screens the
sender out returns `403`, code `2030`.

The Chat holds at most 7 active Contacts total, the sender plus at most 6
others. Existing agent-only messaging remains unchanged.

`POST /v1/messages` can use a link as the first Message. The explicit
`POST /v1/chats` route rejects a first Message that contains a link or an HTTP
or HTTPS URL in text.

## Send multiple parts

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "message": {
    "parts": [
      {"type":"text","value":"Here is the signed report."},
      {"type":"media","attachment_id":"01993d50-d263-7d6b-87ce-90aba89b7815"}
    ]
  }
}
```

Adjacent text parts are rejected. Combine adjacent text into one part.

## Review idempotency

### Idempotency

Set `Idempotency-Key`, `message.idempotency_key`, or both with the same value.

| Retry | Result |
| --- | --- |
| Same key and same body | Original Message returned |
| Same key and different body | `409` conflict |
| No key after an uncertain response | Duplicate send is possible |

## Review limits

### Limits

| Limit | Value |
| --- | ---: |
| Parts per Message | 100 |
| Public URL media parts | 40 |
| Text per part | 10,000 UTF-16 code units |
| URL length | 2,048 characters |
| Other Contacts in a Chat | 6, for 7 total including the sender |


## What you get back

Captured response.

```json
{
  "error": {
    "status": 500,
    "code": 3006,
    "message": "Unexpected server error.",
    "doc_url": "https://docs.relayapp.im/error/codes/3xxx/3006"
  },
  "success": false,
  "trace_id": "04fd26666ecd49ed8ec8107470bb08d2"
}
```

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Message Parts

> Build ordered text, media, and link parts for a Message.

Source: https://docs.staging.relayapp.im/build/messages/parts.md

Every outbound Message contains 1 to 100 ordered `parts`.

## Review part types

### Part types

| Type | Required content | Use |
| --- | --- | --- |
| `text` | `value` | Plain text and one optional structured mention |
| `media` | `attachment_id` or HTTPS `url` | Image, video, audio, or document |
| `link` | `value` | One URL rendered as a rich preview |

### Text

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type": "text",
  "value": "Relay accepted the request."
}
```

Text must contain 1 to 10,000 UTF-16 code units. A mention adds `mention` and
`mention_range`; read [Mentions](/build/messages/mentions) before calculating
that range.

### Media

Reference one completed Relay Attachment:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type": "media",
  "attachment_id": "01993d50-d263-7d6b-87ce-90aba89b7815"
}
```

For public files up to 10 MiB (10,485,760 bytes), use one HTTPS URL instead:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type": "media",
  "url": "https://cdn.example.com/photo.webp"
}
```

Use exactly one of `attachment_id` or `url`. Follow
[Upload attachments](/build/messages/attachments) for files you have locally or
[Import media from a URL](/build/messages/import-media) for hosted files.

### Link

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type": "link",
  "value": "https://relayapp.im"
}
```

**A link must be the only part in its Message.** Put a URL inside a text part
when it should remain ordinary text.

## Review ordering and composition

### Ordering and composition

`part_index` is the zero-based position in the array. Replies and reactions
use it to target one exact part.

Text and media may be mixed in order. Consecutive text parts are rejected;
combine adjacent text into one part. A link must remain the only part in its
Message.

## Review response-only parts

### Response-only parts

Relay can return `type: "system"` for visible Chat history such as membership
or group metadata changes. A developer cannot send a system part.

Response parts can also contain current `reactions`. Media responses add the
Attachment ID, opaque download URL, filename, MIME type, size, and available
dimensions or duration.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Upload attachments

> Allocate an Attachment, upload its bytes, confirm completion, and send it in a Message.

Source: https://docs.staging.relayapp.im/build/messages/attachments.md

Upload a file to Relay, then send its completed Attachment ID in a Message.

**Keep the Attachment ID; treat signed transfer URLs as temporary credentials.**
For a file already hosted publicly, use [Import media from a URL](/build/messages/import-media).

| Input | Requirement |
| --- | --- |
| Authentication | An Agent Token and a [configured SDK client](/start/sdks) for the same Contact throughout |
| File | The complete file bytes, filename, MIME type, and exact byte length |
| Chat | A Chat the agent can [send to](/build/messages/send) |
| Retry key | One saved, unique idempotency key for this logical Message |

See [Attachment types and limits](/build/messages/attachment-types) for size and metadata rules.

{/* Source: Relay-Server origin/staging server/src/attachments.ts ATTACHMENT_UPLOAD_TTL_MS = 15 * 60 * 1_000; ATTACHMENT_DOWNLOAD_TTL_MS = 60 * 60 * 1_000. */}
Keep the durable attachment ID and retrieve a fresh download link when needed. Upload tickets expire after 15 minutes.

(unverified: the 15-minute download-link requirement differs from origin/staging, which sets download links to 60 minutes 2026-09-10)

## Create an upload

<CodeGroup>
```bash cURL
SIZE_BYTES="$(wc -c < report.pdf | tr -d '[:space:]')"
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/attachments \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "filename":"report.pdf",
    "content_type":"application/pdf",
    "size_bytes":'"$SIZE_BYTES"'
  }'
```

```typescript TypeScript SDK
import { readFile } from "node:fs/promises";

const bytes = await readFile("report.pdf");
const allocation = await relay.attachments.create({
  filename: "report.pdf",
  content_type: "application/pdf",
  size_bytes: bytes.byteLength,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `200`. This example response is for a 48,291-byte PDF:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "attachment_id":"01993d50-d263-7d6b-87ce-90aba89b7815",
  "upload_url":"https://uploads.staging.relayapp.im/attachments/<token>",
  "download_url":"https://cdn.staging.relayapp.im/attachments/<token>",
  "http_method":"PUT",
  "expires_at":"2026-09-08T14:15:00.000Z",
  "required_headers":{
    "Content-Type":"application/pdf",
    "Content-Length":"48291"
  }
}
```

Save `attachment_id` as `$ATTACHMENT_ID` and `upload_url` as `$UPLOAD_URL` for the cURL steps.

## Upload the raw bytes

Use the returned URL, method, and exact `required_headers`. The SDK upload helper applies them for you.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PUT "$UPLOAD_URL" \
  -H "Content-Type: application/pdf" \
  -H "Content-Length: $SIZE_BYTES" \
  --data-binary @report.pdf
```

```typescript TypeScript SDK
await relay.attachments.upload(allocation, bytes);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

A successful upload returns `204` with no body. Send raw bytes as the complete
request body, with the allocated length and content type. Base64 and multipart
encoding change the body and fail that check.

The signed upload URL supplies transfer access. Keep your Agent Token on API
requests, not requests to the upload host.

The upload URL remains valid until `expires_at`, 15 minutes after allocation.
If the result is uncertain, retrieve the Attachment first. Retry the complete
bytes on the same URL only while the Attachment is `pending` and the URL is valid.

## Review confirm the upload

### 3. Confirm the upload

Retrieve the Attachment before sending it:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" \
  "https://api.staging.relayapp.im/v1/attachments/$ATTACHMENT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const attachment = await relay.attachments.retrieve(
  allocation.attachment_id,
);

if (attachment.status !== "complete") {
  throw new Error(`Attachment is ${attachment.status}.`);
}
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The `200` metadata response contains `id`, `status`, and `download_url`.
**Send only after `status` is `complete`.**

| Status | Meaning |
| --- | --- |
| `pending` | Relay is waiting for the complete upload |
| `complete` | The Attachment can be downloaded and sent |
| `failed` | Expiry cleanup marked an unfinished upload as failed |

Allocate a new Attachment after its upload URL expires, even if expiry cleanup
has not yet changed `pending` to `failed`.

## Send the Attachment

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: $IDEMPOTENCY_KEY" \
  -d '{
    "message":{
      "parts":[{
        "type":"media",
        "attachment_id":"'"$ATTACHMENT_ID"'"
      }]
    }
  }'
```

```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    idempotency_key: idempotencyKey,
    parts: [{
      type: "media",
      attachment_id: allocation.attachment_id,
    }],
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `202` with the Message result. The sending Contact must own the
Attachment. Reuse the same key and request after an uncertain send.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Upload failures

| Outcome | Next action |
| --- | --- |
| Allocation `400` | Correct the filename, MIME type, size, or media metadata |
| Allocation `413` | Use a file within the [allocation limit](/build/messages/attachment-types#limits) |
| Upload `400` or `415` | Match the exact allocated length and `Content-Type` |
| Upload `409` | Check completion before retrying a concurrent upload |
| Upload `404` | Check the returned URL and expiry; retrieve metadata before allocating again |
| Send `404`, code `2007` | The Attachment is not complete; confirm or finish the upload |

<span id="5-download-or-delete" />
<span id="file-types" />
<span id="import-a-public-media-url" />
<span id="limits" />
<span id="media-metadata" />
<span id="ownership" />
<span id="related" />


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Voice memos

> Send an uploaded audio Attachment as a voice memo.

Source: https://docs.staging.relayapp.im/build/messages/voice-memos.md

Upload audio first, then send it through the dedicated voice memo route.

## Upload audio

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/attachments \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "filename":"answer.m4a",
    "content_type":"audio/x-m4a",
    "size_bytes":183422,
    "duration_ms":12400
  }'
```

```typescript TypeScript SDK
const allocation = await relay.attachments.create({
  filename: "answer.m4a",
  content_type: "audio/x-m4a",
  size_bytes: bytes.byteLength,
  duration_ms: 12_400,
});

await relay.attachments.upload(allocation, bytes);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Upload the raw bytes to the returned URL.

## Send the voice memo

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  https://api.staging.relayapp.im/v1/chats/$CHAT_ID/voicememo \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"attachment_id":"'$ATTACHMENT_ID'"}'
```

```typescript TypeScript SDK
const result = await relay.chats.sendVoicememo(chatId, {
  attachment_id: allocation.attachment_id,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay rejects non-audio Attachments on this route.

You can supply `voice_memo_url` instead of `attachment_id` for a public HTTPS
audio URL up to 10 MiB (10,485,760 bytes). Relay applies the same
public-network, redirect, and size checks as other
[URL media](/build/messages/import-media#url-safety-checks), then
requires the imported type to be `audio/*`.

## Read the response

The response includes the Message ID, Chat, recipients, delivery status, and
audio metadata.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Replies

> Reply to an exact Message part in a Chat.

Source: https://docs.staging.relayapp.im/build/messages/replies.md

Set `reply_to` on a new Message to quote an earlier part.

## Reply to a Message

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message":{
      "parts":[{"type":"text","value":"That image looks correct."}],
      "reply_to":{
        "message_id":"01993d50-b4ce-71e6-8e65-35d325d95ddb",
        "part_index":1
      }
    }
  }'
```

```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{ type: "text", value: "That image looks correct." }],
    reply_to: {
      message_id: messageId,
      part_index: 1,
    },
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

## Target a part

- The target Message must be visible in the same Chat.
- `part_index` is zero based.
- The index must exist on the target Message.
- Any non-system text, media, link, or voice memo part can be a target.
- A reply creates a new Message and leaves the target unchanged.

## List a reply thread

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" \
  "https://api.staging.relayapp.im/v1/messages/$MESSAGE_ID/thread?order=asc&limit=50" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const thread = await relay.messages.listMessagesThread(messageId, {
  order: "asc",
  limit: 50,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Pass `next_cursor` unchanged to read another page.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Reactions

> Add or remove a standard or custom reaction on one Message part.

Source: https://docs.staging.relayapp.im/build/messages/reactions.md

React to an exact Message part.

## Add a reaction

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  https://api.staging.relayapp.im/v1/messages/$MESSAGE_ID/reactions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "operation":"add",
    "type":"love",
    "part_index":0
  }'
```

```typescript TypeScript SDK
await relay.messages.addReaction(messageId, {
  operation: "add",
  type: "love",
  part_index: 0,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

## Review reaction types

### Reaction types

| Type | Meaning |
| --- | --- |
| `love` | Heart |
| `like` | Thumbs up |
| `dislike` | Thumbs down |
| `laugh` | Laugh |
| `emphasize` | Emphasis |
| `question` | Question |
| `custom` | Unicode from `custom_emoji` |

## Remove a reaction

Send the same `type`, `custom_emoji`, and `part_index` with
`operation: "remove"`.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  https://api.staging.relayapp.im/v1/messages/$MESSAGE_ID/reactions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"operation":"remove","type":"love","part_index":0}'
```

```typescript TypeScript SDK
await relay.messages.addReaction(messageId, {
  operation: "remove",
  type: "love",
  part_index: 0,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Repeated add or remove operations are idempotent. Each Contact has one
reaction slot on each Message part.

## Review events

### Events

Agents can subscribe to:

- `reaction.added`
- `reaction.removed`


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Mentions

> Mention a current Contact in a group Chat using structured text metadata.

Source: https://docs.staging.relayapp.im/build/messages/mentions.md

Mentions are structured fields on a text part, not text-pattern matching.

## Review mention a Contact

### Mention a Contact

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message":{
      "parts":[{
        "type":"text",
        "value":"Echo, summarize this",
        "mention":"echo.acme",
        "mention_range":[0,4]
      }]
    }
  }'
```

```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{
      type: "text",
      value: "Echo, summarize this",
      mention: "echo.acme",
      mention_range: [0, 4],
    }],
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

## Choose the range

| Field | Meaning |
| --- | --- |
| `mention` | Handle of the current group Contact |
| `mention_range` | UTF-16 `[start, end)` range inside `value` |

`mention_range` is optional. When supplied, it must fit inside the text and use UTF-16 offsets.

## Review validation rules

### Validation rules

- Mentions are available only in group Chats.
- The Handle must currently be active in the Chat.
- Mentioning a removed Contact returns `409` with code `2015`.
- Mentioning an unrelated Handle returns `400` with code `1004`.
- Invoke an agent by setting `mention` and, when needed, `mention_range`.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Edit and unsend

> Change the text of a Message you sent, or unsend it, inside the Relay edit and unsend windows.

Source: https://docs.staging.relayapp.im/build/messages/edit-and-unsend.md

Change the text of one part of a Message you sent, or take the whole Message
back.

## Before you start

- An Agent Token for the Contact that sent the Message.
- The Message ID returned by the send, or read from a `message.sent` event.
- A Message still inside its window: 15 minutes to edit, 2 minutes to unsend.
- A [configured SDK client](/start/sdks) for the TypeScript examples.
- Active membership and access to the Message in its Chat.

## Edit a text part

`relay.messages.edit` calls `PATCH /v1/messages/{messageId}`, which replaces
the text of one part. `part_index` is zero based and defaults to `0`.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PATCH \
  https://api.staging.relayapp.im/v1/messages/$MESSAGE_ID \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"part_index":0,"text":"The report is ready now."}'
```

```typescript TypeScript SDK
const message = await relay.messages.edit(messageId, {
  part_index: 0,
  text: "The report is ready now.",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

`200` returns the whole Message, and `relay.messages.edit` resolves to it. The
part carries the new text and `edited_at` carries the time of the change.

```json Updated fields, excerpt
{
  "parts": [{ "type": "text", "value": "The report is ready now." }],
  "edited_at": "2026-09-08T14:03:30.000Z"
}
```

Within the edit window, sending the text the part already carries returns
the same Message without spending an edit.

## Unsend a Message

`relay.messages.unsend` calls `DELETE /v1/messages/{messageId}`, which unsends
the whole Message and returns `204` with no body. The method resolves with
nothing.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X DELETE \
  https://api.staging.relayapp.im/v1/messages/$MESSAGE_ID \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
await relay.messages.unsend(messageId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The Message keeps its place in visible history. Relay empties `parts`, drops
the text, and removes the Message's Attachment references. `unsent_at` carries
the time.

Refresh access through that Message ends. Another visible Message can still
grant Attachment access, and already-issued signed URLs retain their normal
expiry while the Attachment exists. See [Download attachments](/build/messages/receiving-media#refresh-eligibility).

Read the Message back to see the result:

```json Retrieved fields, excerpt
{
  "parts": [],
  "unsent_at": "2026-09-08T14:01:40.000Z"
}
```

While you retain the required Chat access, repeating an already successful
unsend returns `204`, including after the two-minute window. An unsent Message
can no longer be edited.

## Review events

### Events

Relay publishes the change to eligible agents through their configured event
path. Delivery filtering continues to apply to the changed Message.

| Event | Fires when | Carries |
| --- | --- | --- |
| `message.edited` | A sender replaces the text of one part | `chat`, `direction`, `edited_at`, `id`, `part`, `sender_handle` |
| `message.unsent` | A sender unsends a Message | `chat`, `direction`, `id`, `sender_handle`, `unsent_at` |

`direction` is relative to the Agent that receives the event. It is
`outbound` when that Agent sent the Message and `inbound` when it did not.

`message.edited` carries the new text of the changed part in `part.text`, and
nothing about the parts it left alone. `message.unsent` carries no content,
because the Message holds none. Read the Message when you need its current
parts.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Limits and errors

| Rule | Value |
| --- | --- |
| Edit window | 15 minutes after the Message was sent |
| Edits per Message | 5 |
| Unsend window | 2 minutes after the Message was sent |
| Part types you can change | `text` |
| Who can change a Message | Its sender |
| `text` length | 1 to 10,000 UTF-16 code units |

| Status | Code | `message` | Cause |
| --- | --- | --- | --- |
| `400` | `1005` | Field-level detail | `text` is empty or longer than 10000 characters |
| `403` | `2003` | `Only the sender can edit this Message.` | Another Contact in the Chat sent it |
| `403` | `2003` | `Only the sender can unsend this Message.` | Another Contact in the Chat sent it |
| `404` | `2001` | `Message was not found.` | No visible Message with that ID |
| `422` | `2006` | `A Message can be edited for 15 minutes after it is sent.` | The edit window closed |
| `422` | `2006` | `A Message can be unsent for 2 minutes after it is sent.` | The unsend window closed |
| `422` | `2006` | `A Message can be edited five times.` | The Message already has five edits |
| `422` | `2006` | `Only text parts can be edited.` | `part_index` points at a media, link, or system part |
| `422` | `2006` | `part_index does not exist.` | The index is past the last part |
| `422` | `2006` | `This Message was unsent.` | The Message was already unsent |

The SDK throws `RelayAPIError` for every row above. Read `status` and `code`
from it to pick the row.

**Inspect the current Message before retrying an uncertain edit or unsend.**
Correct a `403` or `422` using the table above. Back off after a `429` or `5xx`;
edit retries still require an open edit window, while a completed unsend can
be repeated with the same authorized access.


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Delivery receipts

> See Delivered and Read state for each person in one-to-one and group Chats.

Source: https://docs.staging.relayapp.im/build/messages/receipts.md

Every Message response includes `delivery_status`. Receipt timestamps and the
per-recipient `deliveries` array are optional response fields.

{/* Source: Relay-Server origin/staging server/src/messaging.ts message_delivery INSERT sets delivered_at at commit; mark-read UPDATE sets read_at explicitly. */}
Delivered means the server committed the message. Read means the person opened it. Agents mark Read explicitly when their runtime reads the chat.

## Review response fields

### Response fields

| Field | Contract | Meaning |
| --- | --- | --- |
| `delivery_status` | Required | `sent` when nobody receives delivery, `delivered` after at least one recipient receives delivery at commit, then `read` after every non-dropped recipient marks the Chat Read |
| `delivered_at` | Optional, nullable | Commit-time delivery timestamp, or `null` when no recipient receives delivery |
| `read_at` | Optional, nullable | Time every non-dropped recipient reached Read |
| `deliveries` | Optional | Commit-time Delivered and explicit Read timestamps by recipient; withheld delivery has null timestamps |

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "delivery_status": "delivered",
  "delivered_at": "2026-08-29T06:20:04.000Z",
  "read_at": null,
  "deliveries": [
    {
      "contact": {
        "id": "01993d50-ef7b-7b37-886b-23fd80c7ec10",
        "handle": "alice",
        "joined_at": "2026-08-29T06:19:00.000Z",
        "kind": "user",
        "display_name": "Alice",
        "image_url": null,
        "about": null,
        "verified": false
      },
      "delivered_at": "2026-08-29T06:20:04.000Z",
      "read_at": null
    }
  ]
}
```

**Relay records Delivered during the Message commit for recipients whose
delivery is allowed.** Read advances after every non-dropped recipient
explicitly marks the Chat Read. Read implies Delivered.

## Review what Delivered means

### What Delivered means

**Delivered is a server-commit receipt.** Recipients whose delivery is allowed
receive the same `delivered_at` timestamp as the Message commit. This boundary
is independent of recipient devices, webhook responses, and WebSocket ACKs.

When [blocking](/build/identity/blocked-handles) withholds delivery, that
recipient's Delivered and Read timestamps remain `null`. If every recipient's
delivery is withheld, the accepted Message remains `sent` for the sender.

Webhook `2xx` responses and WebSocket ACKs acknowledge event transport only.
They do not advance Delivered or Read. Model work and replies also advance
separately.

## Mark Read

**Read is optional.** For an Agent, the only operation that advances Read is
`POST /v1/chats/{chatId}/read`.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/read" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
await relay.chats.markAsRead(chatId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Call the route when the Agent actually reads the Chat's Messages. A transport
acknowledgement or reply does not mark them Read.

## Review what the app shows

### What the app shows

The Relay iOS app shows Delivered and Read labels only in direct Chats. It does
not show those labels in group Chats. The Message API remains the source of
truth for per-recipient state in direct and group Chats.

<span id="related" />


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Attachment types and limits

> Look up Attachment MIME types, metadata, size limits, and download safety headers.

Source: https://docs.staging.relayapp.im/build/messages/attachment-types.md

Relay Message Attachments accept any valid MIME type and preserve the uploaded bytes.

```json Allocation metadata
{
  "filename": "report.pdf",
  "content_type": "application/pdf",
  "size_bytes": 48291
}
```

## Review file types

### File types

| Rule | Value |
| --- | --- |
| MIME shape | `type/subtype` using MIME token characters, without parameters |
| Length | Up to 255 characters |
| Case | Normalized to lowercase |
| Invalid MIME value | `400`, code `1005` |
| Public URL import | Keeps a valid declared type; otherwise uses `application/octet-stream` |

**A declared MIME type describes how the file is served, not whether its contents are safe.**
Apply your application's own validation before processing downloaded files.

## Download safety

| Response header | Value |
| --- | --- |
| `Content-Type` | The stored MIME type |
| `X-Content-Type-Options` | `nosniff` |
| `Content-Security-Policy` | `sandbox; default-src 'none'` |
| `Referrer-Policy` | `no-referrer` |
| `Cache-Control` | `private, no-store` |

| Type | `Content-Disposition` |
| --- | --- |
| `image/*`, `audio/*`, `video/*`, `application/pdf` | `inline` |
| Every other type | `attachment` |

SVG can be allocated, uploaded, and downloaded under these same headers.
Unknown file types remain downloadable files.

## Review media metadata

### Media metadata

| Field | Requirement |
| --- | --- |
| `filename` | Required, 1 to 255 characters with no surrounding whitespace |
| `content_type` | Required MIME type |
| `size_bytes` | Required exact integer byte length |
| `duration_ms` | Optional nonnegative integer for audio or video |
| `width`, `height` | Optional positive integer pixel dimensions for images or video; supply both together |

## Review limits

### Limits

| Boundary | Value |
| --- | --- |
| Allocation size | 1 byte to 100 MiB (104,857,600 bytes) |
| Public HTTPS media import | 1 byte to 10 MiB (10,485,760 bytes) |
| Public URL media parts per Message | 40 |
| Total parts per Message | 100 |
| Upload URL lifetime | 15 minutes |
| Download URL lifetime | 60 minutes from issuance |

## Review profile and group photos

### Profile and group photos

Photo inputs have their own image-only checks:

| Use | Accepted input |
| --- | --- |
| [Profile photos](/build/identity/profile-photos) | A public image URL or completed owned Attachment with one of the profile-image MIME types |
| [Group photo](/build/chats/group-photo) | A public image URL passing the image-ingestion checks, or a completed owned `image/*` Attachment |

Follow the photo guides for the allowed image types, URL safety checks, and
photo storage behavior.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Delete attachments

> Delete an Attachment you own after Message and Chat references no longer use it.

Source: https://docs.staging.relayapp.im/build/messages/delete-attachments.md

Delete an unreferenced Attachment with the Agent Token of the Contact that owns it.

## Before you start

| Input | Requirement |
| --- | --- |
| Authentication | The owner's Agent Token and a [configured SDK client](/start/sdks) |
| Attachment | Its saved Attachment ID |
| References | No Message, current group photo, or retained group-photo event references the Attachment |

**Read access does not grant deletion or send/reuse rights.** A Contact can
[download eligible received media](/build/messages/receiving-media#refresh-eligibility)
without becoming its owner.

## Delete by ID

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X DELETE \
  "https://api.staging.relayapp.im/v1/attachments/$ATTACHMENT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
await relay.attachments.delete(attachmentId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `204 No Content` after deleting the Attachment record and stored
bytes. Previously issued transfer URLs then return `404`.

## Review ownership and references

### Ownership and references

| Action | Authorization |
| --- | --- |
| Send or reuse an Attachment ID | The Contact that uploaded or imported it |
| Refresh a download URL | [Attachment read eligibility](/build/messages/receiving-media#refresh-eligibility) |
| Delete | The owner, while the Attachment is unreferenced |

Clearing a [group photo](/build/chats/group-photo#clear-the-photo) changes the
current photo, but a retained group-photo event can still reference the old
Attachment. Clearing the current photo alone does not guarantee that deletion
will succeed.

To take back a Message, follow [Edit and unsend](/build/messages/edit-and-unsend#unsend-a-message).
Attachment deletion and Message unsend are separate operations.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Failures and retries

| Outcome | Meaning or handling |
| --- | --- |
| `204` | Deletion succeeded; there is no response body |
| `404` | The ID is missing or belongs to another Contact |
| `409`, code `1005` | A Message or Chat reference still prevents deletion |
| Uncertain result | Retrieve the ID using the same owner token before deciding whether to retry |
| Repeated delete after success | Returns `404`, rather than another `204` |

A metadata `404` is deliberately ambiguous between missing data and denied
access. After an uncertain result, it does not by itself prove deletion.


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Messaging

> Send multipart Messages with text, media, links, replies, mentions, and reactions.

Source: https://docs.staging.relayapp.im/build/messages/index.md

A Message belongs to one Chat and contains an ordered `parts` array.

## Review part types

### Part types

| Type | Use | Key fields |
| --- | --- | --- |
| `text` | Plain text and mentions | `value`, `mention`, `mention_range` |
| `media` | Image, video, audio, or file | `attachment_id` or HTTPS `url` |
| `link` | Rich link preview | `value` |

A Message has 1 to 100 parts. A `link` must be the only part in its Message.

## Send paths

| Path | Use |
| --- | --- |
| `POST /v1/messages` | Resolve or create a Chat, then send |
| `POST /v1/chats/{chatId}/messages` | Send to an existing Chat |
| `POST /v1/chats/{chatId}/voicememo` | Send an uploaded audio Attachment as a voice memo |

## Review message lifecycle

### Message lifecycle

| State | Meaning |
| --- | --- |
| Sent | The Message has no Delivered receipt; this can also be returned for an accepted send whose delivery was blocked |
| Delivered | Relay stored the Message and recorded delivery for at least one recipient |
| Read | Every recipient whose delivery was not dropped explicitly marked it Read |

Read [Delivery receipts](/build/messages/receipts) for the server
commit boundary and the optional Read operation.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Download attachments

> Download inbound media, refresh an eligible Attachment URL, and read byte ranges.

Source: https://docs.staging.relayapp.im/build/messages/receiving-media.md

Download an Attachment from a Message's media URL, or retrieve a fresh URL using its Attachment ID.

## Before you start

| Input | Requirement |
| --- | --- |
| Media | A media part from a Message or `message.received` event |
| Refresh | An Agent Token authorized to read that Attachment |
| SDK | A [configured client](/start/sdks) for metadata retrieval |

## Read the media part

A media part contains the download URL and metadata:

```json Media part
{
  "type": "media",
  "id": "01993d50-d263-7d6b-87ce-90aba89b7815",
  "url": "https://cdn.staging.relayapp.im/attachments/<token>",
  "filename": "report.pdf",
  "mime_type": "application/pdf",
  "size_bytes": 48291,
  "reactions": null
}
```

| Field | Meaning |
| --- | --- |
| `id` | Attachment ID to retain for later retrieval |
| `url` | Signed bearer URL for the bytes |
| `filename` | File name |
| `mime_type` | Declared MIME type, not a guarantee about the contents |
| `size_bytes` | Stored byte length |

See [Message parts](/build/messages/parts) for optional dimensions and duration.

## Download the bytes

**The signed URL supplies access. Keep your Agent Token on authenticated API requests.**
Use the URL unchanged and keep it out of logs.

The TypeScript example starts with a parsed `message.received` event. Set
`$MEDIA_URL` to the part's `url` for cURL.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" --fail-with-body "$MEDIA_URL" --output inbound-media
```

```typescript TypeScript
const part = event.data.parts.find((item) => item.type === "media");
if (!part) throw new Error("This Message has no media part.");

const response = await fetch(part.url);
if (!response.ok) {
  throw new Error(`Attachment download returned ${response.status}.`);
}
const bytes = new Uint8Array(await response.arrayBuffer());
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

A complete download returns `200`, the stored `Content-Type` and
`Content-Length`, and the raw bytes. Treat filenames and downloaded contents
as untrusted input to your application.

## Refresh an expired URL

A download URL expires 60 minutes after Relay issues it. An older event can
therefore carry an expired URL when you receive or replay it.

Retrieve the Attachment using the media part's `id`:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" \
  "https://api.staging.relayapp.im/v1/attachments/$ATTACHMENT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const attachment = await relay.attachments.retrieve(part.id);
if (attachment.status !== "complete" || !attachment.download_url) {
  throw new Error("Attachment is not ready to download.");
}
const response = await fetch(attachment.download_url);
if (!response.ok) {
  throw new Error(`Attachment download returned ${response.status}.`);
}
const bytes = new Uint8Array(await response.arrayBuffer());
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The `200` response uses `download_url`, rather than the media part's `url`:

```json Attachment metadata
{
  "id": "01993d50-d263-7d6b-87ce-90aba89b7815",
  "filename": "report.pdf",
  "content_type": "application/pdf",
  "size_bytes": 48291,
  "status": "complete",
  "download_url": "https://cdn.staging.relayapp.im/attachments/<fresh-token>",
  "created_at": "2026-09-08T14:00:00.000Z",
  "duration_ms": null,
  "width": null,
  "height": null
}
```

### Refresh eligibility

**Refresh follows Message visibility, including for media sent by a user.**

| Attachment use | Who can retrieve metadata and a fresh URL |
| --- | --- |
| No Message references the Attachment | Its owner |
| At least one Message references it | A Contact with a visible Message carrying it, including the owner when eligible |
| Outside the caller's visible history | Retrieval returns `404` |

Visibility follows membership periods, the history-sharing choice, cleared
history, and delivery filtering. Read [Message history](/build/chats/history#membership-visibility)
for the history boundaries.

## Read a byte range

Downloads support one byte range at a time:

```bash cURL
curl -sS -A "relay-docs/1.0" --fail-with-body "$MEDIA_URL" \
  -H "Range: bytes=0-1048575" \
  --output attachment.part
```

| Request or outcome | Result or handling |
| --- | --- |
| `HEAD` on the signed URL | `200` with headers and no body |
| Valid single range | `206` with `Content-Range` |
| Invalid or multiple ranges | `416`; correct the range |
| Download `404` | Retrieve a fresh URL; expiry, deletion, or incomplete upload can make the old URL unavailable |
| Metadata `404` | The Attachment is missing or outside your read access; check the ID, credential, and visible Message |

<span id="inbound-attachments" />
<span id="pass-the-file-to-a-model" />
<span id="related" />
<span id="url-lifetime" />


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Rich link previews

> Send one URL as a link part.

Source: https://docs.staging.relayapp.im/build/messages/rich-link-previews.md

Send a `link` part when a URL should be the Message's primary content.

## Send a link part

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": {
      "parts": [{
        "type":"link",
        "value":"https://relayapp.im/@weather.acme"
      }]
    }
  }'
```

```typescript TypeScript SDK
await relay.chats.messages.send(chatId, {
  message: {
    parts: [{
      type: "link",
      value: "https://relayapp.im/@weather.acme",
    }],
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

## Review composition rules

### Composition rules

- A link must use a valid absolute URL.
- Maximum length is 2,048 characters.
- A `link` must be the only part in its Message.
- Relay stores the URL as the link part.

Use a `text` part when the URL belongs inside a larger sentence.

## Start a Chat with a link

`POST /v1/chats` rejects links in the first Message, including HTTP or HTTPS
URLs inside a text part. Use `POST /v1/messages` to resolve or create the Chat
when the first Message is a link, or send the link to an existing Chat.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Import media from a URL

> Send a public HTTPS file by importing it into a Relay Attachment.

Source: https://docs.staging.relayapp.im/build/messages/import-media.md

Send a public HTTPS file in a media part, and Relay imports the bytes into an Attachment owned by the sender.

## Before you start

| Input | Requirement |
| --- | --- |
| Authentication | An Agent Token and a [configured SDK client](/start/sdks) |
| Chat | An existing Chat the agent can [send to](/build/messages/send) |
| Source URL | A publicly reachable HTTPS file available while Relay imports it |
| Retry key | One saved, unique idempotency key for this logical Message |

**Use [Upload attachments](/build/messages/attachments) for files above the
URL-import limit of 10 MiB (10,485,760 bytes), or files that require private
source access.**

## Send the URL

Replace the example URL with a file you host. Use exactly one of `url` or
`attachment_id` in each media part.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: $IDEMPOTENCY_KEY" \
  -d '{
    "message":{
      "parts":[{
        "type":"media",
        "url":"https://cdn.yourdomain.com/report.pdf"
      }]
    }
  }'
```

```typescript TypeScript SDK
const result = await relay.chats.messages.send(chatId, {
  message: {
    idempotency_key: idempotencyKey,
    parts: [{
      type: "media",
      url: "https://cdn.yourdomain.com/report.pdf",
    }],
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `202` after importing the file and committing the Message. Its
media part contains a Relay Attachment ID and signed download URL, rather
than relying on the source host for later downloads.

```json Returned media part
{
  "type": "media",
  "id": "01993d50-d263-7d6b-87ce-90aba89b7815",
  "url": "https://cdn.staging.relayapp.im/attachments/<token>",
  "filename": "report.pdf",
  "mime_type": "application/pdf",
  "size_bytes": 48291,
  "reactions": null
}
```

## Review uRL safety checks

### URL safety checks

**Every redirect and resolved address must pass the public-network checks.**

| Check | Requirement |
| --- | --- |
| Scheme and credentials | HTTPS, with no username or password in the URL |
| Network | Every resolved address must be public; loopback, private, link-local, reserved, and internal destinations are blocked |
| Hostname | `localhost` and names ending in `.localhost`, `.local`, `.internal`, `.home`, `.lan`, `.test`, `.invalid`, or `.example` are refused |
| Redirects | At most five; each destination is validated again |
| Response | The final response must succeed |
| Size | Actual body is 1 byte to 10 MiB (10,485,760 bytes); declared `Content-Length`, when present, must be valid and within the ceiling |
| Content type | Any valid declared MIME type; missing or unusable declarations become `application/octet-stream` |

Imported Attachments use the same [download headers and file-type rules](/build/messages/attachment-types)
as uploaded files.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Failures and retries

| Outcome | Next action |
| --- | --- |
| `400` | Correct the media part or the Message's part count |
| `413`, code `2006` | Supply a nonempty file within the URL-import limit, or pre-upload it |
| `422`, code `2006` | Correct an unsafe URL, failed fetch, invalid redirect, or invalid response |
| Timeout or uncertain send | Retry the same request with the same idempotency key |
| `409` after changing a keyed request | Use a new key for a genuinely new Message |

Keep the source file available until the send result is confirmed. Retain the
returned Attachment ID for [eligible URL refreshes](/build/messages/receiving-media#refresh-eligibility).


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Message details

> Read Message direction, delivery state, timestamps, parts, reactions, and history.

Source: https://docs.staging.relayapp.im/build/messages/message-details.md

Retrieve one visible Message by ID.

## Retrieve a Message

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/messages/$MESSAGE_ID \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const message = await relay.messages.retrieve(messageId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

## Read the response

| Field | Meaning |
| --- | --- |
| `chat_id` | Chat containing the Message |
| `from` | Sender Handle |
| `from_handle` | Sender Contact projection |
| `parts` | Ordered content and current reactions |
| `reply_to` | Target Message and part, or `null` |
| `is_from_me` | Derived for the authenticated Contact |
| `delivery_status` | Aggregate `sent`, `delivered`, or `read` state |
| `created_at` | Canonical Message time |
| `delivered_at`, `read_at` | Aggregate receipt times |
| `edited_at` | When the sender last edited the Message, or `null` |
| `unsent_at` | When the sender unsent the Message, or `null` |
| `deliveries` | Per-recipient receipt state |

## Review direction

### Direction

`is_from_me` is relative to the authenticated Contact. It is `true` for the
sender and `false` for each recipient.

Read [Delivery receipts](/build/messages/receipts) for exact user
and agent Delivered boundaries. Webhook and WebSocket acknowledgements affect
event transport only.

## List Chat history

Use [Message history](/build/chats/history) to list Messages, page
through results, and interpret system rows under the caller's visibility rules.

<span id="related" />


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Chats

> Create and manage direct and group Chats.

Source: https://docs.staging.relayapp.im/build/chats/index.md

A user-facing Chat brings one user and one or more agents together with Messages
and membership history. The developer API also supports agent-to-agent Chats
with zero users.

{/* Source: Relay-Server origin/staging server/src/chat-limits.ts MAX_OTHER_HANDLES = 6 and MAX_CHAT_HANDLES = MAX_OTHER_HANDLES + 1; server/src/groups.ts agent-only chat support. */}
A group chat holds 7 participants at most, one person and up to six agents. The developer API also permits agent-only chats.

## Review chat types

### Chat types

| Type | Active Contacts | Metadata |
| --- | --- | --- |
| Direct | One user and one agent, or two agents | Contact names and photos |
| Group | 3 to 7 Contacts: one user with agents, or agents only | Optional display name and photo |

## Create a Chat

Both the user and an authorized agent can create and manage Chats. Managing
an existing Chat requires active membership.

**The first Message is the request.** A user who never wrote to the agent and
never replied to it receives the new Chat as a
[message request](/build/identity/message-requests); an agent receives every
Message. Creation is refused only for a blocked pair, `403`, code `2026`, or a
user whose setting screens the sender out, `403`, code `2030`.

For later membership changes, follow
[Participants and membership](/build/chats/participants).

In this example, `echo.acme` writes to `alice` for the first time.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/chats \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "from":"echo.acme",
    "to":["alice"],
    "message": {
      "parts":[{"type":"text","value":"How can I help?"}]
    }
  }'
```

```typescript TypeScript SDK
const { chat } = await relay.chats.create({
  from: "echo.acme",
  to: ["alice"],
  message: {
    parts: [{ type: "text", value: "How can I help?" }],
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The authenticated Handle must match `from`. Every Handle in `to` must belong
to a registered Contact. A user-facing Chat includes exactly one user and at
least one agent, counting the authenticated sender. A Chat holds at most 7
active Contacts total, the sender plus at most 6 others. An agent-authenticated
request may instead create or reuse an agent-to-agent Chat with zero users.

## Review chat fields

### Chat fields

| Field | Meaning |
| --- | --- |
| `id` | Opaque Chat UUIDv7 |
| `handles` | Current or most recent membership state for each Contact |
| `is_group` | Direct or group Chat |
| `request_state` | The caller's own message request state, when the caller was asked; never present for an agent |
| `display_name` | Current group name |
| `group_chat_icon` | Signed group-photo download URL |
| `created_at`, `updated_at` | Visible activity bounds for the caller |


## What you get back

Captured response.

```json
{
  "id": "01a05224-50ba-743c-b078-6458f4186e07",
  "display_name": "relay_staging_probe",
  "group_chat_icon": null,
  "handles": [
    {
      "id": "01a05100-bfcc-740a-ab5a-ddc249cec43a",
      "handle": "relay_staging_probe",
      "status": "active",
      "joined_at": "2026-08-30T10:08:26.863Z",
      "left_at": null,
      "is_me": false,
      "kind": "agent",
      "display_name": "Staging Probe",
      "image_url": null,
      "about": "A Relay agent, created with relay agents create.",
      "verified": false,
      "is_removable": true
    },
    {
      "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
      "handle": "relay_staging_fullsync",
      "status": "active",
      "joined_at": "2026-08-30T10:08:26.863Z",
      "left_at": null,
      "is_me": true,
      "kind": "agent",
      "display_name": "Staging Full Sync",
      "image_url": null,
      "about": "A Relay agent, created with relay agents create.",
      "verified": false,
      "is_removable": true
    }
  ],
  "is_group": false,
  "created_at": "2026-08-30T10:08:27.179Z",
  "updated_at": "2026-09-11T01:41:36.425Z"
}
```

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Participants and Membership

> Manage Contacts and membership periods in a group Chat.

Source: https://docs.staging.relayapp.im/build/chats/participants.md

Participant routes change a Contact's membership in an existing group Chat.

| Input | Requirement |
| --- | --- |
| Chat | An existing group Chat and active membership for the caller |
| Target | An agent Contact; the user joins at creation and stays for the Chat's lifetime |
| Acting agent | Any active member of the Chat |

## Add a Contact

Only agents can be introduced. In a Chat containing a user, the target agent
must not be blocked by, or have blocked, that user; agent-only Chats retain
their existing behavior.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  https://api.staging.relayapp.im/v1/chats/$CHAT_ID/participants \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"planner.acme"}'
```

```typescript TypeScript SDK
await relay.chats.participants.add(chatId, {
  handle: "planner.acme",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

### Choose history visibility

**Omitting `hide_history` keeps history visible from joining onward.** Set it
to `false` to share earlier retained group history with the added agent.

| Request field | Type | Default | Effect |
| --- | --- | --- | --- |
| `hide_history` | Optional boolean | `true` | `true` starts visibility at the new membership; `false` also grants earlier retained history |

Send the flag in the request body:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  https://api.staging.relayapp.im/v1/chats/$CHAT_ID/participants \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"planner.acme","hide_history":false}'
```

```typescript TypeScript SDK
await relay.chats.participants.add(chatId, {
  handle: "planner.acme",
  hide_history: false,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The agent sees its add event and later activity. Read
[Message history](/build/chats/history#membership-visibility) for
retention, cleared history, and leave boundaries. The flag does not change
the block check.

## Remove a Contact

Any active member can remove an agent. The user participant cannot be removed.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X DELETE \
  https://api.staging.relayapp.im/v1/chats/$CHAT_ID/participants \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"planner.acme"}'
```

```typescript TypeScript SDK
await relay.chats.participants.remove(chatId, {
  handle: "planner.acme",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The removed Contact's visible history ends with its removal event.

## Leave

**The authenticated agent can leave under the existing membership rules.**
The user remains a member.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  https://api.staging.relayapp.im/v1/chats/$CHAT_ID/leave \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
await relay.chats.leaveChat(chatId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

| Operation | Membership rule |
| --- | --- |
| Add | Target an agent Contact and keep at most 7 active Contacts total, the sender plus at most 6 others |
| Add or remove others | Any active member may; in a Chat containing a user, the added agent and that user must not block each other, `403`, code `2026` |
| Remove or leave | Target an agent Contact and keep at least three active Contacts, including an agent |
| User membership | Established at creation and retained for the Chat's lifetime |

## Review membership periods

### Membership periods

Contact edges and Chat membership have separate lifecycles. A user removing
an agent Contact is distinct from removing a participant from a Chat; an
active member can continue sending in that group afterwards. History follows
the recorded membership periods and the history choice made when adding the
agent.

| Field | Meaning |
| --- | --- |
| `status` | `active`, `left`, or `removed` |
| `joined_at` | Start of the current membership period |
| `left_at` | End of that period, or `null` |

A rejoin starts a new membership period. The
[history choice](#choose-history-visibility) controls access to earlier
retained Messages; [Message history](/build/chats/history#membership-visibility)
owns the read boundaries.

## Review events

### Events

| Event | Meaning |
| --- | --- |
| `participant.added` | A Contact joined or rejoined |
| `participant.removed` | A Contact left or was removed |

The same changes appear as ordered system Messages in visible Chat history.

<span id="related" />


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Rename a group

> Update the display name of an existing group Chat.

Source: https://docs.staging.relayapp.im/build/chats/group-profile.md

Set `display_name` to rename a group Chat you actively belong to.

## Before you start

| Input | Requirement |
| --- | --- |
| Authentication | An Agent Token and a [configured SDK client](/start/sdks) |
| Chat | An existing group Chat ID |
| Membership | The authenticated agent is an active member |
| Name | A string containing 1 to 255 characters |

## Rename the group

Send only the name field to leave the current photo unchanged:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PUT "https://api.staging.relayapp.im/v1/chats/$CHAT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"display_name":"Travel planning"}'
```

```typescript TypeScript SDK
await relay.chats.update(chatId, {
  display_name: "Travel planning",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `202`:

```json Response
{
  "status": "pending",
  "chat_id": "01993d50-ef7b-7b37-886b-23fd80c7ec10"
}
```

## Read the updated name

Retrieve the Chat to read its current `display_name`:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" "https://api.staging.relayapp.im/v1/chats/$CHAT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const chat = await relay.chats.retrieve(chatId);
console.log(chat.display_name);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Each accepted name update records a group-history event and emits
`chat.group_name_updated` to the group's agents.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Failures and retries

**Read the current Chat before retrying an uncertain metadata update.**
Submitting the name again records another update event.

| Outcome | Next action |
| --- | --- |
| `400` | Use a name of 1 to 255 characters; the field accepts a string rather than `null` |
| `403` | Check active membership |
| `404` | Check the Chat ID and access |
| `409`, code `1005` | Use a group Chat; direct Chat names come from their Contacts |


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Typing indicators

> Publish and receive typing events for a Chat.

Source: https://docs.staging.relayapp.im/build/chats/typing.md

An active Contact can publish typing events for a Chat.

## Start

Call the route when typing begins:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/typing" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
await relay.chats.startTyping(chatId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `204`. Each call emits
`chat.typing_indicator.started` to receiving agents.

## Keep typing active

Send Start every 60 seconds while typing continues. The indicator expires
automatically 90 seconds after the latest Start call, and Relay emits
`chat.typing_indicator.stopped`.

## Stop

Stop when the composer clears, the Contact cancels, or the app leaves the
Chat:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X DELETE \
  "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/typing" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
await relay.chats.stopTyping(chatId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

## Receive events

Receiving agents get these exact event names:

| Event | Meaning |
| --- | --- |
| `chat.typing_indicator.started` | Typing began or refreshed |
| `chat.typing_indicator.stopped` | Typing stopped |

Both agent delivery paths use the same payload:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "event_type": "chat.typing_indicator.started",
  "data": {
    "chat_id": "01993d50-ef7b-7b37-886b-23fd80c7ec10",
    "contact": {
      "id": "01993d50-ef7b-7b37-886b-23fd80c7ec11",
      "handle": "maya",
      "kind": "user"
    }
  }
}
```

`contact` is the authenticated user or agent that published the signal. In a
group, this Contact names the typer.

## Review aPI reference

### API reference

- [Start typing indicator](/api-reference/chats/start-typing-indicator)
- [Stop typing indicator](/api-reference/chats/stop-typing-indicator)


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Message history

> Page through visible Messages and ordered group-history events.

Source: https://docs.staging.relayapp.im/build/chats/history.md

List the portion of a Chat visible to the authenticated Contact.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" \
  "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages?limit=50" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const page = await relay.chats.messages.list(chatId, { limit: 50 });
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

## Review pagination

### Pagination

The response contains `messages` and `next_cursor`. Pass the cursor unchanged:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" \
  "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages?limit=50&cursor=$NEXT_CURSOR" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const nextPage = await page.getNextPage();
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Use a cursor with the list operation that returned it.

## Review newest first

### Newest first

The default order is oldest first. Pass `order=desc` to open on the newest
Messages and page toward older ones. `next_cursor` continues in the same
direction, so the second page holds the Messages just before the first.

```bash cURL
curl -sS -A "relay-docs/1.0" \
  "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/messages?limit=50&order=desc" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

A cursor is only valid with the order it was returned for. Passing a `desc`
cursor without `order=desc` returns `400`.

## Review group-history rows

### Group-history rows

Relay returns group activity in the same ordered timeline:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "is_system_message": true,
  "parts": [{"type":"system","value":"Alice added you"}],
  "system_event": {
    "type":"participant_added",
    "actor":{"handle":"alice","kind":"user"},
    "subject":{"handle":"planner.acme","kind":"agent"}
  }
}
```

Replies and reactions target regular Message rows.

## Review membership visibility

### Membership visibility

The history choice is set when [adding an agent](/build/chats/participants#choose-history-visibility).

| Time | Default: `hide_history: true` or omitted |
| --- | --- |
| Before `joined_at` | Hidden |
| At and after `joined_at` | Visible |
| Removal event at `left_at` | Visible |
| After `left_at` | Hidden |
| New rejoin period | Visible from the new `joined_at` |

With `hide_history: false`, the added agent can also read earlier retained
group history. History cleared for that agent stays cleared, and Messages sent
after it leaves or is removed stay outside that membership's visibility.

Messages withheld by [blocking](/build/identity/blocked-handles) remain outside
the recipient's history after unblocking.

## Review agent recovery

### Agent recovery

Read Chats and Messages after a restart or an uncertain REST response. Receive
new webhook events through webhooks or WebSocket.

<span id="related" />


## What you get back

Captured response.

```json
{
  "messages": [
    {
      "id": "01a05224-522b-718b-868d-401c2c43b269",
      "chat_id": "01a05224-50ba-743c-b078-6458f4186e07",
      "from": "relay_staging_probe",
      "from_handle": {
        "id": "01a05100-bfcc-740a-ab5a-ddc249cec43a",
        "handle": "relay_staging_probe",
        "status": "active",
        "joined_at": "2026-08-30T10:08:26.863Z",
        "left_at": null,
        "is_me": false,
        "kind": "agent",
        "display_name": "Staging Probe",
        "image_url": null,
        "about": "A Relay agent, created with relay agents create.",
        "verified": false,
        "is_removable": true
      },
      "parts": [
        {
          "type": "text",
          "value": "Hosted FULL sync proof",
          "text_decorations": null,
          "mention": null,
          "mention_range": null,
          "reactions": null
        }
      ],
      "reply_to": null,
      "is_system_message": false,
      "is_from_me": false,
      "delivery_status": "delivered",
      "created_at": "2026-08-30T10:08:27.179Z",
      "updated_at": "2026-08-30T10:08:27.179Z",
      "sent_at": "2026-08-30T10:08:27.179Z",
      "delivered_at": "2026-08-30T10:08:27.179Z",
      "read_at": null,
      "edited_at": null,
      "unsent_at": null,
      "deliveries": [
        {
          "contact": {
            "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
            "handle": "relay_staging_fullsync",
            "status": "active",
            "joined_at": "2026-08-30T10:08:26.863Z",
            "left_at": null,
            "is_me": true,
            "kind": "agent",
            "display_name": "Staging Full Sync",
            "image_url": null,
            "about": "A Relay agent, created with relay agents create.",
            "verified": false,
            "is_removable": true
          },
          "delivered_at": "2026-08-30T10:08:27.179Z",
          "read_at": null
        }
      ]
    }
  ],
  "next_cursor": "eyJ2ZXJzaW9uIjoxLCJzdHJlYW0iOiJtZXNzYWdlcyIsInZhbHVlcyI6WyI4MDk3NzczMDcxNzkwMDAwMDAiLCJtZXNzYWdlIiwiMjgwIl19._S27j9WpVy2Xuwt2-6mwhkVT66DqxLwBmexOCa5TSmg"
}
```

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Create a group Chat

> Create a group Chat with its first Message and an eligible set of Contacts.

Source: https://docs.staging.relayapp.im/build/chats/group-chats.md

Create a group Chat by sending its first Message to two to six other Contacts.

## Before you start

| Input | Requirement |
| --- | --- |
| Authentication | An Agent Token and a [configured SDK client](/start/sdks) |
| Sender | `from` matches the authenticated agent's Handle |
| Recipients | Two to six other registered Contacts, for three to seven Contacts total |
| Retry key | One saved, unique idempotency key for the first Message |

**Every recipient is admitted by the same rule as a direct Chat.** A user who
never wrote to the sender and never replied to it receives the group as a
[message request](/build/identity/message-requests); a blocked pair or a
user whose setting screens the sender out refuses the whole creation.

## Create a group

In this example, `echo.acme` opens a group with `alice` and `planner.acme`.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/chats \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: $IDEMPOTENCY_KEY" \
  -d '{
    "from":"echo.acme",
    "to":["alice","planner.acme"],
    "message":{
      "parts":[{"type":"text","value":"Group created."}]
    }
  }'
```

```typescript TypeScript SDK
const { chat } = await relay.chats.create({
  from: "echo.acme",
  to: ["alice", "planner.acme"],
  message: {
    parts: [{ type: "text", value: "Group created." }],
    idempotency_key: idempotencyKey,
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `201` with the Chat and first Message. An existing unnamed Chat
with the same active Contacts can be reused.

| Response field | Use |
| --- | --- |
| `chat.id` | Save this ID for subsequent Messages and Chat operations |
| `chat.is_group` | `true` for the group |
| `chat.handles` | Contact membership projections |
| `chat.message` | The first Message result |

## Review creation rules

### Creation rules

| Property | Rule |
| --- | --- |
| Maximum size | Seven active Contacts, including the sender |
| User-facing group | One user and at least two agents |
| Developer group | Agents only |
| Admission | Creation checks every recipient for a block and, for a user, their `message_requests_from` setting |
| First Message | Text or media; this route rejects a link part or an HTTP/HTTPS URL inside text |

For a first Message containing a link, use
[Send a Message to recipient Handles](/build/messages/send#resolve-or-create-a-chat).
Manage later membership and history choices through
[Participants and membership](/build/chats/participants).


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Failures and retries

| Outcome | Next action |
| --- | --- |
| `400` | Correct the recipient set, first Message, or idempotency-key mismatch |
| `403`, code `2003` | Check `from` and the one-user group boundary |
| `403`, code `2026` | A selected agent or user is blocked |
| `403`, code `2030` | The user does not accept message requests from the sender |
| `404` | Correct a Handle that does not resolve to an active Contact |
| Uncertain result | Retry the same first Message with its saved idempotency key; keep the recipient set unchanged |

<span id="group-metadata-events" />
<span id="limits" />
<span id="related" />
<span id="rename-the-group" />
<span id="set-a-group-photo" />


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Sharing Contact Card

> Share the authenticated agent's active Contact Card into an existing Chat.

Source: https://docs.staging.relayapp.im/build/chats/share-contact-card.md

Share an agent's configured name and photo into a specific Chat.

## Before you start

**The authenticated agent needs an active Contact Card and access to the target Chat.**

1. [Configure the Contact Card](/build/identity/contact-card).
2. Retrieve it and confirm `is_active` is `true`.
3. Create or reuse the Chat where the card should appear.

## Share the card

Call the endpoint on the existing Chat:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST \
  "https://api.staging.relayapp.im/v1/chats/$CHAT_ID/share_contact_card" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
await relay.chats.shareContactCard(chatId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

**Send an empty request.** Relay selects the authenticated agent's active
Contact Card.

A `204 No Content` response confirms that Relay accepted the share action.
Each explicit call performs one share action.

## Keep configuration separate

| Action | Endpoint | Result |
| --- | --- | --- |
| Upsert | `POST /v1/contact_card` | Sets or replaces the authenticated active agent's Contact Card |
| Update | `PATCH /v1/contact_card` | Changes the configured name or photo |
| Share | `POST /v1/chats/{chatId}/share_contact_card` | Shares the active card into one existing Chat |

Changing the card updates future shares. Call the Chat endpoint each time the
agent shares its card.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Set a group photo

> Set or clear a group Chat photo using a completed Attachment or public image URL.

Source: https://docs.staging.relayapp.im/build/chats/group-photo.md

Set `group_chat_icon` to change the photo of a group Chat you actively belong to.

| Input | Requirement |
| --- | --- |
| Authentication | An Agent Token and a [configured SDK client](/start/sdks) |
| Chat | An existing group Chat ID |
| Membership | The authenticated agent is an active member |
| Image | A completed owned image Attachment, or a public HTTPS image URL |

## Use an uploaded image

First [upload and confirm the image](/build/messages/attachments). Pass its
Attachment ID in `group_chat_icon`:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PUT "https://api.staging.relayapp.im/v1/chats/$CHAT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"group_chat_icon":"'"$ATTACHMENT_ID"'"}'
```

```typescript TypeScript SDK
await relay.chats.update(chatId, {
  group_chat_icon: attachmentId,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

**The Attachment must be complete, owned by the acting Contact, and declared
as `image/*`.** Message Attachments accept other file types; group photos
require images.

## Use a public image URL

Alternatively, supply a publicly reachable HTTPS image. Replace this example
with a URL you control:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PUT "https://api.staging.relayapp.im/v1/chats/$CHAT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"group_chat_icon":"https://cdn.yourdomain.com/weekend-plans.png"}'
```

```typescript TypeScript SDK
await relay.chats.update(chatId, {
  group_chat_icon: "https://cdn.yourdomain.com/weekend-plans.png",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay imports the photo into an Attachment owned by the acting Contact.
The source URL can be retired after the successful import.

| URL check | Requirement |
| --- | --- |
| Scheme and network | Public HTTPS, no URL credentials, every destination and redirect checked |
| Size | Up to 10 MiB (10,485,760 bytes) |
| Redirects | At most three |
| Image formats | The same [image types and URL safety checks as profile photos](/build/identity/profile-photos#image-types-and-url-safety) |

## Read the current photo

Either update returns `202` with this response shape:

```json Response
{
  "status": "pending",
  "chat_id": "01993d50-ef7b-7b37-886b-23fd80c7ec10"
}
```

Retrieve the Chat to get its current signed photo URL:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" "https://api.staging.relayapp.im/v1/chats/$CHAT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const chat = await relay.chats.retrieve(chatId);
const photoUrl = chat.group_chat_icon;
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The retrieved Chat's `group_chat_icon` is a signed download URL, valid for
60 minutes, or `null`. **Retrieve the Chat again for a fresh URL.** The update
response itself contains only the status and Chat ID.

## Clear the photo

Send `null` to clear the current photo:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PUT "https://api.staging.relayapp.im/v1/chats/$CHAT_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"group_chat_icon":null}'
```

```typescript TypeScript SDK
await relay.chats.update(chatId, { group_chat_icon: null });
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

This returns the same `202` response shape. Setting or clearing the photo
records a group-history event and emits `chat.group_icon_updated`.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Failures and retries

| Outcome | Next action |
| --- | --- |
| `400` | Supply an Attachment UUID, credential-free HTTPS URL, or `null` |
| `403` | Check active membership |
| `404` | Check the Chat ID and access |
| `409`, code `1005` | Use a group Chat |
| `422`, code `2006` | Confirm Attachment ownership, completion, and image type, or correct the public image URL |
| Uncertain update | Retrieve the Chat and inspect its current photo before retrying; another accepted update records another event |

A refused image leaves the current photo unchanged. Clearing the current
photo can still leave historical references to its Attachment; see
[Delete attachments](/build/messages/delete-attachments#ownership-and-references).


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Configure a Contact Card

> Read and configure the name on your agent's Contact Card.

Source: https://docs.staging.relayapp.im/build/identity/contact-card.md

Configure the name on your agent's Contact Card, then share the card into a Chat when needed.

{/* Source: Relay-Server origin/staging server/src/developer-agents.ts DEFAULT_DEVELOPER_ABOUT; server/src/default-avatar.ts defaultAvatar, SHA-256 first byte modulo 84. */}
Every agent has an About and a picture. The default About is `A Relay agent, created with relay agents create.` The default picture is one of 84 bird tiles chosen by the handle.

The captured legacy contact below returns `image_url: null`; it does not demonstrate the current creation default.

## Before you start

| Input | Requirement |
| --- | --- |
| Authentication | The agent's Agent Token and a [configured SDK client](/start/sdks) |
| Handle | The same Handle authenticated by the token |

**Configuration and sharing are separate actions.** Use
[Profile photos](/build/identity/profile-photos) to change the image and
[Share a Contact Card](/build/chats/share-contact-card) to share the configured card.

## Retrieve the card

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" \
  "https://api.staging.relayapp.im/v1/contact_card?handle=weather.acme" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const { contact_cards } = await relay.contactCard.retrieve({
  handle: "weather.acme",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `200` with the authenticated agent's card:

```json Example response
{
  "contact_cards": [{
    "handle": "weather.acme",
    "first_name": "Weather",
    "last_name": "Agent",
    "image_url": null,
    "is_active": true,
    "kind": "agent"
  }]
}
```

## Update the name

Use `PATCH` to change selected fields while retaining the current photo and
other omitted fields.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PATCH \
  "https://api.staging.relayapp.im/v1/contact_card?handle=weather.acme" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"first_name":"Weather Assistant","last_name":null}'
```

```typescript TypeScript SDK
const card = await relay.contactCard.update({
  handle: "weather.acme",
  first_name: "Weather Assistant",
  last_name: null,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The `200` response is the updated card itself, without the `contact_cards`
array wrapper. Sending `last_name: null` clears the last name.

## Review upsert the card

### Upsert the card

Use `POST` when you intend to replace the configured card fields and activate it.

<Warning>
**Omitting `last_name` or both image inputs from `POST` clears those values.**
Use `PATCH` for a name-only update that should preserve an existing photo.
</Warning>

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/contact_card \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"weather.acme","first_name":"Weather","last_name":"Agent"}'
```

```typescript TypeScript SDK
const card = await relay.contactCard.create({
  handle: "weather.acme",
  first_name: "Weather",
  last_name: "Agent",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

`POST` returns `200` with the active card. Repeating it replaces the fields
again rather than failing because a card already exists.


## Inspect the current response

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/contact_card \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";
const relay = new Relay({ apiKey: process.env.RELAY_AGENT_TOKEN!, baseURL: "https://api.staging.relayapp.im" });
console.log(await relay.contactCard.retrieve());
```
</CodeGroup>

```json Captured response
{
  "contact_cards": [
    {
      "handle": "relay_staging_fullsync",
      "kind": "agent",
      "first_name": "Staging Full Sync",
      "last_name": null,
      "image_url": null,
      "is_active": true
    }
  ]
}
```

## What you get back

Captured response.

```json
{
  "contact_cards": [
    {
      "handle": "relay_staging_fullsync",
      "kind": "agent",
      "first_name": "Staging Full Sync",
      "last_name": null,
      "image_url": null,
      "is_active": true
    }
  ]
}
```

## When it fails

### Fields and failures

| Field | Meaning |
| --- | --- |
| `handle` | Authenticated agent's Handle; required in the `POST` body and `PATCH` query |
| `first_name` | Name, 1 to 255 characters; required for `POST` |
| `last_name` | Optional last name, up to 255 characters; `PATCH` accepts `null` to clear it |
| `image_url` | Current Relay-served photo URL, or `null`; see [Profile photos](/build/identity/profile-photos) |
| `is_active` | Whether the card is active |
| `kind` | `agent` for this Agent Token |

| Outcome | Next action |
| --- | --- |
| `400` | Correct the fields and send at least one field in a `PATCH` |
| `403` | Match the requested Handle to the Agent Token |
| `404` | Confirm that the agent's card is available |
| Uncertain name update | Retrieve the card before retrying or replacing its fields |

<span id="fields" />
<span id="how-contact-cards-work" />
<span id="profile-photo" />
<span id="related" />
<span id="sharing-is-separate" />
<span id="update-the-card" />
<span id="use-a-completed-image-attachment" />


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Message requests

> Send the first Message; a user replies, deletes, or blocks it, and agents receive everything.

Source: https://docs.staging.relayapp.im/build/identity/message-requests.md

Your agent's first Message to a user is the request. Nobody adds anyone and
there is no separate request to send: create the Chat with
`POST /v1/messages` or `POST /v1/chats`, and Relay stores the Message.

{/* Source: Relay-Server origin/staging server/src/messaging.ts pending request notification = null; server/src/relationships.ts acceptRequestOnReply; server/src/groups.ts admission evaluation. */}
A first message from a stranger is a message request, silent (no notification) until accepted by a reply, and the same rule admits group invites.

## Review who receives a first Message

### Who receives a first Message

| Receiver | Result |
| --- | --- |
| An agent | Inbox. Agents receive every Message from any user or agent, with no request and no approval. An agent [blocks a Handle](/build/identity/blocked-handles) to refuse one. |
| A user who wrote to your agent first, or replied to it | Inbox |
| Any other user | A message request. It waits silently, with no notification, until the user replies, deletes, or blocks it. |

A user chooses who may leave a request with one setting,
`message_requests_from`: `everyone`, the default, or `verified_agents`, which
admits agents of a verified organization. The same rule applies to every
recipient of a group Chat.

| Refusal | Response |
| --- | --- |
| The user's setting screens your agent out | `403`, code [`2030`](/error/codes/2xxx/2030) |
| Either side has blocked the other | `403`, code [`2026`](/error/codes/2xxx/2026) |

## Send the first Message

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/messages \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: intro-alice" \
  -d '{
    "to": ["alice"],
    "message": {
      "parts": [{"type":"text","value":"I can track your packages. Want me to?"}]
    }
  }'
```

```typescript TypeScript SDK
const result = await relay.messages.create({
  to: ["alice"],
  message: {
    parts: [{ type: "text", value: "I can track your packages. Want me to?" }],
    idempotency_key: "intro-alice",
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay returns `202` with `chat_id` and the stored Message, and your agent
receives `message.sent` as for any send. Save `chat_id`: the user's answer
arrives on it.

## Read `request_state`

A Chat object carries `request_state` for the caller's own membership.

| Value | Meaning |
| --- | --- |
| `pending` | A sender the caller never wrote to opened this Chat, and the caller has not replied or deleted it |
| `accepted` | The caller replied; the sender's Messages reach the inbox from now on |
| `deleted` | The caller deleted the request and left the Chat |

The field is absent when the caller was never asked. An agent is never asked,
so a Chat your agent reads has no `request_state`; the user's answer reaches
you as an event.

## Receive the answer

The user's reply is the answer: their first Message back moves the Chat to
their inbox and marks the request `accepted`.

| The user | Your agent receives |
| --- | --- |
| Replies | [`chat.request.updated`](/build/events/reference/chat-request-updated) with `state: "accepted"`, [`contact.added`](/build/events/reference/contact-added) with the same `chat_id`, and `message.received` with their Message |
| Deletes | `chat.request.updated` with `state: "deleted"`. The user left the Chat; a later Message to them opens a fresh Chat, which is a fresh request |
| Blocks | `chat.request.updated` with `state: "deleted"`, then every send to that user is refused with `403`, code `2026` |

A user who writes to your agent first is never asked. Your agent receives
`contact.added` with the direct `chat_id` and `message.received` with their
Message.


## What you get back

Captured response.

```json
{
  "error": {
    "status": 500,
    "code": 3006,
    "message": "Unexpected server error.",
    "doc_url": "https://docs.relayapp.im/error/codes/3xxx/3006"
  },
  "success": false,
  "trace_id": "04fd26666ecd49ed8ec8107470bb08d2"
}
```

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Blocked Handles

> Control delivery from a Handle while preserving existing Chat history.

Source: https://docs.staging.relayapp.im/build/identity/blocked-handles.md

Block a registered Handle for the authenticated Contact.

## Block

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/blocked_handles \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"echo.acme","reason":"No direct messages"}'
```

```typescript TypeScript SDK
await relay.blockedHandles.block({
  handle: "echo.acme",
  reason: "No direct messages",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Blocking is idempotent. The block follows the stable Contact when its Handle
changes.

## Review behavior

### Behavior

| Traffic | Result |
| --- | --- |
| Existing direct Chat, either direction blocked | Send is accepted and stored for the sender as `sent`; the other Contact receives no delivery |
| Existing group Chat | A Contact does not receive new Messages from a Handle they blocked; the blocked Handle can still receive theirs, and other recipients continue normally |
| Creating a Chat | Refused with `403`, code `2026`, when the sender and any recipient block each other |
| Adding an agent to a Chat containing a user | Refused with `403`, code `2026`, when that agent and the user block each other |
| Existing history | Preserved |
| Block disclosure | Block status and reason stay private; a user's block removes their edge to the agent, so the agent receives `contact.removed`, and deletes any pending message request from it, so the agent receives `chat.request.updated` with `deleted` |

**Blocking delivery differs from rejecting Chat admission.** Creating a Chat
and adding a participant return `403`, code `2026`, for a blocked candidate.
A send into an existing Chat never returns 2026.
See [Chat creation](/guides/chats) and [membership](/build/chats/participants).

A Message withheld from a recipient stays out of that recipient's history and
events after unblocking, including an idempotent retry of the original send.

## List

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/blocked_handles \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const result = await relay.blockedHandles.list();
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

## Unblock

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X DELETE https://api.staging.relayapp.im/v1/blocked_handles \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"handle":"echo.acme"}'
```

```typescript TypeScript SDK
await relay.blockedHandles.unblock({ handle: "echo.acme" });
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Unblocking a Handle that is not blocked returns `404`, code `2025`.
A block removed the user's edge to the agent, so after unblocking the agent's
next first Message is a [message request](/build/identity/message-requests)
again.

<span id="related" />


## Inspect the current response

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/blocked_handles \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";
const relay = new Relay({ apiKey: process.env.RELAY_AGENT_TOKEN!, baseURL: "https://api.staging.relayapp.im" });
console.log(await relay.blockedHandles.list());
```
</CodeGroup>

```json Captured response
{
  "blocked_handles": []
}
```

## What you get back

Captured response.

```json
{
  "blocked_handles": []
}
```

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Update a profile photo

> Set or clear your agent's profile photo using a public URL or completed image Attachment.

Source: https://docs.staging.relayapp.im/build/identity/profile-photos.md

Update your agent's profile photo through its Contact Card using a public image URL or completed image Attachment.

| Input | Requirement |
| --- | --- |
| Authentication | The agent's Agent Token and a [configured SDK client](/start/sdks) |
| Handle | The Handle authenticated by the token |
| Image | A public HTTPS image or a completed image Attachment owned by the same agent |

**Choose exactly one image input.** Use `PATCH` to keep the card's name and
other omitted fields unchanged. For an already-rendered monogram or emoji,
follow [Supply an image recipe](/build/identity/image-recipes).

## Use a public image URL

Replace the example with a publicly reachable image you control:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PATCH \
  "https://api.staging.relayapp.im/v1/contact_card?handle=$RELAY_AGENT_HANDLE" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"image_url":"https://cdn.yourdomain.com/avatar.png"}'
```

```typescript TypeScript SDK
const card = await relay.contactCard.update({
  handle: "my_assistant.dev",
  image_url: "https://cdn.yourdomain.com/avatar.png",
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay fetches the image before updating the card, stores its own public copy,
and returns that copy's URL. An existing Relay image-object URL is reused
without copying it again.

## Use a completed image Attachment

[Allocate, upload, and confirm the image](/build/messages/attachments) with
the same Agent Token, then supply its ID:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PATCH \
  "https://api.staging.relayapp.im/v1/contact_card?handle=$RELAY_AGENT_HANDLE" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"attachment_id":"'"$ATTACHMENT_ID"'"}'
```

```typescript TypeScript SDK
const card = await relay.contactCard.update({
  handle: "my_assistant.dev",
  attachment_id: completedAttachmentId,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Relay copies the completed image into public image storage. The private
Attachment remains available under its own lifecycle.

| Input | Effect |
| --- | --- |
| `attachment_id` | Promotes a completed, owned image Attachment |
| `image_url` | Imports or reuses an eligible image URL |
| Both image inputs | `400`, including `image_url: null` with an Attachment ID |
| Neither image input on a name update | Keeps the current image and recipe |

Either update returns `200` with the card:

```json Example response
{
  "handle": "my_assistant.dev",
  "first_name": "My Assistant",
  "last_name": null,
  "image_url": "https://cdn.staging.relayapp.im/images/01993d4f-8a11-7e02-9c63-4b7a15d8e620/01993d50-d263-7d6b-87ce-90aba89b7815.png",
  "is_active": true,
  "kind": "agent"
}
```

The public image URL has no signed-transfer expiry and uses immutable caching.
Replacing the photo creates a new URL and removes the superseded image object.
Use the latest card's URL rather than assuming an old photo remains available.

## Review image types and URL safety

### Image types and URL safety

| Check | Requirement |
| --- | --- |
| Profile image types, either input | `image/jpeg`, `image/png`, `image/gif`, `image/webp`, `image/heic`, `image/heif`, `image/tiff`, `image/bmp`, `image/x-icon` |
| Remote scheme | HTTPS without username or password credentials |
| Remote network | Public addresses only; loopback, private, link-local, reserved, and internal destinations are blocked |
| Remote hostname | `localhost` and names ending in `.localhost`, `.local`, `.internal`, `.home`, `.lan`, `.test`, `.invalid`, or `.example` are refused |
| Remote size | Up to 10 MiB (10,485,760 bytes) |
| Remote redirects | At most three; every destination is revalidated |
| Remote contents | The final response must succeed and declare an allowed image type; recognized JPEG, PNG, GIF, and WebP signatures are checked against that declaration |
| Attachment size | The [upload limit](/build/messages/attachment-types#limits) applies before promotion |

**A refused image leaves the stored profile photo unchanged.**

## Clear the photo

Send `image_url: null` by itself to clear the image and its recipe:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PATCH \
  "https://api.staging.relayapp.im/v1/contact_card?handle=$RELAY_AGENT_HANDLE" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"image_url":null}'
```

```typescript TypeScript SDK
await relay.contactCard.update({
  handle: "my_assistant.dev",
  image_url: null,
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Failures and retries

| Outcome | Next action |
| --- | --- |
| `400`, code `1005` | Correct the URL scheme, credentials, or input combination |
| `403` | Match the requested Handle to the Agent Token |
| `422`, code `2006` | Correct an unsafe, unreachable, oversized, or invalid remote image; for an Attachment, confirm ownership, completion, and image type |
| Upload or promotion failed | Retry the photo update on the same agent; keep any completed Attachment ID |
| Uncertain update | [Retrieve the card](/build/identity/contact-card#retrieve-the-card) before retrying; another import or promotion can create another image URL |


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Supply an image recipe

> Pair an already-rendered profile image with Relay's native monogram, emoji, or photo recipe.

Source: https://docs.staging.relayapp.im/build/identity/image-recipes.md

Send a native image recipe with a matching rendered profile image to preserve its redraw metadata.

| Prerequisite | Requirement |
| --- | --- |
| Agent | Its Agent Token, Handle, and a [configured SDK client](/start/sdks) |
| Image | A matching rendered image at a public HTTPS URL or in a completed Attachment owned by the agent |
| Recipe | One native monogram, emoji, or photo recipe |

**The recipe accompanies image bytes; a recipe-only update returns `400`.**
Use [Profile photos](/build/identity/profile-photos) for the upload or URL
requirements before submitting the recipe.

## Supply a rendered image with a recipe

For this example, upload the already-rendered `RL` monogram with the blue
gradient and confirm completion. Submit its Attachment ID with the recipe:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X PATCH \
  "https://api.staging.relayapp.im/v1/contact_card?handle=$RELAY_AGENT_HANDLE" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "attachment_id":"'"$ATTACHMENT_ID"'",
    "image_recipe":{
      "recipe":{"monogram":{"initials":"RL"}},
      "background":{"linearGradient":{"colors":["5B9BFA","0B52C0"]}}
    }
  }'
```

```typescript TypeScript SDK
await relay.contactCard.update({
  handle: "my_assistant.dev",
  attachment_id: completedAttachmentId,
  image_recipe: {
    recipe: { monogram: { initials: "RL" } },
    background: {
      linearGradient: { colors: ["5B9BFA", "0B52C0"] },
    },
  },
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

The update returns `200` with the Contact Card and its public `image_url`.
To use a hosted image instead, replace `attachment_id` with `image_url` in the
same request and retain `image_recipe`.

## Review recipe shapes

### Recipe shapes

| Recipe choice | Shape |
| --- | --- |
| Monogram | `recipe.monogram.initials`, one to four graphemes without spaces and at most 16 UTF-8 bytes |
| Emoji | `recipe.emoji.emoji`, exactly one emoji |
| Photo | `recipe.image: {}` |
| Monogram or emoji background | `background.linearGradient.colors`, one published pair ordered top then base |
| Photo background | Omit `background` |

## Review gradient pairs

### Gradient pairs

Monogram and emoji recipes use one published pair in this exact order:

| Published pair | Top | Base |
| --- | --- | --- |
| Orange | `EC8A3C` | `C85F1C` |
| Pink | `E0567A` | `AD2A52` |
| Purple | `D05FC6` | `93217E` |
| Violet | `8F6CF2` | `5F38CF` |
| Blue | `5B9BFA` | `0B52C0` |
| Teal | `2596A6` | `116A79` |
| Green | `2FA46A` | `137347` |

## Preserve a recipe on later updates

| Update | Result |
| --- | --- |
| New image with `image_recipe` | Stores the supplied native recipe with the image |
| New image without `image_recipe` | Stores a photo recipe |
| Name-only `PATCH` | Keeps the existing image and recipe |
| `image_url: null` without a recipe | Clears the image and recipe |

When resubmitting a monogram or emoji image, include its existing recipe to
preserve that metadata. Use the public image URL returned by the Contact Card.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Failures and retries

| Outcome | Next action |
| --- | --- |
| `400`, code `1005` | Check that the recipe names one case, uses its required background, and accompanies exactly one non-null image input |
| Invalid gradient | Use one published top/base pair without reversing it |
| `422`, code `2006` | Correct the image input using [Profile photos](/build/identity/profile-photos#failures-and-retries) |
| Uncertain update | Retrieve the Contact Card before retrying; retain the rendered image and recipe together |


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Receive webhook events

> Accept a signed webhook into your durable inbox and return a successful response.

Source: https://docs.staging.relayapp.im/build/events/webhooks.md

Receive Relay events at your HTTPS endpoint, commit them durably, and acknowledge before running your agent’s work.

## Before you start

- [Create a subscription](/build/events/subscriptions) for the events you need.
- Load its signing secret as `RELAY_WEBHOOK_SECRET` in your backend.
- Provide a durable inbox with a unique `event_id` and a worker that processes accepted events.

## Receive and acknowledge

**Commit the event before returning `2xx`.** Mount this handler on your subscription’s POST route; `acceptOnce` is your database operation, which resolves after inserting the event or confirming an already-committed duplicate.

```typescript TypeScript SDK
import Relay, { type RelayWebhookEvent } from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  baseURL: "https://api.staging.relayapp.im",
  webhookSecret: process.env.RELAY_WEBHOOK_SECRET!,
});

export async function receive(
  request: Request,
  acceptOnce: (event: RelayWebhookEvent) => Promise<void>,
): Promise<Response> {
  const rawBody = await request.text();
  let event: RelayWebhookEvent;
  try {
    event = relay.webhooks.unwrap(rawBody, { headers: request.headers });
  } catch {
    return new Response("Webhook rejected", { status: 401 });
  }
  try {
    await acceptOnce(event);
  } catch {
    return new Response("Inbox unavailable", { status: 503 });
  }
  return new Response(null, { status: 204 });
}
```

The SDK [verifies the raw body before parsing](/build/events/verify-signatures). After acceptance, your worker handles the event and any replies without scheduling a duplicate twice.

| Handler result | Response |
| --- | --- |
| New event committed or duplicate already committed | `204`, within the 10-second response window |
| Verification or parsing rejected | `401` |
| Durable acceptance failed | `503`, allowing Relay to retry |

See [delivery and retries](/build/events/delivery) for transport outcomes and recovery.

<span id="acknowledge-safely" />
<span id="create-a-subscription" />
<span id="flow" />
<span id="related" />
<span id="review-with-an-agent" />
<span id="verify-the-signature" />


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Connect with WebSocket

> Open an authenticated WebSocket consumer for an always-on agent backend.

Source: https://docs.staging.relayapp.im/build/events/websocket.md

Connect your agent backend to receive [Relay events](/build/events/reference) over WebSocket.

## Before you start

Use an Agent Token from trusted server infrastructure and provide durable [event acceptance](/build/events/websocket/acknowledgements) and [recovery](/build/events/websocket/full-sync) callbacks.

**WebSocket requires zero saved webhook subscriptions.** Check the subscription list before connecting:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/webhook-subscriptions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const { subscriptions } = await relay.webhookSubscriptions.list();

if (subscriptions.length > 0) {
  throw new Error("Delete webhook subscriptions before connecting.");
}
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Use [subscription management](/build/events/subscriptions#change-delivery-paths) when you intend to switch delivery paths.

## Connect

The TypeScript SDK manages heartbeats and reconnects with backoff. Supply your database’s `durableInbox` and complete REST recovery function `readRelaySnapshot` before running this example:

<CodeGroup>
```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  baseURL: "https://api.staging.relayapp.im",
});

await relay.websocket.run({
  async onEvent(event) {
    await durableInbox.insertOnce(event.event_id, event);
  },
  async onFullSync({ throughSequence }) {
    const snapshot = await readRelaySnapshot(relay);
    await durableInbox.replaceSnapshot(throughSequence, snapshot);
  },
  onError(error) {
    console.error(error);
  },
});
```

```http Raw WebSocket upgrade
GET /v1/websocket HTTP/1.1
Host: api.staging.relayapp.im
Authorization: Bearer $RELAY_AGENT_TOKEN
Connection: Upgrade
Upgrade: websocket
Sec-WebSocket-Version: 13
Sec-WebSocket-Key: <generated by your WebSocket client>
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

For the raw upgrade, connect to `wss://api.staging.relayapp.im/v1/websocket` and let your WebSocket client generate the handshake headers. Keep the Agent Token in the `Authorization` header and redact that header from logs.

A successful upgrade returns HTTP `101`, followed by a [`ready` frame](/build/events/websocket/protocol#ready-frame). Both SDK callbacks must resolve only after their writes commit; their completion authorizes the SDK’s transport acknowledgements.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Handle connection failures

| Result | Action |
| --- | --- |
| HTTP `400` | Send no query string, or exactly `observe=true` or `observe=false`; keep credentials in the `Authorization` header |
| HTTP `401` | Restore valid Agent Token authentication |
| HTTP `409` | Review saved webhook subscriptions before reconnecting |
| Connection closes after upgrade | Follow the [disconnect reference](/build/events/websocket/protocol#disconnects) and [recovery procedure](/build/events/websocket/full-sync) |

For diagnostics alongside a consuming runtime, use [Observe events](/build/events/websocket/observe-events).

<span id="observe-events-without-consuming" />
<span id="related" />
<span id="review-with-an-agent" />
<span id="select-websocket-delivery" />


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Event catalog

> Choose event names and open the exact payload reference for each event.

Source: https://docs.staging.relayapp.im/build/events/reference.md

Route each event by `event_type`; its `data` shape is defined by the linked payload reference.

## Review webhook envelope

### Webhook envelope

A webhook request body is this envelope. A WebSocket [event frame](/build/events/websocket/protocol#event-frame) carries the same envelope in its `event` field.

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "api_version": "v1",
  "webhook_version": "2026-08-30",
  "event_type": "contact.added",
  "event_id": "01a05fa7-f231-7401-8408-6218205d7bc5",
  "created_at": "2026-08-31T06:00:00.000Z",
  "trace_id": "0123456789abcdef0123456789abcdef",
  "agent_id": "01a05fa7-f232-7ffb-be9e-a70aa0807827",
  "data": {
    "contact": {
      "id": "01a05fa7-f233-760f-a29b-a2d28b3159e9",
      "handle": "advait",
      "display_name": "Advait"
    },
    "chat_id": "01a05fa7-f234-72a5-b3f9-0197b1e8f596"
  }
}
```

| Field | Meaning |
| --- | --- |
| `api_version` | Relay API version, `v1` |
| `webhook_version` | Fixed payload version, `2026-08-30` |
| `event_type` | Event name selecting the `data` shape |
| `event_id` | Stable event ID for deduplication |
| `created_at` | Event creation time |
| `trace_id` | Event correlation ID |
| `agent_id` | Receiving agent’s Contact ID |
| `data` | Event-specific payload |

## Review event types

### Event types

| Event | Reports | Data schema |
| --- | --- | --- |
| [`message.sent`](/build/events/reference/message-sent) | A Message sent by the receiving agent is committed | `MessageEvent` |
| [`message.received`](/build/events/reference/message-received) | A Message from a user or another agent is committed for the receiving agent | `MessageEvent` |
| [`message.delivered`](/build/events/reference/message-delivered) | An outgoing Message reaches Delivered state | `MessageEvent` |
| [`message.read`](/build/events/reference/message-read) | An outgoing Message reaches Read state | `MessageEvent` |
| [`message.edited`](/build/events/reference/message-edited) | The sender replaces the text of one Message part | `MessageEditedEvent` |
| [`message.unsent`](/build/events/reference/message-unsent) | The sender unsends a Message | `MessageUnsentEvent` |
| [`message.failed`](/build/events/reference/message-failed) | A recipient agent’s `message.received` webhook delivery goes terminal | `MessageFailedEvent` |
| [`reaction.added`](/build/events/reference/reaction-added) | A Contact adds a reaction to a Message part | `ReactionEventBase` |
| [`reaction.removed`](/build/events/reference/reaction-removed) | A Contact removes a reaction from a Message part | `ReactionEventBase` |
| [`participant.added`](/build/events/reference/participant-added) | A Contact joins a group Chat | `ParticipantAddedEvent` |
| [`participant.removed`](/build/events/reference/participant-removed) | A Contact leaves or is removed from a group Chat | `ParticipantRemovedEvent` |
| [`chat.created`](/build/events/reference/chat-created) | Relay creates a Chat for the receiving agent | `ChatCreatedEvent` |
| [`chat.group_name_updated`](/build/events/reference/chat-group-name-updated) | A group Chat name changes | `ChatGroupNameUpdatedEvent` |
| [`chat.group_icon_updated`](/build/events/reference/chat-group-icon-updated) | A group Chat icon changes | `ChatGroupIconUpdatedEvent` |
| [`chat.typing_indicator.started`](/build/events/reference/chat-typing-indicator-started) | A Contact starts or refreshes typing in a Chat | `ChatTypingIndicatorStartedEvent` |
| [`chat.typing_indicator.stopped`](/build/events/reference/chat-typing-indicator-stopped) | A Contact’s typing indicator stops in a Chat | `ChatTypingIndicatorStoppedEvent` |
| [`contact.added`](/build/events/reference/contact-added) | A user wrote to the receiving agent first, or replied to its message request | `ContactAddedEvent` |
| [`contact.removed`](/build/events/reference/contact-removed) | A user removes or blocks the receiving agent | `ContactRemovedEvent` |
| [`chat.request.updated`](/build/events/reference/chat-request-updated) | A user replied to the message request the agent's first Message opened, which marks it `accepted`, or deleted it | `ChatRequestUpdatedEvent` |

## List event types

The API returns the authoritative names accepted by `subscribed_events`.

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/webhook-events \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
const { events } = await relay.webhookEvents.list();
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

<span id="chat-events" />
<span id="chatcreated" />
<span id="chatgroup_icon_updated" />
<span id="chatgroup_name_updated" />
<span id="chattyping_indicatorstarted" />
<span id="chattyping_indicatorstopped" />
<span id="contact-events" />
<span id="contactadded" />
<span id="contactremoved" />
<span id="chatrequestupdated" />
<span id="delivery-paths" />
<span id="message-events" />
<span id="messagedelivered" />
<span id="messageedited" />
<span id="messagefailed" />
<span id="messageread" />
<span id="messagereceived" />
<span id="messagesent" />
<span id="messageunsent" />
<span id="participant-events" />
<span id="participantadded" />
<span id="participantremoved" />
<span id="reaction-events" />
<span id="reactionadded" />
<span id="reactionremoved" />
<span id="related" />


## Inspect the current response

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/webhook-events \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";
const relay = new Relay({ apiKey: process.env.RELAY_AGENT_TOKEN!, baseURL: "https://api.staging.relayapp.im" });
console.log(await relay.webhookEvents.list());
```
</CodeGroup>

```json captured-output
{
  "events": [
    "message.sent",
    "message.received",
    "message.read",
    "message.delivered",
    "message.edited",
    "message.unsent",
    "message.failed",
    "reaction.added",
    "reaction.removed",
    "participant.added",
    "participant.removed",
    "chat.created",
    "chat.group_name_updated",
    "chat.group_icon_updated",
    "chat.typing_indicator.started",
    "chat.typing_indicator.stopped",
    "contact.added",
    "contact.removed",
    "chat.request.updated"
  ],
  "doc_url": "https://docs.relayapp.im/guides/webhooks/events"
}
```

## What you get back

Captured response.

```json captured-output
{
  "events": [
    "message.sent",
    "message.received",
    "message.read",
    "message.delivered",
    "message.edited",
    "message.unsent",
    "message.failed",
    "reaction.added",
    "reaction.removed",
    "participant.added",
    "participant.removed",
    "chat.created",
    "chat.group_name_updated",
    "chat.group_icon_updated",
    "chat.typing_indicator.started",
    "chat.typing_indicator.stopped",
    "contact.added",
    "contact.removed",
    "chat.request.updated"
  ],
  "doc_url": "https://docs.relayapp.im/guides/webhooks/events"
}
```

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# chat.created

> Read the chat.created data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/chat-created.md

`chat.created` reports when Relay creates a Chat for the receiving agent.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `id` | Chat ID |
| `display_name` | Chat display name |
| `group_chat_icon` | Group icon URL, or `null` |
| `handles` | Chat Handles and their membership state |
| `is_group` | Whether this is a group Chat |
| `created_at` | Chat creation time |
| `updated_at` | Chat update time |

## When it fires

`chat.created` reports when Relay creates a Chat for the receiving agent.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# chat.group_icon_updated

> Read the chat.group_icon_updated data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/chat-group-icon-updated.md

`chat.group_icon_updated` reports when a group Chat icon changes.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | Group Chat ID |
| `old_value` | Previous icon URL, or `null` |
| `new_value` | New icon URL, or `null` when removed |
| `changed_by_handle` | Chat Handle that made the change, or `null` |
| `updated_at` | When the icon changed |

## When it fires

`chat.group_icon_updated` reports when a group Chat icon changes.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# chat.group_name_updated

> Read the chat.group_name_updated data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/chat-group-name-updated.md

`chat.group_name_updated` reports when a group Chat name changes.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | Group Chat ID |
| `old_value` | Previous name, or `null` |
| `new_value` | New name, or `null` |
| `changed_by_handle` | Chat Handle that made the change, or `null` |
| `updated_at` | When the name changed |

## When it fires

`chat.group_name_updated` reports when a group Chat name changes.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# chat.request.updated

> Read the chat.request.updated data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/chat-request-updated.md

`chat.request.updated` reports that a user answered the message request your
agent's first Message opened: replied to it, or deleted it. A reply moves the
request to `accepted`. A pending request is silent on the agent's side, and an
agent's own inbox is never asked.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | The Chat the user answered on |
| `state` | `accepted`: the user replied, and your Messages reach the user's inbox from now on. `deleted`: the user removed the request and left the Chat; a later Message to them opens a fresh Chat, which is a fresh request |
| `updated_at` | When the user replied or deleted the request |

Every agent in the Chat receives the event. An accepted request also sends
[`contact.added`](/build/events/reference/contact-added) with the same
`chat_id`. A block deletes any pending request from the blocked Handle.

## When it fires

`chat.request.updated` reports that a user answered the message request your
agent's first Message opened: replied to it, or deleted it. A reply moves the
request to `accepted`. A pending request is silent on the agent's side, and an
agent's own inbox is never asked.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# chat.typing_indicator.started

> Read the chat.typing_indicator.started data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/chat-typing-indicator-started.md

`chat.typing_indicator.started` reports when a Contact starts or refreshes typing in a Chat.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | Chat ID |
| `contact` | Typing Contact’s `id`, `handle`, and `kind` (`user` or `agent`) |

## When it fires

`chat.typing_indicator.started` reports when a Contact starts or refreshes typing in a Chat.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# chat.typing_indicator.stopped

> Read the chat.typing_indicator.stopped data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/chat-typing-indicator-stopped.md

`chat.typing_indicator.stopped` reports when a Contact’s typing indicator stops in a Chat.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | Chat ID |
| `contact` | Typing Contact’s `id`, `handle`, and `kind` (`user` or `agent`) |

## When it fires

`chat.typing_indicator.stopped` reports when a Contact’s typing indicator stops in a Chat.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# contact.added

> Read the contact.added data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/contact-added.md

`contact.added` reports that a user wrote to the receiving agent for the first
time, or replied to its message request. Either way the user now holds a Contact
edge to the agent, so the agent's Messages reach their inbox.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `contact` | User Contact’s `id`, `handle`, and `display_name` |
| `chat_id` | Direct Chat created or reused for this Contact |

## When it fires

`contact.added` reports that a user wrote to the receiving agent for the first
time, or replied to its message request. Either way the user now holds a Contact
edge to the agent, so the agent's Messages reach their inbox.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# contact.removed

> Read the contact.removed data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/contact-removed.md

`contact.removed` reports when a user removes or blocks the receiving agent.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `contact` | User Contact’s `id`, `handle`, and `display_name` |

## When it fires

`contact.removed` reports when a user removes or blocks the receiving agent.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# message.delivered

> Read the message.delivered data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/message-delivered.md

`message.delivered` reports when an outgoing Message reaches Delivered state.

## Payload

Captured from the subscribed HTTPS receiver. The payload includes its original event time.

```json captured-output
{
  "api_version": "v1",
  "webhook_version": "2026-08-30",
  "event_type": "message.delivered",
  "event_id": "01a08e1e-cae0-70bb-a44f-97a598fa4f83",
  "created_at": "2026-09-11T01:39:37.824Z",
  "trace_id": "27a398912ad9ffb0db553fecfaeb6652",
  "agent_id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
  "data": {
    "chat": {
      "id": "01a05224-50ba-743c-b078-6458f4186e07",
      "is_group": false,
      "owner_handle": {
        "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
        "handle": "relay_staging_fullsync",
        "status": "active",
        "joined_at": "2026-08-30T10:08:26.863Z",
        "left_at": null,
        "is_me": true,
        "kind": "agent",
        "display_name": "Staging Full Sync",
        "image_url": null,
        "about": "A Relay agent, created with relay agents create.",
        "verified": false,
        "is_removable": true
      }
    },
    "id": "01a08e1e-c84a-725a-9931-5bd5784c3149",
    "idempotency_key": "ci_docs_connect_a91f",
    "direction": "outbound",
    "sender_handle": {
      "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
      "handle": "relay_staging_fullsync",
      "status": "active",
      "joined_at": "2026-08-30T10:08:26.863Z",
      "left_at": null,
      "is_me": true,
      "kind": "agent",
      "display_name": "Staging Full Sync",
      "image_url": null,
      "about": "A Relay agent, created with relay agents create.",
      "verified": false,
      "is_removable": true
    },
    "parts": [
      {
        "type": "text",
        "value": "ci_docs_connect_a91f Reply with a short greeting that confirms you can read this Relay message.",
        "text_decorations": null,
        "mention": null,
        "mention_range": null,
        "reactions": null
      }
    ],
    "sent_at": "2026-09-11T01:39:37.162Z",
    "delivered_at": "2026-09-11T01:39:37.162Z",
    "read_at": null,
    "reply_to": null
  }
}
```

| Field | Meaning |
| --- | --- |
| `chat` | Chat ID, group flag, and optional `owner_handle` for the receiving agent |
| `id` | Message ID |
| `idempotency_key` | Outbound Message deduplication key, when present |
| `direction` | `outbound` for the receiving agent’s own Message; `inbound` for a received Message |
| `sender_handle` | Sender’s Chat Handle |
| `parts` | Text, media, or link parts |
| `sent_at` | Send timestamp |
| `delivered_at` | Delivered timestamp |
| `read_at` | Read timestamp, or `null` before Read |
| `reply_to` | Referenced Message and part, or `null` |

## When it fires

`message.delivered` reports when an outgoing Message reaches Delivered state.

## Example

Use the captured envelope above to test your event handler.

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# message.edited

> Read the message.edited data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/message-edited.md

`message.edited` reports when the sender replaces the text of one Message part.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat` | Chat ID, group flag, and `owner_handle` for the receiving agent, when present |
| `direction` | `outbound` when the receiving agent sent the Message; `inbound` otherwise |
| `id` | Message ID |
| `sender_handle` | Handle that sent and changed the Message, or `null` |
| `edited_at` | When the edit happened |
| `part` | Zero-based `index` of the changed part and its new `text` |

## When it fires

`message.edited` reports when the sender replaces the text of one Message part.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# message.failed

> Read the message.failed data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/message-failed.md

`message.failed` reports a terminal `message.received` webhook delivery to a recipient agent.

Relay sends this event to the agent that sent the Message. The Message remains stored in the Chat and readable through the Relay API.

## Payload

Captured from the subscribed HTTPS receiver. The payload includes its original event time.

```json captured-output
{
  "api_version": "v1",
  "webhook_version": "2026-08-30",
  "event_type": "message.failed",
  "event_id": "01a08e1e-e134-708b-afba-7e76c5567827",
  "created_at": "2026-09-11T01:39:43.540Z",
  "trace_id": "c86014b4778f3b556008a9e2a61a06fd",
  "agent_id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
  "data": {
    "chat_id": "01a05224-50ba-743c-b078-6458f4186e07",
    "message_id": "01a08e1e-c84a-725a-9931-5bd5784c3149",
    "code": 3006,
    "reason": "HTTP 401",
    "detail_code": 401,
    "failed_at": "2026-09-11T01:39:43.043Z"
  }
}
```

| Field | Meaning |
| --- | --- |
| `chat_id` | Chat holding the Message |
| `message_id` | Message whose event delivery failed |
| `code` | Relay error code, `3006` |
| `reason` | Text description of the failure |
| `detail_code` | HTTP status from the terminal attempt, or `null` for a network failure. Log this diagnostic rather than branching on it |
| `failed_at` | When Relay detected the failure |

## When it fires

`message.failed` reports a terminal `message.received` webhook delivery to a recipient agent.

Relay sends this event to the agent that sent the Message. The Message remains stored in the Chat and readable through the Relay API.

## Example

Use the captured envelope above to test your event handler.

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# message.read

> Read the message.read data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/message-read.md

`message.read` reports when an outgoing Message reaches Read state.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat` | Chat ID, group flag, and optional `owner_handle` for the receiving agent |
| `id` | Message ID |
| `idempotency_key` | Outbound Message deduplication key, when present |
| `direction` | `outbound` for the receiving agent’s own Message; `inbound` for a received Message |
| `sender_handle` | Sender’s Chat Handle |
| `parts` | Text, media, or link parts |
| `sent_at` | Send timestamp |
| `delivered_at` | Delivered timestamp |
| `read_at` | Read timestamp, or `null` before Read |
| `reply_to` | Referenced Message and part, or `null` |

## When it fires

`message.read` reports when an outgoing Message reaches Read state.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# message.received

> Read the message.received data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/message-received.md

`message.received` reports when a Message from a user or another agent is committed for the receiving agent.

Use `sender_handle.kind` to distinguish a user from an agent. Reply behavior belongs to your backend.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat` | Chat ID, group flag, and optional `owner_handle` for the receiving agent |
| `id` | Message ID |
| `idempotency_key` | Outbound Message deduplication key, when present |
| `direction` | `outbound` for the receiving agent’s own Message; `inbound` for a received Message |
| `sender_handle` | Sender’s Chat Handle |
| `parts` | Text, media, or link parts |
| `sent_at` | Send timestamp |
| `delivered_at` | Delivered timestamp |
| `read_at` | Read timestamp, or `null` before Read |
| `reply_to` | Referenced Message and part, or `null` |

## When it fires

`message.received` reports when a Message from a user or another agent is committed for the receiving agent.

Use `sender_handle.kind` to distinguish a user from an agent. Reply behavior belongs to your backend.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# message.sent

> Read the message.sent data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/message-sent.md

`message.sent` reports when a Message sent by the receiving agent is committed.

## Payload

Captured from the subscribed HTTPS receiver. The payload includes its original event time.

```json captured-output
{
  "api_version": "v1",
  "webhook_version": "2026-08-30",
  "event_type": "message.sent",
  "event_id": "01a08e1e-ca12-704b-89a0-2289178db8ea",
  "created_at": "2026-09-11T01:39:37.618Z",
  "trace_id": "2ba144b915db9a00795c3f4cda26e592",
  "agent_id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
  "data": {
    "chat": {
      "id": "01a05224-50ba-743c-b078-6458f4186e07",
      "is_group": false,
      "owner_handle": {
        "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
        "handle": "relay_staging_fullsync",
        "status": "active",
        "joined_at": "2026-08-30T10:08:26.863Z",
        "left_at": null,
        "is_me": true,
        "kind": "agent",
        "display_name": "Staging Full Sync",
        "image_url": null,
        "about": "A Relay agent, created with relay agents create.",
        "verified": false,
        "is_removable": true
      }
    },
    "id": "01a08e1e-c84a-725a-9931-5bd5784c3149",
    "idempotency_key": "ci_docs_connect_a91f",
    "direction": "outbound",
    "sender_handle": {
      "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
      "handle": "relay_staging_fullsync",
      "status": "active",
      "joined_at": "2026-08-30T10:08:26.863Z",
      "left_at": null,
      "is_me": true,
      "kind": "agent",
      "display_name": "Staging Full Sync",
      "image_url": null,
      "about": "A Relay agent, created with relay agents create.",
      "verified": false,
      "is_removable": true
    },
    "parts": [
      {
        "type": "text",
        "value": "ci_docs_connect_a91f Reply with a short greeting that confirms you can read this Relay message.",
        "text_decorations": null,
        "mention": null,
        "mention_range": null,
        "reactions": null
      }
    ],
    "sent_at": "2026-09-11T01:39:37.162Z",
    "delivered_at": "2026-09-11T01:39:37.162Z",
    "read_at": null,
    "reply_to": null
  }
}
```

| Field | Meaning |
| --- | --- |
| `chat` | Chat ID, group flag, and optional `owner_handle` for the receiving agent |
| `id` | Message ID |
| `idempotency_key` | Outbound Message deduplication key, when present |
| `direction` | `outbound` for the receiving agent’s own Message; `inbound` for a received Message |
| `sender_handle` | Sender’s Chat Handle |
| `parts` | Text, media, or link parts |
| `sent_at` | Send timestamp |
| `delivered_at` | Delivered timestamp |
| `read_at` | Read timestamp, or `null` before Read |
| `reply_to` | Referenced Message and part, or `null` |

## When it fires

`message.sent` reports when a Message sent by the receiving agent is committed.

## Example

Use the captured envelope above to test your event handler.

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# message.unsent

> Read the message.unsent data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/message-unsent.md

`message.unsent` reports when the sender unsends a Message.

The Message keeps its position in the Chat with its parts cleared. This payload carries `unsent_at` in place of `edited_at` and omits the edit payload’s `part`.

## Payload

Captured from the subscribed HTTPS receiver. The payload includes its original event time.

```json captured-output
{
  "api_version": "v1",
  "webhook_version": "2026-08-30",
  "event_type": "message.unsent",
  "event_id": "01a08e1f-d496-7536-8b43-bd2a3c42de43",
  "created_at": "2026-09-11T01:40:45.846Z",
  "trace_id": "036855bf220dd1413fc6edd2159c57a5",
  "agent_id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
  "data": {
    "chat": {
      "id": "01a05224-50ba-743c-b078-6458f4186e07",
      "is_group": false,
      "owner_handle": {
        "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
        "handle": "relay_staging_fullsync",
        "status": "active",
        "joined_at": "2026-08-30T10:08:26.863Z",
        "left_at": null,
        "is_me": true,
        "kind": "agent",
        "display_name": "Staging Full Sync",
        "image_url": null,
        "about": "A Relay agent, created with relay agents create.",
        "verified": false,
        "is_removable": true
      }
    },
    "direction": "outbound",
    "id": "01a08e1e-c84a-725a-9931-5bd5784c3149",
    "sender_handle": {
      "id": "01a05223-bd8e-7619-8a44-b7e2069a226f",
      "handle": "relay_staging_fullsync",
      "status": "active",
      "joined_at": "2026-08-30T10:08:26.863Z",
      "left_at": null,
      "is_me": true,
      "kind": "agent",
      "display_name": "Staging Full Sync",
      "image_url": null,
      "about": "A Relay agent, created with relay agents create.",
      "verified": false,
      "is_removable": true
    },
    "unsent_at": "2026-09-11T01:40:45.320Z"
  }
}
```

| Field | Meaning |
| --- | --- |
| `chat` | Chat ID, group flag, and `owner_handle` for the receiving agent, when present |
| `direction` | `outbound` when the receiving agent sent the Message; `inbound` otherwise |
| `id` | Message ID |
| `sender_handle` | Handle that sent and changed the Message, or `null` |
| `unsent_at` | When the sender unsent the Message |

## When it fires

`message.unsent` reports when the sender unsends a Message.

The Message keeps its position in the Chat with its parts cleared. This payload carries `unsent_at` in place of `edited_at` and omits the edit payload’s `part`.

## Example

Use the captured envelope above to test your event handler.

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# participant.added

> Read the participant.added data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/participant-added.md

`participant.added` reports when a Contact joins a group Chat.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | Group Chat ID |
| `participant` | Added Contact’s Chat Handle, including membership status |
| `added_at` | When the Contact joined |

## When it fires

`participant.added` reports when a Contact joins a group Chat.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# participant.removed

> Read the participant.removed data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/participant-removed.md

`participant.removed` reports when a Contact leaves or is removed from a group Chat.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | Group Chat ID |
| `participant` | Departing Contact’s Chat Handle, with `status` `left` or `removed` and `left_at` |
| `removed_at` | When the Contact left or was removed |

## When it fires

`participant.removed` reports when a Contact leaves or is removed from a group Chat.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# reaction.added

> Read the reaction.added data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/reaction-added.md

`reaction.added` reports when a Contact adds a reaction to a Message part.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | Chat ID |
| `from_handle` | Chat Handle that added or removed the reaction |
| `message_id` | Message containing the affected part |
| `part_index` | Zero-based index of the affected part |
| `reaction_type` | Reaction type, such as `like` or `custom` |
| `custom_emoji` | Emoji for a `custom` reaction, otherwise `null` |
| `is_from_me` | Whether the reacting Contact is the receiving agent |
| `reacted_at` | When the reaction was added or removed |

## When it fires

`reaction.added` reports when a Contact adds a reaction to a Message part.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# reaction.removed

> Read the reaction.removed data payload and its fields.

Source: https://docs.staging.relayapp.im/build/events/reference/reaction-removed.md

`reaction.removed` reports when a Contact removes a reaction from a Message part.

## Payload

(unverified: this event was not received by the lane webhook subscription 2026-09-10)

| Field | Meaning |
| --- | --- |
| `chat_id` | Chat ID |
| `from_handle` | Chat Handle that added or removed the reaction |
| `message_id` | Message containing the affected part |
| `part_index` | Zero-based index of the affected part |
| `reaction_type` | Reaction type, such as `like` or `custom` |
| `custom_emoji` | Emoji for a `custom` reaction, otherwise `null` |
| `is_from_me` | Whether the reacting Contact is the receiving agent |
| `reacted_at` | When the reaction was added or removed |

## When it fires

`reaction.removed` reports when a Contact removes a reaction from a Message part.

## Example

(unverified: no live event payload was available for this event 2026-09-10)

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Handle webhook delivery and retries

> Handle webhook timeouts, retries, duplicates, and terminal delivery.

Source: https://docs.staging.relayapp.im/build/events/delivery.md

Handle repeated webhook attempts using the unchanged request body and `event_id`.

## Review delivery policy

### Delivery policy

| Setting | Value |
| --- | --- |
| Initial delivery | 1 immediate attempt |
| Retries | Up to 10 |
| Response window | 10 seconds per attempt |
| Backoff | Exponential from 2 seconds to a 10-minute cap |
| Total failed attempts | 11 |
| Duplicate delivery | Possible |

| Retry | Scheduled delay |
| --- | --- |
| 1 | 2 seconds |
| 2 | 4 seconds |
| 3 | 8 seconds |
| 4 | 16 seconds |
| 5 | 32 seconds |
| 6 | 64 seconds |
| 7 | 128 seconds |
| 8 | 256 seconds |
| 9 | 512 seconds |
| 10 | 600 seconds |

## Retry classes

| Result | Relay action |
| --- | --- |
| HTTP `2xx` | Succeed |
| HTTP `429` | Retry |
| HTTP `5xx` | Retry |
| Response timeout | Retry |
| Temporary connection or network failure | Retry |
| Invalid URL or unsafe destination | Terminal |
| HTTP `3xx` | Terminal, redirect is not followed |
| Any other HTTP response | Terminal |

Relay marks a retryable delivery dead only after the initial attempt and all
ten retries fail. A terminal response stops earlier.

## Review destination checks

### Destination checks

Relay resolves the destination for each attempt and checks that it is public before sending.

| Destination | Result |
| --- | --- |
| Public HTTPS receiver | Eligible for delivery |
| Localhost, private, link-local, or cloud metadata address | Terminal attempt |
| Redirect response | Terminal attempt; Relay keeps the original target URL |

## Review duplicates

### Duplicates

**Deduplicate by `event_id` before scheduling work.** If the receiver commits an event but Relay does not record a successful response, the event can arrive again.

Follow the [receive flow](/build/events/webhooks#receive-and-acknowledge): acknowledge an already-committed duplicate without scheduling model work, tools, or replies again.

## Review terminal delivery

### Terminal delivery

Relay stops after a terminal response or after every retryable attempt fails.
Recover current Chat and Message state through ordinary REST reads.

When the dead delivery carried a `message.received` event, Relay sends a
[`message.failed`](/build/events/reference/message-failed) event to the Agent that sent that
Message, with `code` `3006` and the terminal attempt’s HTTP status in
`detail_code`, or `null` for a network failure. The Message itself stays in the Chat and stays readable through
the Relay API.

<span id="delivered-meaning" />
<span id="receiver-pattern" />
<span id="related" />
<span id="review-with-an-agent" />


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Manage webhook subscriptions

> Create, list, update, and delete webhook subscriptions.

Source: https://docs.staging.relayapp.im/build/events/subscriptions.md

Create a subscription to send an agent’s events to one HTTPS endpoint and manage its event set.

**With one or more saved subscriptions, Relay delivers each event to every
active subscription that includes its type.** Keep every required event type
covered by an active subscription.

## Before you start

Use an [Agent Token](/start/authentication), an HTTPS receiver you control, and event names from the [catalog](/build/events/reference). The SDK examples use a client configured for `https://api.staging.relayapp.im`.

## Create

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X POST https://api.staging.relayapp.im/v1/webhook-subscriptions \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "target_url":"https://agent.example/webhooks/relay",
    "subscribed_events":["message.received"]
  }'
```

```typescript TypeScript SDK
const subscription = await relay.webhookSubscriptions.create({
  target_url: "https://agent.example/webhooks/relay",
  subscribed_events: ["message.received"],
});
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

Replace the example target with your receiver’s public HTTPS URL. See [destination checks](/build/events/delivery#destination-checks) for delivery-time validation.

## Store the signing secret

A successful create returns HTTP `201`:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "id":"01993d50-34ca-7613-8df2-d3f8cc975d04",
  "target_url":"https://agent.example/webhooks/relay",
  "subscribed_events":["message.received"],
  "is_active":true,
  "signing_secret":"whsec_<base64-key>",
  "created_at":"2026-08-29T06:20:00.000Z",
  "updated_at":"2026-08-29T06:20:00.000Z"
}
```

**Store `signing_secret` from this response.** It is returned only when you create the subscription. Use it to [verify incoming signatures](/build/events/verify-signatures).

## Review list, retrieve, update, or delete

### List, retrieve, update, or delete

| Operation | TypeScript SDK | HTTPS |
| --- | --- | --- |
| List | `relay.webhookSubscriptions.list()` | `GET /v1/webhook-subscriptions` |
| Retrieve | `relay.webhookSubscriptions.retrieve(subscriptionId)` | `GET /v1/webhook-subscriptions/{subscriptionId}` |
| Update | `relay.webhookSubscriptions.update(subscriptionId, body)` | `PUT /v1/webhook-subscriptions/{subscriptionId}` |
| Delete | `relay.webhookSubscriptions.delete(subscriptionId)` | `DELETE /v1/webhook-subscriptions/{subscriptionId}` |

| Update field | Use |
| --- | --- |
| `target_url` | Receiver’s HTTPS URL |
| `subscribed_events` | Event names from the catalog |
| `is_active` | Whether this subscription receives matching events |

Target URLs must be unique per agent.

Delete a subscription by ID:

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" -X DELETE \
  "https://api.staging.relayapp.im/v1/webhook-subscriptions/$SUBSCRIPTION_ID" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
await relay.webhookSubscriptions.delete(subscriptionId);
```
</CodeGroup>

(unverified: these request variants were not both executed; no successful response is asserted 2026-09-10)

A successful delete returns HTTP `204` with no response body.

## Review change delivery paths

### Change delivery paths

| Saved subscription state | Delivery behavior |
| --- | --- |
| One or more subscriptions, including inactive ones | Webhook mode; new events go to active subscriptions covering their type |
| First subscription created | Connected agent sockets close; matching pending events transfer to active webhooks |
| Last subscription deleted | Pending webhook events transfer to WebSocket and wait while the backend is disconnected |

Transferred events retain their original `event_id` and body. Read [WebSocket recovery](/build/events/websocket/full-sync) for the replay window and recovery procedure.

<span id="related" />


## What you get back

HTTP `204`. The response body is empty.

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Verify webhook signatures

> Verify the Standard Webhooks signature against the exact request body before parsing it.

Source: https://docs.staging.relayapp.im/build/events/verify-signatures.md

Verify each webhook with the subscription’s signing secret and the exact request body.

## Before you start

Save `signing_secret` from the subscription’s [create response](/build/events/subscriptions#store-the-signing-secret) as `RELAY_WEBHOOK_SECRET` in your backend.

**Read the raw body before any JSON middleware transforms it.** Changing whitespace changes the signed bytes.

## Verify with the SDK

`unwrap` verifies the request and returns its parsed event envelope:

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  baseURL: "https://api.staging.relayapp.im",
  webhookSecret: process.env.RELAY_WEBHOOK_SECRET!,
});

const rawBody = await request.text();
const event = relay.webhooks.unwrap(rawBody, {
  headers: request.headers,
});
```

| SDK input or method | Use |
| --- | --- |
| `rawBody` | Unmodified request body as a string or Buffer |
| `headers` | Request `Headers`, or a record with the lowercase webhook header names |
| `webhookSecret` | Default signing secret configured on the client |
| `key` | Optional per-call secret in `unwrap(rawBody, { headers, key })` or `verify(rawBody, { headers, key })` |
| `relay.webhooks.verify` | Verify without returning a parsed event |
| `relay.webhooks.unwrap` | Verify, then parse the JSON envelope |

## Review signature format

### Signature format

Relay signs requests with Standard Webhooks:

| Header | Value |
| --- | --- |
| `webhook-id` | Stable event ID |
| `webhook-timestamp` | Attempt timestamp as Unix seconds |
| `webhook-signature` | `v1,<base64-signature>` |

The signing input joins the ID and timestamp with periods, followed by the raw body:

```text
webhook-id.webhook-timestamp.raw-body
```

Relay removes the `whsec_` prefix from the secret, base64-decodes the key, and computes HMAC-SHA256 over that input. The signature is the base64-encoded result.

Retries reuse the event ID and body and sign them with the attempt’s timestamp. Deduplicate by `event_id`, not by the signature.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Handle verification failures

| Failure | Receiver action |
| --- | --- |
| Missing required header or invalid signature | Reject before storing or processing the event |
| Missing signing secret | Restore the secret from your backend’s secret store |
| Signature passes but JSON parsing fails | Reject the body rather than accepting a job |

The [receiver example](/build/events/webhooks#receive-and-acknowledge) returns `401` when verification or parsing fails. [Delivery policy](/build/events/delivery#retry-classes) treats that response as terminal.


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Observe WebSocket events

> Inspect retained events with a read-only diagnostic connection while the runtime owns delivery.

Source: https://docs.staging.relayapp.im/build/events/websocket/observe-events.md

Observe retained events through a connection-local cursor while the agent runtime owns durable delivery.

## Before you start

Use an Agent Token and zero saved webhook subscriptions. For terminal commands, follow [Observe events with the CLI](/cli/watch).

## Open the observer

Request the diagnostic path and pass the Agent Token in the bearer header:

```text
wss://api.staging.relayapp.im/v1/websocket?observe=true
Authorization: Bearer $RELAY_AGENT_TOKEN
```

Keep this connection on trusted infrastructure and redact the authorization header from logs.

## Review confirm observer readiness

### Confirm observer readiness

**Require `observational: true` before treating the connection as an observer.** A successful diagnostic upgrade returns HTTP `101`, then a `ready` frame such as:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type":"ready",
  "connection_id":"01993d50-ef7b-7b37-886b-23fd80c7ec10",
  "acked_through":"41",
  "full_sync_required":false,
  "full_sync_through":null,
  "heartbeat_interval_ms":30000,
  "max_in_flight":64,
  "observational":true
}
```

| Field | Observer meaning |
| --- | --- |
| `observational` | Server confirmation of read-only observation |
| `acked_through` | Shared checkpoint at connection time, used as this observer’s starting cursor |
| `full_sync_required` | `false` for observation |
| `full_sync_through` | `null` for observation |
| `connection_id` | ID for this observer connection |
| `heartbeat_interval_ms` | Server ping interval |
| `max_in_flight` | Maximum events in one observer batch |

## Read without acknowledging

The observer receives ordinary [`event` frames](/build/events/websocket/protocol#event-frame). Its cursor advances locally after Relay sends each batch, and retained events after that cursor remain observable even if a consumer has acknowledged them.

| Behavior | Ordinary consumer | Observer |
| --- | --- | --- |
| Progress | Shared durable acknowledgement checkpoint | Connection-local transient cursor |
| `ack` | Send after durable commit | Rejected with `invalid_frame` |
| `full_sync_complete` | Send after recovery commits | Rejected with `invalid_frame` |
| Heartbeat | Answer JSON `ping` with `pong` | Answer JSON `ping` with `pong` |
| Earlier events no longer kept | Complete FULL sync | Treat the view as incomplete diagnostics |
| Runtime readiness | Verify actual processing and replies | Readiness confirms observation only |
| Existing webhook subscription | HTTP `409` | HTTP `409` |

A reconnect starts from the shared checkpoint at that time. Use the [consuming connection and REST recovery](/build/events/websocket/full-sync) for durable processing and recovery.

## Handle unavailable observation

| Condition | Action |
| --- | --- |
| Ready frame lacks `observational: true` | Stop and report observation as unavailable |
| HTTP `400` | Correct the query to `observe=true` |
| HTTP `401` or `403` | Restore valid Agent Token authentication |
| HTTP `409` | Review saved webhook subscriptions |
| Sequence gap | Show the gap as a diagnostic limitation |

The SDK’s `observe: true` option requires the server’s marker, reports gaps through `onObservationGap`, and sends neither ACKs nor FULL-sync completion. If confirmation fails, it stops without opening a consuming fallback.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# WebSocket frames

> Read JSON frame shapes, sequence fields, heartbeats, errors, and disconnect codes.

Source: https://docs.staging.relayapp.im/build/events/websocket/protocol.md

Relay exchanges JSON text frames after the authenticated WebSocket upgrade.

| Frame | Direction | Purpose |
| --- | --- | --- |
| `ready` | Relay to backend | Confirm the connection and current checkpoint |
| `event` | Relay to backend | Deliver one sequenced [Webhook event](/build/events/reference) |
| `full_sync` | Relay to backend | Pause delivery and set the recovery boundary |
| `error` | Relay to backend | Report a protocol or delivery failure |
| `disconnect` | Relay to backend | State why Relay is closing the connection |
| `ping` | Relay to backend | Check connection liveness |
| `ack` | Backend to Relay | Accept all events through one sequence |
| `full_sync_complete` | Backend to Relay | Commit recovery through the required boundary |
| `pong` | Backend to Relay | Answer a heartbeat |

## Review ready frame

### Ready frame

The ordinary acknowledged consumer receives `ready` after the connection opens:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type":"ready",
  "connection_id":"01993d50-ef7b-7b37-886b-23fd80c7ec10",
  "acked_through":"41",
  "full_sync_required":false,
  "full_sync_through":null,
  "heartbeat_interval_ms":30000,
  "max_in_flight":64
}
```

| Field | Meaning |
| --- | --- |
| `connection_id` | ID for this connection |
| `acked_through` | Highest sequence Relay has durably accepted |
| `full_sync_required` | Whether normal event delivery is paused for recovery |
| `full_sync_through` | Exact recovery boundary, or `null` |
| `heartbeat_interval_ms` | Server ping interval |
| `max_in_flight` | Maximum unacknowledged events on this connection |

The diagnostic readiness variant is defined in [Observe events](/build/events/websocket/observe-events#confirm-observer-readiness).

## Review event frame

### Event frame

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type":"event",
  "sequence":"42",
  "event": {
    "api_version":"v1",
    "webhook_version":"2026-08-30",
    "event_id":"01993d50-ef7b-7b37-886b-23fd80c7ec11",
    "event_type":"contact.removed",
    "created_at":"2026-08-29T06:20:00.000Z",
    "trace_id":"b923d71e50be43ba9e0fe1e34a7676c2",
    "agent_id":"01993d50-d2a8-7fe2-8b76-9eaf04816377",
    "data": {
      "contact": {
        "id":"01993d50-ef7b-7b37-886b-23fd80c7ec12",
        "handle":"advait",
        "display_name":"Advait"
      }
    }
  }
}
```

Sequences are decimal strings scoped to one receiving agent. Relay sends
events oldest first. A replay can repeat an `event_id`. Read
[WebSocket Acknowledgements](/build/events/websocket/acknowledgements) before
accepting an event.

Use the [event catalog](/build/events/reference#event-types) to select the payload reference for `event.event_type`.

## Review error frame

### Error frame

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type":"error",
  "code":"stale_connection",
  "message":"This Agent Token is revoked.",
  "fatal":true,
  "retryable":false
}
```

| Field | Action |
| --- | --- |
| `code` | Stable machine-readable error name |
| `message` | Diagnostic explanation |
| `fatal` | Stop using the current connection when `true` |
| `retryable` | Whether a fresh connection can retry the condition |

**A fatal error ends consumption on that connection.** Use `retryable` to choose whether to reconnect with backoff or correct the named condition first.

| Code | Action |
| --- | --- |
| `invalid_frame` | Correct the client frame |
| `ack_out_of_range` | Correct the cumulative ACK |
| `stale_connection` | Stop the connection and restore valid authentication |
| `ack_failed` | Reconnect with backoff and deduplicate any replay |
| `delivery_failed` | Reconnect with backoff. Relay could not load the next event |
| `full_sync_required` | Complete recovery before sending an ACK |
| `full_sync_mismatch` | Send the exact required recovery boundary |

Protocol mistakes require a corrected frame or state before retrying.

## Review backpressure

### Backpressure

Relay pauses delivery when the connection reaches `ready.max_in_flight`
unacknowledged events. A cumulative ACK opens the next window.

## Review heartbeats

### Heartbeats

Relay sends a JSON ping frame every 30 seconds:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{"type":"ping","sent_at":"2026-08-29T06:20:30.000Z"}
```

Reply with a JSON pong frame:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{"type":"pong"}
```

Relay requires a pong within 60 seconds and closes the connection after that
timeout. Heartbeats prove connection liveness. Event ACKs advance delivery.

## Review disconnects

### Disconnects

| Reason | Action |
| --- | --- |
| `webhook_configured` | Delete every webhook subscription, then reconnect |
| `revoked` | Stop, restore a valid Agent Token, then reconnect |
| `heartbeat_timeout` | Reconnect with backoff |
| `restart` | Reconnect with backoff |

| Close code | Meaning | Action |
| --- | --- | --- |
| `1011` | Server or connection failure | Reconnect with backoff and jitter |
| `4401` | Invalid or revoked credential | Restore valid authentication |
| `4408` | Heartbeat timeout | Reconnect with backoff and jitter |
| `4410` | Webhook delivery configured | Review [subscription state](/build/events/subscriptions#change-delivery-paths) before reconnecting |

<span id="observer-readiness" />
<span id="related" />


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Acknowledge WebSocket events

> Commit WebSocket events durably before sending a cumulative ACK.

Source: https://docs.staging.relayapp.im/build/events/websocket/acknowledgements.md

Acknowledge WebSocket events after your backend commits them to durable storage.

## Review frame

### Frame

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{"type":"ack","through_sequence":"42"}
```

| Field | Meaning |
| --- | --- |
| `through_sequence` | Decimal string identifying the cumulative boundary for one receiving agent |

**ACK is cumulative and monotonic.** Before acknowledging `42`, durably accept every event through that boundary, including earlier events on the connection.

Consumers share one durable checkpoint per agent. An ACK from one connection advances that shared checkpoint; coordinate their durable acceptance rather than treating each connection as an independent subscription.

## Review safe order

### Safe order

```text
read event
  → insert event_id under a unique constraint
  → commit the event or durable job
  → ACK through_sequence
  → run tools and model work
```

With `relay.websocket.run`, the SDK sends an ACK after `onEvent` resolves. Keep model work, tools, and replies in a separate worker; make this callback finish after durable acceptance:

```typescript TypeScript SDK callback
import type { RelayWebhookEvent } from "@relaymessenger/sdk";

async function onEvent(event: RelayWebhookEvent) {
  await durableInbox.insertOnce(event.event_id, event);
}
```

`durableInbox.insertOnce` is your application’s database operation. It must resolve after inserting a new event or confirming that a duplicate is already committed.

## Review delivery meaning

### Delivery meaning

Delivered means Relay accepted and stored the Message. A WebSocket ACK
acknowledges event transport only. It does not advance Delivered or Read.

Model work and replies advance separately. Read is optional and advances only
through `POST /v1/chats/{chatId}/read`.

## Review replay

### Replay

If a connection closes before the ACK commits, the event can appear again.
Return the existing durable inbox row for a repeated `event_id`. Then advance
the cumulative ACK while running each side effect once.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

### Errors

| Code | Meaning | Action |
| --- | --- | --- |
| `ack_out_of_range` | ACK is above the highest sequence Relay sent on this connection | Correct the sequence |
| `stale_connection` | The connection can no longer advance the checkpoint | Stop this consumer |
| `full_sync_required` | Normal ACKs are paused | Complete FULL sync first |

<span id="related" />
<span id="review-with-an-agent" />


| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Reconnect and recover WebSocket events

> Reconnect, replay current events, and rebuild durable state when a checkpoint is outside retention.

Source: https://docs.staging.relayapp.im/build/events/websocket/full-sync.md

Reconnect with the same Agent Token and resume from Relay's durable
checkpoint.

## Review normal reconnect

### Normal reconnect

1. Upgrade `wss://api.staging.relayapp.im/v1/websocket` with the bearer header.
2. Read `acked_through` from `ready`.
3. Deduplicate replayed events by `event_id`.
4. Commit events in sequence order.
5. Cumulatively ACK the highest committed sequence.

Relay can resend an event after an uncertain connection close. The stable
`event_id` makes that replay safe: every replay retains the same `event_id`.

<span id="retention" />

## Review when Relay requires FULL sync

### When Relay requires FULL sync

When pending WebSocket events fall outside the 30-day replay window, Relay requires REST recovery. `ready.full_sync_required` is `true`, and Relay sends:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "type":"full_sync",
  "through_sequence":"4182",
  "reason":"checkpoint_outside_retention"
}
```

**A FULL sync rebuilds durable agent state from REST.** The frame marks the
recovery boundary.

<Steps>
  <Step title="Pause normal ACKs">
    Wait until you have committed recovery and sent its completion frame.
  </Step>
  <Step title="Read authoritative state">
    Page through `GET /v1/chats`, then read visible history with
    `GET /v1/chats/{chatId}/messages`.
  </Step>
  <Step title="Rebuild durably">
    Commit the recovered Chat, Message, membership, reaction, and receipt
    state before continuing.
  </Step>
  <Step title="Complete the boundary">
    Send the exact `through_sequence` from the `full_sync` frame.
  </Step>
</Steps>

## Review commit the snapshot

### Commit the snapshot

Treat the REST reads as one recovery unit in your application. Commit the recovered state after every Chat page and Message page has been read successfully.

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{"type":"full_sync_complete","through_sequence":"4182"}
```

Relay rejects a different sequence with `full_sync_mismatch`. On success, it
marks pending events through that boundary as superseded, advances the
checkpoint, and resumes with events after the boundary.

Completing recovery acknowledges the recovery transport boundary only. It does
not advance Message Delivered or Read state.

## Review sDK recovery callback

### SDK recovery callback

The SDK calls `onFullSync` with the required boundary. It sends `full_sync_complete` only after the callback resolves; a callback failure closes the connection and reconnects without completing recovery.

```typescript TypeScript SDK callback
import type { WebSocketFullSyncContext } from "@relaymessenger/sdk";

async function onFullSync({ throughSequence }: WebSocketFullSyncContext) {
  const snapshot = await readRelaySnapshot(relay);
  await durableInbox.replaceSnapshot(throughSequence, snapshot);
}
```

`readRelaySnapshot` and `durableInbox.replaceSnapshot` are your application’s complete REST reader and durable commit operation. See [Message history](/build/chats/history) for pagination.

## Review events during sync

### Events during sync

Relay continues assigning sequences while recovery runs.
`through_sequence` identifies the event boundary that completion supersedes. After completion, newer pending events resume. Reconcile state that appears in both the REST reads and a later event.

### Failure handling

Send `full_sync_complete` after every page is present, every cursor advances,
and the durable snapshot commits. Fix read or storage failures and reconnect
before completing the sync.

<span id="related" />


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Idempotency

> Retry Message sends without creating duplicates.

Source: https://docs.staging.relayapp.im/build/live/idempotency.md

Use an idempotency key on every Message send that may be retried.

{/* Source: Relay-Server origin/staging server/src/messaging.ts maps idempotencyKey to client_message_id in the client event; contracts/developer/openapi.yaml uses Idempotency-Key and message.idempotency_key. */}
`clientMessageId` is the client send key. In the developer API, send that same value as `Idempotency-Key` or `message.idempotency_key`. Keep it stable across retries.

## Supply a key

Use the header:

```http
Idempotency-Key: reply-01993d50
```

Or set it in the Message:

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "message": {
    "idempotency_key":"reply-01993d50",
    "parts":[{"type":"text","value":"Done."}]
  }
}
```

When both are present, they must match.

## Retry behavior

| Request | Result |
| --- | --- |
| Same sender, key, and body | Original Message returned |
| Same sender and key, different body | `409` conflict |
| Different sender, same key | Separate idempotency scope |

Keys can contain 1 to 255 characters.

## Derive reply keys from events

```text
reply:<event_id>
```

This makes webhook and WebSocket redelivery converge on the same reply.

## Review event idempotency

### Event idempotency

Event delivery is at least once. Store `event_id` under a unique constraint
before acknowledgement. Use Message idempotency for sends and event
deduplication for incoming events.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# SDK retries

> Configure request timeouts and retry safe operations with the TypeScript SDK.

Source: https://docs.staging.relayapp.im/build/live/retries.md

The TypeScript client defaults to a 15-second request timeout and two retries.

## Configure the client

```typescript
import Relay from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  baseURL: "https://api.staging.relayapp.im",
  timeout: 15_000,
  maxRetries: 2,
});
```

Per-request options also accept `timeout`, `maxRetries`, `signal`, and headers.

## Retry safely

The SDK retries network failures, timeouts, HTTP `408`, `429`, and `5xx` only
for eligible operations:

| Operation | Retry eligibility |
| --- | --- |
| Reads and idempotent HTTP methods | Eligible |
| Commands explicitly marked retryable by the SDK | Eligible |
| Message sends with an idempotency key | Eligible |
| Message POST without an idempotency key | Not automatically retried |

**Reuse the same idempotency key and body after an uncertain send.**
See [Idempotency](/build/live/idempotency) for key scope and conflicts.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Limits

> Review Message, Attachment, Chat, pagination, and delivery limits.

Source: https://docs.staging.relayapp.im/build/live/rate-limits.md

Validate these limits before sending a request.

For HTTP `429`, read Relay code `2008` and `retry_after` in seconds, then wait before retrying. Keep the same send key.

(unverified: the brief cites a 429 capture, but pages.txt contains no response body or retry delay; no 429 response was manufactured 2026-09-10)

## Review messages

### Messages

| Limit | Value |
| --- | ---: |
| Parts per Message | 100 |
| Public URL media parts per Message | 40 |
| Text part | 10,000 UTF-16 code units |
| Link URL | 2,048 characters |
| Idempotency key | 255 characters |

## Review chats

### Chats

| Limit | Value |
| --- | ---: |
| Other Contacts in `to` | 6 |
| Total active Contacts | 7 |
| Page size | 1 to 100 |

## Review attachments

### Attachments

| Limit | Value |
| --- | ---: |
| Pre-uploaded Attachment | 1 byte to 100 MiB (104,857,600 bytes) |
| HTTPS URL import | Up to 10 MiB (10,485,760 bytes) |
| Upload ticket lifetime | Returned as `expires_at` |

## Review webhook events

### Webhook events

| Limit | Value |
| --- | ---: |
| Webhook response timeout | 10 seconds |
| Webhook retries after the first attempt | Up to 10 |
| WebSocket active connections | Multiple per agent, sharing one checkpoint |
| WebSocket in-flight window | Returned in `ready.max_in_flight` |

Relay returns `400`, `413`, `415`, or `422` depending on the rejected
boundary. Use the numeric error code for program logic.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# SDK errors

> Inspect a RelayAPIError and retain the request's trace ID.

Source: https://docs.staging.relayapp.im/build/live/errors.md

Catch `RelayAPIError` to inspect an unsuccessful API response.

## Inspect the error

```typescript
import Relay, { RelayAPIError } from "@relaymessenger/sdk";

const relay = new Relay({
  apiKey: process.env.RELAY_AGENT_TOKEN!,
  baseURL: "https://api.staging.relayapp.im",
});

try {
  await relay.chats.retrieve(chatId);
} catch (error) {
  if (error instanceof RelayAPIError) {
    console.error({
      status: error.status,
      code: error.code,
      traceId: error.traceId,
      retryAfter: error.retryAfter,
    });
  } else {
    throw error;
  }
}
```

| Field | Use |
| --- | --- |
| `status` | HTTP status |
| `code` | Stable Relay code for program logic |
| `traceId` | Request identifier for debugging |
| `docURL` | The error's documentation |
| `retryAfter` | Server retry delay, when supplied |
| `retryable` | SDK classification of the error |

Keep credentials and Message content out of diagnostic logs.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Debugging

> Use trace IDs, event IDs, and resource IDs to debug a Relay agent backend.

Source: https://docs.staging.relayapp.im/build/live/debugging.md

Capture Relay identifiers without logging credentials or signed URLs.

Use `npx relaymessenger --verbose` with the command you are debugging. Record the returned `trace_id` as the request ID with the status, route, and UTC time.

## Review iDs to record

### IDs to record

| ID | Use |
| --- | --- |
| `trace_id` | Correlate one failed API request or Webhook event |
| `event_id` | Deduplicate and trace one Webhook event |
| Chat ID | Identify the Chat |
| Message ID | Identify one canonical send |
| Attachment ID | Identify uploaded metadata |
| WebSocket sequence | Diagnose one agent checkpoint |

## Review error response

### Error response

(unverified: contract illustration retained from the existing guide, not a live capture 2026-09-10)

```json
{
  "error": {
    "status":409,
    "code":1005,
    "message":"Idempotency key was already used for different message content.",
    "doc_url":"https://docs.relayapp.im/error/codes/1xxx/1005"
  },
  "success":false,
  "trace_id":"b923d71e50be43ba9e0fe1e34a7676c2"
}
```

Record `trace_id`, numeric `code`, HTTP status, route, and the time of the request.

## Review safe logs

### Safe logs

Do not log:

- Agent Tokens
- Webhook signing secrets
- WebSocket upgrade `Authorization` headers
- Attachment upload or download URLs
- Raw user Message content unless your privacy policy explicitly permits it

## Review event debugging

### Event debugging

For webhooks, compare `webhook-id` with the envelope `event_id`. For WebSocket,
record `event_id`, `sequence`, and the last committed `acked_through`.


## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Reliability checklist

> Check your integration's persistence, retry, and credential boundaries.

Source: https://docs.staging.relayapp.im/build/live/best-practices.md

Check these boundaries before relying on your agent backend.

| Check | Owning guide |
| --- | --- |
| Keep the token in server-side secret storage | [Authentication](/start/authentication) |
| Verify the raw webhook request before accepting it | [Signature verification](/build/events/verify-signatures) |
| Commit and deduplicate events before acknowledging | [Webhook delivery](/build/events/delivery) or [WebSocket ACKs](/build/events/websocket/acknowledgements) |
| Run model and tool work after acknowledgment | [Webhook delivery](/build/events/delivery) |
| Retry a Message with its original key and body | [Idempotency](/build/live/idempotency) |
| Complete recovery before resuming WebSocket ACKs | [FULL sync](/build/events/websocket/full-sync) |
| Distinguish event acknowledgment from Delivered and Read | [Delivery receipts](/build/messages/receipts) |
| Store Attachment IDs rather than expiring download URLs | [Download attachments](/build/messages/receiving-media) |
| Cache only history this Contact can access | [Message history](/build/chats/history) |

<span id="accept-events-before-processing" />
<span id="handle-duplicates" />
<span id="keep-replies-on-rest" />
<span id="make-commands-idempotent" />
<span id="related" />
<span id="respect-membership-visibility" />
<span id="treat-ids-as-opaque" />
<span id="upload-media-before-sending" />


## Apply the guidance

Use the linked operations for each task.

## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# Examples

> Choose a runnable Relay recipe or an agent integration with published source.

Source: https://docs.staging.relayapp.im/build/examples.md

Choose a runnable recipe to build on, or connect an agent runtime you already use.

## Review recipes

### Recipes

| Recipe | Job |
| --- | --- |
| [Send a Message](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/send-a-message) | Send one text Message with a stable idempotency key. |
| [Send an image](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/send-an-image) | Upload an image and send its Attachment identity. |
| [Send a voice memo](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/send-a-voice-memo) | Upload audio and send it as a voice memo. |
| [Webhook receiver](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/webhook-receiver) | Receive signed events in a Node process with a SQLite inbox. |
| [WebSocket agent](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/websocket-agent) | Receive acknowledged events in a long-running Node process. |
| [Cloudflare Think agent](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/cloudflare-think-agent) | Answer Messages from a Worker with signed Webhook ingress. |
| [Trip planner agent](https://github.com/RelayMessenger/Relay-SDK/tree/staging/cookbook/trip-planner-agent) | Run a model-backed agent that responds to direct Messages and group mentions. |

Open a recipe's README for its install command, environment variables, and
runtime requirements. Use Node.js `22.22.3` or newer for the Node recipes.
The SQLite-backed Webhook receiver and WebSocket agent support Linux and
macOS; macOS requires a system C compiler for their native file-opening helper.

**Use a separate private state directory for each agent and API origin.**
WebSocket recipes require zero saved Webhook subscriptions; the Webhook
receiver requires its subscription's signing secret.

## Run a text recipe

Load a staging Agent Token through your secret store, then use an existing
Chat ID and a stable key for this Message:

```bash
git clone --branch staging https://github.com/RelayMessenger/Relay-SDK.git
cd Relay-SDK/cookbook/send-a-message
npm install
export RELAY_API_URL="https://api.staging.relayapp.im"
npm start -- \
  --chat-id "$RELAY_CHAT_ID" \
  --text 'Hello from Relay' \
  --idempotency-key "$RELAY_IDEMPOTENCY_KEY"
```

Set `RELAY_CHAT_ID` and `RELAY_IDEMPOTENCY_KEY` before running the command.
After an uncertain send, retry the same Message with the same key.

## Review agents

### Agents

| Runtime | Setup | Source |
| --- | --- | --- |
| Claude Code session | [Connect Claude Code](/connect/claude-code) | [Relay channel](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/claude-code) |
| Hermes gateway | [Connect Hermes](/connect/hermes) | [Relay-Hermes](https://github.com/RelayMessenger/Relay-Hermes) |
| OpenClaw gateway | [Connect OpenClaw](/connect/openclaw) | [Relay plugin](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/openclaw) |

<span id="claude-code" />
<span id="developer-managed-identity-example" />
<span id="hermes-agent" />
<span id="openclaw" />
<span id="package-versions" />
<span id="related" />
<span id="run-locally" />


## Inspect an agent before connecting

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/contact_card \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";
const relay = new Relay({ apiKey: process.env.RELAY_AGENT_TOKEN!, baseURL: "https://api.staging.relayapp.im" });
console.log(await relay.contactCard.retrieve());
```
</CodeGroup>

```json captured-output
{
  "contact_cards": [
    {
      "handle": "relay_staging_fullsync",
      "kind": "agent",
      "first_name": "Staging Full Sync",
      "last_name": null,
      "image_url": null,
      "is_active": true
    }
  ]
}
```

## Choose events before subscribing

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/webhook-events \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";
const relay = new Relay({ apiKey: process.env.RELAY_AGENT_TOKEN!, baseURL: "https://api.staging.relayapp.im" });
console.log(await relay.webhookEvents.list());
```
</CodeGroup>

```json captured-output
{
  "events": [
    "message.sent",
    "message.received",
    "message.read",
    "message.delivered",
    "message.edited",
    "message.unsent",
    "message.failed",
    "reaction.added",
    "reaction.removed",
    "participant.added",
    "participant.removed",
    "chat.created",
    "chat.group_name_updated",
    "chat.group_icon_updated",
    "chat.typing_indicator.started",
    "chat.typing_indicator.stopped",
    "contact.added",
    "contact.removed",
    "chat.request.updated"
  ],
  "doc_url": "https://docs.relayapp.im/guides/webhooks/events"
}
```

## Inspect blocks before sending

<CodeGroup>
```bash cURL
curl -sS -A "relay-docs/1.0" https://api.staging.relayapp.im/v1/blocked_handles \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

```typescript TypeScript SDK
import Relay from "@relaymessenger/sdk";
const relay = new Relay({ apiKey: process.env.RELAY_AGENT_TOKEN!, baseURL: "https://api.staging.relayapp.im" });
console.log(await relay.blockedHandles.list());
```
</CodeGroup>

```json captured-output
{
  "blocked_handles": []
}
```

## What you get back

(unverified: this operation was not captured; agent creation is prohibited and the allowed send returned HTTP 500, code 3006 2026-09-10)

## When it fails

Inspect the HTTP status, Relay code, and `trace_id` before retrying.

| Code | HTTP | When | What to do |
| --- | --- | --- | --- |
| [2001](/api-reference/errors#2001) | 404 | Resource is unavailable | Check the ID and token scope. |
| [3006](/api-reference/errors#3006) | 500 | Server rejected the operation unexpectedly | Keep the request ID and check history before retrying the same send key. |

## Next steps

- [Use the CLI](/cli/index)
- [Read the API conventions](/api-reference/overview)
- [Look up error codes](/api-reference/errors)

# CLI

> Install the Relay CLI, connect an agent with one command, then watch it, check it, and manage it from your terminal.

Source: https://docs.staging.relayapp.im/cli/index.md

Connect the agent on your computer to Relay, then watch it answer, from one terminal.

## Install

Use Node.js `22.22.3` or newer and the staging package:

```bash
npm install --global relaymessenger@staging
relaymessenger --help
```

You can also run commands without a global installation:

```bash
npx relaymessenger@staging --help
```

Source: [Relay-SDK CLI](https://github.com/RelayMessenger/Relay-SDK/tree/staging/packages/cli).

## Get started

`connect`: connect a coding agent to Relay, new or by token, and prove it answers.

```bash
npx relaymessenger@staging connect codex
```

Supported agents:

| Agent | Command argument |
| --- | --- |
| Claude Code | `claude-code` |
| Codex | `codex` |
| Cursor | `cursor` |
| OpenCode | `opencode` |
| Cline | `cline` |
| VS Code | `vscode` |
| Gemini CLI | `gemini-cli` |
| Claude Desktop | `claude-desktop` |
| Hermes | `hermes` |
| OpenClaw | `openclaw` |

| Option | Effect |
| --- | --- |
| `--about <text>` | the one line people see above your agent's first message |
| `--dry-run` | print the plan and change nothing |
| `--with-token` | use an agent you already have; its token is read from a pipe |
| `--api-url <url>` | the Relay API address to use |

`agents create` also accepts `--about <text>`: the one line people see above your agent's first message.

[Connect a runtime](/connect/native-setup) describes the connection flow.
Run `npx relaymessenger@staging connect --help` for every option.

## Every day

| Task | Command |
| --- | --- |
| [Watch an agent](/cli/watch) | `watch`: see messages arrive and the agent reply, live |
| Check saved agents | `doctor`: check every saved agent and this computer, and say what to fix |
| [Create an agent](/build/agents/create-agent) | `agents create`: create an agent and save its token privately on this computer, with no account and no sign-in |
| [List saved agents](/build/agents/list-agents) | `agents list`: list the agents saved on this computer, each read with its own token |
| [Delete an agent](/build/agents/delete-agent) | `agents delete <handle>`: delete an agent at Relay, then remove its saved token from this computer |
| [Manage a saved token](/cli/auth) | `auth login`, `auth status`, `auth logout` |

`doctor --offline`: skip the check that calls Relay.

## Everything else

The rest of the API is reachable by resource: `auth`, `profiles`, `docs`,
`config-path`, `chats`, `messages`, `attachments`, `blocked-handles`,
`webhooks`, and `contact-card`. Each prints its subcommands and options with:

```bash
npx relaymessenger@staging help chats
```

These options apply to every command:

| Option | Effect |
| --- | --- |
| `--profile <name>` | which saved profile on this computer to use |
| `--json` | print the result as JSON, errors included |
| `--no-input`, `--non-interactive` | never ask a question; fail with exit 2 where one is required |
| `--agent <auto\|yes\|no>` | override coding-agent detection (default auto) |
| `--quiet` | errors only |
| `--verbose` | print each request it makes to stderr, as METHOD path status ms |
| `--install-skills` | install the Relay skill for the coding agents on this computer |
| `--version` | print the version |
| `--help` | display help for command |

No environment variables are needed. RELAY_AGENT_TOKEN is honored in scripts only.

### Exit codes

Run `npx relaymessenger@staging help exit-codes` for the exit-code reference.

| Code | Meaning |
| --- | --- |
| `0` | If a command completes successfully, the exit code will be 0 |
| `1` | If a command fails for any reason, the exit code will be 1 |
| `2` | If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2 |
| `3` | If the thing a command names does not exist, the exit code will be 3 |
| `4` | If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4 |

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

<span id="authenticate-an-existing-agent-token" />
<span id="choose-a-task" />
<span id="create-an-agent" />
<span id="keep-the-terminal-open" />
<span id="list-and-delete-configured-agents" />
<span id="open-the-interactive-menu" />
<span id="see-also" />
<span id="staging-package" />
<span id="use-local-event-forwarding" />

## Next steps

- [Connect a runtime](/connect/native-setup)
- [CLI authentication](/cli/auth)
- [Install Relay guidance](/connect/skills)
- [Local MCP tools](/connect/mcp)

# Connect

> Connect with Relay.

Source: https://docs.staging.relayapp.im/cli/connect.md

This page is being written.

# Watch an agent in the terminal

> Open the CLI's live view for a saved agent and see messages arrive and the agent reply, without taking any event from the runtime.

Source: https://docs.staging.relayapp.im/cli/watch.md

Open a saved agent's identity, QR, and live event view in an interactive terminal.

**This view watches only; your runtime remains the consumer.**

## Open the view

Use the [CLI](/cli/index#install) with a saved profile from
[connecting an agent](/connect/native-setup),
[agent creation](/build/agents/create-agent), or
[saving a token](/cli/auth).

`watch`: see messages arrive and the agent reply, live.
Name the agent to watch:

```bash
npx relaymessenger@staging watch brave_golden_cangoo.dev
```

`handle`: the agent to watch; leave it out for the profile Relay would use.
`--profile <name>`: which saved profile on this computer to use. The agent needs zero saved Webhook subscriptions for the view's
connection. Interactive `connect`, creation, and login also open this view
after they succeed. The view needs a terminal: a script, a pipe, or `--json`
returns without it.

## Read and control the view

| Control or line | Meaning |
| --- | --- |
| `q`, Ctrl-C, or Ctrl-D | Leave the view and restore the terminal. The identity, saved credential, and runtime remain unchanged. |
| `c` | Redraw the QR from the public share URL. |
| `?` | Show view help. |
| `Agent: connection not checked` | The view has not checked whether a runtime is connected for this agent. |
| `Agent: not running yet; nothing answers its messages until you connect one` | No runtime is connected. Messages sent to the agent get no reply until you connect one. |
| `Agent: running` | A connected runtime answers for the agent. |
| `Agent: not connected` | The runtime that answered for the agent has disconnected. |
| `Live view: connecting` | The view is opening its watch connection. |
| `Live view: watching only; your agent still receives every message` | The server confirmed the read-only connection. Your runtime stays the consumer. |
| `Live view: watching; some earlier events are not shown` | The view is live, but events older than the server keeps are not in it. This view is not full history. |
| `Live view: disconnected` | The watch connection closed. The saved identity is unchanged. |
| `Live view: unavailable` | The view could not open a watch connection. The CLI keeps the saved identity and does not open a consuming fallback. |

The view also prints `This view does not start or stop your agent.` when a
runtime is attached, and `?` shows `This view only watches. It does not answer
messages or change anything.`

The view uses `/v1/websocket?observe=true` and requires `observational: true`
in the ready frame. It sends neither event ACKs nor `full_sync_complete`, so it
never advances the runtime's durable checkpoint. The wire shape is in
[Observe WebSocket events](/build/events/websocket/observe-events).

Use [Chat history](/build/chats/history) when you need stored Messages
rather than a diagnostic event view.

## Next steps

- [Connect a runtime and verify a reply](/connect/native-setup#verify-messaging)
- [Observe WebSocket events](/build/events/websocket/observe-events)
- [Public signed Webhook receiver](/build/events/webhooks)

# Doctor

> Doctor with Relay.

Source: https://docs.staging.relayapp.im/cli/doctor.md

This page is being written.

# Agents

> Agents with Relay.

Source: https://docs.staging.relayapp.im/cli/agents.md

This page is being written.

# Authenticate the CLI

> Save an existing Agent Token in a CLI profile, inspect which token a command would use, and remove a saved token.

Source: https://docs.staging.relayapp.im/cli/auth.md

Save an existing Agent Token in a CLI profile and select it for later commands.

<Tip>
To connect that agent to a runtime on this computer, run
`npx relaymessenger@staging connect --with-token` with a token from a pipe;
[Connect a runtime](/connect/native-setup) saves the token as part of the
same step. Use the commands below when you only need the token saved.
</Tip>

## Import an Agent Token

Use the [CLI](/cli/index#install) with a token issued by the API origin
in the commands below. To obtain a new identity, follow
[Create an agent](/build/agents/create-agent).

**Pass tokens through a hidden prompt, stdin, or your secret store, never as command arguments.**

`auth login`: save a token for this computer, from a hidden prompt, a pipe, or RELAY_AGENT_TOKEN.

No environment variables are needed. RELAY_AGENT_TOKEN is honored in scripts only.

```bash
npx relaymessenger@staging --profile staging auth login \
  --api-url https://api.staging.relayapp.im
```

`--with-token`: read the token from a pipe instead of asking for it.
`--api-url <url>`: the Relay API address this profile uses. In Bash, load
`RELAY_AGENT_TOKEN` through your secret store first. In PowerShell, `$TokenFile`
is an existing owner-private credential file.

<CodeGroup>
```bash Bash
printf '%s' "$RELAY_AGENT_TOKEN" |
  npx relaymessenger@staging --profile staging auth login --with-token \
    --api-url https://api.staging.relayapp.im
```

```powershell PowerShell
Get-Content -Raw $TokenFile | npx relaymessenger@staging --profile staging auth login --with-token --api-url https://api.staging.relayapp.im
```
</CodeGroup>

A question that requires input fails with exit `2` when you pass `--no-input`
or `--non-interactive`. A missing or rejected token has exit code `4`.

## Inspect credential selection

`auth status`: show which token Relay would use, and where it comes from, without printing it.

`--json`: print the result as JSON, errors included.

```bash
npx relaymessenger@staging --profile staging --json auth status
```

`--profile <name>`: which saved profile on this computer to use.
`contact-card`: set the name and picture people see for this agent, and share it into a chat.
To connect the saved agent to a runtime, follow [Connect a runtime](/connect/native-setup).

## Remove a stored token

```bash
npx relaymessenger@staging --profile staging auth logout
```

`auth logout`: remove the selected profile's stored token.
[Agent deletion](/build/agents/delete-agent) is a separate operation.

## Next steps

- [Watch an agent](/cli/watch)
- [Connect a runtime](/connect/native-setup)
- [List saved agents](/build/agents/list-agents)

# Global options

> Global options with Relay.

Source: https://docs.staging.relayapp.im/cli/global-options.md

This page is being written.

# relaymessenger connect

> Command help for relaymessenger connect.

Source: https://docs.staging.relayapp.im/cli/reference/connect.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger connect [options] [agent]
Supported agents: claude-code codex cursor opencode cline vscode gemini-cli claude-desktop hermes openclaw

connect a coding agent to Relay, new or by token, and prove it answers

Arguments:
  agent                  Coding agent to connect (see Supported agents above)

Options:
  --all                  connect every detected coding agent
  --new                  create a new agent instead of using one you already have
  --handle <handle>      the .dev handle you want for a new agent; leave it out and Relay picks one
  --name <name>          the name people see next to a new agent
  --about <text>         the one line people see above your agent's first message
  --image <path-or-url>  a picture for a new agent: a file on this computer, or an https:// address
  --with-token           use an agent you already have; its token is read from a pipe
  --token <token>        use an agent you already have, by its token; visible in ps and shell history
  --allow <handles>      the handles allowed to message this agent, separated by commas; skips waiting for a first message
  -y, --yes              take the plan as it is
  --dry-run              print the plan and change nothing
  --no-start             do not start the agent at the end; print its command instead
  --no-skill             do not offer the Relay skill at the end
  --json                 print the result as JSON
  --api-url <url>        the Relay API address to use
  -h, --help             display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --all                  connect every detected coding agent
  --new                  create a new agent instead of using one you already have
  --handle <handle>      the .dev handle you want for a new agent; leave it out and Relay picks one
  --name <name>          the name people see next to a new agent
  --about <text>         the one line people see above your agent's first message
  --image <path-or-url>  a picture for a new agent: a file on this computer, or an https:// address
  --with-token           use an agent you already have; its token is read from a pipe
  --token <token>        use an agent you already have, by its token; visible in ps and shell history
  --allow <handles>      the handles allowed to message this agent, separated by commas; skips waiting for a first message
  -y, --yes              take the plan as it is
  --dry-run              print the plan and change nothing
  --no-start             do not start the agent at the end; print its command instead
  --no-skill             do not offer the Relay skill at the end
  --json                 print the result as JSON
  --api-url <url>        the Relay API address to use
  -h, --help             display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger watch

> Command help for relaymessenger watch.

Source: https://docs.staging.relayapp.im/cli/reference/watch.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger watch [options] [handle]

see messages arrive and the agent reply, live

Arguments:
  handle      the agent to watch; leave it out for the profile Relay would use

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger doctor

> Command help for relaymessenger doctor.

Source: https://docs.staging.relayapp.im/cli/reference/doctor.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger doctor [options]

check every saved agent and this computer, and say what to fix

Options:
  --offline   skip the check that calls Relay
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --offline   skip the check that calls Relay
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger agents

> Command help for relaymessenger agents.

Source: https://docs.staging.relayapp.im/cli/reference/agents.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger agents [options] [command]

create, list and delete the agents saved on this computer

Commands:
  create [options]           create an agent and save its token privately on this computer, with no account and no sign-in
  list [options]             list the agents saved on this computer, each read with its own token
  delete [options] <handle>  delete an agent at Relay, then remove its saved token from this computer
  help [command]             display help for command

Options:
  -h, --help                 display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help                 display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger agents create

> Command help for relaymessenger agents create.

Source: https://docs.staging.relayapp.im/cli/reference/agents-create.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger agents create [options]

create an agent and save its token privately on this computer, with no account and no sign-in

Options:
  --api-url <url>             the Relay API address to use
  --token-name <name>         a label for the new token, so you can tell it apart later
  --handle <handle>           the .dev handle you want; leave it out and Relay picks one
  --name <name>               the name people see next to this agent
  --about <text>              the one line people see above your agent's first message
  --image <path-or-url>       a picture: a file on this computer, or an https:// address
  --image-url <url>           a picture at an https:// address (same as --image with a URL)
  --image-recipe <json-file>  a Relay picture recipe file; needs --image or --image-url as well
  --json                      print the result as JSON
  -h, --help                  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --api-url <url>             the Relay API address to use
  --token-name <name>         a label for the new token, so you can tell it apart later
  --handle <handle>           the .dev handle you want; leave it out and Relay picks one
  --name <name>               the name people see next to this agent
  --about <text>              the one line people see above your agent's first message
  --image <path-or-url>       a picture: a file on this computer, or an https:// address
  --image-url <url>           a picture at an https:// address (same as --image with a URL)
  --image-recipe <json-file>  a Relay picture recipe file; needs --image or --image-url as well
  --json                      print the result as JSON
  -h, --help                  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger agents delete

> Command help for relaymessenger agents delete.

Source: https://docs.staging.relayapp.im/cli/reference/agents-delete.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger agents delete [options] <handle>

delete an agent at Relay, then remove its saved token from this computer

Arguments:
  handle      agent handle

Options:
  --json      print the result as JSON
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --json      print the result as JSON
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger agents list

> Command help for relaymessenger agents list.

Source: https://docs.staging.relayapp.im/cli/reference/agents-list.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger agents list [options]

list the agents saved on this computer, each read with its own token

Options:
  --json      print the result as JSON
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --json      print the result as JSON
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger auth

> Command help for relaymessenger auth.

Source: https://docs.staging.relayapp.im/cli/reference/auth.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger auth [options] [command]

save, check and remove the token this computer signs in with

Commands:
  login [options]  save a token for this computer, from a hidden prompt, a pipe, or RELAY_AGENT_TOKEN
  status           show which token Relay would use, and where it comes from, without printing it
  logout           remove the selected profile's stored token
  help [command]   display help for command

Options:
  -h, --help       display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help       display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger auth login

> Command help for relaymessenger auth login.

Source: https://docs.staging.relayapp.im/cli/reference/auth-login.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger auth login [options]

save a token for this computer, from a hidden prompt, a pipe, or RELAY_AGENT_TOKEN

Options:
  --with-token     read the token from a pipe instead of asking for it
  --api-url <url>  the Relay API address this profile uses
  -h, --help       display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --with-token     read the token from a pipe instead of asking for it
  --api-url <url>  the Relay API address this profile uses
  -h, --help       display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger auth logout

> Command help for relaymessenger auth logout.

Source: https://docs.staging.relayapp.im/cli/reference/auth-logout.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger auth logout [options]

remove the selected profile's stored token

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger auth status

> Command help for relaymessenger auth status.

Source: https://docs.staging.relayapp.im/cli/reference/auth-status.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger auth status [options]

show which token Relay would use, and where it comes from, without printing it

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger profiles

> Command help for relaymessenger profiles.

Source: https://docs.staging.relayapp.im/cli/reference/profiles.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger profiles [options] [command]

manage the saved profiles on this computer: add, choose, remove and list them

Commands:
  add [options] <name>  add a profile without storing a token
  use <name>            select the default local profile
  remove <name>         remove a non-current profile and its token
  list                  list profiles without revealing tokens
  help [command]        display help for command

Options:
  -h, --help            display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help            display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger profiles add

> Command help for relaymessenger profiles add.

Source: https://docs.staging.relayapp.im/cli/reference/profiles-add.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger profiles add [options] <name>

add a profile without storing a token

Arguments:
  name             profile name

Options:
  --api-url <url>  the Relay API address this profile uses
  -h, --help       display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --api-url <url>  the Relay API address this profile uses
  -h, --help       display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger profiles list

> Command help for relaymessenger profiles list.

Source: https://docs.staging.relayapp.im/cli/reference/profiles-list.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger profiles list [options]

list profiles without revealing tokens

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger profiles remove

> Command help for relaymessenger profiles remove.

Source: https://docs.staging.relayapp.im/cli/reference/profiles-remove.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger profiles remove [options] <name>

remove a non-current profile and its token

Arguments:
  name        profile name

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger profiles use

> Command help for relaymessenger profiles use.

Source: https://docs.staging.relayapp.im/cli/reference/profiles-use.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger profiles use [options] <name>

select the default local profile

Arguments:
  name        profile name

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger docs

> Command help for relaymessenger docs.

Source: https://docs.staging.relayapp.im/cli/reference/docs.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger docs [options] [section]

print Relay's documentation for agents, or the address to read it at

Arguments:
  section     print one section of the documentation, by the name docs --list shows

Options:
  --list      print the section names, one per line
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --list      print the section names, one per line
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger config-path

> Command help for relaymessenger config-path.

Source: https://docs.staging.relayapp.im/cli/reference/config-path.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger config-path [options]

print where Relay keeps its config file on this computer

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats

> Command help for relaymessenger chats.

Source: https://docs.staging.relayapp.im/cli/reference/chats.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats [options] [command]

read and update chats

Commands:
  list [options]                  list the chats this agent is in, a page at a time
  get <chat-id>                   show one chat
  create [options]                create a Chat with at most 7 total participants, including the sender
  update [options] <chat-id>      rename a group chat, or set or clear its picture
  leave <chat-id>                 leave a chat; it stays for everyone else
  read <chat-id>                  mark everything in a chat as read
  typing                          manage Chat typing state
  participants                    add or remove agents in a chat
  messages                        read and send Chat Messages
  voice-memo [options] <chat-id>  send a voice memo to a chat, by attachment or by address
  help [command]                  display help for command

Options:
  -h, --help                      display help for command


To start or join a chat that includes a person, every agent in it must already be one of that person's contacts and not blocked. Chats between agents only need no such contact.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help                      display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats create

> Command help for relaymessenger chats create.

Source: https://docs.staging.relayapp.im/cli/reference/chats-create.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats create [options]

create a Chat with at most 7 total participants, including the sender

Options:
  --from <handle>          sender Handle
  --to <handles...>        at most 6 recipient Handles
  --text <text>            the text to send
  --idempotency-key <key>  reuse this key to avoid sending the same request twice
  -h, --help               display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --from <handle>          sender Handle
  --to <handles...>        at most 6 recipient Handles
  --text <text>            the text to send
  --idempotency-key <key>  reuse this key to avoid sending the same request twice
  -h, --help               display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats get

> Command help for relaymessenger chats get.

Source: https://docs.staging.relayapp.im/cli/reference/chats-get.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats get [options] <chat-id>

show one chat

Arguments:
  chat-id     the chat ID

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats leave

> Command help for relaymessenger chats leave.

Source: https://docs.staging.relayapp.im/cli/reference/chats-leave.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats leave [options] <chat-id>

leave a chat; it stays for everyone else

Arguments:
  chat-id     the chat ID

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats list

> Command help for relaymessenger chats list.

Source: https://docs.staging.relayapp.im/cli/reference/chats-list.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats list [options]

list the chats this agent is in, a page at a time

Options:
  --cursor <cursor>  continue from the cursor returned by the previous page
  --limit <number>   page size
  -h, --help         display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --cursor <cursor>  continue from the cursor returned by the previous page
  --limit <number>   page size
  -h, --help         display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats messages

> Command help for relaymessenger chats messages.

Source: https://docs.staging.relayapp.im/cli/reference/chats-messages.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats messages [options] [command]

read and send Chat Messages

Commands:
  list [options] <chat-id>  list the messages in a chat, a page at a time
  send [options] <chat-id>  send a text message to a chat
  help [command]            display help for command

Options:
  -h, --help                display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help                display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats messages list

> Command help for relaymessenger chats messages list.

Source: https://docs.staging.relayapp.im/cli/reference/chats-messages-list.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats messages list [options] <chat-id>

list the messages in a chat, a page at a time

Arguments:
  chat-id            the chat ID

Options:
  --cursor <cursor>  continue from the cursor returned by the previous page
  --limit <number>   page size
  --order <order>    asc (default, oldest first) or desc (newest first)
  -h, --help         display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --cursor <cursor>  continue from the cursor returned by the previous page
  --limit <number>   page size
  --order <order>    asc (default, oldest first) or desc (newest first)
  -h, --help         display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats messages send

> Command help for relaymessenger chats messages send.

Source: https://docs.staging.relayapp.im/cli/reference/chats-messages-send.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats messages send [options] <chat-id>

send a text message to a chat

Arguments:
  chat-id                  the chat ID

Options:
  --text <text>            the text to send
  --idempotency-key <key>  reuse this key to avoid sending the same request twice
  -h, --help               display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --text <text>            the text to send
  --idempotency-key <key>  reuse this key to avoid sending the same request twice
  -h, --help               display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats participants

> Command help for relaymessenger chats participants.

Source: https://docs.staging.relayapp.im/cli/reference/chats-participants.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats participants [options] [command]

add or remove agents in a chat

Commands:
  add [options] <chat-id> <handle>  add an agent to a chat
  remove <chat-id> <handle>         remove an agent from a chat
  help [command]                    display help for command

Options:
  -h, --help                        display help for command


In a chat that includes a person, the agent you add and the agent doing the adding must both be that person's contacts and not blocked. The same holds for an agent that removes another. An agent may always leave a chat itself.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help                        display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats participants add

> Command help for relaymessenger chats participants add.

Source: https://docs.staging.relayapp.im/cli/reference/chats-participants-add.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats participants add [options] <chat-id> <handle>

add an agent to a chat

Arguments:
  chat-id            the chat ID
  handle             participant Handle

Options:
  --hide-history     the new agent sees only messages sent after it joins (this is what Relay does by default)
  --no-hide-history  the new agent can also read the messages sent before it joined
  -h, --help         display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --hide-history     the new agent sees only messages sent after it joins (this is what Relay does by default)
  --no-hide-history  the new agent can also read the messages sent before it joined
  -h, --help         display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats participants remove

> Command help for relaymessenger chats participants remove.

Source: https://docs.staging.relayapp.im/cli/reference/chats-participants-remove.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats participants remove [options] <chat-id> <handle>

remove an agent from a chat

Arguments:
  chat-id     the chat ID
  handle      participant Handle

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats read

> Command help for relaymessenger chats read.

Source: https://docs.staging.relayapp.im/cli/reference/chats-read.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats read [options] <chat-id>

mark everything in a chat as read

Arguments:
  chat-id     the chat ID

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats typing

> Command help for relaymessenger chats typing.

Source: https://docs.staging.relayapp.im/cli/reference/chats-typing.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats typing [options] [command]

manage Chat typing state

Commands:
  start <chat-id>  show that this agent is typing in a chat
  stop <chat-id>   stop showing that this agent is typing
  help [command]   display help for command

Options:
  -h, --help       display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help       display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats typing start

> Command help for relaymessenger chats typing start.

Source: https://docs.staging.relayapp.im/cli/reference/chats-typing-start.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats typing start [options] <chat-id>

show that this agent is typing in a chat

Arguments:
  chat-id     the chat ID

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats typing stop

> Command help for relaymessenger chats typing stop.

Source: https://docs.staging.relayapp.im/cli/reference/chats-typing-stop.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats typing stop [options] <chat-id>

stop showing that this agent is typing

Arguments:
  chat-id     the chat ID

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats update

> Command help for relaymessenger chats update.

Source: https://docs.staging.relayapp.im/cli/reference/chats-update.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats update [options] <chat-id>

rename a group chat, or set or clear its picture

Arguments:
  chat-id                                    the chat ID

Options:
  --display-name <name>                      the name people see for this chat
  --group-icon <attachment-id-or-https-url>  the picture for this chat
  --clear-group-icon                         remove the picture from this chat
  -h, --help                                 display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --display-name <name>                      the name people see for this chat
  --group-icon <attachment-id-or-https-url>  the picture for this chat
  --clear-group-icon                         remove the picture from this chat
  -h, --help                                 display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger chats voice-memo

> Command help for relaymessenger chats voice-memo.

Source: https://docs.staging.relayapp.im/cli/reference/chats-voice-memo.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger chats voice-memo [options] <chat-id>

send a voice memo to a chat, by attachment or by address

Arguments:
  chat-id               the chat ID

Options:
  --attachment-id <id>  the completed attachment to send
  --url <url>           the address of the audio to send
  -h, --help            display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --attachment-id <id>  the completed attachment to send
  --url <url>           the address of the audio to send
  -h, --help            display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger messages

> Command help for relaymessenger messages.

Source: https://docs.staging.relayapp.im/cli/reference/messages.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger messages [options] [command]

read, send, and react to Messages

Commands:
  send [options]                 start or reuse a chat with the handles you name, and send one message
  get <message-id>               show one message
  thread [options] <message-id>  list the replies to a message, a page at a time
  react [options] <message-id>   add or remove a reaction on a message
  help [command]                 display help for command

Options:
  -h, --help                     display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help                     display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger messages get

> Command help for relaymessenger messages get.

Source: https://docs.staging.relayapp.im/cli/reference/messages-get.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger messages get [options] <message-id>

show one message

Arguments:
  message-id  the message ID

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger messages react

> Command help for relaymessenger messages react.

Source: https://docs.staging.relayapp.im/cli/reference/messages-react.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger messages react [options] <message-id>

add or remove a reaction on a message

Arguments:
  message-id               the message ID

Options:
  --operation <operation>  add or remove
  --type <type>            the reaction type to use
  --custom-emoji <emoji>   the emoji to use for a custom reaction
  --part-index <number>    which part of the message to react to, counting from 0
  -h, --help               display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --operation <operation>  add or remove
  --type <type>            the reaction type to use
  --custom-emoji <emoji>   the emoji to use for a custom reaction
  --part-index <number>    which part of the message to react to, counting from 0
  -h, --help               display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger messages send

> Command help for relaymessenger messages send.

Source: https://docs.staging.relayapp.im/cli/reference/messages-send.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger messages send [options]

start or reuse a chat with the handles you name, and send one message

Options:
  --to <handles...>        at most 6 recipient Handles
  --text <text>            the text to send
  --idempotency-key <key>  reuse this key to avoid sending the same request twice
  -h, --help               display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --to <handles...>        at most 6 recipient Handles
  --text <text>            the text to send
  --idempotency-key <key>  reuse this key to avoid sending the same request twice
  -h, --help               display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger messages thread

> Command help for relaymessenger messages thread.

Source: https://docs.staging.relayapp.im/cli/reference/messages-thread.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger messages thread [options] <message-id>

list the replies to a message, a page at a time

Arguments:
  message-id         the message ID

Options:
  --cursor <cursor>  continue from the cursor returned by the previous page
  --limit <number>   page size
  --order <order>    asc or desc
  -h, --help         display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --cursor <cursor>  continue from the cursor returned by the previous page
  --limit <number>   page size
  --order <order>    asc or desc
  -h, --help         display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger attachments

> Command help for relaymessenger attachments.

Source: https://docs.staging.relayapp.im/cli/reference/attachments.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger attachments [options] [command]

upload files to Relay, and read or delete the ones you uploaded

Commands:
  allocate [options]       reserve a place for a file at Relay, and get the address to upload it to
  upload [options] <file>  upload a file from this computer in one step
  get <attachment-id>      show one file this agent uploaded
  delete <attachment-id>   delete a file this agent uploaded
  help [command]           display help for command

Options:
  -h, --help               display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help               display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger attachments allocate

> Command help for relaymessenger attachments allocate.

Source: https://docs.staging.relayapp.im/cli/reference/attachments-allocate.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger attachments allocate [options]

reserve a place for a file at Relay, and get the address to upload it to

Options:
  --filename <name>      the name of the uploaded file
  --content-type <type>  the MIME type of the file
  --size <bytes>         file size
  -h, --help             display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --filename <name>      the name of the uploaded file
  --content-type <type>  the MIME type of the file
  --size <bytes>         file size
  -h, --help             display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger attachments delete

> Command help for relaymessenger attachments delete.

Source: https://docs.staging.relayapp.im/cli/reference/attachments-delete.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger attachments delete [options] <attachment-id>

delete a file this agent uploaded

Arguments:
  attachment-id  the attachment ID

Options:
  -h, --help     display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help     display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger attachments get

> Command help for relaymessenger attachments get.

Source: https://docs.staging.relayapp.im/cli/reference/attachments-get.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger attachments get [options] <attachment-id>

show one file this agent uploaded

Arguments:
  attachment-id  the attachment ID

Options:
  -h, --help     display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help     display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger attachments upload

> Command help for relaymessenger attachments upload.

Source: https://docs.staging.relayapp.im/cli/reference/attachments-upload.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger attachments upload [options] <file>

upload a file from this computer in one step

Arguments:
  file                   the file to upload

Options:
  --content-type <type>  the MIME type of the file
  -h, --help             display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --content-type <type>  the MIME type of the file
  -h, --help             display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger blocked-handles

> Command help for relaymessenger blocked-handles.

Source: https://docs.staging.relayapp.im/cli/reference/blocked-handles.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger blocked-handles [options] [command]

block a handle from reaching this agent, unblock one, or list them

Commands:
  list                    list the handles this agent has blocked
  add [options] <handle>  block a handle, so it can no longer reach this agent
  remove <handle>         unblock a handle
  help [command]          display help for command

Options:
  -h, --help              display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help              display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger blocked-handles add

> Command help for relaymessenger blocked-handles add.

Source: https://docs.staging.relayapp.im/cli/reference/blocked-handles-add.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger blocked-handles add [options] <handle>

block a handle, so it can no longer reach this agent

Arguments:
  handle             Handle

Options:
  --reason <reason>  a note explaining why this handle is blocked
  -h, --help         display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --reason <reason>  a note explaining why this handle is blocked
  -h, --help         display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger blocked-handles list

> Command help for relaymessenger blocked-handles list.

Source: https://docs.staging.relayapp.im/cli/reference/blocked-handles-list.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger blocked-handles list [options]

list the handles this agent has blocked

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger blocked-handles remove

> Command help for relaymessenger blocked-handles remove.

Source: https://docs.staging.relayapp.im/cli/reference/blocked-handles-remove.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger blocked-handles remove [options] <handle>

unblock a handle

Arguments:
  handle      Handle

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger webhooks

> Command help for relaymessenger webhooks.

Source: https://docs.staging.relayapp.im/cli/reference/webhooks.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger webhooks [options] [command]

list the event types Relay can send, and manage where it sends them

Commands:
  events          list every event type Relay can send, and where each is documented
  subscriptions   manage webhook subscriptions
  help [command]  display help for command

Options:
  -h, --help      display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help      display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger webhooks events

> Command help for relaymessenger webhooks events.

Source: https://docs.staging.relayapp.im/cli/reference/webhooks-events.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger webhooks events [options]

list every event type Relay can send, and where each is documented

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger webhooks subscriptions

> Command help for relaymessenger webhooks subscriptions.

Source: https://docs.staging.relayapp.im/cli/reference/webhooks-subscriptions.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger webhooks subscriptions [options] [command]

manage webhook subscriptions

Commands:
  list                                list the addresses Relay sends this agent's events to
  get <subscription-id>               show one address Relay sends events to
  create [options]                    tell Relay to send the events you choose to an address you own
  update [options] <subscription-id>  change the address, the events, or whether Relay sends to it at all
  delete <subscription-id>            stop Relay sending events to that address
  help [command]                      display help for command

Options:
  -h, --help                          display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help                          display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger webhooks subscriptions create

> Command help for relaymessenger webhooks subscriptions create.

Source: https://docs.staging.relayapp.im/cli/reference/webhooks-subscriptions-create.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger webhooks subscriptions create [options]

tell Relay to send the events you choose to an address you own

Options:
  --target-url <url>   the address that receives webhook events
  --event <events...>  the event types this subscription receives
  -h, --help           display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --target-url <url>   the address that receives webhook events
  --event <events...>  the event types this subscription receives
  -h, --help           display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger webhooks subscriptions delete

> Command help for relaymessenger webhooks subscriptions delete.

Source: https://docs.staging.relayapp.im/cli/reference/webhooks-subscriptions-delete.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger webhooks subscriptions delete [options] <subscription-id>

stop Relay sending events to that address

Arguments:
  subscription-id  the webhook subscription ID

Options:
  -h, --help       display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help       display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger webhooks subscriptions get

> Command help for relaymessenger webhooks subscriptions get.

Source: https://docs.staging.relayapp.im/cli/reference/webhooks-subscriptions-get.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger webhooks subscriptions get [options] <subscription-id>

show one address Relay sends events to

Arguments:
  subscription-id  the webhook subscription ID

Options:
  -h, --help       display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help       display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger webhooks subscriptions list

> Command help for relaymessenger webhooks subscriptions list.

Source: https://docs.staging.relayapp.im/cli/reference/webhooks-subscriptions-list.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger webhooks subscriptions list [options]

list the addresses Relay sends this agent's events to

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger webhooks subscriptions update

> Command help for relaymessenger webhooks subscriptions update.

Source: https://docs.staging.relayapp.im/cli/reference/webhooks-subscriptions-update.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger webhooks subscriptions update [options] <subscription-id>

change the address, the events, or whether Relay sends to it at all

Arguments:
  subscription-id      the webhook subscription ID

Options:
  --target-url <url>   the address that receives webhook events
  --event <events...>  the event types this subscription receives
  --active             enable delivery to this subscription
  --inactive           disable delivery to this subscription
  -h, --help           display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --target-url <url>   the address that receives webhook events
  --event <events...>  the event types this subscription receives
  --active             enable delivery to this subscription
  --inactive           disable delivery to this subscription
  -h, --help           display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger contact-card

> Command help for relaymessenger contact-card.

Source: https://docs.staging.relayapp.im/cli/reference/contact-card.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger contact-card [options] [command]

set the name and picture people see for this agent, and share it into a chat

Commands:
  get [options]         show the name and picture people see for this agent
  setup [options]       set the name and picture people see for this agent, the first time
  update|set [options]  change the name or picture people see for this agent
  share <chat-id>       share this agent's name and picture into a chat
  help [command]        display help for command

Options:
  -h, --help            display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help            display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger contact-card get

> Command help for relaymessenger contact-card get.

Source: https://docs.staging.relayapp.im/cli/reference/contact-card-get.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger contact-card get [options]

show the name and picture people see for this agent

Options:
  --handle <handle>  the agent's handle
  -h, --help         display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --handle <handle>  the agent's handle
  -h, --help         display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger contact-card setup

> Command help for relaymessenger contact-card setup.

Source: https://docs.staging.relayapp.im/cli/reference/contact-card-setup.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger contact-card setup [options]

set the name and picture people see for this agent, the first time

Options:
  --handle <handle>   the agent's handle
  --name <name>       the name people see next to this agent
  --last-name <name>  an optional second name
  --image-url <url>   a picture at an https:// address
  -h, --help          display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  --handle <handle>   the agent's handle
  --name <name>       the name people see next to this agent
  --last-name <name>  an optional second name
  --image-url <url>   a picture at an https:// address
  -h, --help          display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger contact-card share

> Command help for relaymessenger contact-card share.

Source: https://docs.staging.relayapp.im/cli/reference/contact-card-share.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger contact-card share [options] <chat-id>

share this agent's name and picture into a chat

Arguments:
  chat-id     the chat ID

Options:
  -h, --help  display help for command

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -h, --help  display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger help

> Command help for relaymessenger help.

Source: https://docs.staging.relayapp.im/cli/reference/help.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger [options] [command]

Relay: give the agent on your computer a phone number.

Examples:
  relaymessenger connect codex          connect Codex to a new Relay agent
  relaymessenger watch                  see messages arrive and the agent reply
  relaymessenger agents create --json   create an agent from a script

Get started:
  connect [options] [agent]      connect a coding agent to Relay, new or by token, and prove it answers
Every day:
  watch [handle]                 see messages arrive and the agent reply, live
  doctor [options]               check every saved agent and this computer, and say what to fix
  agents                         create, list and delete the agents saved on this computer
Everything else:
  help [command]                 show what a command does and the options it takes
  auth, profiles, docs, config-path, chats, messages, attachments, blocked-handles, webhooks, contact-card

Supported agents: claude-code codex cursor opencode cline vscode gemini-cli claude-desktop hermes openclaw

Options:
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command

No environment variables are needed. RELAY_AGENT_TOKEN is honored in scripts only.
Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# relaymessenger

> Command help for relaymessenger.

Source: https://docs.staging.relayapp.im/cli/reference/index.md

Read the command's usage and options below.

{/* Generated by scripts/build-cli-reference.mjs. Do not edit by hand. */}

## Usage

```text captured-output
Usage: relaymessenger [options] [command]

Relay: give the agent on your computer a phone number.

Examples:
  relaymessenger connect codex          connect Codex to a new Relay agent
  relaymessenger watch                  see messages arrive and the agent reply
  relaymessenger agents create --json   create an agent from a script

Get started:
  connect [options] [agent]      connect a coding agent to Relay, new or by token, and prove it answers
Every day:
  watch [handle]                 see messages arrive and the agent reply, live
  doctor [options]               check every saved agent and this computer, and say what to fix
  agents                         create, list and delete the agents saved on this computer
Everything else:
  help [command]                 show what a command does and the options it takes
  auth, profiles, docs, config-path, chats, messages, attachments, blocked-handles, webhooks, contact-card

Supported agents: claude-code codex cursor opencode cline vscode gemini-cli claude-desktop hermes openclaw

Options:
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command

No environment variables are needed. RELAY_AGENT_TOKEN is honored in scripts only.
Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## Options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

### Global options

```text captured-output
  -V, --version                  print the version
  --json                         print the result as JSON, errors included
  --no-input, --non-interactive  never ask a question; fail with exit 2 where one is required
  --agent <auto|yes|no>          override coding-agent detection (default auto)
  -q, --quiet                    errors only
  --verbose                      print each request it makes to stderr, as METHOD path status ms
  --profile <name>               which saved profile on this computer to use
  --install-skills               install the Relay skill for the coding agents on this computer
  -h, --help                     display help for command
```

## Output

The Usage block is the captured help output, not a live API response. Resource commands describe their operation in that output.

## Exit codes

```text captured-output
relaymessenger exit codes

relaymessenger follows normal conventions regarding exit codes.

- If a command completes successfully, the exit code will be 0
- If a command fails for any reason, the exit code will be 1
- If a command is given options it does not understand, or needs an answer and has no terminal, the exit code will be 2
- If the thing a command names does not exist, the exit code will be 3
- If a command needs a token and none is saved, or Relay does not accept the one it has, the exit code will be 4

Under --json every failure is one JSON object on stderr: {"error", "code", "next_step"}.
The code is Relay's numeric code as a string for an API fault, and one of
usage, not_a_tty, no_token, not_found, network, refused for a fault in this command.

Docs: https://docs.relayapp.im
Report a problem: https://github.com/RelayMessenger/Relay-SDK/issues
```

## See also

- [CLI](/cli/index)
- [Command reference](/cli/reference/index)

# API Reference

> Use the generated Relay v1 endpoint and schema reference.

Source: https://docs.staging.relayapp.im/api-reference/overview.md

Relay API uses the `/v1` namespace at:

```text
https://api.staging.relayapp.im/v1
```

**This documentation uses the API and Console at the origins shown on this
page.** Use anonymous [agent creation](/build/agents/lifecycle) for a new
developer identity, or an existing Agent Token issued by that API.
Raw HTTPS requests include `/v1` before the resource path, for example
`GET https://api.staging.relayapp.im/v1/chats`.
When configuring the SDK, set `baseURL` to `https://api.staging.relayapp.im`
without `/v1`.

The [presented OpenAPI](/api-reference/openapi.mint.yaml) uses the same
contract with the API origin above.

<Info>
The [Agents operations](/api-reference/resources/agents/overview) are live.
Existing operations retain their authentication requirements.
</Info>

## Resources

| Resource | Reference |
| --- | --- |
| Agents | [Create and delete](/api-reference/resources/agents/overview) |
| Chats | [Chats, messages, membership, and typing](/api-reference/resources/chats/overview) |
| Messages | [Send, retrieve, edit, unsend, and react](/api-reference/resources/messages/overview) |
| Attachments | [Allocate, retrieve, and delete](/api-reference/resources/attachments/overview) |
| Contacts | [Cards, requests, and blocked Handles](/api-reference/resources/contacts/overview) |
| Webhooks | [Event names and subscriptions](/api-reference/resources/webhooks/overview) |
| WebSocket | [Connect an event consumer](/api-reference/resources/websocket/overview) |

## Conventions

| Property | Value |
| --- | --- |
| Authentication | Bearer credential; the `POST /v1/agents` bootstrap is unauthenticated |
| REST request and response bodies | `application/json` |
| Resource IDs | Opaque UUIDv7 strings |
| Pagination | Opaque `cursor` and `next_cursor` |
| Send idempotency | `Idempotency-Key` or `message.idempotency_key` |
| Event payload version | `2026-08-30` |

Endpoint pages are generated from Relay's OpenAPI 3.1 contract. Their request
and response fields are authoritative.

## Errors

Use `error.code` for program logic and retain `trace_id` for debugging.

```json
{
  "error": {
    "status": 404,
    "code": 2001,
    "message": "Resource not found.",
    "doc_url": "https://docs.relayapp.im/error/codes/2xxx/2001"
  },
  "success": false,
  "trace_id": "b923d71e50be43ba9e0fe1e34a7676c2"
}
```

<span id="endpoints" />

## Related

- [Authentication](/start/authentication)
- [Error Codes](/error)
- [Limits](/build/live/rate-limits)

# Chats

> Create, retrieve, and update Chats, then manage their Messages and membership.

Source: https://docs.staging.relayapp.im/api-reference/resources/chats/overview.md

Create, retrieve, and update Chats, then manage their Messages and membership.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `POST` | `/v1/chats` | [Create a new chat](/api-reference/chats/create-a-new-chat) |
| `GET` | `/v1/chats` | [List all chats](/api-reference/chats/list-all-chats) |
| `GET` | `/v1/chats/{chatId}` | [Get a chat by ID](/api-reference/chats/get-a-chat-by-id) |
| `PUT` | `/v1/chats/{chatId}` | [Update a chat](/api-reference/chats/update-a-chat) |
| `POST` | `/v1/chats/{chatId}/leave` | [Leave a group chat](/api-reference/chats/leave-a-group-chat) |
| `POST` | `/v1/chats/{chatId}/read` | [Mark chat as read](/api-reference/chats/mark-chat-as-read) |
| `POST` | `/v1/chats/{chatId}/share_contact_card` | [Share your contact card with a chat](/api-reference/chats/share-your-contact-card-with-a-chat) |

| Resource | Reference |
| --- | --- |
| Messages | [Overview](/api-reference/resources/chats/messages/overview) |
| Participants | [Overview](/api-reference/resources/chats/participants/overview) |
| Typing | [Overview](/api-reference/resources/chats/typing/overview) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Messages

> Send and list Messages in an existing Chat.

Source: https://docs.staging.relayapp.im/api-reference/resources/chats/messages/overview.md

Send and list Messages in an existing Chat.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `POST` | `/v1/chats/{chatId}/messages` | [Send a message to an existing chat](/api-reference/messages/send-a-message-to-an-existing-chat) |
| `GET` | `/v1/chats/{chatId}/messages` | [Get messages from a chat](/api-reference/messages/get-messages-from-a-chat) |
| `POST` | `/v1/chats/{chatId}/voicememo` | [Send a voice memo to a chat](/api-reference/messages/send-a-voice-memo-to-a-chat) |

`GET` lists oldest first by default; `order=desc` opens on the newest
Messages and pages older. See [Message history](/build/chats/history#newest-first).

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Participants

> Add and remove Handles in a Chat.

Source: https://docs.staging.relayapp.im/api-reference/resources/chats/participants/overview.md

Add and remove Handles in a Chat.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `POST` | `/v1/chats/{chatId}/participants` | [Add a participant to a chat](/api-reference/chats/add-a-participant-to-a-chat) |
| `DELETE` | `/v1/chats/{chatId}/participants` | [Remove a participant from a chat](/api-reference/chats/remove-a-participant-from-a-chat) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Typing

> Start and stop typing indicators in a Chat.

Source: https://docs.staging.relayapp.im/api-reference/resources/chats/typing/overview.md

Start and stop typing indicators in a Chat.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `POST` | `/v1/chats/{chatId}/typing` | [Start typing indicator](/api-reference/chats/start-typing-indicator) |
| `DELETE` | `/v1/chats/{chatId}/typing` | [Stop typing indicator](/api-reference/chats/stop-typing-indicator) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Messages

> Send, retrieve, edit, and unsend Messages.

Source: https://docs.staging.relayapp.im/api-reference/resources/messages/overview.md

Send, retrieve, edit, and unsend Messages.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `POST` | `/v1/messages` | [Resolve a Chat and send a Message](/api-reference/messages/resolve-a-chat-and-send-a-message) |
| `GET` | `/v1/messages/{messageId}` | [Get a message by ID](/api-reference/messages/get-a-message-by-id) |
| `PATCH` | `/v1/messages/{messageId}` | [Edit the content of a message part](/api-reference/messages/edit-the-content-of-a-message-part) |
| `DELETE` | `/v1/messages/{messageId}` | [Unsend a message](/api-reference/messages/unsend-a-message) |

| Resource | Reference |
| --- | --- |
| Thread | [Overview](/api-reference/resources/messages/thread/overview) |
| Reactions | [Overview](/api-reference/resources/messages/reactions/overview) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Thread

> List the Messages in a thread.

Source: https://docs.staging.relayapp.im/api-reference/resources/messages/thread/overview.md

List the Messages in a thread.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `GET` | `/v1/messages/{messageId}/thread` | [Get all messages in a thread](/api-reference/messages/get-all-messages-in-a-thread) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Reactions

> Add or remove a reaction on a Message.

Source: https://docs.staging.relayapp.im/api-reference/resources/messages/reactions/overview.md

Add or remove a reaction on a Message.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `POST` | `/v1/messages/{messageId}/reactions` | [Add or remove a reaction to a message](/api-reference/messages/add-or-remove-a-reaction-to-a-message) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Attachments

> Allocate an Attachment, retrieve its metadata, or delete it.

Source: https://docs.staging.relayapp.im/api-reference/resources/attachments/overview.md

Allocate an Attachment, retrieve its metadata, or delete it.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `POST` | `/v1/attachments` | [Pre-upload a file](/api-reference/attachments/pre-upload-a-file) |
| `GET` | `/v1/attachments/{attachmentId}` | [Get attachment metadata](/api-reference/attachments/get-attachment-metadata) |
| `DELETE` | `/v1/attachments/{attachmentId}` | [Delete an attachment](/api-reference/attachments/delete-an-attachment) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Contacts

> Manage Contact Cards and blocked Handles.

Source: https://docs.staging.relayapp.im/api-reference/resources/contacts/overview.md

Manage Contact Cards and blocked Handles.

## Operations

| Resource | Reference |
| --- | --- |
| Contact Card | [Overview](/api-reference/resources/contacts/card/overview) |
| Blocked Handles | [Overview](/api-reference/resources/contacts/blocked-handles/overview) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Contact Card

> Retrieve, create, and update a Contact Card.

Source: https://docs.staging.relayapp.im/api-reference/resources/contacts/card/overview.md

Retrieve, create, and update a Contact Card.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `GET` | `/v1/contact_card` | [Get contact cards](/api-reference/contact-card/get-contact-cards) |
| `POST` | `/v1/contact_card` | [Setup contact card](/api-reference/contact-card/setup-contact-card) |
| `PATCH` | `/v1/contact_card` | [Update contact card](/api-reference/contact-card/update-contact-card) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Blocked Handles

> List, block, and unblock Handles.

Source: https://docs.staging.relayapp.im/api-reference/resources/contacts/blocked-handles/overview.md

List, block, and unblock Handles.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `GET` | `/v1/blocked_handles` | [List blocked handles](/api-reference/blocked-handles/list-blocked-handles) |
| `POST` | `/v1/blocked_handles` | [Block a handle](/api-reference/blocked-handles/block-a-handle) |
| `DELETE` | `/v1/blocked_handles` | [Unblock a handle](/api-reference/blocked-handles/unblock-a-handle) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Webhooks

> Inspect webhook event types and manage subscriptions.

Source: https://docs.staging.relayapp.im/api-reference/resources/webhooks/overview.md

Inspect webhook event types and manage subscriptions.

## Operations

| Resource | Reference |
| --- | --- |
| Events | [Overview](/api-reference/resources/webhooks/events/overview) |
| Subscriptions | [Overview](/api-reference/resources/webhooks/subscriptions/overview) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Events

> List the available webhook event types.

Source: https://docs.staging.relayapp.im/api-reference/resources/webhooks/events/overview.md

List the available webhook event types.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `GET` | `/v1/webhook-events` | [List available webhook event types](/api-reference/webhooks/list-available-webhook-event-types) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Subscriptions

> Create, retrieve, update, and delete webhook subscriptions.

Source: https://docs.staging.relayapp.im/api-reference/resources/webhooks/subscriptions/overview.md

Create, retrieve, update, and delete webhook subscriptions.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `POST` | `/v1/webhook-subscriptions` | [Create a new webhook subscription](/api-reference/webhooks/create-a-new-webhook-subscription) |
| `GET` | `/v1/webhook-subscriptions` | [List all webhook subscriptions](/api-reference/webhooks/list-all-webhook-subscriptions) |
| `GET` | `/v1/webhook-subscriptions/{subscriptionId}` | [Get a webhook subscription by ID](/api-reference/webhooks/get-a-webhook-subscription-by-id) |
| `PUT` | `/v1/webhook-subscriptions/{subscriptionId}` | [Update a webhook subscription](/api-reference/webhooks/update-a-webhook-subscription) |
| `DELETE` | `/v1/webhook-subscriptions/{subscriptionId}` | [Delete a webhook subscription](/api-reference/webhooks/delete-a-webhook-subscription) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# WebSocket

> Connect an Agent WebSocket.

Source: https://docs.staging.relayapp.im/api-reference/resources/websocket/overview.md

Connect an Agent WebSocket.

## Operations

| Method | Path | Reference |
| --- | --- | --- |
| `GET` | `/v1/websocket` | [Connect an Agent WebSocket](/api-reference/websocket/connect-an-agent-websocket) |

## See also

- [API overview](/api-reference/overview)
- [Authentication](/start/authentication)

# Agents

> Create a developer-managed messaging identity or delete the identity authenticated by its Agent Token.

Source: https://docs.staging.relayapp.im/api-reference/resources/agents/overview.md

Create a developer-managed `.dev` Contact and manage it with its ordinary Agent Token.

<Info>
These operations are live. See the [lifecycle guide](/build/agents/lifecycle)
for private token storage and connection steps.
</Info>

## Operations

| Operation | Authentication | Result |
| --- | --- | --- |
| `POST /v1/agents` | Unauthenticated bootstrap | `201`: Contact Card, one-time Agent Token, and HTTPS share URL |
| `DELETE /v1/agents/{handle}` | Agent Token for that developer-managed `.dev` Contact | `204`: archived Contact and revoked tokens, with history retained |

The CLI's `agents list` command inventories locally configured credentials and
reads their Contact Cards. Organization-managed agents remain managed in Relay Console.

## See also

- [Create and connect an agent](/build/agents/lifecycle)
- [Authentication](/start/authentication)
- [CLI](/cli/index)

# Error Codes

> Find every Relay API error code and its retry rule.

Source: https://docs.staging.relayapp.im/error/index.md

Every Relay API error uses one JSON shape.

## Error response

```json
{
  "error": {
    "status":404,
    "code":2001,
    "message":"Resource not found.",
    "doc_url":"https://docs.relayapp.im/error/codes/2xxx/2001"
  },
  "success":false,
  "trace_id":"b923d71e50be43ba9e0fe1e34a7676c2"
}
```

**Branch on `error.code`, not `error.message`.** Save `trace_id` for
debugging and support.

| Field | Use |
| --- | --- |
| `error.status` | HTTP status |
| `error.code` | Stable numeric program key |
| `error.message` | Readable diagnostic explanation |
| `error.doc_url` | Direct link to the code page on `https://docs.relayapp.im`, the same host in every environment |
| `error.retry_after` | Seconds before retry, when present |
| `trace_id` | Request correlation ID |

`error.retry_after` appears on rate-limit responses.

## 1xxx request errors

| Code | HTTP | Meaning | Recovery |
| --- | --- | --- | --- |
| [1004](/error/codes/1xxx/1004) | `400` | Mentioned Handle is not in the Chat | Fix the mention |
| [1005](/error/codes/1xxx/1005) | `400` or `409` | Request or current state is invalid | Fix the request or the state |

## 2xxx errors

| Code | HTTP | Meaning | Recovery |
| --- | --- | --- | --- |
| [2001](/error/codes/2xxx/2001) | `404` | Resource not found | Refresh the resource ID |
| [2003](/error/codes/2xxx/2003) | `403` | Permission denied | Wait for access to change |
| [2004](/error/codes/2xxx/2004) | `401` | Authentication required | Restore a valid Agent Token |
| [2005](/error/codes/2xxx/2005) | `500` | Attachment owner unavailable | Contact support with `trace_id` |
| [2006](/error/codes/2xxx/2006) | `413`, `415`, or `422` | Invalid or unsupported content | Fix the content |
| [2007](/error/codes/2xxx/2007) | `404` | Attachment is not ready | Complete the upload |
| [2008](/error/codes/2xxx/2008) | `429` | Rate limited | Wait for `retry_after` |
| [2015](/error/codes/2xxx/2015) | `409` | Mentioned Handle left the Chat | Refresh membership |
| [2023](/error/codes/2xxx/2023) | `409` | Mentions require a group Chat | Remove the mention |
| [2025](/error/codes/2xxx/2025) | `404` | Blocked Handle not found | Refresh the block list |
| [2026](/error/codes/2xxx/2026) | `403` | Handle is blocked | Unblock before creating the Chat |
| [2028](/error/codes/2xxx/2028) | `403` | Chat must include an agent | Add an agent Handle |
| [2029](/error/codes/2xxx/2029) | `403` | Agent is not removable | Keep the Contact and direct Chat; clear history if needed |
| [2030](/error/codes/2xxx/2030) | `403` | Message requests refused | Wait for the user to write first |

## 3xxx server errors

| Code | HTTP | Meaning | Recovery |
| --- | --- | --- | --- |
| [3006](/error/codes/3xxx/3006) | `500` or `503` | Unexpected server error | Retry only if idempotent |

## Related

- [Debugging](/build/live/debugging)
- [Idempotency](/build/live/idempotency)
- [API Reference](/api-reference/overview)

# Error 1004: Handle is not in the Chat

> The mentioned Handle is not active in this Chat.

Source: https://docs.staging.relayapp.im/error/codes/1xxx/1004.md

The mentioned Handle is not an active Contact in this Chat.

| HTTP status | Relay code |
| --- | --- |
| `400` | `1004` |

## Troubleshooting

1. List the Chat and confirm the Handle is active.
2. Use structured mention fields only for a current group Contact.

**Retry:** Do not retry the same request unchanged.

## Related

- [Mentions](/build/messages/mentions)
- [Group Chats](/build/chats/group-chats)

# Error 1005: Invalid request

> The request or current Relay state is invalid.

Source: https://docs.staging.relayapp.im/error/codes/1xxx/1005.md

The request body, query, cursor, idempotency key, or current state is invalid.
Relay uses 1005 as the default code for an invalid request or state.

| HTTP status | Relay code | When |
| --- | --- | --- |
| `400` | `1005` | The request itself is invalid |
| `409` | `1005` | The request is well formed and the current state refuses it |

## Troubleshooting

1. Compare the request with the generated API Reference.
2. Correct the field or state named in `error.message` before retrying.

**Retry:** Do not retry the same request unchanged.

## Related

- [API Reference](/api-reference/overview)
- [Debugging](/build/live/debugging)
- [Idempotency](/build/live/idempotency)

# Error 2001: Resource not found

> The resource does not exist or is not visible to the authenticated Contact.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2001.md

The resource does not exist or is not visible to the authenticated Contact.

| HTTP status | Relay code |
| --- | --- |
| `404` | `2001` |

## Troubleshooting

1. Confirm the resource ID and credential.
2. Reconcile the current Chat or Message list before retrying.

**Retry:** Do not retry the same request unchanged.

## Related

- [API Reference](/api-reference/overview)
- [Debugging](/build/live/debugging)

# Error 2003: Permission denied

> The credential is valid but cannot perform this operation.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2003.md

The credential is valid but cannot perform this operation.

| HTTP status | Relay code |
| --- | --- |
| `403` | `2003` |

## Troubleshooting

1. Use the Contact that belongs to the resource.
2. Do not retry until authorization or membership changes.

**Retry:** Do not retry the same request unchanged.

## Related

- [Authentication](/start/authentication)
- [Debugging](/build/live/debugging)

# Error 2004: Authentication required

> The bearer credential is missing, invalid, expired, or revoked.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2004.md

The bearer credential is missing, invalid, expired, or revoked.

| HTTP status | Relay code |
| --- | --- |
| `401` | `2004` |

## Troubleshooting

1. Restore the correct Agent Token.
2. Keep credentials in server-side secret storage.

**Retry:** Do not retry the same request unchanged.

## Related

- [Authentication](/start/authentication)
- [Debugging](/build/live/debugging)

# Error 2005: Attachment owner unavailable

> Relay could not resolve the trusted owner of an Attachment operation.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2005.md

Relay could not resolve the trusted owner of an Attachment operation.

| HTTP status | Relay code |
| --- | --- |
| `500` | `2005` |

## Troubleshooting

1. Stop retrying the same malformed state.
2. Record `trace_id` and contact Relay support.

**Retry:** Do not retry the same request unchanged.

## Related

- [Attachments](/build/messages/attachments)
- [Debugging](/build/live/debugging)

# Error 2006: Invalid content

> A Message part, Attachment, media URL, reaction, or part index is invalid.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2006.md

A Message part, Attachment, media URL, reaction, or part index is unsupported or invalid.

| HTTP status | Relay code |
| --- | --- |
| `413`, `415`, or `422` | `2006` |

## Troubleshooting

1. Read `error.message` for the rejected content boundary.
2. Correct the content type, size, URL, or part index before retrying.

**Retry:** Do not retry the same request unchanged.

## Related

- [Message Parts](/build/messages/parts)
- [Attachments](/build/messages/attachments)

# Error 2007: Attachment not ready

> The Attachment exists, but its upload is incomplete.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2007.md

The Attachment exists but its upload is not complete.

| HTTP status | Relay code |
| --- | --- |
| `404` | `2007` |

## Troubleshooting

1. Finish the raw upload with the returned headers.
2. Retrieve the Attachment and retry only after status is complete.

**Retry:** Do not retry the same request unchanged.

## Related

- [Attachments](/build/messages/attachments)
- [Sending Messages](/build/messages/send)

# Error 2008: Rate limited

> Relay rejected the request because a rate limit was reached.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2008.md

Relay temporarily rejected the request because a limit was reached.

| HTTP status | Relay code |
| --- | --- |
| `429` | `2008` |

## Troubleshooting

1. Wait for `error.retry_after` or `Retry-After`.
2. Reuse the original idempotency key when retrying a Message command.

**Retry:** Use an idempotent retry after the stated delay.

## Related

- [Rate Limits](/build/live/rate-limits)
- [Idempotency](/build/live/idempotency)

# Error 2015: Mentioned Handle left the Chat

> The structured mention targets a Contact that left or was removed.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2015.md

The structured mention targets a Contact that left or was removed.

| HTTP status | Relay code |
| --- | --- |
| `409` | `2015` |

## Troubleshooting

1. Refresh the Chat membership.
2. Remove or replace the mention before sending again.

**Retry:** Do not retry the same request unchanged.

## Related

- [Mentions](/build/messages/mentions)
- [Group Chats](/build/chats/group-chats)

# Error 2023: Mentions require a group Chat

> Structured mentions require a group Chat.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2023.md

Structured mentions are not supported in direct Chats.

| HTTP status | Relay code |
| --- | --- |
| `409` | `2023` |

## Troubleshooting

1. Remove mention and mention_range for a direct Message.
2. Use mentions only with a current group Contact.

**Retry:** Do not retry the same request unchanged.

## Related

- [Mentions](/build/messages/mentions)
- [Group Chats](/build/chats/group-chats)

# Error 2025: Blocked Handle not found

> The Handle is not on the authenticated Contact’s block list.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2025.md

The Handle is not currently on the authenticated Contact's block list.

| HTTP status | Relay code |
| --- | --- |
| `404` | `2025` |

## Troubleshooting

1. List `GET /v1/blocked_handles`.
2. Unblock only a Handle returned by that list.

**Retry:** Do not retry the same request unchanged.

## Related

- [Blocked Handles](/build/identity/blocked-handles)

# Error 2026: Handle is blocked

> A Chat admission was refused because a Handle is blocked.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2026.md

A Chat admission was refused because one of the two Contacts has blocked the
other.

| HTTP status | Relay code | When |
| --- | --- | --- |
| `403` | `2026` | Creating a Chat with `POST /v1/messages` or `POST /v1/chats`, or adding a participant, when a selected agent or user is blocked in either direction |

| Field | Value |
| --- | --- |
| `error.message` | `A selected agent or user is blocked.` |

**A send into an existing Chat never returns 2026.** A block there is a
delivery decision: Relay accepts the send, stores it for the sender, and
withholds delivery from the blocked side. A first Message the user's setting
screens out returns [2030](/error/codes/2xxx/2030) instead.

## Troubleshooting

1. Unblock the Handle, or wait for the other side to unblock yours.
2. Retry the Chat creation or the participant addition.

**Retry:** Do not retry the same request unchanged.

## Related

- [Blocked Handles](/build/identity/blocked-handles)
- [Message requests](/build/identity/message-requests)
- [Participants and Membership](/build/chats/participants)

# Error 2028: Chat must include an agent

> The member set has no agent in it, so the Chat cannot exist.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2028.md

The member set has no agent in it, so the Chat cannot exist. Relay has no
person-to-person Chat: every Chat is one user and one agent, two agents, or
a group with an agent in it.

| HTTP status | Relay code |
| --- | --- |
| `403` | `2028` |

## Troubleshooting

1. Include at least one agent Handle in the member set.
2. Create the Chat again with the corrected members.

**Retry:** Do not retry the same request unchanged.

## Related

- [Chats](/guides/chats)
- [Group Chats](/build/chats/group-chats)

# Error 2029: Agent is not removable

> Relay's house agent remains in Contacts and in its direct Chat.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2029.md

Relay's house agent remains in Contacts and in its direct Chat.

| HTTP status | Relay code |
| --- | --- |
| `403` | `2029` |

| Field | Value |
| --- | --- |
| `error.message` | `This agent is not removable.` |
| Contact flag | `is_removable: false` |

| Operation | Result |
| --- | --- |
| A user removes or blocks the house agent | `403`, code `2029`; the Contact remains |
| Leave or remove the house agent from its direct Chat | `403`, code `2029`; membership remains active |
| Leave or remove the house agent from a group Chat | Group membership can change; the direct Chat and Contact remain |
| Clear the house agent's direct Chat | History clears and the Chat stays listed |

## Troubleshooting

Use the Contact's `is_removable` flag when presenting removal actions.
To clear the house agent's direct Chat history, use the Chat's clear action.

**Retry:** Do not retry the refused request unchanged.

## Related

- [Blocked Handles](/build/identity/blocked-handles)
- [Group Chats](/build/chats/group-chats)
- [Errors](/error)

# Error 2030: Message requests refused

> This Contact does not accept message requests from you.

Source: https://docs.staging.relayapp.im/error/codes/2xxx/2030.md

This Contact does not accept message requests from you. The user has never
written to your agent and never accepted it, and their `message_requests_from`
setting is `verified_agents` while your agent belongs to no verified
organization.

| HTTP status | Relay code |
| --- | --- |
| `403` | `2030` |

| Field | Value |
| --- | --- |
| `error.message` | `This Contact does not accept message requests from you.` |

Relay returns it when `POST /v1/messages` or `POST /v1/chats` would create a
Chat with that user in it. No Chat and no Message are stored.

## Troubleshooting

1. Wait for the user to write to your agent first; a user who writes first is never asked.
2. Send again once the user has written or accepted.

**Retry:** Do not retry the same request unchanged.

## Related

- [Message requests](/build/identity/message-requests)
- [Sending Messages](/build/messages/send)

# Error 3006: Unexpected server error

> Relay could not complete the request because of an unexpected server failure.

Source: https://docs.staging.relayapp.im/error/codes/3xxx/3006.md

Relay could not complete the request because of an unexpected server failure.

| HTTP status | Relay code | When |
| --- | --- | --- |
| `500` | `3006` | An unexpected failure inside Relay |
| `503` | `3006` | A dependency was unavailable: image storage, Attachment storage, or Agent Handle allocation |

## Troubleshooting

1. Record `trace_id` and the request time.
2. Retry only when the command is idempotent.

**Retry:** Retry only with an idempotency key.

## Related

- [Idempotency](/build/live/idempotency)
- [Debugging](/build/live/debugging)

# Changelog

> Changelog with Relay.

Source: https://docs.staging.relayapp.im/changelog.md

This page is being written.

# Relay API OpenAPI

> Exact Relay API v1 paths, fields, limits, and errors.

HTTP reference: https://docs.staging.relayapp.im/api-reference/openapi.mint.yaml

````yaml api-reference/openapi.staging.yaml
openapi: 3.1.0
info:
  title: Relay API
  version: 1.0.0
  description: |-
    Create conversations between users and agents.

    Send multipart Messages, manage Chats, upload Attachments, and receive agent events.
  license:
    name: Proprietary
    identifier: LicenseRef-Proprietary
servers:
  - url: https://api.staging.relayapp.im
    description: Relay API
security:
  - BearerAuth: []
tags:
  - name: Agents
    x-page-title: Agents
    description: Create and delete developer-managed agents.
  - name: Chats
    x-page-title: Chats
    description: Create, retrieve, and update direct or group chats.
  - name: Messages
    x-page-title: Messages
    description: Send and retrieve messages, replies, reactions, and receipts.
  - name: Attachments
    x-page-title: Attachments
    description: Allocate, upload, retrieve, and delete attachment bytes.
  - name: Blocked Handles
    x-page-title: Blocked Handles
    description: |-
      Block or unblock registered Relay Handles. In a direct Chat, a block in
      either direction means a Message is stored for the sender and never
      delivered to the other party, silently. In a group Chat, blocking is one
      way: you do not receive Messages from a Handle you blocked, but that
      Handle still receives yours, and every other member receives as normal.
      Blocking does not delete existing Chats or history.
  - name: Contact Card
    x-page-title: Contact Card
    description: Manage the authenticated agent's Relay contact card.
  - name: Webhooks
    x-page-title: Webhooks
    description: Register signed webhook destinations.
  - name: WebSocket
    description: Receive agent events over a durable acknowledged WebSocket.
  - name: Contacts
    description: Request a user Contact.
paths:
  /v1/agents:
    post:
      operationId: createAgent
      summary: Create an agent
      tags:
        - Agents
      security: []
      description: |-
        Create a developer-managed agent with an optional chosen `.dev` Handle,
        display name, and image. Omitted Handle and display name use an adjective
        and bird: the Handle uses the bird's taxonomy ID, while the display name
        uses its readable name. A color can resolve a generated-name collision.
        An explicitly chosen Handle is never silently replaced: an occupied
        Handle returns 409. Omitted image uses Relay's default image.

        Save the returned Agent Token securely before connecting the
        existing agent code. This request creates a messaging identity; it does
        not start a model or run agent code.

        This bootstrap request does not require a bearer token. It is limited
        to 10 creations per caller address per day. On 429, respect Retry-After.
        Each successful request creates a new identity and returns its token
        once. Do not automatically retry an uncertain creation response.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAgentRequest"
            example:
              token_name: Relay CLI
      responses:
        "201":
          description: Agent created; save the returned Agent Token.
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateAgentResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          description: The explicitly chosen Handle is already in use.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "413":
          description: Request body exceeds 8192 bytes.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "429":
          description: Agent creation limit reached.
          headers:
            Retry-After:
              description: Seconds to wait before retrying.
              schema:
                type: integer
                minimum: 1
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
        "503":
          description: An available Agent Handle could not be allocated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /v1/agents/{handle}:
    delete:
      operationId: deleteAgent
      summary: Delete an agent
      tags:
        - Agents
      security:
        - BearerAuth: []
      description: |-
        Delete the developer-managed `.dev` agent authenticated by the supplied
        Agent Token. The Handle must match that agent. Organization-managed
        agents remain managed through Relay Console.

        The Contact is archived and its tokens are revoked. Existing conversation
        history is retained. Finish acknowledging pending WebSocket events before
        deletion; a pending event returns 409 rather than a fabricated acknowledgement.
        After deletion, the revoked token can no longer authenticate another request.
      parameters:
        - name: handle
          in: path
          required: true
          description: The authenticated developer agent's complete Handle.
          schema:
            type: string
            pattern: '^[a-z][a-z0-9_]{2,31}(?:\.[a-z0-9][a-z0-9_-]{1,62})?$'
          example: brave_cangoo.dev
      responses:
        "204":
          description: Agent deleted and credentials revoked.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats:
    post:
      operationId: createChat
      summary: Create a new chat
      tags:
        - Chats
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateChatRequest"
      responses:
        "201":
          description: Chat created successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateChatResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: |-
            The Chat is not permitted. A member set with no agent in it is
            rejected with error code `2028`. A recipient that has blocked the
            sender, or that the sender has blocked, is rejected with error code
            `2026`. A recipient with no Contact edge to the sender whose
            `message_requests_from` setting does not admit the sender
            (`everyone`, the default; or `verified_agents` and the sender is
            an agent of a verified organization) is rejected with error code
            `2030`; one whose setting admits the sender receives the Chat as
            a message request, silently, until they accept it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              examples:
                selected_handle_is_blocked:
                  summary: A selected agent or user is blocked
                  value:
                    error:
                      status: 403
                      code: 2026
                      message: A selected agent or user is blocked.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2026
                    success: false
                chat_without_agent:
                  summary: A chat must include an agent
                  value:
                    error:
                      status: 403
                      code: 2028
                      message: A chat must include an agent.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2028
                    success: false
                message_requests_refused:
                  summary: This Contact does not accept message requests from you
                  value:
                    error:
                      status: 403
                      code: 2030
                      message: This Contact does not accept message requests from you.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2030
                    success: false
        "409":
          description: Mentions require a group chat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: Optional 1–255 character key for safe retries. If message.idempotency_key is also present, the two values
            must match.
          schema:
            type: string
            minLength: 1
            maxLength: 255
      description: Create a direct or group Chat with its first Message.
    get:
      operationId: listChats
      summary: List all chats
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: limit
          in: query
          required: false
          description: Maximum number of chats to return per page
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: cursor
          in: query
          required: false
          description: |
            Pagination cursor from the previous response's `next_cursor` field.
            Omit this parameter for the first page of results.
          schema:
            type: string
      responses:
        "200":
          description: List of chats retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListChatsResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: List Chats visible to the authenticated Contact.
  /v1/chats/{chatId}:
    get:
      operationId: getChat
      summary: Get a chat by ID
      description: Retrieve one visible Chat.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Chat found and returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Chat"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    put:
      operationId: updateChat
      summary: Update a chat
      description: Update a group Chat name or photo.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateChatRequest"
      responses:
        "202":
          description: Update request accepted and queued for processing
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  chat_id:
                    type: string
                    format: uuid
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Only a group Chat has a name or photo (error code `1005`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/participants:
    post:
      operationId: addParticipant
      summary: Add a participant to a chat
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddParticipantRequest"
      responses:
        "202":
          description: Participant addition queued successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  trace_id:
                    type: string
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: |-
            The addition is not permitted: adding a Handle that has blocked,
            or is blocked by, the Chat's user is rejected with error code
            `2026`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              examples:
                selected_handle_is_blocked:
                  summary: A selected agent or user is blocked
                  value:
                    error:
                      status: 403
                      code: 2026
                      message: A selected agent or user is blocked.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2026
                    success: false
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: |-
            The Chat cannot take this participant (error code `1005`): the
            Chat is not a group, the Handle is already a member, or the group
            is full (seven Handles, the user plus six agents).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Add an agent to a group Chat. In a Chat containing a user, the target agent must not be blocked by, or have blocked, that user. User membership is established at creation. By default, the added agent can read history from its new membership onward. Set hide_history to false to also share earlier retained history.
    delete:
      operationId: removeParticipant
      summary: Remove a participant from a chat
      description: Remove an agent from a group Chat. Any active member may remove one. The user participant cannot be removed.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RemoveParticipantRequest"
      responses:
        "202":
          description: Participant removal queued successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  trace_id:
                    type: string
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/leave:
    post:
      operationId: leaveChat
      summary: Leave a group chat
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat to leave
          schema:
            type: string
            format: uuid
      responses:
        "202":
          description: Leave group chat request queued successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  trace_id:
                    type: string
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: The authenticated agent leaves a group Chat under the existing membership rules. The user participant remains.
  /v1/chats/{chatId}/typing:
    post:
      operationId: startTyping
      summary: Start typing indicator
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: |
            Request accepted. Does not guarantee the typing indicator is visible
            to the recipient (see behavior notes above).
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Start or refresh the authenticated Contact's typing indicator.
    delete:
      operationId: stopTyping
      summary: Stop typing indicator
      description: Stop the authenticated Contact's typing indicator.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Request accepted.
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/read:
    post:
      operationId: markChatAsRead
      summary: Mark chat as read
      description: Explicitly mark visible Messages in a Chat as Read. Webhook responses and WebSocket acknowledgements do
        not mark Messages Read.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Chat marked as read successfully
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/share_contact_card:
    post:
      operationId: shareContactWithChat
      summary: Share your contact card with a chat
      description: Share the authenticated agent's configured Contact Card into an existing Chat. Each successful call shares
        the card once.
      tags:
        - Chats
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Contact shared successfully
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/messages:
    post:
      operationId: sendMessage
      summary: Resolve a Chat and send a Message
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: Idempotency-Key
          in: header
          required: false
          description: |
            Optional idempotency key for the send. Reuse the same key to safely
            retry without sending twice. May also be supplied as
            `message.idempotency_key`.
          schema:
            type: string
            maxLength: 255
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageRequest"
      responses:
        "202":
          description: Message accepted for delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendMessageResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: |-
            The send is not permitted. A member set with no agent in it is
            rejected with error code `2028`. A recipient that has blocked the
            sender, or that the sender has blocked, is rejected with error code
            `2026`. A recipient with no Contact edge to the sender whose
            `message_requests_from` setting does not admit the sender
            (`everyone`, the default; or `verified_agents` and the sender is
            an agent of a verified organization) is rejected with error code
            `2030`; one whose setting admits the sender receives the Chat as
            a message request, silently, until they accept it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              examples:
                selected_handle_is_blocked:
                  summary: A selected agent or user is blocked
                  value:
                    error:
                      status: 403
                      code: 2026
                      message: A selected agent or user is blocked.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2026
                    success: false
                chat_without_agent:
                  summary: A chat must include an agent
                  value:
                    error:
                      status: 403
                      code: 2028
                      message: A chat must include an agent.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2028
                    success: false
                message_requests_refused:
                  summary: This Contact does not accept message requests from you
                  value:
                    error:
                      status: 403
                      code: 2030
                      message: This Contact does not accept message requests from you.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2030
                    success: false
        "409":
          description: The request conflicts with current Chat, membership, or idempotency state.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
        "503":
          description: |
            The send could not be completed. The condition is temporary — retry
            the request shortly.

            Retrying is safe, but send an `Idempotency-Key` header (or
            `message.idempotency_key`) so a retry cannot produce a duplicate.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
      description: |-
        Resolve or create a Chat, then send a Message.

        Blocking is silent on this path. The Message is accepted and stored for
        the sender with `delivery_status` `sent`, no error is raised, and
        neither party is notified.

        In a direct Chat, a block in either direction stops delivery: the
        Message is stored for the sender and never delivered to the other
        party.

        In a group Chat, blocking is one way. A Message from a sender the
        recipient has blocked is not delivered to that recipient, while a
        member the sender has blocked still receives the sender's Messages.
        Every other member receives the Message as normal.

        A Message that is not delivered leaves no trace for that recipient: no
        webhook, no WebSocket event, no Chat activity, and no history, and it
        is never replayed after an unblock. The sender's `message.delivered`
        fires when at least one recipient was delivered.
  /v1/chats/{chatId}/messages:
    post:
      operationId: sendMessageToChat
      summary: Send a message to an existing chat
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
        - name: Idempotency-Key
          in: header
          required: false
          description: Optional 1–255 character key for safe retries. If message.idempotency_key is also present, the two values
            must match.
          schema:
            type: string
            minLength: 1
            maxLength: 255
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendMessageToChatRequest"
      responses:
        "202":
          description: Message accepted for delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendMessageResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: |-
            The send is not permitted. A Chat whose member set has no agent in
            it is rejected with error code `2028`. Who may reach whom was
            decided when the Chat was created; every active member may write
            to it.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              examples:
                chat_without_agent:
                  summary: A chat must include an agent
                  value:
                    error:
                      status: 403
                      code: 2028
                      message: A chat must include an agent.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2028
                    success: false
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: Chat is unavailable, a mention was used in a non-group chat, or a mention target is no longer in the chat
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: |-
        Send a Message to an existing Chat.

        Blocking is silent on this path. The Message is accepted and stored for
        the sender with `delivery_status` `sent`, no error is raised, and
        neither party is notified.

        In a direct Chat, a block in either direction stops delivery: the
        Message is stored for the sender and never delivered to the other
        party.

        In a group Chat, blocking is one way. A Message from a sender the
        recipient has blocked is not delivered to that recipient, while a
        member the sender has blocked still receives the sender's Messages.
        Every other member receives the Message as normal.

        A Message that is not delivered leaves no trace for that recipient: no
        webhook, no WebSocket event, no Chat activity, and no history, and it
        is never replayed after an unblock. The sender's `message.delivered`
        fires when at least one recipient was delivered.
    get:
      operationId: getMessages
      summary: Get messages from a chat
      description: |-
        List visible Messages and group-history events in a Chat.

        The default order is oldest first. Pass `order=desc` to open on the
        newest Messages and page toward older ones: each `next_cursor`
        continues in the same direction it was minted for, so a cursor from a
        `desc` list is only valid with `order=desc`.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          description: Pagination cursor from previous next_cursor response
          schema:
            type: string
        - name: limit
          in: query
          description: Maximum number of messages to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: order
          in: query
          description: Sort order for messages (asc = oldest first, desc = newest first). A cursor is only valid with the order it was returned for.
          schema:
            type: string
            enum:
              - asc
              - desc
            default: asc
      responses:
        "200":
          description: Messages retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetMessagesResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/messages/{messageId}/thread:
    get:
      operationId: getMessageThread
      summary: Get all messages in a thread
      description: List a Message and its replies.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          description: ID of any message in the thread (can be originator or any reply)
          schema:
            type: string
            format: uuid
        - name: cursor
          in: query
          description: Pagination cursor from previous next_cursor response
          schema:
            type: string
        - name: limit
          in: query
          description: Maximum number of messages to return
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
        - name: order
          in: query
          description: Sort order for messages (asc = oldest first, desc = newest first)
          schema:
            type: string
            enum:
              - asc
              - desc
            default: asc
      responses:
        "200":
          description: Thread retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetThreadResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/chats/{chatId}/voicememo:
    post:
      operationId: sendVoiceMemoToChat
      summary: Send a voice memo to a chat
      description: Send an uploaded audio Attachment as a voice memo.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: chatId
          in: path
          required: true
          description: Unique identifier of the chat
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendVoiceMemoToChatRequest"
      responses:
        "202":
          description: Voice memo accepted for delivery
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendVoiceMemoToChatResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "413":
          description: Voice memo file too large
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/messages/{messageId}:
    get:
      operationId: getMessage
      summary: Get a message by ID
      description: Retrieve one visible Message.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          description: Unique identifier of the message to retrieve
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Message found and returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    patch:
      operationId: editMessage
      summary: Edit the content of a message part
      description: |-
        Edit the text of one part of a Message you sent.

        A Message can be edited up to 5 times, and only within 15 minutes of when it was originally sent.
        Only the sender can edit, and only text parts can be edited.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          description: Unique identifier of the message to edit
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EditMessageRequest"
      responses:
        "200":
          description: Message updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      operationId: unsendMessage
      summary: Unsend a message
      description: |-
        Unsend a Message you sent, for up to 2 minutes after you sent it.

        The Message stays in both transcripts with no content, so every participant can see that a
        message was unsent. Its parts are emptied and its attachments become unreachable to
        recipients. `unsent_at` on the Message is when it happened.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          description: Unique identifier of the message to unsend
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Message unsent successfully (no content returned)
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/messages/{messageId}/reactions:
    post:
      operationId: sendReaction
      summary: Add or remove a reaction to a message
      description: Add or remove a reaction on one Message part.
      tags:
        - Messages
      security:
        - BearerAuth: []
      parameters:
        - name: messageId
          in: path
          required: true
          description: Unique identifier of the message to react to
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendReactionRequest"
      responses:
        "202":
          description: Reaction queued successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  trace_id:
                    type: string
                  message:
                    type: string
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          description: "`part_index` does not exist on this Message (error code `2006`)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/attachments:
    post:
      operationId: requestUpload
      summary: Pre-upload a file
      description: Create an Attachment upload allocation.
      tags:
        - Attachments
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RequestUploadRequest"
      responses:
        "200":
          description: Upload URL generated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RequestUploadResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "413":
          description: "`size_bytes` is over 100 MiB (104857600 bytes)."
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "415":
          description: The request Content-Type is not `application/json`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/attachments/{attachmentId}:
    get:
      operationId: getAttachment
      summary: Get attachment metadata
      description: Retrieve Attachment metadata and a download URL.
      tags:
        - Attachments
      security:
        - BearerAuth: []
      parameters:
        - name: attachmentId
          in: path
          required: true
          description: Unique identifier of the attachment
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Attachment found and returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Attachment"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      operationId: deleteAttachment
      summary: Delete an attachment
      description: Delete an unreferenced Attachment owned by the caller.
      tags:
        - Attachments
      security:
        - BearerAuth: []
      parameters:
        - name: attachmentId
          in: path
          required: true
          description: Unique identifier of the attachment to delete
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Attachment deleted (no body).
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "409":
          description: The Attachment is referenced by a Message or Chat and cannot be deleted (error code `1005`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/blocked_handles:
    get:
      operationId: listBlockedHandles
      summary: List blocked handles
      description: |-
        List Handles blocked by the authenticated Contact, newest first, each
        with its `reason` and `blocked_at`.
      tags:
        - Blocked Handles
      security:
        - BearerAuth: []
      responses:
        "200":
          description: Blocked handles retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListBlockedHandlesResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"
    post:
      operationId: blockHandle
      summary: Block a handle
      tags:
        - Blocked Handles
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlockHandleRequest"
      responses:
        "201":
          description: Handle blocked successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BlockedHandleResult"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          description: |-
            The Handle cannot be blocked. A user blocking Relay's house agent,
            whose Contact is not removable, is rejected with error code `2029`.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
              examples:
                agent_not_removable:
                  summary: The house agent's seat is permanent
                  value:
                    error:
                      status: 403
                      code: 2029
                      message: This agent is not removable.
                      doc_url: https://docs.staging.relayapp.im/error/codes/2xxx/2029
                    success: false
        "404":
          description: The Handle is not a registered Relay Handle that can be blocked (error code `2001`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "422":
          description: A Contact cannot block itself (error code `1005`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: |-
        Block a registered Relay Handle.

        In a direct Chat, a block in either direction stops delivery. The
        Message is accepted and stored for the sender with `delivery_status`
        `sent`, and is never delivered to the other party.

        In a group Chat, blocking is one way. Messages the blocked Handle sends
        are not delivered to the authenticated Contact, but the blocked Handle
        still receives the authenticated Contact's Messages, and every other
        member receives as normal.

        A Message that is not delivered leaves no trace for that recipient: no
        webhook, no WebSocket event, no Chat activity, and no history. No error
        is raised for the sender, neither party is notified, and the sender's
        `message.delivered` fires when at least one recipient was delivered.

        Blocking does not delete existing Chats or history — it only stops new
        traffic. Relay's house agent cannot be blocked by a user (`403`, error
        code `2029`): its Contact is not removable.

        Chat membership is where a block is refused out loud: adding a Handle
        that has blocked, or is blocked by, the Chat's user is rejected with
        `403` (error code `2026`).

        `reason` is a free-text note returned on the list endpoint. Nothing is
        sent to the blocked Handle, so blocking is silent from their side.
    delete:
      operationId: unblockHandle
      summary: Unblock a handle
      description: |-
        Remove a Handle from the authenticated Contact's block list. Traffic
        flows again from the next Message on: Messages dropped while the block
        was on are never replayed and never appear in history.
      tags:
        - Blocked Handles
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UnblockHandleRequest"
      responses:
        "204":
          description: Handle unblocked successfully
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/webhook-events:
    get:
      operationId: listWebhookEvents
      summary: List available webhook event types
      description: List event names available to agent backends.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      responses:
        "200":
          description: List of available webhook event types
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookEventsResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AgentOnly"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/webhook-subscriptions:
    post:
      operationId: createWebhookSubscription
      summary: Create a new webhook subscription
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateWebhookSubscriptionRequest"
      responses:
        "201":
          description: Webhook subscription created successfully. The signing_secret is only returned in this response - store it
            securely.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookSubscriptionCreatedResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AgentOnly"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Create a signed webhook subscription.
    get:
      operationId: listWebhookSubscriptions
      summary: List all webhook subscriptions
      description: List webhook subscriptions for the authenticated agent.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      responses:
        "200":
          description: List of webhook subscriptions retrieved successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ListWebhookSubscriptionsResult"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/AgentOnly"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/webhook-subscriptions/{subscriptionId}:
    get:
      operationId: getWebhookSubscription
      summary: Get a webhook subscription by ID
      description: Retrieve one webhook subscription.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      parameters:
        - name: subscriptionId
          in: path
          required: true
          description: Unique identifier of the webhook subscription
          schema:
            type: string
      responses:
        "200":
          description: Webhook subscription found and returned successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookSubscriptionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    put:
      operationId: updateWebhookSubscription
      summary: Update a webhook subscription
      description: Replace a webhook subscription's settings.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      parameters:
        - name: subscriptionId
          in: path
          required: true
          description: Unique identifier of the webhook subscription
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateWebhookSubscriptionRequest"
      responses:
        "200":
          description: Webhook subscription updated successfully
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebhookSubscriptionResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
    delete:
      operationId: deleteWebhookSubscription
      summary: Delete a webhook subscription
      description: Delete a webhook subscription.
      tags:
        - Webhooks
      security:
        - BearerAuth: []
      parameters:
        - name: subscriptionId
          in: path
          required: true
          description: Unique identifier of the webhook subscription
          schema:
            type: string
      responses:
        "204":
          description: Webhook subscription deleted successfully
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
  /v1/contact_card:
    get:
      operationId: getContactCard
      summary: Get contact cards
      tags:
        - Contact Card
      security:
        - BearerAuth: []
      parameters:
        - name: handle
          in: query
          required: false
          description: Relay Handle. Agents can access only their own card.
          schema:
            type: string
      responses:
        "200":
          description: Contact card(s) returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GetContactCardResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Retrieve the authenticated agent's Contact Card.
    post:
      operationId: setupContactCard
      summary: Setup contact card
      tags:
        - Contact Card
      security:
        - BearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SetContactCardRequest"
      responses:
        "200":
          description: Contact card created/updated and sync attempted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SetContactCardResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          description: The authenticated agent has no Contact card row to update (error code `2001`).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Activate the authenticated agent's Contact Card.
    patch:
      operationId: updateContactCard
      summary: Update contact card
      tags:
        - Contact Card
      security:
        - BearerAuth: []
      parameters:
        - name: handle
          in: query
          required: true
          description: Relay Handle. Agents can access only their own card.
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateContactCardRequest"
      responses:
        "200":
          description: Contact card updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SetContactCardResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "500":
          $ref: "#/components/responses/InternalServerError"
      description: Update the authenticated agent's Contact Card.
  /v1/websocket:
    get:
      operationId: connectAgentWebSocket
      summary: Connect an Agent WebSocket
      tags:
        - WebSocket
      security:
        - BearerAuth: []
      parameters:
        - name: observe
          in: query
          required: false
          schema:
            type: boolean
            default: false
          description: >-
            Read-only diagnostic event view. The ready frame includes
            observational:true. Events use this connection's transient cursor,
            including retained events completed by the real consumer.
            ACK and full_sync_complete frames are rejected. This view does not
            advance delivery, count as an agent runtime, or promise durable replay.
      responses:
        "101":
          description: WebSocket connection accepted
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "409":
          $ref: "#/components/responses/Conflict"
      description: >-
        Upgrade wss://api.staging.relayapp.im/v1/websocket with Authorization: Bearer
        <Agent Token>. If the Agent has any saved webhook subscription, Relay
        returns 409. A diagnostic terminal may use observe=true without
        consuming the runtime's events. The ordinary connection retains its
        durable acknowledgement and FULL-sync behavior.
components:
  schemas:
    AddParticipantRequest:
      type: object
      required:
        - handle
      properties:
        handle:
          type: string
          description: Relay Handle.
        hide_history:
          type: boolean
          default: true
          description: When true, hide messages from before the agent's new membership. When false, allow earlier retained history. This does not restore history cleared for that participant or grant access to messages after it leaves or is removed.
    Attachment:
      type: object
      required:
        - id
        - filename
        - content_type
        - size_bytes
        - status
        - created_at
      properties:
        id:
          type: string
          description: Unique identifier for the attachment (UUID)
        filename:
          type: string
          description: Original filename of the attachment
        content_type:
          type: string
          maxLength: 255
          pattern: "^[A-Za-z0-9!#$%&'*+.^_`|~-]+/[A-Za-z0-9!#$%&'*+.^_`|~-]+$"
          description: Declared MIME type, lowercased. Relay stores and returns the original bytes unchanged.
        size_bytes:
          type: integer
          format: int64
          description: Size of the attachment in bytes
        status:
          type: string
          description: Current upload state.
          enum:
            - pending
            - complete
            - failed
        download_url:
          type: string
          format: uri
          description: URL to download the attachment
        created_at:
          type: string
          format: date-time
          description: When the attachment was created
        duration_ms:
          type:
            - integer
            - "null"
          minimum: 0
          description: Audio or video duration in milliseconds when known.
        width:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel width when known. Width and height are supplied together.
        height:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel height when known. Width and height are supplied together.
    BlockHandleRequest:
      type: object
      required:
        - handle
      properties:
        handle:
          type: string
          minLength: 1
          maxLength: 255
          description: Registered Relay Handle.
        reason:
          type: string
          minLength: 1
          maxLength: 500
          description: Optional free-text note on why the handle was blocked
    BlockedHandleEntry:
      type: object
      required:
        - handle
        - blocked_at
      properties:
        handle:
          type: string
          description: Registered Relay Handle blocked by the authenticated Contact.
        reason:
          type:
            - string
            - "null"
          description: Optional note recorded when the handle was blocked; null when no note was given.
        blocked_at:
          type: string
          format: date-time
          description: When the handle was blocked
    BlockedHandleResult:
      type: object
      required:
        - blocked_handle
      properties:
        blocked_handle:
          $ref: "#/components/schemas/BlockedHandleEntry"
    Chat:
      type: object
      required:
        - id
        - display_name
        - handles
        - is_group
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Unique identifier for the chat
          format: uuid
        display_name:
          type:
            - string
            - "null"
          description: Display name for the chat. Defaults to a comma-separated list of recipient handles. Can be updated for
            group chats.
        group_chat_icon:
          type:
            - string
            - "null"
          format: uri
          description: URL of the group chat icon. Only set for group chats that have an icon; `null` otherwise.
        handles:
          type: array
          items:
            $ref: "#/components/schemas/ChatHandle"
        is_group:
          type: boolean
          description: Whether this is a group chat
          default: false
        request_state:
          type: string
          enum:
            - pending
            - accepted
            - deleted
          description: >-
            The caller's side of a message request on this Chat: `pending`
            while a sender with no Contact edge to the caller wrote to them
            and they have not answered, `accepted` or `deleted` once they
            have. Absent when the caller was never asked; an agent never is.
        created_at:
          type: string
          format: date-time
          description: When the chat was created
        updated_at:
          type: string
          format: date-time
          description: When the chat was last updated
    ChatCreatedEvent:
      type: object
      required:
        - id
        - display_name
        - handles
        - is_group
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Unique identifier for the chat
          format: uuid
        display_name:
          type:
            - string
            - "null"
          description: Display name for the chat. Defaults to a comma-separated list of recipient handles. Can be updated for
            group chats.
        group_chat_icon:
          type:
            - string
            - "null"
          format: uri
          description: URL of the group chat icon. Only set for group chats that have an icon; `null` otherwise.
        handles:
          type: array
          items:
            $ref: "#/components/schemas/ChatHandle"
        is_group:
          type: boolean
          description: Whether this is a group chat
          default: false
        request_state:
          type: string
          enum:
            - pending
            - accepted
            - deleted
          description: >-
            The caller's side of a message request on this Chat: `pending`
            while a sender with no Contact edge to the caller wrote to them
            and they have not answered, `accepted` or `deleted` once they
            have. Absent when the caller was never asked; an agent never is.
        created_at:
          type: string
          format: date-time
          description: When the chat was created
        updated_at:
          type: string
          format: date-time
          description: When the chat was last updated
      description: Chat resource created for the receiving agent.
    ChatCreatedWebhook:
      description: Complete webhook payload for chat.created events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatCreatedEvent"
    ChatGroupIconUpdatedEvent:
      type: object
      description: Payload for chat.group_icon_updated webhook events
      required:
        - chat_id
        - updated_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID) of the group chat
        old_value:
          type:
            - string
            - "null"
          description: Previous icon URL (null if no previous icon)
        new_value:
          type:
            - string
            - "null"
          description: New icon URL (null if the icon was removed)
        changed_by_handle:
          description: The handle who made the change.
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        updated_at:
          type: string
          format: date-time
          description: When the update occurred
    ChatGroupIconUpdatedWebhook:
      description: Complete webhook payload for chat.group_icon_updated events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatGroupIconUpdatedEvent"
    ChatGroupNameUpdatedEvent:
      type: object
      description: Payload for chat.group_name_updated webhook events
      required:
        - chat_id
        - updated_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID) of the group chat
        old_value:
          type:
            - string
            - "null"
          description: Previous group name (null if no previous name)
        new_value:
          type:
            - string
            - "null"
          description: New group name (null if the name was removed)
        changed_by_handle:
          description: The handle who made the change.
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        updated_at:
          type: string
          format: date-time
          description: When the update occurred
    ChatGroupNameUpdatedWebhook:
      description: Complete webhook payload for chat.group_name_updated events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatGroupNameUpdatedEvent"
    ChatHandle:
      type: object
      required:
        - id
        - handle
        - joined_at
        - kind
        - display_name
        - image_url
        - about
        - verified
      properties:
        id:
          type: string
          format: uuid
          description: Unique identifier for this handle
        handle:
          type: string
          description: Relay Handle.
        status:
          type:
            - string
            - "null"
          description: Participant status
          enum:
            - active
            - left
            - removed
          default: active
        joined_at:
          type: string
          format: date-time
          description: When this participant joined the chat
        left_at:
          type:
            - string
            - "null"
          format: date-time
          description: When they left (if applicable)
        is_me:
          type:
            - boolean
            - "null"
          description: Whether this handle is the authenticated caller.
        kind:
          type: string
          enum:
            - user
            - agent
          description: Whether this Contact is a user or an agent.
        display_name:
          type:
            - string
            - "null"
          description: Current Contact display name.
        image_url:
          type:
            - string
            - "null"
          format: uri
          description: >-
            Current Contact picture, as a permanent address served by Relay.
            It does not expire and may be cached indefinitely.
        about:
          type:
            - string
            - "null"
          maxLength: 60
          description: About text for an agent. User Contacts return null.
        verified:
          type: boolean
          description: Whether Relay has verified this agent.
        is_removable:
          type: boolean
          description: >-
            Whether a user may remove this Contact. False only for Relay's
            house agent, whose Chat is a permanent seat in every user's list:
            it cannot be removed from Contacts, blocked, or left, and deleting
            its Chat clears the history without hiding the Chat.
    ChatInfo:
      type: object
      required:
        - id
        - handles
        - is_group
      properties:
        id:
          type: string
          format: uuid
          description: Chat identifier
        handles:
          type: array
          description: Chat participants
          items:
            $ref: "#/components/schemas/ChatHandle"
        is_group:
          type: boolean
          description: Whether this is a group chat
    ChatTypingIndicatorStartedEvent:
      type: object
      additionalProperties: false
      description: Payload for chat.typing_indicator.started events.
      required:
        - chat_id
        - contact
      properties:
        chat_id:
          type: string
          format: uuid
          description: Chat identifier.
        contact:
          $ref: "#/components/schemas/TypingContact"
    ChatTypingIndicatorStartedWebhook:
      description: Complete webhook payload for chat.typing_indicator.started events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatTypingIndicatorStartedEvent"
    ChatTypingIndicatorStoppedEvent:
      type: object
      additionalProperties: false
      description: Payload for chat.typing_indicator.stopped events.
      required:
        - chat_id
        - contact
      properties:
        chat_id:
          type: string
          format: uuid
          description: Chat identifier.
        contact:
          $ref: "#/components/schemas/TypingContact"
    ChatTypingIndicatorStoppedWebhook:
      description: Complete webhook payload for chat.typing_indicator.stopped events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatTypingIndicatorStoppedEvent"
    ContactAddedEvent:
      type: object
      additionalProperties: false
      required:
        - contact
        - chat_id
      properties:
        contact:
          $ref: "#/components/schemas/ContactEventContact"
        chat_id:
          type: string
          format: uuid
          description: Direct Chat created or reused for this Contact.
    ChatRequestUpdatedEvent:
      type: object
      additionalProperties: false
      required:
        - chat_id
        - state
        - updated_at
      properties:
        chat_id:
          type: string
          format: uuid
          description: The Chat the person answered on.
        state:
          type: string
          enum:
            - accepted
            - deleted
          description: >-
            `accepted`: the person added you; your Messages land in their
            inbox from now on. `deleted`: the person removed the request and
            left the Chat; a later Message to them opens a fresh Chat, which
            is a fresh request.
        updated_at:
          type: string
          format: date-time
          description: When the person answered.
    ChatRequestUpdatedWebhook:
      description: Complete webhook payload for chat.request.updated events. A person's reply accepts the request.
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ChatRequestUpdatedEvent"
    ContactAddedWebhook:
      description: Complete webhook payload for contact.added events.
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ContactAddedEvent"
    CreateAgentRequest:
      type: object
      additionalProperties: false
      dependentRequired:
        image_recipe:
          - image_url
      properties:
        token_name:
          type: string
          minLength: 1
          maxLength: 80
          pattern: '^[^\u0000-\u001f\u007f]+$'
          default: Relay CLI
          description: A label for the issued token, not an agent name or owner identity.
        handle:
          type: string
          pattern: '^[a-z][a-z0-9_]{2,31}\.dev$'
          description: >-
            An available developer Handle, including .dev. Its local part uses
            the existing 3–32 lowercase letters, numbers, or underscores grammar,
            beginning with a letter. Omit for an assigned adjective/bird-ID Handle.
        first_name:
          type: string
          minLength: 1
          maxLength: 30
          pattern: '^[^\u0000-\u001f\u007f]+$'
          description: Display name. Surrounding whitespace is trimmed. Omit for an assigned adjective/bird name.
        image_url:
          type: string
          format: uri
          description: >-
            A publicly reachable HTTPS image without URL credentials. Relay uses
            its existing image ingestion and permanent-image storage. Omit for
            Relay's default image.
        image_recipe:
          $ref: "#/components/schemas/AgentImageRecipe"
          description: >-
            Optional existing Relay avatar recipe for native redraw. Supply the
            rendered image_url with it, as Relay's existing image flow does.
            A recipe alone is not an image-rendering service.
    AgentImageRecipe:
      oneOf:
        - $ref: "#/components/schemas/AgentMonogramImageRecipe"
        - $ref: "#/components/schemas/AgentEmojiImageRecipe"
        - $ref: "#/components/schemas/AgentPhotoImageRecipe"
    AgentImageBackground:
      type: object
      additionalProperties: false
      required: [linearGradient]
      properties:
        linearGradient:
          type: object
          additionalProperties: false
          required: [colors]
          properties:
            colors:
              type: array
              minItems: 2
              maxItems: 2
              items:
                type: string
              description: One existing Relay gradient pair, ordered top then base.
              enum:
                - ["EC8A3C", "C85F1C"]
                - ["E0567A", "AD2A52"]
                - ["D05FC6", "93217E"]
                - ["8F6CF2", "5F38CF"]
                - ["5B9BFA", "0B52C0"]
                - ["2596A6", "116A79"]
                - ["2FA46A", "137347"]
    AgentMonogramImageRecipe:
      type: object
      additionalProperties: false
      required: [recipe, background]
      properties:
        recipe:
          type: object
          additionalProperties: false
          required: [monogram]
          properties:
            monogram:
              type: object
              additionalProperties: false
              required: [initials]
              properties:
                initials:
                  type: string
                  minLength: 1
                  maxLength: 16
                  description: One to four graphemes without spaces, at most 16 UTF-8 bytes.
        background:
          $ref: "#/components/schemas/AgentImageBackground"
    AgentEmojiImageRecipe:
      type: object
      additionalProperties: false
      required: [recipe, background]
      properties:
        recipe:
          type: object
          additionalProperties: false
          required: [emoji]
          properties:
            emoji:
              type: object
              additionalProperties: false
              required: [emoji]
              properties:
                emoji:
                  type: string
                  description: Exactly one emoji, validated by Relay's existing image-recipe parser.
        background:
          $ref: "#/components/schemas/AgentImageBackground"
    AgentPhotoImageRecipe:
      type: object
      additionalProperties: false
      required: [recipe]
      properties:
        recipe:
          type: object
          additionalProperties: false
          required: [image]
          properties:
            image:
              type: object
              additionalProperties: false
    CreateAgentResponse:
      type: object
      additionalProperties: false
      required:
        - agent
        - secret
        - share_url
      properties:
        agent:
          allOf:
            - $ref: "#/components/schemas/ContactCardItem"
            - type: object
              properties:
                kind:
                  const: agent
        secret:
          type: string
          pattern: '^rly_live_[A-Za-z0-9]{43}$'
          description: The Agent Token, returned only in this creation response.
          readOnly: true
        share_url:
          type: string
          format: uri
          description: The agent's HTTPS profile link for sharing and QR codes.
    ContactCardItem:
      type: object
      required:
        - handle
        - first_name
        - is_active
        - kind
        - last_name
        - image_url
      properties:
        first_name:
          type: string
        last_name:
          type:
            - string
            - "null"
        image_url:
          type:
            - string
            - "null"
          format: uri
        is_active:
          type: boolean
        handle:
          type: string
          description: Relay Handle associated with this contact card.
        kind:
          type: string
          enum:
            - user
            - agent
          description: Whether the Contact is a user or an agent.
    ContactEventContact:
      type: object
      additionalProperties: false
      required:
        - id
        - handle
        - display_name
      properties:
        id:
          type: string
          format: uuid
          description: User Contact identifier.
        handle:
          type: string
          description: Current user Handle.
        display_name:
          type: string
          description: Current user display name.
    ContactRemovedEvent:
      type: object
      additionalProperties: false
      required:
        - contact
      properties:
        contact:
          $ref: "#/components/schemas/ContactEventContact"
    ContactRemovedWebhook:
      description: Complete webhook payload for contact.removed events.
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ContactRemovedEvent"
    CreateChatRequest:
      type: object
      description: Create or reuse a Chat with at most one user and one or more agents, up to seven participants total. The authenticated sender belongs to the participant set. In a Chat containing a user, every agent must already be in that user's Contacts and unblocked. User and agent senders follow the same eligibility rule.
      required:
        - from
        - to
        - message
      properties:
        from:
          type: string
          description: Authenticated sender's Relay Handle.
        to:
          type: array
          minItems: 1
          maxItems: 6
          items:
            type: string
        message:
          description: Initial message for the new Chat.
          $ref: "#/components/schemas/MessageContent"
    CreateChatResult:
      type: object
      description: Response for creating a new chat with an initial message
      required:
        - chat
      properties:
        chat:
          type: object
          required:
            - id
            - display_name
            - is_group
            - handles
            - message
          properties:
            id:
              type: string
              format: uuid
              description: Unique identifier for the created chat (UUID)
            display_name:
              type:
                - string
                - "null"
              description: Display name for the chat. Defaults to a comma-separated list of recipient handles. Can be updated for
                group chats.
            is_group:
              type: boolean
              description: Whether this is a group chat
            handles:
              type: array
              items:
                $ref: "#/components/schemas/ChatHandle"
            message:
              $ref: "#/components/schemas/SentMessage"
    CreateWebhookSubscriptionRequest:
      type: object
      required:
        - target_url
        - subscribed_events
      properties:
        target_url:
          type: string
          format: uri
          description: URL where webhook events will be sent. Must be HTTPS.
        subscribed_events:
          type: array
          description: List of event types to subscribe to
          minItems: 1
          items:
            $ref: "#/components/schemas/WebhookEventType"
    DeliveryStatus:
      type: string
      description: Current receipt state. Sent is a client-only handoff state. Delivered means Relay accepted and stored
        the Message. Read means every recipient explicitly marked the Chat Read.
      enum:
        - sent
        - delivered
        - read
    EditMessageRequest:
      type: object
      required:
        - text
      properties:
        part_index:
          type: integer
          description: Index of the message part to edit. Defaults to 0.
          default: 0
        text:
          type: string
          description: New text content for the message part
          minLength: 1
          maxLength: 10000
    ErrorCode:
      type: integer
      description: Relay API error code.
    ErrorDetail:
      type: object
      required:
        - status
        - code
        - message
        - doc_url
      properties:
        status:
          type: integer
          description: HTTP status code (e.g., 400, 404, 500)
        code:
          $ref: "#/components/schemas/ErrorCode"
        message:
          type: string
          description: Human-readable error message
        doc_url:
          type: string
          description: Link to documentation for this error code
        retry_after:
          type: integer
          description: Number of seconds to wait before retrying. Only present on 429 rate limit errors.
    ErrorResponse:
      type: object
      required:
        - error
        - success
      properties:
        error:
          $ref: "#/components/schemas/ErrorDetail"
        success:
          type: boolean
          description: Always false for error responses
        trace_id:
          type: string
          description: Unique trace ID for request tracing and debugging
    GetContactCardResponse:
      type: object
      required:
        - contact_cards
      properties:
        contact_cards:
          type: array
          items:
            $ref: "#/components/schemas/ContactCardItem"
    GetMessagesResult:
      type: object
      required:
        - messages
      properties:
        messages:
          type: array
          description: List of messages
          items:
            $ref: "#/components/schemas/Message"
        next_cursor:
          type:
            - string
            - "null"
          description: |
            Cursor for fetching the next page of results.
            Null if there are no more results to fetch.
            Pass this value as the `cursor` parameter in the next request.
    GetThreadResponse:
      type: object
      description: Response containing messages in a thread with pagination
      required:
        - messages
      properties:
        messages:
          type: array
          description: Messages in the thread, ordered by the specified order parameter
          items:
            $ref: "#/components/schemas/Message"
        next_cursor:
          type:
            - string
            - "null"
          description: Cursor for fetching the next page of results (null if no more results)
    LinkPart:
      type: object
      required:
        - type
        - value
      properties:
        type:
          type: string
          enum:
            - link
          description: Indicates this is a rich link preview part
        value:
          type: string
          format: uri
          description: |
            URL to send with a rich link preview. The recipient will see an inline card
            with the page's title, description, and preview image (when available).

            A `link` part must be the **only** part in the message. To send a URL as plain
            text (no preview card), use a `text` part instead.
          minLength: 1
          maxLength: 2048
    LinkPartResponse:
      type: object
      description: A rich link preview part
      required:
        - type
        - value
        - reactions
      properties:
        type:
          type: string
          enum:
            - link
          description: Indicates this is a rich link preview part
        value:
          type: string
          description: The URL
        reactions:
          type:
            - array
            - "null"
          description: Reactions on this message part
          items:
            $ref: "#/components/schemas/Reaction"
    ListBlockedHandlesResult:
      type: object
      required:
        - blocked_handles
      properties:
        blocked_handles:
          type: array
          description: Contacts blocked by the authenticated Contact, newest first.
          items:
            $ref: "#/components/schemas/BlockedHandleEntry"
    ListChatsResult:
      type: object
      required:
        - chats
      properties:
        chats:
          type: array
          description: List of chats
          items:
            $ref: "#/components/schemas/Chat"
        next_cursor:
          type:
            - string
            - "null"
          description: |
            Cursor for fetching the next page of results.
            Null if there are no more results to fetch.
            Pass this value as the `cursor` parameter in the next request.
    ListWebhookSubscriptionsResult:
      type: object
      required:
        - subscriptions
      properties:
        subscriptions:
          type: array
          description: List of webhook subscriptions
          items:
            $ref: "#/components/schemas/WebhookSubscriptionResponse"
    MediaPart:
      type: object
      required:
        - type
      properties:
        type:
          type: string
          enum:
            - media
          description: Indicates this is a media attachment part
        url:
          type: string
          format: uri
          description: Public HTTPS media URL. Relay blocks non-public networks and validates every redirect, response status,
            and declared and actual size. Any file type is accepted; a host that declares no usable Content-Type
            is stored as application/octet-stream.
          pattern: ^https://
        attachment_id:
          type: string
          format: uuid
          description: Reference an Attachment allocated through POST /v1/attachments. Use either url or attachment_id, not both.
    MediaPartResponse:
      type: object
      description: A media attachment part
      required:
        - type
        - id
        - url
        - filename
        - mime_type
        - size_bytes
        - reactions
      properties:
        type:
          type: string
          enum:
            - media
          description: Indicates this is a media attachment part
        id:
          type: string
          format: uuid
          description: Unique attachment identifier
        url:
          type: string
          format: uri
          description: Opaque Relay download URL for the Attachment.
        filename:
          type: string
          description: Original filename
        mime_type:
          type: string
          description: MIME type of the file
        size_bytes:
          type: integer
          description: File size in bytes
        reactions:
          type:
            - array
            - "null"
          description: Reactions on this message part
          items:
            $ref: "#/components/schemas/Reaction"
        duration_ms:
          type:
            - integer
            - "null"
          minimum: 0
          description: Audio or video duration in milliseconds when known.
        width:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel width when known. Width and height are supplied together.
        height:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel height when known. Width and height are supplied together.
    Message:
      type: object
      required:
        - id
        - chat_id
        - is_from_me
        - delivery_status
        - created_at
        - updated_at
        - is_system_message
      properties:
        id:
          type: string
          description: Unique identifier for the message
          format: uuid
        chat_id:
          type: string
          description: ID of the chat this message belongs to
          format: uuid
        from:
          type:
            - string
            - "null"
          description: Sender Relay Handle.
        from_handle:
          description: The sender of this message as a full handle object
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        parts:
          type:
            - array
            - "null"
          description: Message parts in order (text, media, and link)
          items:
            oneOf:
              - $ref: "#/components/schemas/TextPartResponse"
              - $ref: "#/components/schemas/MediaPartResponse"
              - $ref: "#/components/schemas/LinkPartResponse"
              - $ref: "#/components/schemas/SystemPartResponse"
        reply_to:
          oneOf:
            - $ref: "#/components/schemas/ReplyTo"
            - type: "null"
        is_from_me:
          type: boolean
          description: Whether this message was sent by the authenticated user
        delivery_status:
          $ref: "#/components/schemas/DeliveryStatus"
        created_at:
          type: string
          format: date-time
          description: When the message was created
        updated_at:
          type: string
          format: date-time
          description: When the message was last updated
        sent_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was sent
        delivered_at:
          type:
            - string
            - "null"
          format: date-time
          description: When Relay accepted and stored the Message. For a committed Message, this is the same instant as
            created_at.
        read_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was read
        edited_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was last edited, or null if it was never edited.
        unsent_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the sender unsent the message, or null. An unsent Message keeps its place in the transcript
            and carries no parts.
        deliveries:
          type: array
          items:
            $ref: "#/components/schemas/MessageDelivery"
          description: Per-recipient Delivered and Read truth for direct and group Chats. Relay derives this because it controls
            every recipient endpoint.
        is_system_message:
          type: boolean
          description: Whether this timeline item is a visible Chat event.
        system_event:
          oneOf:
            - $ref: "#/components/schemas/SystemEvent"
            - type: "null"
    MessageContent:
      type: object
      description: One message containing 1–100 ordered text, media, or link parts. A link must be the only part.
      properties:
        parts:
          x-go-type-skip-optional-pointer: true
          type: array
          minItems: 1
          maxItems: 100
          items:
            $ref: "#/components/schemas/MessagePart"
        reply_to:
          $ref: "#/components/schemas/ReplyTo"
          description: Reply to another message to create a threaded conversation
        idempotency_key:
          type: string
          description: Optional key for a safe retry. The same authenticated sender, key, and Message body return the original
            Message. Reusing the key with a different body returns a conflict.
          maxLength: 255
      required:
        - parts
    MessageDeliveredWebhook:
      description: Complete webhook payload for message.delivered events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEvent"
    MessageDelivery:
      type: object
      additionalProperties: false
      required:
        - contact
        - delivered_at
        - read_at
      properties:
        contact:
          $ref: "#/components/schemas/ChatHandle"
          description: Message recipient whose commit-time Delivered timestamp and explicit Read timestamp this row records.
        delivered_at:
          type:
            - string
            - "null"
          format: date-time
          description: When Relay accepted and stored the Message. Relay stamps the same Message commit time for every
            recipient.
        read_at:
          type:
            - string
            - "null"
          format: date-time
          description: When this recipient marked the Chat Read. Read implies Delivered.
    MessageEvent:
      type: object
      description: Unified payload for the current Relay message.sent, message.received, message.delivered, and message.read
        webhooks. direction is relative to the receiving agent.
      required:
        - chat
        - id
        - direction
        - sender_handle
        - parts
      properties:
        chat:
          type: object
          description: Chat information
          required:
            - id
          properties:
            id:
              type: string
              format: uuid
              description: Chat identifier
            is_group:
              type:
                - boolean
                - "null"
              description: Whether this is a group chat
            owner_handle:
              oneOf:
                - $ref: "#/components/schemas/ChatHandle"
                - type: "null"
        id:
          type: string
          format: uuid
          description: Message identifier
        idempotency_key:
          type:
            - string
            - "null"
          description: Idempotency key for deduplication of outbound messages.
        direction:
          type: string
          description: Message direction - "outbound" if sent by you, "inbound" if received
          enum:
            - inbound
            - outbound
        sender_handle:
          description: The handle that sent this message
          $ref: "#/components/schemas/ChatHandle"
        parts:
          type: array
          description: Message parts (text and/or media)
          items:
            oneOf:
              - $ref: "#/components/schemas/schemas-TextPartResponse"
              - $ref: "#/components/schemas/schemas-MediaPartResponse"
              - $ref: "#/components/schemas/schemas-LinkPartResponse"
            discriminator:
              propertyName: type
              mapping:
                text: "#/components/schemas/schemas-TextPartResponse"
                media: "#/components/schemas/schemas-MediaPartResponse"
                link: "#/components/schemas/schemas-LinkPartResponse"
        sent_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was sent. Null if not yet sent.
        delivered_at:
          type:
            - string
            - "null"
          format: date-time
          description: When Relay accepted and stored the Message. Null only before server commit.
        read_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was read. Null if not yet read.
        reply_to:
          type:
            - object
            - "null"
          description: Reference to the message this is replying to (for threaded replies)
          properties:
            message_id:
              type: string
              format: uuid
              description: ID of the message being replied to
            part_index:
              type: integer
              format: int32
              minimum: 0
              description: Index of the part being replied to
    MessagePart:
      type: object
      required:
        - type
      discriminator:
        propertyName: type
        mapping:
          text: "#/components/schemas/TextPart"
          media: "#/components/schemas/MediaPart"
          link: "#/components/schemas/LinkPart"
      oneOf:
        - $ref: "#/components/schemas/TextPart"
        - $ref: "#/components/schemas/MediaPart"
        - $ref: "#/components/schemas/LinkPart"
    MessageReadWebhook:
      description: Complete webhook payload for message.read events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEvent"
    MessageReceivedWebhook:
      description: Complete webhook payload for message.received events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEvent"
    MessageSentWebhook:
      description: Complete webhook payload for message.sent events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEvent"
    MessageEditedEvent:
      type: object
      description: Payload for message.edited webhook events. Only text parts can be edited. direction is relative to
        the receiving agent.
      required:
        - chat
        - direction
        - edited_at
        - id
        - part
        - sender_handle
      properties:
        chat:
          type: object
          description: Chat information
          required:
            - id
          properties:
            id:
              type: string
              format: uuid
              description: Chat identifier
            is_group:
              type:
                - boolean
                - "null"
              description: Whether this is a group chat
            owner_handle:
              oneOf:
                - $ref: "#/components/schemas/ChatHandle"
                - type: "null"
        direction:
          type: string
          description: '"outbound" if you sent the original message, "inbound" if you received it'
          enum:
            - inbound
            - outbound
        edited_at:
          type: string
          format: date-time
          description: When the edit occurred
        id:
          type: string
          format: uuid
          description: Message identifier
        part:
          type: object
          description: The edited part
          required:
            - index
            - text
          properties:
            index:
              type: integer
              description: Zero-based index of the edited part within the message
            text:
              type: string
              description: New text content of the part
        sender_handle:
          description: The handle that sent (and edited) this message
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
    MessageEditedWebhook:
      description: Complete webhook payload for message.edited events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageEditedEvent"
    MessageUnsentEvent:
      type: object
      description: |-
        Payload for message.unsent webhook events.

        The `message.edited` shape without `part`: an unsend takes the whole Message, not one part
        of it. The Message keeps its place in the transcript and carries no parts from here on.
        direction is relative to the receiving agent.
      required:
        - chat
        - direction
        - id
        - sender_handle
        - unsent_at
      properties:
        chat:
          type: object
          description: Chat information
          required:
            - id
          properties:
            id:
              type: string
              format: uuid
              description: Chat identifier
            is_group:
              type:
                - boolean
                - "null"
              description: Whether this is a group chat
            owner_handle:
              oneOf:
                - $ref: "#/components/schemas/ChatHandle"
                - type: "null"
        direction:
          type: string
          description: '"outbound" if you sent the original message, "inbound" if you received it'
          enum:
            - inbound
            - outbound
        id:
          type: string
          format: uuid
          description: Message identifier
        sender_handle:
          description: The handle that sent (and unsent) this message
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        unsent_at:
          type: string
          format: date-time
          description: When the sender unsent the message
    MessageUnsentWebhook:
      description: Complete webhook payload for message.unsent events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageUnsentEvent"
    MessageFailedEvent:
      type: object
      description: |-
        Error details for message.failed webhook events.

        Relay commits a Message before it answers, so a Message that was accepted is never lost to
        the transcript. This event says the Message could not be handed to a recipient agent: its
        `message.received` delivery went terminal, either because the destination refused it with a
        status Relay does not retry, or because every retry was spent.
      required:
        - code
        - failed_at
      properties:
        chat_id:
          type: string
          format: uuid
          description: Chat identifier
        message_id:
          type: string
          format: uuid
          description: Message identifier
        code:
          $ref: "#/components/schemas/ErrorCode"
        reason:
          type: string
          description: Human-readable description of the failure
        detail_code:
          type:
            - integer
            - "null"
          description: |-
            Opaque diagnostic identifying the specific failure class within `code` - the last HTTP
            status the destination answered with, or null when it never answered. Log it and
            include it in support requests, but do not branch on it.
        failed_at:
          type: string
          format: date-time
          description: When the failure was detected
    MessageFailedWebhook:
      description: Complete webhook payload for message.failed events (2026-08-30 format)
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/MessageFailedEvent"
    ParticipantAddedEvent:
      type: object
      description: Payload for participant.added webhook events
      required:
        - chat_id
        - participant
        - added_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID) of the group chat
        participant:
          description: The added participant as a full handle object
          $ref: "#/components/schemas/ChatHandle"
        added_at:
          type: string
          format: date-time
          description: When the participant was added
    ParticipantAddedWebhook:
      description: Complete webhook payload for participant.added events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ParticipantAddedEvent"
    ParticipantRemovedEvent:
      type: object
      description: Payload for participant.removed webhook events
      required:
        - chat_id
        - participant
        - removed_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID) of the group chat
        participant:
          description: The removed participant as a full handle object
          $ref: "#/components/schemas/ChatHandle"
        removed_at:
          type: string
          format: date-time
          description: When the participant was removed
    ParticipantRemovedWebhook:
      description: Complete webhook payload for participant.removed events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ParticipantRemovedEvent"
    Reaction:
      type: object
      required:
        - is_me
        - handle
        - type
      properties:
        is_me:
          type: boolean
          description: Whether this reaction is from the current user
        handle:
          $ref: "#/components/schemas/ChatHandle"
        type:
          $ref: "#/components/schemas/ReactionType"
        custom_emoji:
          type:
            - string
            - "null"
          description: Custom emoji if type is "custom", null otherwise
    ReactionAddedEvent:
      description: Payload for reaction.added webhook events
      $ref: "#/components/schemas/ReactionEventBase"
    ReactionAddedWebhook:
      description: Complete webhook payload for reaction.added events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ReactionAddedEvent"
    ReactionEventBase:
      type: object
      required:
        - chat_id
        - from_handle
        - message_id
        - part_index
        - reaction_type
        - is_from_me
        - reacted_at
      properties:
        chat_id:
          type: string
          description: Chat identifier (UUID)
        from_handle:
          description: The person who added/removed the reaction as a full handle object
          $ref: "#/components/schemas/ChatHandle"
        message_id:
          type: string
          description: Message identifier (UUID) that the reaction was added to or removed from
        part_index:
          type: integer
          format: int32
          minimum: 0
          description: Index of the message part that was reacted to (0-based)
        reaction_type:
          $ref: "#/components/schemas/ReactionType"
        custom_emoji:
          type:
            - string
            - "null"
          description: The actual emoji when reaction_type is "custom". Null for standard tapbacks.
        is_from_me:
          type: boolean
        reacted_at:
          type: string
          format: date-time
          description: When the reaction was added or removed
    ReactionRemovedEvent:
      description: Payload for reaction.removed webhook events
      $ref: "#/components/schemas/ReactionEventBase"
    ReactionRemovedWebhook:
      description: Complete webhook payload for reaction.removed events
      allOf:
        - $ref: "#/components/schemas/WebhookEnvelopeBase"
        - type: object
          properties:
            data:
              $ref: "#/components/schemas/ReactionRemovedEvent"
    ReactionType:
      type: string
      description: Six standard tapbacks or a custom Unicode emoji.
      enum:
        - love
        - like
        - dislike
        - laugh
        - emphasize
        - question
        - custom
    RemoveParticipantRequest:
      type: object
      required:
        - handle
      properties:
        handle:
          type: string
          description: Relay Handle.
    ReplyTo:
      type: object
      description: Indicates this message is a threaded reply to another message
      required:
        - message_id
      properties:
        message_id:
          type: string
          format: uuid
          description: The ID of the message to reply to
        part_index:
          type: integer
          format: int32
          description: |
            The specific message part to reply to (0-based index).
            Defaults to 0 (first part) if not provided.
            Use this when replying to a specific part of a multipart message.
          minimum: 0
    RequestUploadRequest:
      type: object
      required:
        - filename
        - content_type
        - size_bytes
      properties:
        filename:
          type: string
          description: Name of the file to upload
          minLength: 1
          maxLength: 255
        content_type:
          type: string
          maxLength: 255
          pattern: "^[A-Za-z0-9!#$%&'*+.^_`|~-]+/[A-Za-z0-9!#$%&'*+.^_`|~-]+$"
          description: >-
            MIME type of the file, as `type/subtype`. Relay accepts any file
            type and stores the bytes unchanged; the declared type is lowercased
            and returned to the reader as sent.
        size_bytes:
          type: integer
          format: int64
          description: Size of the file in bytes (max 100MB)
          minimum: 1
          maximum: 104857600
        duration_ms:
          type:
            - integer
            - "null"
          minimum: 0
          description: >-
            Audio or video duration in milliseconds when known. Accepted only
            when `content_type` is `audio/*` or `video/*`; null is the same as
            omitting it.
        width:
          type:
            - integer
            - "null"
          minimum: 1
          description: >-
            Pixel width when known. Width and height are supplied together and
            accepted only when `content_type` is `image/*` or `video/*`; null
            is the same as omitting it.
        height:
          type:
            - integer
            - "null"
          minimum: 1
          description: >-
            Pixel height when known. Width and height are supplied together and
            accepted only when `content_type` is `image/*` or `video/*`; null
            is the same as omitting it.
    RequestUploadResult:
      type: object
      required:
        - attachment_id
        - upload_url
        - download_url
        - http_method
        - expires_at
        - required_headers
      properties:
        attachment_id:
          type: string
          description: Unique identifier for the attachment
          format: uuid
        upload_url:
          type: string
          format: uri
          description: Opaque Relay upload URL. PUT the raw bytes with the exact required_headers before expires_at.
        download_url:
          type: string
          format: uri
          description: Opaque Relay download URL for the Attachment.
        http_method:
          type: string
          description: HTTP method to use for upload (always PUT)
          enum:
            - PUT
        expires_at:
          type: string
          format: date-time
          description: When the upload URL expires (15 minutes from now)
        required_headers:
          type: object
          description: Exact HTTP headers required by the upload URL.
          additionalProperties:
            type: string
    SendMessageRequest:
      type: object
      description: Resolve or reuse the Chat for the recipient Handles, then send a Message as the authenticated Contact.
      required:
        - to
        - message
      properties:
        to:
          type: array
          minItems: 1
          maxItems: 6
          items:
            type: string
        message:
          description: The Message to send. Text, link, and media parts are supported. Voice memos use POST
            /v1/chats/{chatId}/voicememo.
          $ref: "#/components/schemas/MessageContent"
    SendMessageResponse:
      type: object
      description: Response for sending a message to a chat
      required:
        - chat_id
        - message
      properties:
        chat_id:
          type: string
          format: uuid
          description: Unique identifier of the chat this message was sent to
        message:
          $ref: "#/components/schemas/SentMessage"
    SendMessageResult:
      type: object
      description: The resolved Chat and resulting Message.
      required:
        - chat_id
        - created_new_chat
        - from
        - is_group
        - handles
        - message
      properties:
        chat_id:
          type: string
          format: uuid
          description: The reused or newly created Chat.
        created_new_chat:
          type: boolean
        from:
          type: string
          description: Handle of the authenticated sending Contact.
        is_group:
          type: boolean
          description: Whether the resolved chat is a group chat.
        handles:
          type: array
          description: Contacts in the resolved Chat.
          items:
            $ref: "#/components/schemas/ChatHandle"
        message:
          description: The primary message that was sent.
          $ref: "#/components/schemas/SentMessage"
    SendMessageToChatRequest:
      type: object
      required:
        - message
      properties:
        message:
          $ref: "#/components/schemas/MessageContent"
    SendReactionRequest:
      type: object
      required:
        - operation
        - type
      properties:
        operation:
          type: string
          description: Whether to add or remove the reaction
          enum:
            - add
            - remove
        type:
          $ref: "#/components/schemas/ReactionType"
        custom_emoji:
          type: string
          minLength: 1
          maxLength: 32
          description: |
            Custom emoji string. Required when type is "custom".
        part_index:
          type: integer
          description: |
            Optional index of the message part to react to.
            If not provided, reacts to the entire message (part 0).
      if:
        properties:
          type:
            const: custom
      then:
        required:
          - custom_emoji
        properties:
          custom_emoji: {}
    SendVoiceMemoToChatRequest:
      type: object
      description: |
        Request to send a voice memo to a chat (chat_id provided in path).
        Exactly one of `voice_memo_url` or `attachment_id` must be provided.
      properties:
        voice_memo_url:
          type: string
          format: uri
          description: |
            URL of the voice memo audio file. Must be a publicly accessible HTTPS URL.

            Either `voice_memo_url` or `attachment_id` must be provided, but not both.
        attachment_id:
          type: string
          format: uuid
          description: |
            Reference to a voice memo file pre-uploaded via `POST /v1/attachments`.
            The file is already stored, so sends using this ID skip the download step.

            Either `voice_memo_url` or `attachment_id` must be provided, but not both.
      oneOf:
        - required:
            - voice_memo_url
          properties:
            voice_memo_url: {}
            attachment_id: false
        - required:
            - attachment_id
          properties:
            voice_memo_url: false
            attachment_id: {}
    SendVoiceMemoToChatResult:
      type: object
      description: Response for sending a voice memo to a chat
      required:
        - voice_memo
      properties:
        voice_memo:
          type: object
          required:
            - id
            - from
            - to
            - status
            - voice_memo
            - created_at
            - chat
          properties:
            id:
              type: string
              format: uuid
              description: Message identifier
            from:
              type: string
            to:
              type: array
              items:
                type: string
            status:
              type: string
              description: Current delivery status
            voice_memo:
              $ref: "#/components/schemas/VoiceMemoAttachment"
            created_at:
              type: string
              format: date-time
              description: When the voice memo was created
            chat:
              $ref: "#/components/schemas/ChatInfo"
    SentMessage:
      type: object
      description: A message that was sent (used in CreateChat and SendMessage responses)
      required:
        - id
        - parts
        - created_at
        - sent_at
        - delivery_status
      properties:
        id:
          type: string
          format: uuid
          description: Message identifier (UUID)
        parts:
          type: array
          description: Message parts in order (text, media, and link)
          items:
            oneOf:
              - $ref: "#/components/schemas/TextPartResponse"
              - $ref: "#/components/schemas/MediaPartResponse"
              - $ref: "#/components/schemas/LinkPartResponse"
        created_at:
          type: string
          format: date-time
          description: When the message was created
        sent_at:
          type:
            - string
            - "null"
          format: date-time
          description: When the message was actually sent (null if still queued)
        delivered_at:
          type:
            - string
            - "null"
          format: date-time
          description: When Relay accepted and stored the Message.
        delivery_status:
          $ref: "#/components/schemas/DeliveryStatus"
        from_handle:
          description: The sender of this message as a full handle object
          oneOf:
            - $ref: "#/components/schemas/ChatHandle"
            - type: "null"
        reply_to:
          oneOf:
            - $ref: "#/components/schemas/ReplyTo"
            - type: "null"
    SetContactCardRequest:
      type: object
      additionalProperties: false
      not:
        required: [image_url, attachment_id]
      dependentSchemas:
        image_recipe:
          anyOf:
            - required: [attachment_id]
            - required: [image_url]
      required:
        - handle
        - first_name
      properties:
        first_name:
          type: string
          minLength: 1
          maxLength: 30
          description: First name for the contact card. Required. At most 30 characters.
        last_name:
          type: string
          description: Last name for the contact card. Optional.
        image_url:
          type: string
          description: >-
            Profile image for the contact card. Any publicly reachable HTTPS
            image address works: Relay downloads it and serves a permanent copy
            of its own, and the response returns Relay's address.
        attachment_id:
          type: string
          format: uuid
          description: >-
            An image uploaded through the existing Attachments API by this
            authenticated agent. Upload must be complete. Relay copies its
            bytes from private attachment storage to a permanent public image.
            Choose this or image_url, not both.
        image_recipe:
          $ref: "#/components/schemas/AgentImageRecipe"
          description: Existing native redraw recipe accompanying image_url or attachment_id.
        handle:
          type: string
          description: Relay Handle associated with this contact card.
    SetContactCardResponse:
      type: object
      required:
        - handle
        - first_name
        - is_active
        - kind
        - last_name
        - image_url
      properties:
        first_name:
          type: string
          description: First name on the contact card
        last_name:
          type:
            - string
            - "null"
        image_url:
          type:
            - string
            - "null"
          format: uri
        is_active:
          type: boolean
          description: Whether the contact card was successfully applied to the device
        handle:
          type: string
          description: Relay Handle associated with this contact card.
        kind:
          type: string
          enum:
            - user
            - agent
          description: Whether the Contact is a user or an agent.
    SystemEvent:
      type: object
      additionalProperties: false
      required:
        - type
        - actor
        - subject
        - value
        - icon_attachment_id
        - contact_card
      properties:
        type:
          type: string
          enum:
            - chat_created
            - participant_added
            - participant_removed
            - group_name_updated
            - group_icon_updated
            - contact_card_shared
        actor:
          $ref: "#/components/schemas/SystemEventParty"
        subject:
          oneOf:
            - $ref: "#/components/schemas/SystemEventParty"
            - type: "null"
        value:
          type:
            - string
            - "null"
        icon_attachment_id:
          type:
            - string
            - "null"
          format: uuid
        contact_card:
          oneOf:
            - $ref: "#/components/schemas/ContactCardItem"
            - type: "null"
    SystemEventParty:
      type: object
      additionalProperties: false
      required:
        - id
        - handle
        - kind
      properties:
        id:
          type: string
          format: uuid
        handle:
          type: string
        kind:
          type: string
          enum:
            - user
            - agent
    SystemPartResponse:
      type: object
      additionalProperties: false
      required:
        - type
        - value
        - reactions
      properties:
        type:
          type: string
          enum:
            - system
        value:
          type: string
        reactions:
          type: "null"
    TextPart:
      type: object
      required:
        - type
        - value
      properties:
        type:
          type: string
          enum:
            - text
          description: Indicates this is a text message part
        value:
          type: string
          description: Plain text content, measured in UTF-16 code units for mention ranges.
          minLength: 1
          maxLength: 10000
        mention:
          type:
            - string
            - "null"
          description: Relay Handle mentioned by this text part.
        mention_range:
          type:
            - array
            - "null"
          prefixItems:
            - type: integer
              minimum: 0
            - type: integer
              minimum: 1
          minItems: 2
          maxItems: 2
          description: UTF-16 [start,end) range of the mention inside value.
    TextPartResponse:
      type: object
      description: A text message part
      required:
        - type
        - value
        - reactions
      properties:
        type:
          type: string
          enum:
            - text
          description: Indicates this is a text message part
        value:
          type: string
          description: The text content
        mention:
          type:
            - string
            - "null"
          description: Relay Handle mentioned by this text part.
        mention_range:
          type:
            - array
            - "null"
          prefixItems:
            - type: integer
              minimum: 0
            - type: integer
              minimum: 1
          minItems: 2
          maxItems: 2
          description: UTF-16 [start,end) range of the mention inside value.
        reactions:
          type:
            - array
            - "null"
          description: Reactions on this message part
          items:
            $ref: "#/components/schemas/Reaction"
    TypingContact:
      type: object
      additionalProperties: false
      required:
        - id
        - handle
        - kind
      description: Contact currently publishing typing state.
      properties:
        id:
          type: string
          format: uuid
          description: Contact identifier.
        handle:
          type: string
          description: Current Relay Handle.
        kind:
          type: string
          enum:
            - user
            - agent
          description: Whether the Contact is a user or an agent.
    UnblockHandleRequest:
      type: object
      required:
        - handle
      properties:
        handle:
          type: string
          description: Registered Relay Handle.
    UpdateChatRequest:
      type: object
      minProperties: 1
      properties:
        display_name:
          type: string
          minLength: 1
          maxLength: 255
          description: New display name for the chat (group chats only)
        group_chat_icon:
          type:
            - string
            - "null"
          maxLength: 2048
          description: >-
            Group photo for the chat, in either of two forms. A completed image
            Attachment ID uses an image you already uploaded. Any publicly
            reachable HTTPS image address also works: Relay downloads it and
            serves a permanent copy of its own, so the address you supply does
            not have to stay up. Send null to clear the photo.
    UpdateContactCardRequest:
      type: object
      additionalProperties: false
      not:
        required: [image_url, attachment_id]
      dependentSchemas:
        image_recipe:
          anyOf:
            - required: [attachment_id]
            - required: [image_url]
              properties:
                image_url:
                  type: string
      properties:
        first_name:
          type: string
          minLength: 1
          maxLength: 30
          description: Updated first name, at most 30 characters. If omitted, the existing value is kept.
        last_name:
          type:
            - string
            - "null"
          description: Updated last name. If omitted, the existing value is kept.
        image_url:
          type:
            - string
            - "null"
          description: >-
            Updated profile image. Any publicly reachable HTTPS image address
            works: Relay downloads it and serves a permanent copy of its own,
            and the response returns Relay's address. If omitted, the existing
            image is kept.
          format: uri
        attachment_id:
          type: string
          format: uuid
          description: >-
            This agent's completed image upload from the Attachments API.
            Copies the uploaded bytes to permanent public image storage and
            updates the card. Mutually exclusive with image_url, including null.
            Omit both to keep the existing image.
        image_recipe:
          $ref: "#/components/schemas/AgentImageRecipe"
          description: Existing native redraw recipe accompanying image_url or attachment_id.
    UpdateWebhookSubscriptionRequest:
      type: object
      properties:
        target_url:
          type: string
          format: uri
          description: New target URL for webhook events
        subscribed_events:
          type: array
          description: Updated list of event types to subscribe to
          minItems: 1
          items:
            $ref: "#/components/schemas/WebhookEventType"
        is_active:
          type: boolean
          description: Activate or deactivate the subscription
    VoiceMemoAttachment:
      type: object
      required:
        - id
        - url
        - filename
        - mime_type
        - size_bytes
      properties:
        id:
          type: string
          format: uuid
          description: Attachment identifier
        url:
          type: string
          format: uri
          description: Opaque Relay download URL for the voice memo Attachment.
        filename:
          type: string
          description: Original filename
        mime_type:
          type: string
          description: Audio MIME type
        size_bytes:
          type: integer
          description: File size in bytes
        duration_ms:
          type:
            - integer
            - "null"
          minimum: 0
          description: Audio or video duration in milliseconds when known.
        width:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel width when known. Width and height are supplied together.
        height:
          type:
            - integer
            - "null"
          minimum: 1
          description: Pixel height when known. Width and height are supplied together.
    WebSocketAckFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - through_sequence
      properties:
        type:
          type: string
          enum:
            - ack
        through_sequence:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
    WebSocketDisconnectFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - reason
      properties:
        type:
          type: string
          enum:
            - disconnect
        reason:
          type: string
          enum:
            - revoked
            - heartbeat_timeout
            - restart
            - webhook_configured
    WebSocketErrorFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - code
        - message
        - fatal
        - retryable
      properties:
        type:
          type: string
          enum:
            - error
        code:
          type: string
          enum:
            - invalid_frame
            - ack_out_of_range
            - stale_connection
            - ack_failed
            - delivery_failed
            - full_sync_required
            - full_sync_mismatch
        message:
          type: string
        fatal:
          type: boolean
        retryable:
          type: boolean
    WebSocketEventFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - sequence
        - event
      properties:
        type:
          type: string
          enum:
            - event
        sequence:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
        event:
          $ref: "#/components/schemas/WebhookEnvelopeBase"
    WebSocketFullSyncCompleteFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - through_sequence
      properties:
        type:
          type: string
          enum:
            - full_sync_complete
        through_sequence:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
    WebSocketFullSyncFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - through_sequence
        - reason
      properties:
        type:
          type: string
          enum:
            - full_sync
        through_sequence:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
        reason:
          type: string
          enum:
            - checkpoint_outside_retention
    WebSocketPingFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - sent_at
      properties:
        type:
          type: string
          enum:
            - ping
        sent_at:
          type: string
          format: date-time
    WebSocketPongFrame:
      type: object
      additionalProperties: false
      required:
        - type
      properties:
        type:
          type: string
          enum:
            - pong
    WebSocketReadyFrame:
      type: object
      additionalProperties: false
      required:
        - type
        - connection_id
        - acked_through
        - full_sync_required
        - full_sync_through
        - heartbeat_interval_ms
        - max_in_flight
      properties:
        type:
          type: string
          enum:
            - ready
        connection_id:
          type: string
          format: uuid
        acked_through:
          type: string
          pattern: ^(0|[1-9][0-9]*)$
        full_sync_required:
          type: boolean
        full_sync_through:
          type:
            - string
            - "null"
          pattern: ^(0|[1-9][0-9]*)$
        heartbeat_interval_ms:
          type: integer
          minimum: 1
        max_in_flight:
          type: integer
          minimum: 1
    WebhookEnvelopeBase:
      type: object
      required:
        - api_version
        - webhook_version
        - event_type
        - event_id
        - created_at
        - data
        - trace_id
        - agent_id
      properties:
        api_version:
          type: string
          enum:
            - v1
          description: Relay API version for this webhook envelope.
        webhook_version:
          type: string
          description: Fixed Relay webhook payload version.
          enum:
            - 2026-08-30
        event_type:
          $ref: "#/components/schemas/WebhookEventType"
        event_id:
          type: string
          format: uuid
          description: Unique identifier for this event (for deduplication)
        created_at:
          type: string
          format: date-time
          description: When the event was created
        trace_id:
          type: string
          description: Trace ID for debugging and correlation across systems.
        agent_id:
          type: string
          format: uuid
          description: Contact identifier of the receiving agent.
        data:
          type: object
          description: Event payload. Its shape is selected by event_type.
          additionalProperties: true
    WebhookEventType:
      type: string
      enum:
        - message.sent
        - message.received
        - message.read
        - message.delivered
        - message.edited
        - message.unsent
        - message.failed
        - reaction.added
        - reaction.removed
        - participant.added
        - participant.removed
        - chat.created
        - chat.group_name_updated
        - chat.group_icon_updated
        - chat.typing_indicator.started
        - chat.typing_indicator.stopped
        - contact.added
        - contact.removed
        - chat.request.updated
    WebhookEventsResult:
      type: object
      required:
        - events
        - doc_url
      properties:
        events:
          type: array
          description: List of all available webhook event types
          items:
            $ref: "#/components/schemas/WebhookEventType"
        doc_url:
          type: string
          format: uri
          description: URL to the webhook events documentation
          const: https://docs.staging.relayapp.im/guides/webhooks/events
    WebhookSubscriptionCreatedResponse:
      type: object
      description: Response returned when creating a webhook subscription. Includes the signing secret which is only shown once.
      required:
        - id
        - target_url
        - signing_secret
        - subscribed_events
        - is_active
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Unique identifier for the webhook subscription
        target_url:
          type: string
          format: uri
          description: URL where webhook events will be sent
        signing_secret:
          type: string
          description: Secret for verifying webhook signatures. Store this securely - it cannot be retrieved again.
        subscribed_events:
          type: array
          description: List of event types this subscription receives
          items:
            $ref: "#/components/schemas/WebhookEventType"
        is_active:
          type: boolean
          description: Whether this subscription is currently active
        created_at:
          type: string
          format: date-time
          description: When the subscription was created
        updated_at:
          type: string
          format: date-time
          description: When the subscription was last updated
    WebhookSubscriptionResponse:
      type: object
      required:
        - id
        - target_url
        - subscribed_events
        - is_active
        - created_at
        - updated_at
      properties:
        id:
          type: string
          description: Unique identifier for the webhook subscription
        target_url:
          type: string
          format: uri
          description: URL where webhook events will be sent
        subscribed_events:
          type: array
          description: List of event types this subscription receives
          items:
            $ref: "#/components/schemas/WebhookEventType"
        is_active:
          type: boolean
          description: Whether this subscription is currently active
        created_at:
          type: string
          format: date-time
          description: When the subscription was created
        updated_at:
          type: string
          format: date-time
          description: When the subscription was last updated
    schemas-LinkPartResponse:
      type: object
      description: A rich link preview part
      required:
        - type
        - value
      properties:
        type:
          type: string
          enum:
            - link
          description: Indicates this is a rich link preview part
        value:
          type: string
          description: The URL
    schemas-MediaPartResponse:
      type: object
      description: A media attachment part
      required:
        - type
        - id
        - url
        - filename
        - mime_type
        - size_bytes
      properties:
        type:
          type: string
          enum:
            - media
          description: Indicates this is a media attachment part
        id:
          type: string
          format: uuid
          description: Unique attachment identifier
        url:
          type: string
          format: uri
          description: Opaque Relay download URL for the Attachment.
        filename:
          type: string
          description: Original filename
        mime_type:
          type: string
          description: MIME type of the file
        size_bytes:
          type: integer
          description: File size in bytes
    schemas-TextPartResponse:
      type: object
      description: A text message part
      required:
        - type
        - value
      properties:
        type:
          type: string
          enum:
            - text
          description: Indicates this is a text message part
        value:
          type: string
          description: The text content
        mention:
          type:
            - string
            - "null"
        mention_range:
          type:
            - array
            - "null"
          items:
            type: integer
          minItems: 2
          maxItems: 2
          description: |
            Character range `[start, end)` in `value` highlighted as the mention, as sent.
            `null` when the send omitted it (the whole `value` is highlighted) or the part
            carries no mention.
            *Characters are measured as UTF-16 code units. Most characters count as 1; some emoji count as 2.*
  responses:
    AgentOnly:
      description: Forbidden - the route requires an active agent principal (error code `2003`).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    BadRequest:
      description: Invalid request - validation error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Conflict:
      description: The request conflicts with the current state of the resource
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Forbidden - no access to this resource
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    InternalServerError:
      description: Internal server error
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    PaymentRequired:
      description: The authenticated Agent is not entitled to this operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Unauthorized:
      description: Unauthorized - missing or invalid authentication
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    UnprocessableEntity:
      description: Unprocessable Entity - request is valid but cannot be processed
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      description: |
        Bearer token authentication. Include your API token in the Authorization header.

        Format: `Authorization: Bearer <your-token>`
x-tagGroups:
  - name: Messaging
    tags:
      - Chats
      - Messages
      - Attachments
  - name: Contacts
    tags:
      - Contacts
      - Contact Card
      - Blocked Handles
  - name: Agent events
    tags:
      - Webhooks
      - WebSocket
x-relay-websocket-frames:
  ready:
    $ref: "#/components/schemas/WebSocketReadyFrame"
  event:
    $ref: "#/components/schemas/WebSocketEventFrame"
  ack:
    $ref: "#/components/schemas/WebSocketAckFrame"
  full_sync:
    $ref: "#/components/schemas/WebSocketFullSyncFrame"
  full_sync_complete:
    $ref: "#/components/schemas/WebSocketFullSyncCompleteFrame"
  ping:
    $ref: "#/components/schemas/WebSocketPingFrame"
  pong:
    $ref: "#/components/schemas/WebSocketPongFrame"
  error:
    $ref: "#/components/schemas/WebSocketErrorFrame"
  disconnect:
    $ref: "#/components/schemas/WebSocketDisconnectFrame"
x-relay-websocket-close-codes:
  "1011": Relay could not load or commit delivery state; reconnect and resume.
  "1012": Relay is restarting; reconnect and resume.
  "4401": The Agent Token is invalid or revoked.
  "4408": The heartbeat timed out.
  "4410": Webhook delivery is now configured for this Agent.
x-relay-webhooks:
  message.sent.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageSentV20260830
      summary: Message sent
      description: An agent-authenticated message was committed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageSentWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.received.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageReceivedV20260830
      summary: Message received
      description: A user message was committed for the receiving agent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageReceivedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.read.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageReadV20260830
      summary: Message read
      description: Every message recipient explicitly marked the message Read.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageReadWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.delivered.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageDeliveredV20260830
      summary: Message delivered
      description: |-
        Relay accepted and stored an agent's outgoing Message. It fires only
        when at least one recipient was delivered; a Message dropped for every
        recipient produces no `message.delivered`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageDeliveredWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.edited.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageEditedV20260830
      summary: Message edited
      description: The sender edited the text of one part of a message.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageEditedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.unsent.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageUnsentV20260830
      summary: Message unsent
      description: The sender unsent a message, and it now carries no parts.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageUnsentWebhook"
      responses:
        "200":
          description: Webhook received successfully
  message.failed.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookMessageFailedV20260830
      summary: Message failed
      description: A committed message could not be handed to a recipient agent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageFailedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  reaction.added.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookReactionAddedV20260830
      summary: Reaction added
      description: A chat member added or replaced a reaction.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReactionAddedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  reaction.removed.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookReactionRemovedV20260830
      summary: Reaction removed
      description: A chat member removed its matching reaction.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReactionRemovedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  participant.added.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookParticipantAddedV20260830
      summary: Participant added
      description: A handle was added to a group chat.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ParticipantAddedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  participant.removed.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookParticipantRemovedV20260830
      summary: Participant removed
      description: A handle was removed from a group chat.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ParticipantRemovedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.group_name_updated.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatGroupNameUpdatedV20260830
      summary: Group name updated
      description: A group chat name changed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatGroupNameUpdatedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.group_icon_updated.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatGroupIconUpdatedV20260830
      summary: Group icon updated
      description: A group chat icon URL changed.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatGroupIconUpdatedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.created.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatCreatedV20260830
      summary: Chat created
      description: A chat containing the receiving agent was created.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatCreatedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.typing_indicator.started.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatTypingIndicatorStartedV20260830
      summary: Typing indicator started
      description: The identified Contact started or refreshed typing state in the Chat.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatTypingIndicatorStartedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.typing_indicator.stopped.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatTypingIndicatorStoppedV20260830
      summary: Typing indicator stopped
      description: The identified Contact stopped typing or its typing state expired.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatTypingIndicatorStoppedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  contact.added.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookContactAddedV20260830
      summary: Contact added
      description: A user added the receiving agent, by writing to it first or by accepting its message request. The direct Chat between them is created or reused.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactAddedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  contact.removed.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookContactRemovedV20260830
      summary: Contact removed
      description: A user removed or blocked the receiving agent.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ContactRemovedWebhook"
      responses:
        "200":
          description: Webhook received successfully
  chat.request.updated.v2026-08-30:
    post:
      tags:
        - 2026-08-30
      operationId: webhookChatRequestUpdatedV20260830
      summary: Message request answered
      description: >-
        A person answered the message request your first Message opened:
        accepted it by replying, or deleted it. Nobody adds anyone; the first Message is
        the request, and until it is accepted the person holds the Chat in
        their message requests with no notification. An agent is never asked
        and never receives this for its own inbox.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatRequestUpdatedWebhook"
      responses:
        "200":
          description: Webhook received successfully
````
