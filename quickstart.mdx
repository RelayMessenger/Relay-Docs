---
title: "Quickstart"
description: "Receive one signed Message and send one reply with the Relay v1 API."
keywords: ["quickstart", "message.received", "webhook", "reply"]
---

Receive one user Message and send one agent reply.

## Prerequisites

| Requirement | Purpose |
| --- | --- |
| Agent Token | Authenticates Relay API calls |
| HTTPS endpoint | Receives agent events |
| Durable storage | Saves an event before acknowledgement |

```bash
export RELAY_API_URL="https://api.relayapp.im"
export RELAY_AGENT_TOKEN="<agent-token>"

curl -sS "$RELAY_API_URL/v1/webhook-events" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"
```

## Create a subscription

```bash
curl -sS -X POST "$RELAY_API_URL/v1/webhook-subscriptions" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "target_url": "https://agent.example/webhooks/relay",
    "subscribed_events": ["message.received"]
  }'
```

Save `signing_secret` from the `201` response. Relay shows it once.

## Accept a Message

<Steps>
<Step title="Read the raw body">
Keep the exact request bytes for signature verification.
</Step>
<Step title="Verify the signature">
Verify `webhook-id`, `webhook-timestamp`, and `webhook-signature`.
</Step>
<Step title="Save the event">
Deduplicate with `event_id`, then commit the event or enqueue it durably.
</Step>
<Step title="Return success">
Return `200` or `204` within 10 seconds. This records Delivered for the agent recipient.
</Step>
</Steps>

Read `data.chat.id` and `data.id` from the `message.received` envelope.

## Mark Read and reply

```bash
curl -sS -X POST "$RELAY_API_URL/v1/chats/$CHAT_ID/read" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN"

curl -sS -X POST "$RELAY_API_URL/v1/chats/$CHAT_ID/messages" \
  -H "Authorization: Bearer $RELAY_AGENT_TOKEN" \
  -H "Content-Type: application/json" \
  -H "Idempotency-Key: reply-$EVENT_ID" \
  --data-binary @- <<JSON
{
  "message": {
    "parts": [{"type":"text","value":"Tomorrow at 2:00 PM works."}],
    "reply_to": {"message_id":"$MESSAGE_ID","part_index":0}
  }
}
JSON
```

## Failure handling

| Failure | Action |
| --- | --- |
| Invalid signature | Return `401` and stop |
| Duplicate `event_id` | Return `2xx` without repeating side effects |
| Model failure | Retry from your saved event |
| Uncertain send response | Reuse the same idempotency key and request |

## Review with an agent

**Audit a Relay integration without changing it.** Copy this prompt into your coding agent.

```text
You are auditing a codebase that integrates with Relay.

This is read-only. Do not change code unless I ask.

1. Read the current Relay documentation and OpenAPI first.
2. Locate bearer authentication, raw-body webhook verification, durable event deduplication, Read, and reply code.
3. Confirm every URL uses https://api.relayapp.im/v1 with no partner or mobile namespace.
4. Confirm the consumer saves event_id before returning 2xx and reuses one idempotency key after uncertain sends.
5. Report: Check | Status | file:line evidence | Fix.
6. Mark anything you cannot prove as unknown.
```

## Next steps

- [Verify webhooks](/guides/webhooks)
- [Compare webhooks and Socket Mode](/guides/agent-events)
- [Send every Message type](/guides/sending-messages)
- [Understand delivery](/guides/read-receipts)
