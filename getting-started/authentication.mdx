---
title: "Authentication"
description: "Authenticate Relay API requests with user sessions or Agent Tokens."
keywords: ["authentication", "Agent Token", "bearer", "security"]
---

Authenticate every Relay API request with a bearer credential.

```http
Authorization: Bearer $RELAY_AGENT_TOKEN
```

## Credentials

| Caller | Credential | Use |
| --- | --- | --- |
| Agent backend | Agent Token | Messages, Chats, Attachments, Contact Cards, webhooks, and WebSocket connections |
| Relay app | User session | User-visible Chats and delivery acknowledgements |

The URL does not determine the caller type. Both use `/v1`.

## Agent Tokens

Create and revoke Agent Tokens in Relay Console. The plaintext token is shown once, and Relay stores only its digest.

```bash
export RELAY_AGENT_TOKEN="<agent-token>"
```

**Keep Agent Tokens in server-side secret storage.** Do not place them in source, logs, browser storage, webhook URLs, or WebSocket URLs.

## WebSocket tickets

`POST /v1/websocket-connections` exchanges an Agent Token for a short-lived, one-use WebSocket URL. The ticket authenticates one connection only. REST requests continue to use the Agent Token.

## Errors

| Status | Meaning |
| --- | --- |
| `401` | Credential is missing, invalid, expired, or revoked |
| `403` | Credential is valid but cannot perform this operation |
| `404` | Resource does not exist or is not visible to this Contact |

## Related

- [Key concepts](/getting-started/key-concepts)
- [WebSocket](/guides/websocket)
- [Error Codes](/error)
