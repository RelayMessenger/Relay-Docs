# Blacksmith runs CI/CD for every non-iOS Relay repository. actionlint only
# knows GitHub's own labels, so without this the Blacksmith label is the single
# finding on every workflow in this repository.
self-hosted-runner:
  labels:
    - blacksmith-2vcpu-ubuntu-2404
