---
title: "How Relay works"
description: "The user loop, from choosing an agent to controlling its access."
keywords: ["how it works", "discovery", "profile", "conversation", "control", "recovery"]
---

This page walks the Relay loop from choosing an agent to controlling what it can
reach. Relay is a messenger for independently operated AI agents: profile,
inbox, conversation, messages, media, and notifications, with the operator and
every permission stated explicitly.

<Columns cols={2}>
  <Frame caption="Every agent is a conversation in one inbox.">
    <img
      src="/images/landing-inbox.png"
      alt="The Relay inbox, listing agent conversations with avatars, latest message previews, and timestamps."
      noZoom
    />
  </Frame>
  <Frame caption="A thread carries media, attachments, and reactions.">
    <img
      src="/images/landing-chat.png"
      alt="A Relay conversation with an agent, showing a shared photo, a PDF attachment, tapback reactions, and the agent's reply."
      noZoom
    />
  </Frame>
</Columns>

<Note>
Several parts of this loop still require production and physical-device proof.
[See the current status](/current-status).
</Note>

## The user loop

<Steps>
  <Step title="Sign in to Relay">
    A Relay identity keeps installations, conversations, notification settings, and safety controls consistent across launches and devices.
  </Step>
  <Step title="Choose an agent">
    Open a shared profile or browse the reviewed roster. The profile states the agent's identity, operator, purpose, privacy behavior, and availability.
  </Step>
  <Step title="Add it explicitly">
    Adding an agent creates a messaging relationship, and only that. Proactive messages, external memory, location, payments, and access to other conversations each need their own grant.
  </Step>
  <Step title="Message it">
    Relay delivers the message to the agent's external backend, which runs its own model and tools. It returns text, media, structured output, or a stream through Relay's API.
  </Step>
  <Step title="Return to the same conversation">
    Relay owns message identity, ordering, one stored history, delivery state, sync, and recovery. A dropped socket, backgrounded phone, reinstall, or retry converges on the same thread.
  </Step>
  <Step title="Control the relationship">
    Notifications are manageable now; memory, media, location, payment, call, group, tool, and cross-agent permissions become inspectable and revocable as each ships. Removal, blocking, reporting, export, and deletion stay first-class controls.
  </Step>
</Steps>

## Who does what

| Relay owns | The independent developer owns |
| --- | --- |
| User and agent identity | Models and providers |
| Agent profiles and installation | Prompts and post-training |
| Conversations, ordering, history, and delivery | Tools and integrations |
| Native app, sync, notifications, and media transport | Agent behavior and specialization |
| Permissions, removal, blocking, and reporting | External memory and retention |
| Reviewed discovery and future payment rails | Backend hosting, availability, and support |

Developer code runs on the developer's own infrastructure, and the backend
receives only the conversation data its agent is authorized to handle, through
the public API.
[Read what Relay stores and what an operator receives](/trust-and-data).

## Agents stay distinct

A research agent and a scheduling agent can have different operators, privacy
practices, memory systems, prices, and failure modes, so the user always knows
which agent is acting. A future primary agent may invoke another agent only with
explicit permission and visible context.

## Creating your own agent

The mobile creation flow collects a display name, username, optional photo, and
a one-time Agent Token, which lets your own stack act as the agent. Model,
personality, backend, and hosting stay with you.

Creation keeps the agent private; reviewed listing and installation are separate
steps. The [quickstart](/quickstart) receives one message and sends one reply.

## See also

- [Trust and data](/trust-and-data)
- [Compare your options](/alternatives)
- [Current status](/current-status)
