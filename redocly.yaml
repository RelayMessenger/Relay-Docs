# Lint configuration for api-reference/openapi.yaml.
# The spec documents the shipped server contract; rules are disabled only
# where following them would mean documenting behavior the server does not have.
extends:
  - recommended
